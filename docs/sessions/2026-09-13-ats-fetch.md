# 2026-09-13 — `ats-fetch`: a regression that was a board nobody pointed a fetcher at

**The number.** The mail's `regressed to zero` read **66** on 2026-09-13 (the spawn prompt's
72 was the 09-11 reading; six cleared on their own and SolarEdge entered). This session moves
**17** of them out by construction — 4 rows whose board now yields Israel postings, 4 whose
native board is `israel_scoped` and answers an honest zero, and 9 parks — so the first
unattended digest on a tree containing these commits should print **49 or fewer**, plus
whatever the day adds. That is a prediction and not a result until the 2026-09-15 row in
`HANDOFF.md` answers it: `stale.json` is rewritten only by a digest.

## 0. The class, re-derived from origin `640eb3c`

| | prompt (09-11) | today |
|---|---|---|
| `regressed to zero` in the mail | 72 | **66** = 59 `scrape` + 7 native |
| scrape rows with rot `why: empty`, `n >= 10` | 37 | **42** of the 59 |
| careers pages whose plain HTML names an ATS (a free GET of all 59) | 18 | **20** |
| Workday PUBLIC sites no detector parses | 4 | **3** (Arrow, Cybersixgill → `bitsight.wd1`, Dynamic Yield → `mastercard.wd1`) + DoubleVerify, found among the native 7 |

ABB's page no longer carries a Workday site (it serves Phenom), and Digital Turbine was
repaired on 09-11. `registry_health.py` still prints no such pool; the class is read off
`cloud_state/stale.json` joined to `cloud_state/scrape_rot.json`.

## 1. The two fetcher changes

**`fetchers.workday_cxs_url`.** `fetch_workday` POSTed `api_url` verbatim, so a public site
URL (`https://arrow.wd1.myworkdayjobs.com/en-US/AC/`) produced `site == ""` and a POST at an
HTML page. The public URL carries the same three facts as the endpoint — host, tenant (the
host's first label), site (the first path segment after an optional `xx-XX`) — so the
endpoint is derived, and `fetch_workday` reads through it. A `cxs` address is returned
unchanged; a host with no site returns None. `check_invariants.PLATFORM_HOST["workday"]` stays
the bare `myworkdayjobs`: tightening it to `/wday/cxs/` would now refuse a row that fetches.

**`fetchers.fetch_teamtailor`.** Netafim was a scrape row on its Teamtailor tenant host,
`why: empty` 18 nights, while `careers.netafim.com/jobs.rss` carried **56** postings, **24** of
them in Israel, each with its full description and a `tt:country` per location. The feed was
measured complete against the paginated HTML (Netafim 56 of 56, Moburst 10 of 10). Not
`israel_scoped` (the feed is the whole board), and it stamps `IL` only — the `_sf_country`
asymmetry — so the fixture's agronomist at *Fresno; Hatserim* stays Israeli.
`health.ATS_HOST` does **not** gain `teamtailor.com`: two ACTIVE scrape rows sit on hosted
tenants (Moburst, AGILINA) and would be flagged `misconfig-scrape-on-ats` into a self-heal that
has no Teamtailor detector (`604`), which buys strikes and nothing else — the BACKLOG 78 rule.

## 2. The rows — every repoint verified through `fetch_company` before `apply_resolved.py` moved it

Each repoint passed `identity_gate.is_foreign` (False on all ten) and `board_vouches` (True or
None, never False) inside the gated applier, and carries a `platform-fix 2026-09-13:
<evidence>; N/M IL` note segment through `pipeline/notes.py`.

| row | from → to | measured |
|---|---|---|
| Arrow Components | scrape → workday `arrow/AC` | **5 / 5 IL** (QC, Field Sales, Inside Sales — Kfar Saba, Petah Tikva) |
| Netafim | scrape → teamtailor | **56 / 24 IL** |
| Exyte | scrape → phenom `careers.exyte.net/widgets` | **19 / 19 IL** (Kiryat Gat, Rehovot) |
| DoubleVerify | greenhouse → workday `doubleverify/DV_Careers` | **3 / 2 IL**; `boards-api.greenhouse.io/v1/boards/doubleverify` answers 200 with 0 jobs, and the careers page embeds `doubleverify.wd5/DV_Careers` |
| ABB · Deutsche Post DHL · OpenText | scrape → phenom | 0 IL by the country facet AND by an unfiltered `keywords: Israel` probe (OpenText: 2 hits, both USA); for the read |
| Lam Research | scrape → eightfold `domain=lamresearch.com` | 0 IL of **1,342** worldwide; for the read |
| BeeHero | scrape → comeet `78.00E` (uid + token from the page's `COMEET.init`) | 200 `[]`; for the read |
| Ludeo | scrape → comeet `D7.008` | 200 `[]`; for the read |

Scoped runs (`python -m pipeline.run --only … --no-llm --db <scratch>`, `JD_BD=0
BD_RUN_CAP=0`): Arrow + Netafim + Exyte → `israel_matched 48`, `scanned 3 cos, 0 failed`,
`cleared: Arrow Components; Exyte; Netafim`; DoubleVerify → `israel_matched 2`, `cleared:
DoubleVerify`. **Board 0 in both** — none of those 50 postings is an analytics role (the
keyword tier refused DoubleVerify's *Manager, Data Science & Research*), so this session adds
coverage, not cards.

**Nine parked**, each re-read and matched by name immediately before the write, each asserted
into `listing_hunt.in_hunt_pool` or carrying a terminal token before anything was written:

| row | verdict | evidence |
|---|---|---|
| Cybersixgill | `duplicate of Bitsight` (terminal) | careers 301 → `bitsight.com/cybersixgill`, whose page reads *"Cybersixgill is now part of Bitsight"* and embeds `bitsight.wd1/Bitsight` — the active `Bitsight` row's board |
| KELA - Cyber Threat Intelligence | `duplicate of KELA` (terminal) | `kelacyber.com/careers` embeds Comeet `2A.004`, the active `KELA` row's tenant |
| REE Automotive | `needs re-resolution` | the page's own `comeetvar` names `D3.00B`, which answers HTTP 400 (a dead tenant) |
| Dynamic Yield | `redirects to careers.mastercard.com` | 301 to the Mastercard row's board; no page states the relation, so not terminal |
| Eureka Security | `redirects to tenable.com` | 301 to a Tenable product page that does not name Eureka; not terminal |
| Talenya | `redirects to paycor.com` | 302 to *"Paycor Smart Sourcing (formerly Talenya)"*; no Paycor row |
| Radiflow | `needs re-resolution` | careers 301 → `/contact-us/` |
| Nova | `needs re-resolution` | careers 301 → a press release; `careers/working-at-nova/` links no board |
| Portnox | `unsupported ATS pinpointhq.com` | careers 301 → `portnox.pinpointhq.com`; `postings.json` 7 postings, 0 Israel (Austin, Ireland, remote) |

**ASTERRA was measured and NOT parked.** Its careers URL lands on the home page, but its note holds `identity 2026-08-23: greenhouse tenant 'asteralabs' is Astera Labs … not ASTERRA` — the verdict that kept 30 Astera Labs roles from publishing under this name — and that segment alone is ~200 of the note's 220 characters, so any appended pool token evicted it (`notes.append` drops the oldest whole segment). The first apply did exactly that and was caught by a before/after segment diff; the row was restored to origin. Rule 3 outranks one row's place in a count.

`check_invariants.py`: `2,380 rows, 1,402 → 1,393 active, 0 orphans, pool 829 → 836`, the two
warning lists byte-identical before and after (20 endpoint-names-another-company, 42
path-platform tenants).

## 3. What stays in `regressed to zero`, and why

**49** predicted: 41 scrape + 8 native.

- **8 native honest zeros** (`606`): six Comeet tenants whose careers page still embeds the
  same uid (NeuReality `89.002`, Nexite `37.000`, Swimm `56.007`, Zesty `06.000`; Sola
  Security and Zemingo unprobed past their stored board) and today's BeeHero and Ludeo. They
  are companies not hiring, in a count read as boards we cannot read.
- **1 kept active on purpose**: ASTERRA (§2 — its careers URL redirects home, and parking it would evict its `identity` verdict).
- **5 unreadable from a plain client**: Hilti Israel, Revolut, SolarEdge (`http:403`), Xtend
  (`500`), and `אסם`, whose stored address is a Facebook page (`400`) — `registry`'s.
- **4 on an ATS the repo does not read**: Arm and Planview (iCIMS — Arm's `/api/jobs` answers
  HTML, Planview's answers JSON with 11 postings, AMD's 0: the Attract protocol is not settled
  and is Open item 2's), Sensos (a Teamtailor widget on the public `api.teamtailor.com` with a
  page-embedded key: 2 postings, one in NYC, none in Israel — not built), Deutsche Telekom (`telekom-growthhub.eightfold.ai`
  401s the page and 404s three `?domain=` guesses).
- **3 SuccessFactors with nothing to read**: Volkswagen (CARIAD) — the tile fragment answers
  today (10 tiles, all Ingolstadt / Mönsheim / Berlin), so `245@ats-fetch`'s "refuses the
  connection" clause is stale, but a conversion would buy 0 Israel postings; Challenge Group
  (0 tiles again, as 09-11); EPAM (the word appears, no career-site-builder tenant).
- **28 careers pages that name no board at all** (Maersk, ASML, AU10TIX, Airspan, AlgoSec,
  Axioma, BigPanda, Bits of Gold, Bright Security, Bulwarx, Cenevo, Datos Health, GenCell,
  Hailo, IAI, K2view, Navan, Predicta Med, Quantum Art, Rashi Foundation, Sanofi, StarkWare,
  Strauss Water, Whalo, Xpend, aiOla, entrypoint, Opswat). Not parked: one GET is not a
  measurement of a JS page (§8 rule 2), and the scraper's own rot entry already says
  `found 0` every night. This is `scraper`'s half of the class.

## 4. The two PARTIAL rows of this lane, read against the runs

**The nine SuccessFactors conversions produce — PASS.** The 09-12 schedule digest (run
34685065990, state commit `71aff56`) moved `cloud_state/health_baseline.json` to exactly the
counts measured on 09-11: EY 9 → **63**, Alstom 17 → **48**, Ormat 15 → **29** (30 on 09-13),
Energean 6 → **16**, West Pharmaceutical 5 → **7**, NovoCure 5 → **6**; Sapiens 4 and Dentsply
Sirona 2 unchanged (both already at their measured count). EY + Alstom + Ormat = **140** where
the scrape read 34; none of the nine is in `stale.json` on 09-13.

**The four walled boards get the rung — PARTIAL, and the missing line was a false negative.**
The rung was ON both nights: the step env dump reads `SCRAPE_VIA_UNLOCKER: 1` in runs
34666654489 and 34731908452 (empty in 34552848178, the `CEILING REACHED` night), `[bd-budget]
… no ceiling in force`, and the `collect:` stamps carry `unlock_calls=86` / `84`. The
`paid residential rung: ON` string exists — `.github/workflows/scrape-refresh.yml:74` — but only
in `$GITHUB_STEP_SUMMARY`, which no log search reaches (`605`). Per board on 09-13: **EPAM**
`0 (81s) unlock=4/4` — the unlocker read the page four times and found nothing, so it left
`fetch errors` for `regressed to zero` (n=2): a JS listing, not a wall. **IEC** `ERROR http:403
unlock=0/1` — the unlocker itself failed. **NSO Group** and **PAPAYA** (and Ayalon Highways)
`ERROR http:403 unlock=1/1` — a page came back through the residential IP and the row still
errored. **The wall beats the rung for four of the five, so the class is `registry`'s, not a
spend question** — the outcome the 09-11 row named in advance. The 09-13 mail's `new: 2 fetch
errors` are Prilenia (Lever read timeout) and Similarweb (Greenhouse network error), neither
walled.

## 5. Clause 4

**What did you delete or unify?** `git diff --stat origin/master...HEAD` is in the gates
section. `−` is the registry lines replaced; no code was deleted. Unified: the Workday endpoint
derivation now exists once, in `fetchers.workday_cxs_url`, and `604` asks the two resolvers to
call it instead of each carrying a `/wday/cxs/`-only regex.

**Which existing function did you extend, and which did you decline to duplicate?**
`fetchers.fetch_workday` (one line: read through the derived endpoint) and the `FETCHERS` map.
Reused, not rewritten: `apply_resolved.py` for all ten repoints (the gated applier with the
twin-board refusal), `pipeline/notes.append` for every segment, `listing_hunt.in_hunt_pool`
and `verdicts.TERM_RX` as the pre-write assertion. Declined: a park tool (a scratchpad script
again, as 09-11), and a Pinpoint fetcher for one row with 0 Israel postings.

**What design did you reject, and on what evidence?** (1) Tightening C2's workday host to
`/wday/cxs/`: after `workday_cxs_url` the public form fetches, so it would refuse a working
row. (2) Adding `teamtailor.com` to `health.ATS_HOST`: 2 active hosted-tenant scrape rows would
enter a self-heal with no Teamtailor detector. (3) Changing `fetch_workday`'s `job_id`
(`311@ats-fetch`) in the same commit: it re-keys every Workday `seen_id` and would re-offer
the fleet's roles as new. (4) Parking the 28 no-marker rows on a GET: §8 rule 2. (5)
`duplicate of Mastercard` / `duplicate of Tenable`: a redirect alone is one source.
(6) Converting VW CARIAD: 10 tiles, 0 Israeli.

**Would the next session find this?** `grep -n "myworkdayjobs" pipeline/fetchers.py` lands on
`_WD_PUBLIC` and `workday_cxs_url`; `grep -rn teamtailor pipeline/fetchers.py` on the fetcher;
`grep "platform-fix 2026-09-13" companies.csv` on every row this session moved.

**What did you make harder for the next lane?** Five things: one platform key
(`teamtailor`, with five `platform_check` source cells MISSING until `604`), one public helper
(`workday_cxs_url`), one private regex (`_WD_PUBLIC`), a second accepted `api_url` shape for
`workday` rows (a reader can no longer assume `/wday/cxs/`), and seventeen `platform-fix 2026-09-13`
note segments the conflict merge keys by tool.

## 6. Gates and spend

- `python -m pytest` (local, final tree): **2 failed, 1,963 passed, 13 skipped**. The two reds are
  `infra`'s `bd` gauge tests (`test_the_gauge_alarms_on_the_free_tier_and_refuses_nothing`,
  `test_an_unreadable_account_still_reports_a_gauge_from_the_repos_own_ledger`), red on origin
  `640eb3c` in CI run 34755843907 before this session touched anything.
- `python check_invariants.py --strict`: `2,380 rows, 1,393 active, 0 orphans, pool=836`.
- `python docs/check_docs.py`: 0 errors, 0 warnings. `python docs/backlog.py check`: no new collision.
- `python tools/guard_kill.py --base origin/master`: **KILLS 2** of 2 new tests; both killers
  classify `behavioural`.
- CI on `64a2cd6`: run **34759619277**, conclusion `failure`, **15 of 16 jobs success** — all eight
  mutation shards, all six rehearsals, guard-kill; `guard` red on the same two inherited tests
  (`2 failed, 1975 passed`).
- `registry_health.py --stale-boards` on the final tree: `ABANDONED 1 - SPARED 1 = REFUSED 0`
  (Tonkean) — the lane's table number, unchanged.

Bright Data: **0 credits** (every probe was free HTTP; the worktree has no key). LLM calls: **0**.
