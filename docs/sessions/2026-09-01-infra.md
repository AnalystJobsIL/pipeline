# 2026-09-01 — `infra`: the step that had no token, the anchor that killed everything, and the wall nobody read

Six tasks, in the operator's priority order. Every number below was re-derived from a run
log or a local run, not copied from a report.

## 1. The jd-quality judge had no token — three mornings, silently

**Symptom.** `matched:jd-quality-unavailable(13 candidates, no verdict: llm-auth13)` in the
09-01 mail; `llm-auth9` on 08-31; `llm-auth7` on 08-30. In the SAME job, on 09-01, classify
reported `llm 372 (102 yes) ... failed calls 0; model claude-sonnet-5 x372`.

**Cause, from the runners' own env dumps** (run 33494404810, job 99813119785; run
33387229779, job 99472488706). The `enrich_matched` step's env block:

```
BD_RUN_CAP: 250 · DRY_RUN: · BRIGHTDATA_API_KEY: *** · BRIGHTDATA_ZONE: *** ·
MATCHED_JD_TIME_BUDGET_MIN: 20
```

No `CLAUDE_CODE_OAUTH_TOKEN`. The `Run the pipeline` step (line 259) and `firmo_drain`
(242) both carry it; the workflow-level `env:` carries only `BD_RUN_CAP` and `DRY_RUN`, and
GitHub injects no secret a step did not ask for. `pipeline/secretsenv.py` resolves
`<root>/secrets.env` on a runner, finds nothing, and returns `""` **silently by design**
(`secretsenv.py:24`), so nothing else could arm it. All 13 calls failed in ~10 s — instant
401s, not timeouts. This is **BACKLOG 448 in the mirror**: that was this same step missing
`BRIGHTDATA_*`, closed 08-30, and nobody checked the inverse direction.

**Why three mornings passed.** The refusal branch in `enrich_matched_jd._quality_pass`
printed nothing at all (`c["unavailable"] += 1; c["why:"+verdict] += 1; continue`), the step
is `continue-on-error`, and `docs/RUN_LOG.md`'s table of *every seam that spends the
subscription* had **no row for the jd-quality judge** — so the one document that would have
prompted anyone to check its step did not know the seam existed.

**Fixed.**
- the token on the step;
- **`llm-no-token`** as a distinct refusal. `pipeline/llm.py` reports 401 and 403 alike as
  `kind="auth"`, so `llm-auth` was telling three mornings to re-mint a token that was never
  sent (HANDOFF Open item 3 said exactly that, and is now corrected). `jdfill._refusal_kind`
  splits it in the `except LLMUnavailable` arm only — **not** as a pre-call check, which
  would have returned `no-token` on the runner before reaching the mock in
  `test_an_unavailable_model_leaves_the_cheap_verdict_standing` and gone red in CI while
  passing on every laptop;
- a **breaker**: the first refusal in `_SEAM_IS_DOWN` stops the calls, and every remaining
  candidate is still tallied **under the same reason string** — a distinct `llm-breaker`
  reason would render one outage as `llm-auth1+llm-breaker12` and break the
  `llm-authN`-is-a-count arithmetic the mail depends on. Transient kinds are deliberately
  excluded: one timed-out call says nothing about the next;
- a print on the refusal branch, worded without `description`/`_store_text` (an AST test
  forbids those literals in `_quality_pass`'s executed code);
- `test_every_llm_step_of_the_digest_carries_the_subscription_token`, over the three step
  ids that actually reach the seam. **Verified red with the token line removed.** The ids
  are listed, not derived: `enrich_scrape` and `stamp_enrich` import `pipeline.jdfill`
  without calling the seam, so an import-transitivity guard would demand the secret on
  steps that never spend it;
- the missing `docs/RUN_LOG.md` row, plus the sentence that a seam is only armed where its
  step is armed.

**N in `llm-authN` is a candidate tally**, not an errno — `matched_llm_candidates=13`,
`matched_llm_calls=13`, `matched_llm_unavailable=13`.

## 2. BACKLOG 540 — an anchor test that killed every mutant

`test_every_company_intel_mutation_still_aims_at_real_code` asserts each record's `find`
occurs once in its file. Applying any mutant deletes that string, so the anchor reddened
under **every** mutant and `tools/mutate.py` scored each one `killed` — by the test whose
only job is to notice catalogue drift. Scored `direct`, not `static`, because
`_classify_killer`'s `_STATIC_MARKERS` knows `inspect.getsource` / `ast.parse` / `ast.walk`
and this anchor reads its file with `open(...).read()`.

Two corrections to the filed item, both measured:

1. **`tests/fixtures/classifier/` has no anchor test and no catalogue.** Its fixtures are
   behavioural golden files (title → verdict); they cannot fire universally. There were
   exactly **two** anchors in the repo, and the other one
   (`test_registry.py::test_no_mutation_record_goes_stale_unnoticed`) has had the
   `AJIL_MUTANT` stand-down since 2026-08-27. The escape existed for one catalogue and not
   the other.
2. **CI never ran the company-intel catalogue at all** — `tools/mutate.py --all` loads only
   `tests/mutations.json`. So the false kills were never in a CI verdict; they were in the
   number this lane quotes.

**Fixed** with the three-line skip, copied from the registry anchor, **plus the class**:
`test_every_catalogue_anchor_test_stands_down_inside_a_mutant` names any future catalogue
anchor lacking the skip. It resolves same-file helpers, because `test_registry.py` does its
counting in `_stale_mutation_anchors` and the guard on its caller is what protects it —
the first version of my test flagged that helper and was wrong.

**Rejected: teaching `_STATIC_MARKERS` about `open().read()`.** Measured over all 1,514
test functions with `_classify_killer`'s own algorithm: 722 behavioural / 75 static / 717
direct, and **45 currently-`direct` tests contain both `open(` and `.read()`** — most
reading YAML or Markdown, not the mutated module (`test_every_long_tests_step_has_a_named_
budget_below_its_job_timeout`, `test_the_mutation_shards_partition_the_whole_catalogue`,
`test_the_drain_capacity_constants_match_the_workflow`). Any record killed solely by one of
them flips `killed` → `FAIL — killed ONLY by source-text guard(s)` on a push that changed
nothing, and a static-only kill stops short-circuiting the subset (`run_one` falls through
to the full suite), adding a full-suite run per record to a gate already over its wall.

**Re-verification.** The whole company-intel catalogue (86 records) re-run against the
fixed tree: every record dies to a **named behavioural test** — `ci-intel-line-md` to
`test_a_research_answer_that_ne…`, `ci-budget-ignored` to `test_one_wall_clock_bounds_blu…`
(behavioural), `ci-front-door-raises` to `test_the_front_door_never_rais…` (behavioural),
and so on. The full table is in the run output; the shape that mattered — a record whose
only killer was the anchor — did not appear.

## 3. Shard capacity: five over the wall, and a ceiling nobody read

Run 33409190938 (green `af7b104`): walls **2096 / 2115 / 2154 / 2096 / 2218 s**, every one
past the 1,800 s rule, and mutation-gate (2) and (3) were killed at their 40-minute budget
on 08-31 (both green on identical-tree re-runs — runner contention, five suites in flight).

From that run's own `baseline` lines: **137–201 s of fixed cost per shard** (mean ~158) and
**~37.5 s per record** over 52 records. So:

| shards | records | projected wall |
|---|---|---|
| 6 | 44 | ~1,808 s — **still over** |
| 7 | 38 | ~1,583 s |
| **8** | **33** | **~1,396 s** |

One added matrix entry would have bought nothing, which is why this is 5 → **8**: about a
week of headroom at the catalogue's measured growth (105 → 260 records, 08-24 → 09-01,
~13/day), and the 540 fix can only push some records from a subset kill to a full-suite
one. Sizes stay even: 33/33/33/33/32/32/32/32. Matrix and `SHARDS` moved in one commit —
`test_the_mutation_shards_partition_the_whole_catalogue` re-derives `n` and fails on any
intermediate state.

**The ceiling now announces itself.** It lived in a workflow comment, a closed backlog item
and a morning-check row, which is how five shards stayed over it for days: `MUTATE_WALL_WARN`
(default 1800) prints `::warning::mutation shard walled N s …` under the timing line, on the
shard's own run page, while it is still green. Verified firing with `MUTATE_WALL_WARN=1`
(`retry-ats-invert`, wall 331 s, record still killed). It cannot be a job-level `if:` —
`test_every_long_tests_step_has_a_named_budget_below_its_job_timeout` forbids `if:` in this
workflow — so it lives in the harness. The budget stays 40 m: raising it is what the
workflow's own `::error::` tells you not to do.

`cancel-in-progress` was **not** added for the contention: a cancelled gate names nothing
(§4, BACKLOG 442).

## 4. BACKLOG 498 — the description text is on Pages

The filed diff named two lists. There are **three**: the dry-run `ls -l` at the top of the
publish step is a third, so a dry run would have printed "WOULD publish" while omitting
exactly the two files the item is about. All three carry both names now, plus the `git add`
basenames.

`cloud_state/roles_text.jsonl` was **already** in `persist_state.SINGLE_WRITER`; only
`roles_archive.csv` was missing. `roles_retractions.jsonl` deliberately stays on the default
`ours` **with** its `NO STRATEGY -- ours wins (origin's version overwritten)` warning: it is
hand-written, so that warning is the point, not noise to silence by declaring a writer that
does not exist.

`pipeline/roles.py` needed **nothing** — `build_meta` has read `ROLES_ARCHIVE_PAGES_URL` and
`ROLES_TEXT_PAGES_URL` since 08-31 and was waiting for infra to set them. Both are now
exported beside `ROLES_PAGES_URL` on the pipeline step.

Not folded in, filed as **542**: `run_gates` has no `.csv` arm (a `.csv` other than
`companies.csv` is staged unread, and `_well_formed` has the same hole), and `PAIRED` is a
1:1 dict that is **one-way by design** — a corrupt receipt must never withdraw a good digest
because `mark_sent` has already burned those roles. Naming the dataset trio needs the value
to become a sequence and the mate-restore loop to iterate, which is a policy change, not
a line.

## 5. The debt this push owed, and the two sweep items

**BACKLOG 458, applied** (touching `persist_state.py` charges it, and the gate wants the
diff applied or cited). `merge_json_cache.merge`'s two deletion arms were asymmetric — ours
honoured, origin's undone — so any process on an older checkout re-added every key origin
had retired since. Measured by `registry` on 08-30: 44 names retired by `1ce0db5` and
`a07e743` between 00:28 and 00:54 were back in `research_companies.json` at 00:41, put there
by `5ccd60e`, the listing-hunt cron's own state commit. The fenced one-liner, verbatim; the
rescue arm still fires when origin is empty or unreadable, which is what it was written for.
`test_the_merge_does_not_resurrect_a_key_the_origin_retired` pins all three arms. The item's
closing observation — `s_company_dict` carries the same asymmetry over eight `cloud_state`
files — is still true and is **541**.

**515's footnote → a guard.** GitHub honours the CI-skip marker anywhere in a message, body
included, so a commit that merely *names* it runs no checks: the commit that CLOSED 515 did
exactly that and had `total_count 0` runs while its parent had one. A prose rule already
existed, and the session that wrote it is the session that tripped it — which is why this is
now `check_no_ci_skip_marker`, pre-push only (the commit it catches is one no runner ever
sees). Cron state commits legitimately carry the marker and sit on origin, before the branch
base. The check's own file never spells the marker as one token.

**The HANDOFF caps move off the runners.** They exist against narrative accretion — a
weekly-scale failure — and CI is a per-push gate on a file with five concurrent writers. What
it actually caught on 08-31 was a race: three doc-only pushes red at **3,201 / 3,203 / 3,206**
words against a 3,200 cap (runs 33363762720, 33444708674, 33448417852), each because another
session's line landed between the local run and the push; each reddened master for every
other lane, over prose; each fix was another doc-only push minutes later. The third was
titled *"the race named"* — a session lost the race while describing it. The caps still bind
on every local `check_docs` run and in the unit test, where the writer of the long line pays.
CI keeps the **shape** check: a missing section is nobody's race. `docs/AGENT_BRIEF.md`'s
enforcement table and `check_docs`'s own local-only paragraph were amended in the same
commit, or they would document a gate that no longer exists.

## What I did not do, and what is unproven

- **Nothing here is proven until tomorrow.** Three 09-02 morning-check rows carry the
  proofs: `matched_llm_unavailable=0` with calls > 0 and no `jd-quality-unavailable` clause;
  `mutation-gate (0)..(7)` all green at 16 jobs with every wall under 1,800 s and no
  `::warning::`; the two files 200 on Pages with `published_on_pages: true` in the meta.
- **SerpApi**: its exhaustion window ended today and nobody re-measured it. Recorded as
  unproven rather than restored; `CLAUDE.md` and `docs/AGENT_BRIEF.md` still say "until
  2026-09-01" (`docs`).
- **541** and **542** filed, not fixed.
- The 08-31 `listing-hunt` row is answered PASS from run 33449138828 — `retire-settled:
  queue 414 -> 374` at 02:00:27Z, before the four `queue-resolve-search:` lines at
  02:00:28Z, `ingested 58 new attempts`, `28–38 s/name over 14–15 scored`. That per-name
  figure is what `491` item 3 asked for.
- Two claims in the 09-01 digest morning-check row were wrong and are corrected in place
  rather than quietly satisfied: `bd_tried` is not a printed key, and CI is **13** jobs,
  not 10 (16 after this push).

## Clause 4 — the change against the problem

- **Deleted / unified**: the rescue arm's asymmetry (458), HANDOFF Open item 4's expired
  claim replaced by an honest one, five folded session lines, ~90 words of prose the caps
  were about to charge someone else for. `git diff --stat` totals are in the push.
- **Extended, not duplicated**: `_refusal_kind` extends the existing `LLMUnavailable`
  handler rather than adding a second seam; the anchor skip is the registry's existing
  idiom copied verbatim; the marker check reuses `_baseline_ref` and the documented
  `GITHUB_ACTIONS` stand-down; `pipeline/roles.py` was left alone because it already had
  the readers.
- **Rejected, with the number**: `_STATIC_MARKERS` (45 tests reclassified); six shards
  (~1,808 s, still over); a doc line for 515 (the prose rule already existed and failed);
  `cancel-in-progress` (a cancelled gate names nothing); folding 542 into 498 (one-way by
  design).
- **Would the next session find this?** `grep -rn "llm-no-token"` hits `jdfill._refusal_kind`
  and its test; `grep -rn "AJIL_MUTANT" tests/` now hits three places, one of which is the
  rule; `grep -n "MUTATE_WALL_WARN"` hits the harness, the workflow comment and
  `ARCHITECTURE.md` §4.
- **What I made harder for the next lane**: four new names — `MUTATE_WALL_WARN`,
  `llm-no-token`, `_SEAM_IS_DOWN`, `check_no_ci_skip_marker` — and three more CI jobs
  (13 → 16). The HANDOFF caps no longer fail a runner, so a lane that ignores its local
  `check_docs` can now push an over-cap file; the pre-push gate and the unit test are the
  only readers left.
