# 2026-08-25 — `registry` lane: today's logs, the pending backlog, §2/§3 re-verified

Scope: step 2 of the flow. The brief was (1) pending backlog, (2) what today's cloud logs
say we should improve, (3) `ARCHITECTURE.md` §2/§3 with every claim reproduced, dated
unverified, or deleted; adversarial Opus waves; no regressions. Every number below was
produced by a command on 2026-08-25 (UTC times where they matter); the commit that carries
each change is named. Plan: `C:\Users\svald\.claude\plans\you-are-to-think-linear-conway.md`.

Files written: `retry_unreachable.py`, `bd_rescue.py`, `wayback_rescue.py`, `validate_empty.py`
(write site only), `auto_expand.py`, `resolve_llm.py`, `repair_extract_gap.py`,
`registry_health.py`, `tools/mutate.py`, `tests/test_registry.py`, `tests/mutations.json`,
`companies.csv`, `scraped_cache.json`, `cloud_state/auto_expand_seen.json` (seeded `{}`),
`ARCHITECTURE.md` §2/§3, `docs/BACKLOG.md`, `HANDOFF.md` (one line), this file.
**Out of lane, disclosed, approved by the operator in the plan:** `persist_state.py` STRATEGY
(+1 line: the rotation key), `.github/workflows/auto-expand.yml` (env + `--own`),
`pipeline/verdicts.py` (`TOKENS` +2). Nothing in `pipeline/run.py`, `tests.yml`, or the other
workflows.

## Baseline (re-derived, not copied)

`python registry_health.py` at 14:2x UTC, before any edit:

```
registry: 1239 rows · 877 active · 362 parked
  native-ATS rows 437 · scrape rows 439 · discovery rows 1
census (vs cloud_state\registry_census.json): 1229 -> 1239 rows · +10 added
pools: triage_dark 223 · listing_hunt 221 · repair_extract_gap 33 · crack_walled 14 ·
       probe_candidates 127 · audit_empty_rows 266 · deep_validate 266 · orphans 1 (SeeTree)
```

Numbers I was handed that were wrong when re-counted: `CLAUDE.md`, `docs/AGENT_BRIEF.md`
and `HANDOFF.md` say **846 active of ~1,200**; live was 877 of 1,239 at the start and is
**865 of 1,239** at the end (twelve rows parked on purpose below). Those files are the
`docs` lane's — reported, not edited. `ARCHITECTURE.md` §3 said tier 2 searches with
"SerpApi" and that failures "get ONE shot at tier 2" — neither was true of the code even
before today (deferred names were re-rendered every run). §2 said whole-row replacement
"is legitimate" for `retry_unreachable._row_for` — that sentence blessed defect B.

## What today's cloud logs showed (run ids)

| # | defect | evidence |
|---|---|---|
| A | `auto_expand` rendered every aggregator-seeded name (Playwright, 17–25 s) BEFORE the 10-call LLM cap check, then discarded the result as `dfer`; the ten that got a call were parked as `scanned; no open Israel roles now` with the posting as their address | run 32828470759 08:52→10:08 = **76 wasted min**, `resolved 0 (LLM-cracked 0), empty 10, deferred 240`; identical on 32774598409 and three earlier. Queue 342, **338 aggregator-seeded** (il.linkedin.com 219, secrethunter.io 91, il.indeed.com 25). `resolve_llm`'s only search rung was SerpApi (0 until 09-01) → `(no pages reachable)` → 0/50 resolved. **28 real employers buried** (ctera, Houzz, yad2, Upwind Security, RISCO Group …). |
| B | `retry_unreachable._row_for` rebuilt the notes cell from a literal on every branch and re-selected any row still containing `unreachable`; `bd_rescue`'s validated branch appended after that token | `git show b3d1d49 -- companies.csv`: 9 rows lose `\| listing-hunt 2026-08-24: no IL listing; monitored candidate`; run 32805087216: BD `validated 4`, then 90 s later retry `recovered 0, still unreachable 9` — BD's verdict and `best_url` gone. |
| C | `tests.yml` `mutation-gate`: 108 records × the whole suite (55 s on the runner) | cancelled at 45 min on every push since `f720627`; 10 red runs today. A test red at HEAD (BACKLOG 158) would have made every mutation look KILLED. |
| D | `registry_health.py --census` printed the full `alarms()` ladder from the census step's env (no BD key, no Playwright) | run 32813499709: two permanently false `rung DOWN` lines top the operator report; the mail was unaffected (`alarms_state`). |
| E | `Tel Aviv` active on `jobs.secrettelaviv.com` | `companies.csv:1212`, 145 cached cards (BACKLOG 167 residue). |

Also seen, filed not fixed: every registry workflow prints `14 rows carry a truncated/unknown
triage mode … no-url` — `no-url` is a mode `triage_dark` writes and `listing_hunt` handles
(`seed_is_bad`) but `check_invariants.TRIAGE_MODES` lacks (BACKLOG 191, infra); Bright Data
at 111% of the free pool (BACKLOG 192, the operator's 4,500 ceiling from September).

## What was built, by commit

### `8f3bbcb` — the 02:30 chain appends instead of rebuilding (defect B)

`retry_unreachable._row_for(name, url, kind, payload, cache, note="")` takes the row's cell;
`_note(base, seg, disproved)` strips the `unreachable` segment only when the attempt
DISPROVED it, then `replace_own(base, "retry", seg)` — one marker for every branch. The
selector excludes terminal rows (`is_terminal`). `bd_rescue._reached_note` removes
`unreachable` + `bd-tried` on the validated and activation branches (BD reached the page: the
token is disproved, and it is BD's own per `TOKENS`); `wayback_rescue` and `validate_empty`'s
promote write go through `replace_own`. Measured on the builders:

```
still unreachable: unreachable; could not scan | listing-hunt 2026-08-24: no IL listing; monitored candidate | retry 2026-08-25: still unreachable
empty:             listing-hunt 2026-08-24: no IL listing; monitored candidate | retry 2026-08-25: scanned; no open Israel roles now
```

Guard: `test_activation_branches_append_to_the_note_instead_of_replacing_it` now walks
`bd_rescue`, `retry_unreachable`, `wayback_rescue` for the whole-row literal shape
(`[…, "true", <not a Call>]`). Four fixtures drive `main()` on a scratch csv incl. the chain
order `B.main()` → `R.main()`; six mutation records, one re-aimed; all killed behaviourally.

### `a2a2f94` + `63d9822` — auto-expand and the LLM tier (defect A, BACKLOG 177)

`resolve_llm._search_candidates`: SerpApi → `deep_validate.ddg` → `google_via_unlocker`, the
paid rung on its own counter `LLM_BD_SEARCH_CAP` (default 5/run; `deep_validate._BD` is
per-process with a 150 default). `_gather` returns `(evidence, n_pages)`; **no page read, no
`claude -p` call** (`LAST["asked"]` is the caller's seam). `auto_expand.main()`: an
aggregator seed skips `resolve_deep` (its render can only yield refusal/empty/unreachable —
read from `resolve_deep.resolve`), two budgets (`LLM_RESOLVE_CAP` counts calls,
`AUTO_EXPAND_SEARCH_CAP` counts entries), deferred names are never parked and print a reason
(`no-llm | cap | no-candidates | llm-none`), the queue rotates least-recently-tried first
(`cloud_state/auto_expand_seen.json`; §2's "a budget without a rotation key re-walks the same
prefix forever"), the registry is re-read before every append (rule 4). `--clear-agg-urls`
(dry-run default) un-buries rows parked on an aggregator shell: cols 2-3 blanked, note
`url-cleared <date>: <host> aggregator seed` — the shape 23 rows already had. Applied:

```
python auto_expand.py --clear-agg-urls --apply   -> 28 rows
pools before/after: audit/deep 266 -> 269, hunt 221 -> 221, orphans 1 -> 1
```

Zero-spend dry run of the new loop (`LLM_RESOLVE_CAP=0`): 250 names, 0 renders for the 246
aggregator seeds, 1m54s total (the 4 real seeds still render). Live positive control of the
ladder with the paid rung at 0: **`Upwind Security` (buried yesterday) → comeet `49.004`,
51 jobs / 15 Israel, verified through `fetch_company`, 29 s, one `claude -p` call, DDG
only.** Seven fixtures (`_expand_env` drives `main()` through `auto_expand.CSV_PATH`, which
the older row-builder tests explained they could not), nine mutation records.

Both writer detectors (`tests/test_registry._registry_writes`, `tools/mutate._registry_writers`)
learned that `fr[3] = ""` is a clearing, not a proposal — the same rule they already had for
`fr[4] = "false"`.

### `701d1a9` — the mutation gate finishes (defect C, BACKLOG 170, 104) + defect D

`tools/mutate.py`: one baseline run of the unmutated archive learns the tests red at HEAD
(deselected from every mutant run, subtracted from killers, `::warning::` each); per record a
derived subset runs first — import-graph reverse closure of the mutated module × each test's
references (function-local imports, module-level aliases, module strings, helpers to a
fixpoint, `Kills \`id\`` docstrings, optional `killers` field) — and a satisfying KILLED ends
there; anything else (green, empty, static-only) runs the FULL suite exactly as before, so
verdict semantics are unchanged. `--jobs` (default 4), `--catalogue` (repeatable),
`--skip-baseline`. Selector: 0.5 s for all records (cached), subset min/median/max
**1 / 86 / 236** of ~620, no empty subsets, longest argv 20.5k chars (Windows cap handled by
a collapse rule). The largest subsets are honest: `identity_gate` imports `bd_rescue.unlock`,
which imports `retry_unreachable.alt_urls`, so everything that gates identity is a transitive
dependent of `retry_unreachable`. Five harness guards (documented killers always selected;
aliases/helpers/strings seen through on a synthetic tree; a baseline-red test is never a
killer; the fallback fires on green/static-only and the subset ends only on a satisfying
KILLED; ids unique + argv length).

`registry_health.py --census` no longer probes the ladder (`_report(..., ladder=False)`;
`alarms_state` only); no `SERPAPI_KEY` reads as such; an unknown flag exits 2 (BACKLOG 44).
`repair_extract_gap` counts a refused row once (BACKLOG 45).

### `bebbee9` — the rows (BACKLOG 76, 133, 167) and a hole the rows exposed

Re-verified live through `fetch_company` before writing: Qualcomm eightfold **37 IL**, GE
HealthCare phenom **23 IL**, Fortinet oraclehcm **15 / 503**. Converted. Ten same-identity
twins parked `alias-of <survivor> 2026-08-25: identical board URL`: Hippo Insurance→Hippo,
Vayyar Imaging→Vayyar, SpearUAV→Spear UAV, GenCell Energy→GenCell, Crazy Labs→CrazyLabs,
Siemens EDA→Siemens, one zero + ONE ZERO Digital Bank→ONE ZERO BANK (the one with an open
role), kornit→Kornit Digital, Primis Tech→Primis. Comeet `87.00C`: the slug page
`comeet.com/jobs/scopiolabs/87.00C` renders, every Sckipio slug redirects to the vendor root
→ it is Scopio Labs' board and Sckipio was scanning it; Sckipio un-addressed (`url-cleared …;
no listing found`, hunt-owned). `Tel Aviv` parked `redundant: not a company (city name …)`
— terminal; `--explain` shows every pool False; 145 cards dropped from `scraped_cache.json`.
Written by one re-read/match-by-name/atomic pass through `pipeline.notes.append`.

**The pools diff caught a B1-class hole the moment the rows landed:** `repair_extract_gap`
33 → 34. Its selector was `MODE ∧ http` with no terminal exclusion, so the 19:00 activating
run would have selected `GenCell Energy` — now `alias-of`, board works — off its own
extract-gap stamp and re-activated the duplicate. `in_extract_gap_pool(r)` now owns the
selector (terminal + recruiter excluded), `main()` selects with it, `registry_health` imports
it; fixture + `extract-gap-terminal-drop`. End state `877 → 865 active, 0 orphans`, pools
`triage 224 · hunt 222 · extract-gap 32 · crack 14 · probe 127 · audit/deep 270`.

## §2 / §3 — what changed and why

Corrected in place (each against the code or a run above): the state-transition diagram
(aggregator seeds deferred, never parked); the verdict corollary (a tool may remove a token
it disproved and owns, with the `b3d1d49` proof); the three pool spellings (`TOKENS` now a
superset — the two one-liners print `[]`/`[]` and `46 1`); "whole-row replacement is
legitimate" deleted and replaced by the current truth and a derivation grep; the ownership
matrix row for `repair_extract_gap`; the 02:30 chain named as an activating, non-`pools()`
selector; `in_extract_gap_pool` added to the guarded constants; the `weak` grep carries the
command (8 lines today) instead of a stale count; §3 tiers 1–2 rewritten with the caps,
the deferral reasons, the rotation key and the live control; the ladder bullet names
`resolve_llm`'s rung and its month-long no-op. Verified and left: the four gates
(`GATE_CALLERS` has 4), the 24-writer grep (24), cap 220, `_stale_hunt` 14d / `_revalidatable`
30d / `_recrackable` 1d, `git log --oneline -- companies.csv | wc -l` → 82 (the doc carries
the command). Claims marked unverified: none in the two sections.

## Spent

Bright Data: **0** new credits (the new rung ran with the cap at 0 locally; the 76
verification used the fetchers, not the unlocker). `claude -p`: **1** (the Upwind Security
control). SerpApi: 0. No workflow dispatches or cancellations. Registry writes: two
(`--clear-agg-urls --apply`, the 15-row pass), both outside 05:00–08:30 UTC, both preceded by
a fresh read and followed by `check_invariants` (clean) and a pools/orphans diff.

## Adversarial waves

See the section appended below once the waves returned.

## Wave 1 — three Opus attackers (write paths · the harness · pools/ownership), all read-only

Bar stated up front: B1 wrong write · B2 silent coverage loss with named rows · B3 a
one-token edit on a changed line leaves the suite green · B4 a silent crash behind
`continue-on-error`. Everything below was reproduced by the attacker AND by me before it
counted; fixed in `14357f5` (+ the fixture follow-up that pinned the embed gate).

| # | bar | finding | fix |
|---|---|---|---|
| A1 | **B1** | `validate_empty` (Sun 04:00, activates) selected on the bare `no open israel roles` substring over every row — no `active`, no terminal filter. Driven on `main()`: **Primis Tech, kornit, Tel Aviv** (all parked today) re-activated, `check_invariants` rc 0. | `in_validate_empty_pool` (parked ∧ http address ∧ not terminal ∧ not recruiter); in `pools()` (28 rows) |
| A2 | B1 | `bd_rescue` (02:30, paid) and `wayback_rescue` (Sun) selectors had no terminal exclusion either — the exclusion `8f3bbcb` gave `retry_unreachable` was left out of its two siblings. | both exclude terminal rows |
| A3 | **B1** | The search ladder puts other companies' pages into the evidence; `_slug_matches` is a 5-char prefix and passes any Comeet uid. `Similarweb → greenhouse similartech`, `Sunflower Sustainable Investments → Claroty's F2.004`, both verified with real jobs; 1,303 cross-accepts measured over active tokens. | `_verify` requires the token in the HTML of a page that is the company's OWN (not an ATS vendor host, not an aggregator, `is_foreign` False) + `embedded_board_ok` on non-Comeet platforms; `_gather` keeps the pages it read |
| A4 | **B3** | `tools/mutate.py`: a node id pytest cannot resolve (ghost `killers`, ghost class method, a test only the working tree has) → rc 4, **zero tests run**, `_verdict` said `KILLED unknown` — final, and it bypassed the behavioural rule. Reproduced with a comment-only mutation + one ghost killer: `killed`. | rc 2/3/4 or an unnamed red → `UNSETTLED` → full-suite fallback; a full-suite UNSETTLED is a harness FAIL; subset ids validated against the archive; a ghost `killers` id is a loud catalogue error |
| A5 | med | Baseline exclusion by bare name retired a whole parametrised test on one red case; a param with a space truncated the deselect; the argv cap was measured before the deselects; `--skip-baseline` reported a comment edit as killed by the red test. | full-id exclusion, `_parse_failures` keeps the bracket, argv measured whole, a loud warning |
| A6 | med | Both writer detectors were blind to `fr[3] += url`, `fr[3:4] = [url]`, `fr.__setitem__(3, url)`, `fr[4] = flag` — the `fr[3] = ""` exemption made the first exploitable in two statements. | one shared `_row_write`, exercised shape by shape; both enumerations asserted equal |
| A7 | B4 | `bd_rescue`'s give-up counter regex was `$`-anchored; retry's segment now follows it, so `x3` never came: ≤5 unlocks × 9 rows every 8 days, forever. | unanchored |
| A8 | B3/B4 | `AUTO_EXPAND_SEARCH_CAP` unpinned; `resolve_llm`'s retry doubled a "call" the budget counted as one; a non-string value in the rotation key would `TypeError` the (non-`continue-on-error`) expand step; `clear_agg_urls` read only the first segment. | pinned; `LAST["calls"]` charged; values coerced; every segment |

Also from the waves, measured and filed: `url-cleared` adds ~50 chars and the oldest
segment (`scanned; no open`) is evicted after triage+hunt+deep-validate — which only
matters to `validate_empty`, and those rows have no address to validate (BACKLOG 197);
`Sckipio`'s "is Scopio Labs'" evidence lives only in the note and is evicted in four
nights — a negative declaration in `identity_facts` is the durable form (198); a
url-cleared row in the hunt costs one unlocker search on a DDG-empty night, bounded by
`DEEP_BD_SEARCH_CAP` (199); BACKLOG 84's live population is 103 of 199 (restated); the
`recruiter` substring hazard (72) now also touches retry's selector; the note cap can evict
a terminal token on 16 named alias rows (no terminal-blind selector reaches them today);
`pool_floor` prints nothing for today's 877→865 (deactivations are in the census diff, not
the mail). What the attackers verified sound: rotation, the dupe guard, the ladder caps,
the evidence-free skip, the 02:30 chain keeping BD's verdict, comma/quote names, 38 of 44
hand-aimed one-token edits on the changed lines killed before the fixes.

Selector deltas on the real registry after the fixes (every drop accounted for):
`validate_empty` 86 → 28 = −25 active rows (a cross-validation of an active row is a
no-op at best), −4 terminal (the B1 above), −29 address-less (nothing to validate);
`bd_rescue` / `wayback_rescue` 9 → 9 (no terminal-and-unreachable row today). Live through
the grounding rule: RISCO ✓ (own page), Similarweb → its OWN `similarweb` board 69/27
(the attack case resolves to the right board), Upwind ✓ via the own-page Comeet read
(refused by the static-HTML rule alone — the uid lives on comeet.com). Spend for the
waves: 5 sonnet calls, 0 Bright Data.

**Batch 2 (`cff7ee3` + follow-up):** `crack_walled.listing_urls` offers the native
`eightfold` (`/api/pcsx/search?domain=<company domain>`) / `phenom` (`/widgets`) endpoint
first and `crack_one` verifies it through `verify()` like oraclehcm (77; phenom's POST-only
widget still fails `ok_to_write`'s page read — the human-URL mapper of the path-tenant batch
settles it); Questar → `scrape` on its applytojob page (79: `scrape_universal` extracts 4
Herzliya roles); Splunk (Cisco), HP Indigo, Habana Labs (Intel), VMware (Broadcom) parked
`alias-of` their parent (194; 865 → 861 active, orphans 1); the LinkedIn slug rung (178) is
built and **off by default**: a guest GET of the company page carried no `about_website` link
for fiverr / riskified / upwind-security (3/3 empty, 0.6–0.9 s each) and each GET would
compete with discovery's LinkedIn budget on the runner. Measured, not changed: Outbrain's EU
Greenhouse board renders **0 job links** in Chromium (80); Ericsson `BoardEmpty` 0 worldwide,
Deutsche Telekom 404, PayPal 0/75 — nothing to convert (78); 196/199 are one week of run
logs away. Records: `crack-eightfold-api-drop`, `expand-slug-seed-drop`, each killed by its
fixture in an isolated archive of HEAD.

**Batch 3 (`03ec78b` + the harness commit) — durable pools.** Pools key on row FACTS:
`probe_candidates.in_probe_pool` = parked + http non-aggregator address − junk − terminal-row
(127 → 228; one simulated all-failing hunt night leaves 228, was 127 → 20); the 02:30 chain
has one selector (`in_retry_pool`, 9 rows, in `pools()`); `validate_empty` is the probe's rows
minus walled hosts with the empty-class token arm today and the probe's `ever` fact behind
`VALIDATE_EMPTY_SIGNALS=1` (28 → 29 → 93 with the arm). `verdicts.TERM_RX` word-bounds
`recruiter`; `is_terminal_row(r)` asks the name. `notes.append` and the conflict merge never
evict a **protected** segment: terminal tokens, `unsupported ATS` (the crack pool's fact),
`dark-triage <mode>` (extract-gap's and triage's own), `no open Israel roles` (validate_empty's,
by its stable substring because older rows carry it as a head-cut fragment). The probe writes
`ever` (set once, never cleared) because one quiet night overwrote 64 "durable" baselines with
zeros. Bit's note carried `unsupported ATS` inside triage's own reason text — fixed by
`replace_own`, not by a harness exception.

`tests/rehearse_registry.py` (BACKLOG 52): every scheduled tool's real `main()` and note
writers over a copy of the registry, sockets forbidden, each per-row verdict seam stubbed at
the tool's own function, a fake clock, the real UTC schedule, `worst`/`mixed` policies,
`--trace NAME`. What it found before the fixes, in order: crack 14 → 9 on night one (27);
probe "durable" baseline erased by a quiet night; the Sunday deep stamp evicting the
extract-gap and empty-class facts; a policy artefact (deep's `unsupported` is a MOVE, not
erosion; triage's eroding verdict is "the same mode again"); Bit. At `e1b55d7` the same
harness lost 19 probe rows and 6 crack rows on night one. At HEAD: **14 nights worst and 14
nights mixed, every pool flat, 0 orphans, no floor alarm, 30 s per run.** Two nights over 200
rows run in pytest; the 14 run in `tests.yml` (one infra line, disclosed).

**Batch 3 wave 1 (`d17fe7a`) — two Opus attackers, both NO-GO on `0a32bf7`, every finding
reproduced by me before acting.** Above the bar and fixed: (1) `mixed` seed 1 failed night 5 —
`append` cut a newcomer to `crack-walled <date>: ` when protected segments filled the cell and
check F blocked the digest (the same cut on the merge's conflict path reproduced the 87-row
`page-empt` bug) → one rule: a protected segment is never evicted and nothing is ever sliced;
when only protected segments remain the newcomer is dropped whole (the full suite caught the
alternative — letting the oldest fact yield — evicting `no open Israel roles` on 12 rows). (2) The probe's wake deleted the
very `dark-triage` fact the batch protected: 39/39 extract-gap and 115/228 triage rows would
leave their pool on the first wake → `_wake_note` strips only `listing-hunt`/`crack-walled`/an
older wake. (3) `probe-woken` was permanent — `triage_dark.main()` skipped it forever while
the ownership matrix counted the row as owned (6 rows: NeoGames, Kirobo, Sight Diagnostics,
Lasso Security, Boolzai, Simply) → the stamp is dated, the hunt's page-empty rule yields to a
wake at least as new as the triage stamp, the hunt consumes the stamp, the exclusion in
`main()` is gone; all 6 are back in tonight's schedule. (4) The harness pinned
`VALIDATE_EMPTY_SIGNALS=1`, which no workflow sets — under production's value the Sunday deep
stamp evicted Enzymit's `empty-but-suspect` and the row left the one pool that re-checks it →
`empty-but-suspect`/`cross-validated` protected, the harness runs production's flags
(`--signals` opts in). (5) `repair_dead_urls` (19:00, rewrites col 3 — the whole fact of the
probe/retry pools) and `wayback_rescue` were scheduled writers the harness never ran, and DNS
escaped `_forbid_sockets` → both scheduled with their seams stubbed (`candidates()` is the
search ladder; stubbing it took a night from 62 s back to 2 s), `getaddrinfo`/`gethostbyname`/
`connect_ex` banned. (6) `deep_validate` → `deep_validate rung` renamed the census key and
skipped that floor silently → label keyed `deep_validate` again. (7) No alarm on pool GROWTH
(127 → 228 re-baselined silently) → `pool_growth` in the mail. (8) check D/E's `_fact_owned`
was a looser mirror of `in_probe_pool` (233 vs 228) → it imports the predicate. (9) Seven
one-token defeats (attacker 2, B3) → records + fixtures: `append-newest-first`,
`protected-mode-empty`, `harness-retention-off` (the harness's own `REHEARSE_SELF_TEST=overwrite`
control must FAIL), `merge-trim-ours-head`, `merge-slice-restore`, `fact-floor-drift`,
`validate-empty-arm-and`, `protect-suspect-drop`, `harness-dns-open`. Stale records:
`probe-pool-match` deleted (its target `PROBE_POOL` no longer exists), `terminal-keep-remove`
re-aimed. Clean: no row lost all ownership (SeeTree gained one; Yael Korentec left the probe
pool correctly — its address is an aggregator), `\brecruiter\b` behaves on every live note,
Census: probe 126 → 228, retry 9, validate 26 → 27. Filed, not blocking (200–205): the
suspect-note guard on 67 signals-arm rows, `ever` from false signals and the 11 constant-signal
Comeet rows, 17 aggregator/agency addresses + a PDF in the probe pool and 57 known-wrong
addresses probed nightly, probe throughput (~130/night, 97 without a baseline), the weaker
`mixed` invariants, terminal-row saturation (accepted). After the fixes: worst 14 nights and
mixed seeds 1–5 flat (27 s each), `--signals` flat too, invariants 0 errors.

**Batch 4 — the third state (Design B).** `identity_facts.not_tenants` (11 negative
declarations, each a recorded wrong write, with evidence; `validate()` refuses a row ACTIVE on
its own negative — declaring forces the park) and two positives (Deutsche Telekom
`telekom-growthhub`, NVIDIA `nvidia`+`mellanox`). `identity_gate.board_vouches` → True/False/
None; `checkable_token` reads a `scrape` row's slug from its URL (24 active rows store the URL
in column 2); `human_board_url` maps eight API shapes by string and learns Comeet's from the
endpoint's own `url_comeet_hosted_page` (measured: `comeet.com/jobs/x/49.004` is a generic
200, `jobs/upwind/49.004` names Upwind); `activation_verdict` → `ok|empty|not-listing|not-ours|
unverified` (a declared negative first, then a held readable page, then the vouch, then ONE
human-page GET, else deferred) and `write_verdict` → `ok|not-ours|unreadable`; `activation_ok`
/ `ok_to_write` are their boolean views (signatures, `GATE_CALLERS` unchanged).
`audit_empty_rows._slug_matches` = `board_vouches(...) is not False` (Bancor/bancorpbank now
refused by declaration; Ibex/ib1 "cannot tell", not refused); `deep_validate` gates on the
verdict and stamps `not this company's board` only on `not-ours` (else `unverified (no
readable page)` / `verified 0 jobs (empty board)` / `not a listings page`); `crack_walled`'s
two write sites stamp `unverified (page unreadable)` on unreadable; `validate_empty` prints
`[??] … deferred` and writes nothing on `unverified`; `apply_resolved`'s veto is `is_foreign or
board_vouches is False` on every row; `resolve_llm._verify` refuses a declared negative before
grounding; `check_invariants` C3b warns with the 28 active path rows whose tenant cannot vouch
(the hand-check list); `--explain` prints the vouch, the human URL and the negatives.
**Census A: `tenant_is_this_company` / `embedded_board_ok` / `is_walled` identical on the 521
ATS-host rows (one named delta, Deutsche Telekom, declared); `activation_ok` with NO readable
page: 197 of 457 active rows admit → unverified — the human-page read replaces the vacuity,
one GET each on re-activation (numbers in the decision record).** Item 22:
NanoLock Security, Sight Diagnostics, Lili `url-cleared` (negatives declared); the rest left
or declared as recorded in the decision record ("The third state"). Records: `facts-not-
tenants-*` ×2, `vouch-*` ×3, `tenant-neg-drop`, `embed-neg-drop`, `human-url-*` ×2,
`activation-*` ×4, `crack-unreadable-stamp`, `validate-empty-unverified-stamp`,
`apply-resolved-vouch-drop`, `c3b-warn-drop`; `apply-resolved-parked-scope` re-aimed. Closed
33, 37, 50, 198, 22, 9-half; 21/61 updated. Deliberately not: a tenant veto, API-endpoint page
reads, `_tenant_near` changes (69), the display-name column (58/61).

**Batch 5 (117, registry half).** `triage_dark.llm_page_verdict` asks the page judge through
`pipeline.llm.call_json` with triage's own schema (`is_careers_page_for_this_company`,
`open_roles`, `note`) and system prompt; `LLMUnavailable` → `None`, the regex verdict stands.
No live control spent: the seam is the one `resolve_llm`/`listing_hunt` already run nightly.

**Confirmation wave (1 Opus, on `59fc85b`) — NO-GO, fixed in the follow-up commit.** R1 (B1):
`_slug_matches` had become vacuous — its five call sites pass two arguments, and with no host
`board_vouches` answered True, so `audit_empty_rows.main()` (still on `tenant_is_this_company`,
vacuous on path platforms) would have WRITTEN `CyberArk -> paloaltonetworks` / `Sckipio ->
scopio` / `Perion -> taboola` on Sunday (27,104 of 39,800 name×token pairs refused before, 0
after). Fixed: no address = `None`; the audit's write is `activation_verdict` (the human-page
read settles serp noise), `deep_validate`'s two call sites pass the URL, GATE_CALLERS lists
`audit_empty_rows` under `activation_verdict`. R2/R3 (B2): "Census A 0 deltas" was true for
three gates and false for `activation_ok` — corrected above; `validate_empty` returned the
deferral as `confirmed` (the shape its own comment forbids) → its own `deferred` verdict, counted
and printed; BACKLOG 200's suspect-note guard removed (the token is protected now). R4 (B3):
`write_verdict`'s human-page redirect was a live one-token defeat → record + fixture. R5 (B4):
`_comeet_human_url` raised on a malformed payload → inside the try; `deep_validate`'s write
wrapped. R6: `checkable_token` returned `lever`/`v3` (host first) → last candidate, and the
Comeet API uid is the checkable token (so `Sckipio`'s negative fires on its own address). R7:
`_NEGATIVE_IDENTITY` now covers all 12 declared tokens and a fixture keeps the two in step.
R8 (docs contract red) did not reproduce locally: `check_docs` is green at every commit here.
Push: `f881713` rebased over the 19:00 cron commit (`companies.csv` conflict resolved with
`merge_csv_rows.py base ours theirs`: 3 rows applied, the cron's 9 activations kept).

### As written in HANDOFF.md, 2026-08-25 (`registry`)

*Moved here verbatim on 2026-08-27 by the `docs` lane, because a whole
session written as one 3,000-character line defeated HANDOFF.md's cap.
sha256(first 16) of the line as it stood: `6596c2f714b60938`.*

- **2026-08-25 `registry` (today's logs, the pending backlog, §2/§3 re-verified — 3 Opus attackers + 1 confirmation wave):** auto-expand rendered every aggregator seed (338 of 342 queued) for 17–25 s AFTER the LLM cap was spent (76 wasted min/run ×2 daily, `resolved 0` on 5 runs) and buried the 10 it did try under the posting's URL; the 02:30 chain erased listing-hunt's and Bright Data's segments on 9 rows nightly (`b3d1d49`); the mutation gate was cancelled at 45 min on every push. Shipped: aggregator seeds deferred + rotated (`cloud_state/auto_expand_seen.json`), `resolve_llm` ladder SerpApi→DDG→unlocker (`LLM_BD_SEARCH_CAP` 5) asked only with a page in hand (live: Upwind Security → comeet 49.004, 51/15 IL), 28 buried rows un-addressed; `retry_unreachable`/`bd_rescue`/`wayback`/`validate_empty` append instead of rebuilding the cell; `tools/mutate.py` derived subset + baseline-red exclusion + `--jobs`; census step no longer probes the ladder; rows: 76 converted (Qualcomm 37 IL, GE HealthCare 23, Fortinet 15), ten `alias-of` twins + Sckipio + `Tel Aviv` parked (877→865 active), `repair_extract_gap` gained the terminal exclusion it lacked. **Out of lane, disclosed:** `persist_state` STRATEGY +1, `auto-expand.yml` env/`--own`, `verdicts.TOKENS` +2. Wave 1 (3 Opus attackers) found and I fixed: `validate_empty`/`bd_rescue`/`wayback_rescue` selectors with no terminal exclusion (would have re-activated Primis Tech, kornit, Tel Aviv on Sunday), the LLM resolver admitting another company's board off search evidence (Similarweb→similartech; now grounded on the company's own page, Comeet via a browser read), a ghost node id reading as a KILL in the harness, and the resolver's bare `claude -p` (now `pipeline/llm.py::call_json`, sonnet, tool-less, schema). 2026-08-26 batches 0–1 (`d4e4cfc`, `8a4deac`): 92/62/63/74 + 7 mislabeled closures; the Saturday deep-validate cron is now the Sunday audit's Chromium rung (6), the audit's rotation key is committed (38/164), suspect verdicts re-arm the hunt (65), apply_resolved vetoes parked rows (56), the unlocker reports its error code and policy-closed hosts are never retried (110), corrupt caches are never overwritten (156). Batch 2 (`cff7ee3`): eightfold API rows from crack_walled (77), Questar a scrape row (79), 4 parent/subsidiary twins alias-of (194, 861 active), the slug rung built but off (178, 3/3 empty live); 78/80 measured, nothing to convert. Batch 3 (`03ec78b`+): pools key on row facts (probe 127→228 and flat under an all-failing night; the 02:30 chain in `pools()`; `validate_empty` staged on the probe's `ever`), `recruiter` word-bounded, protected segments never evicted; `tests/rehearse_registry.py` proves 14 nights flat (worst + mixed, 30 s) and `tests.yml` runs it. NOT finished: batches 4–5 (path-tenant 33/50/22/37/198/9/51; 117; 196/199 after a week of logs). Morning check 2026-08-31 (Sunday): `deep rung: N of M dark rows` in the audit log and `cloud_state/audit_seen.json` in that day's state commit. Morning check 2026-08-26: `tests.yml` mutation-gate green under 15 min; 02:30 log `validated N` with those rows keeping `scanned via brightdata`; 08:00 auto-expand < 10 min with `dfer (<reason>)` and `LLM-cracked N`; digest census step without `rung DOWN`; mail `Registry:` line = SeeTree only. Record: `docs/sessions/2026-08-25-registry.md`. **Batch 3 wave 1 (`d17fe7a`):** two attackers, both NO-GO on the pushed batch — the protection itself cut a newcomer (`crack-walled <date>: `, check F blocked the digest; mixed seed 1 night 5) and the probe's wake deleted the protected `dark-triage` fact (39/39 extract-gap rows) while `probe-woken` was permanent (6 rows out of triage forever); fixed: nothing is ever sliced (a fact yields whole first), the wake keeps facts and is dated + consumed by the hunt, the harness runs production's flags/bans DNS/schedules `repair_dead_urls` + `wayback_rescue`, CI runs mixed seeds 1–5. **Batch 4:** "cannot tell" is a third state — `identity_facts.not_tenants` (11 negatives + Telekom/NVIDIA positives), `board_vouches` True/False/None, `human_board_url` (Comeet's learned from its positions), `activation_verdict`/`write_verdict` with `unverified`/`unreadable` that stamp no claim; Census A 0 deltas on 521 ATS rows; item 22's three wrong addresses cleared. **Batch 5:** triage's page judge through `pipeline.llm.call_json` (117 registry half). **Confirmation wave:** NO-GO caught and fixed — `_slug_matches` had gone vacuous at its two-argument call sites and the Sunday audit would have written a serp-noise board (CyberArk→paloaltonetworks); the audit's write is now `activation_verdict`, no address = cannot tell, `validate_empty` counts its deferrals, BACKLOG 200 closed, 206 filed (197 active rows now re-activate only via a human-page read). NOT finished: 200–205 (preconditions for `VALIDATE_EMPTY_SIGNALS=1`), the 28-row C3b hand-check, the first Sunday's `[??] deferred` count.

