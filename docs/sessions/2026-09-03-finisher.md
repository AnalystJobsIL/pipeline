# 2026-09-03 — finisher: the gate reads the posting, and the last four defective published rows

Primary lane `classifier`, with explicit dispensations for `jd-text` (item 3), `company-intel`
(item 4) and `roles` (item 5). One commit of code and state, one of docs. **~22 LLM calls
(`claude-sonnet-5` through `pipeline/llm.py`), 0 Bright Data credits** — the declared budget
was ~10 and the overrun is named in §7. Four commits, and one adversarial wave whose
findings are §8.

Context: the operator's acceptance test passed on 2026-09-03 with **146 published rows, 142
fully clean**. These five items were the last four defective rows plus one approved-but-unshipped
gate phrase.

**Three things the spawn brief asserted did not survive checking**, and each changed the work.
They are in §1, §3 and §4 rather than in a footnote, because in every case the brief's version
would have produced a change that looked right and did nothing.

---

## 1. Items 1 + 2 — the description appeal, and the number that did not reproduce

### The reopening condition, quoted before anything was written

`542@classifier` gates the description appeal on **"a `classify:` line whose `re-judged N/cap
250` is under its cap"**. Digest run **33739498960** (`schedule`, 2026-09-03) logged
`contract v3.0f84ab84 re-judged 51/cap 250 + 18 stale-yes/cap 150`. Met, with the number.

### What shipped

`_desc_appealed` in `pipeline/seniority.py`, beside `_GATE_APPEAL` so one grep finds both.
`_relevance` grew a third, **defaulted** parameter (`desc=""`) consulted in both refusing
branches; the two classify heads pass the description they already hold (`_classify` reads
`_relevance` at the top and does not touch `job["description"]` until eighteen lines later);
every other caller — `enrich_scrape_jd.py:173,295`, `pipeline/jdfill.py:2621`, three
`tools/*` — passes a title alone and is unchanged by the default. `_gate_appealed` learned the
same arm, so `_sig_accept_nollm` still refuses to accept an appeal-only row without the LLM.

**0 Bright Data, and it is structural rather than promised.** `enrich_scrape_jd.py` skips a
card that already `looks_like_jd` at `:143` — `continue` — *before* it asks the title gate at
`:173`. A rule that only ever fires on a card which already carries text cannot create one
fetch candidate. That is precisely what makes this cheap where `542`'s rejected
`business operations` title phrase would have created 6.

### Measured, on both committed caches, after the wave (see §8)

4,971 cards · **4,546 refused by the gate** · **1,416** of those carrying text
`looks_like_jd` accepts (1,503 clear 300 characters — the gap is the point, see below).

**ONE arm**, and every conjunct has a number: the posting says
`data analytics｜data analysis｜ניתוח נתונים` **and** names an output **and** names a tool.

| predicate | cards | pairs |
|---|---|---|
| **shipped** — phrase AND output AND tool, not ML | **23** | **23** |
| dropping the tool word | 52 | 52 |
| dropping the output word | 28 | 28 |
| the phrase alone | 83 | 83 |
| soft words with no tool word (never an arm) | 377 | 377 |

All 23 land on `signal`; **none on `strong`**; `none→signal` 19, `excluded→signal` 4; all 23
pass `looks_like_jd` and `is_israel_job`. **0 of the 252** title-only golden rows move —
structurally as well as measurably, since `test_classify_keyword_tier_matches_the_golden_fixture`
calls `classify` with no description. `CONTRACT` unchanged at **`v3.0f84ab84`**.

**Cards and pairs are printed side by side on purpose.** This lane has now conflated them
three times; the wave caught the fourth (arm B's "27 cards" was 28 cards / 27 pairs).

### The floor is `looks_like_jd`, and that is what makes the rule free

The first draft floored on `MIN_DESC` and claimed, in three documents, that the rule cost no
Bright Data because `enrich_scrape_jd.py` skips a card that already `looks_like_jd` at `:143`
before the gate at `:173`. **The wave showed the argument did not close**: 2 of the cards a
length floor admitted do NOT pass `looks_like_jd`, so they reach `:173` — the outcome was
still 0 BD, but only because `:173` passes a title alone, which is a different fact than the
one three documents asserted.

Both are fixed by one change. The floor is now `looks_like_jd` — the same predicate `:143`
skips on — so the property is structural: a rule firing only on cards that predicate accepts
cannot create a fetch candidate. And it removed the two worst movers: `Velotix | Marketing
Manager` and `aQurate | Data Engineer` were **4,000 characters of a careers site's own menu**
(*"Integrations BI Tools Connect Tableau, PowerBI, Qlik"*), which satisfies all three arms and
is not a job description. `_classify` records the identical migration thirty lines below,
where `has_text` stopped being `len(raw) >= MIN_DESC` for exactly this reason; the first draft
of this rule reintroduced the bug the same file had already paid to fix.

### The marker arm: designed, measured, and REFUSED

The brief handed it over as approved at **+59 LLM candidates**. Three things then happened.

1. **The figure did not reproduce in any form** — marker-alone 146 cards / 145 pairs, marker +
   `_DESC_ANALYTICS` the same, marker + output 75, two distinct markers 28 / 27.
2. **Its exclusive population had no role in it.** The 8 most plausible arm-B-only cards were
   judged through the production seam under `v3.0f84ab84`: **8 of 8 NO**, including
   `aQurate | BI system analyst` — the analytics-engineer title `542` names as its own class.
3. **Two more independent measurements landed the same day.** `568@classifier`, filed by the
   `discovery` session, judged the **30 marker-densest** gate-rejected postings: **0 in
   scope** — and `568`'s own text says *"Do not build the demotion before running it"*, naming
   exactly the measurement this session ran. And the provenance of `+59` turned out to be 59
   candidates of which **2** were confirmed real roles: Zoll, which the shipped arm catches,
   and `Elbit | Senior Data Product Owner`, which **`542` already records as a correct
   rejection**.

The operator reaffirmed the arm once on this lane's numbers alone, then **dropped it** when
`568` and the provenance of `+59` were put beside them. Marginal yield: ~0 roles for ~18
recurring LLM reads. The refusal is recorded in the source, in `542`, in `568` (now answered),
and in `tests/fixtures/classifier/2026-09-03-desc-appeal.json` — pinned by a test that asserts
against the CODE, not only the file.

### The hazard the design had to answer: the gate reads the text before anything knows whose it is

`_classify` calls `_relevance` at the top; the **shared-text guard is eighteen lines later**
(`pipeline/seniority.py:934-946`), and it is the first thing that knows a description is a careers PAGE stored as
every posting's text. Six companies were in that state on 2026-08-28 and Get SAT had ten roles
sharing one 4,000-character blob. Unguarded, one blob containing `data analytics` and `SQL`
would demote **every** refused title at that employer.

Reordering was rejected on a measured risk, not a preference: hoisting the guard above the
gate lets a *refused* row claim `self._text_owner` first and blank a *passing* role's
description — a live regression bought for nothing.

What shipped instead: **an appeal whose evidence was another posting's page is not an appeal.**
When `shared` fires, a title that is `signal` ONLY because of `_desc_appealed` takes back the
hearing and gets the refusal the gate would have given on the title alone. It is asked as
*what would `_relevance` have said with no text* — never "did the description arm fire" — so a
title carrying its own analytics signal keeps its hearing. Both heads, because the comment
above `judge_backfill`'s deterministic head requires them to stay in step, and the backfill is
the arm more likely to publish a `signal` cell for a row it then rejects.

### The guards, and each verified RED before it was trusted green

**Nine** mutation records, every one verified RED against the mutation it names — five of
them added because the wave found five mutations of this code that the first draft
SURVIVED with the whole suite green:

| mutation | killed by |
|---|---|
| the appeal removed | `..._routes_the_third_measured_false_negative_to_the_llm` |
| promoted to `strong` on the `none` branch | the same |
| the hard-exclude branch loses the appeal | the same |
| `_gate_appealed` loses the description arm | `..._is_never_accepted_without_the_llm` |
| the shared-text un-fire deleted (live head) | `..._takes_back_the_hearing_its_text_bought` |
| the same, backfill head | the same |
| the floor made a length again | `..._reads_a_posting_and_not_a_page` |
| the tool conjunct dropped (23 -> 52) | `..._has_a_posting_that_fails_it` |
| the output conjunct dropped (23 -> 28) | the same |
| the un-fire hard-coded to `none` | `..._takes_back_the_hearing_its_text_bought` |
| the backfill's two counters dropped | the same |

**`tools/guard_kill.py` named a seventh test CANNOT-FAIL, and it was right.** A standalone
`test_the_arm_b_measurement_is_kept_where_the_next_session_will_find_it` read the fixture and
asserted nothing about the code, so reverting every non-test file could not make it fail --
the `480@jd-text` shape exactly. **No mutation record can close that one**: a pure data
assertion has no production change that breaks it. So the artifact became a live guard
instead, folded into the test that already kills the arm-B record and now asserting what the
fixture actually claims about the code -- that all 8 rows are still refused ON THEIR TITLES,
so the REFUSED marker arm is the only thing that would have reached them. `guard_kill --base origin/master` then
read **KILLS 6, CANNOT-FAIL 0**. It also cost a commit to find: the job does not run under
`pytest` and reads `git archive HEAD`, so an uncommitted fix is invisible to it -- this repo
has now paid for that three times.

The "never `strong`" assertion is written as an **outcome over titles from both refusing
branches**, not as `_relevance(...) != "strong"` — that form is vacuous for a hard-excluded
title, which is exactly what an adversarial pass caught in the 09-02 guard.

**Five mutation records added — the classifier lane's first.** `tests/mutations.json` held 293
records naming 42 files and **not one of them `pipeline/seniority.py`**, so the lane had no
mutation-gate coverage at all. Each new record names its killer and was verified singly.

---

## 2. Item 3 — Prisma, which was not one write and had already been repaired once

**The brief's premise was out of date, and the reason matters more than the row.**

| ref | sqlite `matched.description` | `roles.jsonl` `desc_len` |
|---|---|---|
| `4bfc4ae` (`jd-text`'s repair, 09-01) | **2,617** ✅ | 3,276 — never updated |
| `1eb7391` (cloud digest, 09-02) | **3,276** ❌ reverted | 3,276 |
| `origin/master` this morning | 3,276 ❌ | 3,276 |

The 09-01 repair was made and **the next digest undid it**: `reconcile` → `better_description`
keeps the LONGER of two texts that both pass `looks_like_jd`, `daily-digest.yml` runs
`Ledger.open_sync` *after* `enrich_matched_jd.py` in the same job, and the ledger's copy was
never written. A repair to one store is not a repair.

**And it would not have refilled today.** `llm_cache` held a *cached* rejection for that exact
text (`jdq1|ddbee0b8…`, verdict 0). A cached rejection carries no class — `llm_cache.verdict`
is a bool — so `refuted` never opens, `_store_text`'s length ratchet stays shut (2,617 <
3,276) and `_donor_pass` skips the row. The mechanism was jammed by its own cache.

**A content rule cannot separate the two texts, and this was checked rather than assumed.**
`quality_suspect` returns `no-company-echo` for **all five** Prisma cards *including the
correct one*, so the obvious guard — prefer the text that echoes its employer — is dead. The
only thing that knows is that the right card sits at the row's own url, which
`_donor_candidates`' `cache` class already tests and `reconcile` cannot see.

**What was done, in mechanism:** one stale `jdq1` verdict cleared → `JD_BD=0
enrich_matched_jd.py` → the tier answered `not a complete posting (no-company-echo, 3276
chars) -> back in the todo`, `refuted` opened the ratchet, and the donor `cache` class
installed the row's own **2,617**-character card as
`ok:cache:www.prismaphotonics.com` → the ledger's stale text retracted so `_absorb` re-derived
`desc_len` and `desc_sha1` from the repaired store.

Rehearsed on a copy first, and the diff is the claim: **exactly one row's description changed**
in sqlite (3,276 → 2,617, sha `cb390bcd…`), **one record in `roles.jsonl`**, **one in
`roles_text.jsonl`**, one `llm_cache` row added, 0 removed, 0 Bright Data. Then proved stable:
**two further `open_sync` runs left it at 2,617** — the thing that failed on 09-02.

Run in a full copy of `cloud_state/` rather than in the shared checkout, because another
session was spending Bright Data locally in that tree at 11:55Z (`indeed.py`, `indeed_ctl.py`
in `bd_spend.jsonl`). The shared checkout was left untouched.

`572@jd-text/roles` carries the class: the ledger has no refutation channel, and a cached
quality rejection can never unlock the ratchet.

---

## 3. Item 4 — one alias declared, one blocked by the registry

**`DoiT` ships.** `ALIASES["doit"] = "doitintl"` makes `identity_key("DoiT") == "doitintl"`,
`active_by_identity["doitintl"]` is exactly `{"doitintl"}`, and `_plain_norm("DoiT")` is the
alias key — so `_alias_fold_target` returns `("doitintl", "declared")`. Both records publish a
`Product Analyst` (Greenhouse and LinkedIn), so `Ledger.fold_aliases` finds a live twin and
supersedes, unioning `seen_ids` and the `sent` mirror.

**`Investing` is BLOCKED, and the brief did not know it — the Oak/522 shape, second
occurrence.** `Investing` (active) and `Investing.com` (**parked, `active=false`**) are two
`companies.csv` rows carrying the **same `api_url`**. `_alias_fold_target`'s first refusal is
`name in registry_names`, over EVERY row whatever its `active`, so
`ALIASES["investing com"] = "investing"` would be dead on arrival — no log line, no counter,
nothing in the ledger. Exactly what cost `Oak` a day.

The operator chose: retract now, file the registry diff. So the duplicate leaves the dataset by
a url-precise retraction and `571@registry` carries the rename. **Not delivered — the
`Investing` half is still a hand-drain**, and the next `Investing` posting duplicates again.

**Checked before declaring, which is the whole of the Oak lesson**: across 2,162 registry rows,
`roles.jsonl`, `roles.csv` and sqlite `matched`, `doit` matches only `doitintl` + `DoiT` and
`investing` only `Investing` + `Investing.com`. **No third company shares either string.**
`test_the_live_registry_lets_the_doit_declaration_fold` asserts both outcomes on the shipped
registry, and tells the next session when the blocker clears: it asserts `"Investing.com" in
names` with the message *"the parked row went: `investing com` can now be declared"*.

---

## 4. Item 5 — the Nespresso ruling, decided against the tiebreak

No alias, per the operator, and none was possible anyway: `אסם` is vetoed as a registry name
and `Nestlé Nespresso SA` has no active row answering to its identity, while `same_role_twin`
is same-company only.

| | `אסם` (Indeed) | `Nestlé Nespresso SA` (LinkedIn) |
|---|---|---|
| description | **693 chars, the clean JD** | 3,353 — the same 693 wrapped in a cookie notice, `Show more`, and **27 other companies' `עבודות דומות` listings** |
| registry | **active row** | **not a registry row at all** |

**Canonical: `אסם`**, and the evidence that cuts the other way is stated rather than omitted.
The JD's own first line reads `נספרסו מגייסת Data Analyst למחלקת השיווק` — the *brand*
Nespresso hiring for its marketing department — and `אסם` appears nowhere in the text. Against
that: Osem is Nestlé's Israeli operating company and the advertiser of record on Indeed; the
LinkedIn copy is a `567`-class contaminated capture; and **the store has already ruled this
family the same way** — `nestl|…CDT אסם סחר` is `superseded_by` `אסם|…` on a shared platform
id. The brief's fallback ("keep the longer-text copy") is not the tiebreak here, because the
longer text is the polluted one and the tiebreak only applies when the evidence cannot pick.

Both retraction lines name their **url AND their `role_id`** — the 09-02 wave-A lock: a line
that says which record it means never uses the `seen_id` arm.

**The 545 binding check, run with the predicate that decides** (`Retractions.load` →
`bind(records, extra=<sqlite rows>)` → `match_all` over both stores), never a hand-rolled
copy — that mistake nearly shipped a live-role deletion on 09-01: **48 lines, 0 bad, 48 pairs,
0 lines naming no record, 0 naming more than one.** Each new line names exactly the record it
was written for.

`573@scraper/jd-text` carries the 27-companies-in-one-capture defect: it passes
`looks_like_jd`, and it is the tail variant of `567`'s Ballerine chrome prefix.

---

## 5. Green, and where

**Locally, from this worktree at `origin/master` + this diff:** `python -m pytest` (not `-q`)
**1,824 passed, 13 skipped, 2 failed**; `python check_invariants.py` **`companies.csv OK: 2167 rows, 1198 active, 0 orphans, pool=835`**; `python docs/check_docs.py` **0 error(s), 13 warning(s) over 114 documents** — every warning is another lane's not-yet-due morning check or a census floor.

`python tools/guard_kill.py --base origin/master`: **KILLS 6, CANNOT-FAIL 0**. All **nine** classifier mutation records run singly: **1 killed, 0 surviving** each, **0 coverage gaps**.

**The CI verdict is in the follow-up commit**, which is the only place it can honestly be written.

**Three of the four local reds are INHERITED and reproduce on clean `origin/master`**, verified
by running them there:

* `test_triage_does_not_consume_a_probe_wake_before_the_hunt_can_use_it` — `570@registry`, a
  fixture's hard-coded date crossing `TRIAGE_TTL_DAYS` **today**. The calendar broke it, not a
  diff, and it will not recover.
* `test_a_configured_but_unusable_paid_rung_announces_itself` — inherited.
* `test_docs_are_consistent_with_the_code` — three morning-check rows two days past their date
  with no verdict. **Answered in this commit** (see §6), so this one is closed by this session.

**The fourth was my own invocation, not my diff**: `test_bd_rescue_reads_the_unlockers_error_code…`
reds under `BD_RUN_CAP=0`, which I had exported for safety. It passes in this worktree without
it, and fails on clean `origin/master` with it. The trap is already in this repo's memory and
it caught me anyway.

---

## 6. Morning checks — five answered, one written

Three were **two days past their date and red the `guard` job for every lane**, which is why a
`classifier` session answered `company-intel`'s and `render`'s. All from run **33739498960**:

* `company-intel` 09-01 (`firmo_drain`) — **PASS** with the drift the row allows:
  `display_names=108 (+0/-0) divergent=77 sectors_folded=0`.
* `company-intel` 09-01 (identity) — **PASS**: `registry backlog 4` clears a FLOOR of 0, **0**
  occurrences of `model could not identify the name`, `display_names=108`.
* `render` 09-01 — **PARTIAL**: the board half passes (`title="withfaye">Faye`,
  `company=Faye, company_registry=withfaye`, `finbounce` 0, `display-collision` 0) but **no
  `### Faye`-class heading existed to check** — the mail's headings were Fiverr, Rounds,
  Calculum, IAI, aQurate. Re-due the morning a divergent employer posts.
* `classifier` 09-03 — **PASS on every clause.** The one thing this run cannot answer is the
  `llm` delta: **89** calls against 09-02's **376**, because 09-02 was the contract bump's own
  drain, so a 2-call gate delta is not separable from a 287-call fall.
* `roles` 09-03 — **PASS on all four**, including no `roles seen-id collision` (0 in the log,
  against 2 ids on 09-02).

**Two answered rows moved verbatim to `docs/morning-checks.md` to pay for the words** — this
lane's own 09-02 row and one `infra` row answered PASS on 09-01. The file was at **3,171 of
3,200 words** before this session wrote a line; it is at **3,200** now. Moving another lane's
row is named here because it is not a call to make silently.

---

## 7. Clause-by-clause, including the ones that do not flatter

**Clause 1 — the lane's number.** *0 role records without a classifier verdict*: **0 of 146**
before and after. It did not move and nothing here moves it. What this session changed is the
set of postings that can ever REACH a verdict (+40 pairs) and the correctness of four
published rows — neither of which that queue measures. **Reported as not a queue drain.**

**Clause 2 — does it keep moving without a session?**

* **The gate half IS delivered.** `_desc_appealed` runs inside `daily-digest.yml` at 05:00 UTC
  with nobody watching; its alarm is the mail's `classify:` line; the 2026-09-04 morning-check
  row is the unattended proof. Until that row is answered it is a fix, not a delivery.
* **The DoiT fold IS delivered** — `Ledger.fold_aliases` runs in the same cron.
* **The Prisma repair is NOT delivered.** One row, by hand, and `572` is the class.
* **The `Investing` and Nespresso retractions are NOT delivered — still a hand-drain**, one
  line per posting, exactly as the 09-02 record says of its own seven.

**Clause 4.**

* *What did you delete or unify?* `git diff --stat origin/master...HEAD` — `−` is small and
  mostly rewrites. I deleted no code, and that was right this time: every item was a rule to
  add or a row to repair. The one deletion available WAS taken: the marker arm and its
  vocabulary, its mutation record and its test leg all came out on the operator's second
  ruling, so the shipped predicate is one arm rather than two.
* *Which function did you extend, and which did you decline to duplicate?* Extended
  `_relevance`, `_gate_appealed` and `_sig_accept_nollm` rather than adding a parallel gate;
  reused `_desc_is_ml` (`grep -n "def _desc_is_ml"`) and `MIN_DESC` instead of writing a
  second ML veto and a second length floor; reused `tools/measure_title_gate.py`'s `judge` for
  the arm-B measurement rather than opening the seam again; reused `Retractions.bind` /
  `match_all` for the binding check instead of a hand-rolled matcher — the exact mistake that
  nearly deleted a live role on 09-01.
* *What design did you reject, and on what number?* (i) Hoisting the shared-text guard above
  the gate — it lets a refused row claim `_text_owner` first and blank a passing role's
  description. (ii) A `better_description` guard on employer echo — dead, because
  `quality_suspect` says `no-company-echo` for all five Prisma cards including the right one.
  (iii) Arm B as a single marker — 145 cards against 27. (iv) Declaring
  `ALIASES["investing com"]` — vetoed at `roles.py`'s first line, silently.
* *Would the next session find this?* `grep -n "APPEAL" pipeline/seniority.py` hits
  `_GATE_APPEAL` and all four `_DESC_APPEAL_*` constants adjacently, and `_desc_appealed` sits
  beside `_gate_appealed`. `grep -rn "desc-appeal" tests/` hits the five mutation records.
* *What did you make harder for the next lane?* **Four things, counted.** (1) A third
  parameter on `_relevance`, which every future caller must decide about. (2) A second
  vocabulary to keep in step between `_relevance` and `_gate_appealed` — the mirror is
  hand-maintained and now has two arms to forget. (3) A new fixture file. (4) Five mutation
  records in a catalogue eight CI shards must stay under their wall for.

**Spend.** **~22 `claude-sonnet-5` calls, 0 Bright Data.** The declared budget was ~10 and it
was exceeded: 8 for the arm-B false-negative measurement (which the brief did not anticipate
needing, because it presented the marker arm as already measured) and ~7 for the quality tier during
the Prisma rehearsal, of which only 1 was Prisma's — the other 6 are rows the nightly cron
would have judged anyway. Every Bright Data rung ran disarmed (`JD_BD=0`), and the
`0 Bright Data requests spent` line is in the enricher's own summary.

---

## Traps this session hit

* **A heredoc with nested triple quotes and Hebrew died before its delimiter**, so the patch
  scripts are written with the file tool and applied with `python`. Every one asserts
  `s.count(anchor) == 1` before substituting; two anchors had already moved.
* **The HANDOFF morning-check table is `| due | lane | must | answered | verdict |`, and
  `split("|")` puts `answered` at index 4, not 5.** Writing at 5 and 6 produced an 8-cell row
  and five verdicts reading `run 33739498960`, which `check_docs` caught as *"a verdict the
  reader cannot check"*. It was right twice over.
* **A `|` inside a verdict cell shifts the table.** `percepto|senior product analyst` has to be
  written `percepto\|senior product analyst`.
* **The word cap is not a guideline and it took five passes.** 3,171 of 3,200 on arrival;
  every answer had to be paid for by archiving a settled row, and the last edit removed one
  word to land on exactly 3,200.
* **`BD_RUN_CAP=0` reds `test_bd_rescue_reads_the_unlockers_error_code…`** — a red that is the
  invocation, not the diff. It is in this repo's memory and I set it anyway.
* **The tree moved twice mid-session**: four `discovery` commits landed while the gate was
  being written, conflicting in `tests/mutations.json` (both lanes append) and
  `tests/test_units.py`. Backlog numbering was re-derived immediately before filing — `next`
  read 568 at planning time and 571 at filing time.
* **Another session was spending Bright Data in the shared checkout** (`bd_spend.jsonl`,
  11:55Z, `ci:false`). Two unrelated lines that must not be staged, and the reason the Prisma
  repair ran against a full copy of `cloud_state/` rather than in that tree.

---

## 8. What the adversarial wave found in this session's own diff

One Opus wave over the committed diff, before any push. It reproduced **13** findings; every
one that was a defect is fixed above, and the three that were only documentation are corrected
in place. The two that would have shipped a wrong rule:

* **HIGH — the floor.** `MIN_DESC` where this very file's lesson says `looks_like_jd`, with
  the receipts: 2 of the movers failed `looks_like_jd`, 16 of 41 carried page chrome, and the
  shared-text guard **structurally cannot** catch that shape because the card builder prefixes
  each blob with its own role title, so three copies of one careers menu have three different
  sha1s and `shared_text` stays 0.
* **HIGH — the 0-BD argument was false as stated**, above.

And the one that matters most for the next session:

* **HIGH-MED — five mutations of the new code survived the whole suite**: dropping the tool
  conjunct (23 → 52), dropping the output conjunct (23 → 28), deleting the floor (23 → more),
  rewriting the un-fire as `bare = "none"`, and dropping `judge_backfill`'s two counters. Two
  of my assertions were **vacuous**: the floor was pinned with a 200-character prefix that has
  no phrase, no output and no tool (false with or without the floor), and the un-fire's
  "keeps its hearing" clause used `Senior Data Analyst`, which is **`strong`** and therefore
  never enters the branch at all. Both rewritten; all nine records now verified red.

Also corrected from the wave: three arm-B counts were pair counts printed as card counts; the
`576` soft-word figure did not reproduce under the definition given (it is **377** under the
definition now written); `tests/mutations.json`'s base was 293 records, not 291; the suite
counts in §5 were stale; and two stale line references in a docstring and in this record.

**Two findings became backlog items rather than fixes** — `574` (this lane's own
`drain_forecast.py` and `measure_scope_rule.py` now drop the 23 postings the appeal adds,
while their docstrings promise they walk the same gate the run walks) and a note added to
`571` warning the registry lane that the `Investing.com` assertion is a tripwire that reds
`tests.yml` the moment they do the rename, with the two-line fix to make in the same push.

**One finding is accepted and stated rather than fixed:** `Velotix | Senior Machine Learning
Engineer` still reaches the LLM on the same careers-menu text, because that card *does* pass
`looks_like_jd`. It costs one cached NO. It is the `567`/`573` class — furniture that reads as
a job description — and the appeal will read whatever text a card carries until that class is
closed.
