# jd-text, 2026-08-29 — four green mornings that filled nothing

Files owned: `pipeline/jdfill.py`, `enrich_scrape_jd.py`, `enrich_matched_jd.py`.
Doc section: `ARCHITECTURE.md` §7a. **Outside the lane, by an explicit scoped exception from
the orchestrator (no `infra` session was running):** a NEW `.github/workflows/jd-archive.yml`,
one mechanical row in the §4 cron table, and the continue-on-error ratio those steps moved
(`38 of 83` → `39 of 87`) in the four documents that quote it. No existing workflow, and
nothing in `persist_state.py` or the shared plumbing, was touched.

Worked in a `git worktree` at `origin/master` (`.claude/worktrees/jdtext-0829`), base
**`4bca457`**. The shared checkout was 139 commits behind and was not used for any number here.

## The headline is a measurement of production, not of the code

The brief asked for an archive gate. The first thing to establish was whether the layer that
would carry it works at all. It does not:

| digest run | scrape driver | matched driver | via Bright Data | step time used |
|---|---|---|---|---|
| 08-26 `32934864207` | 6 filled | 0 | 0 of 3 calls | — |
| 08-27 `33092547374` | 0 filled | 0 | 0 calls | — |
| 08-28 `33193786610` | 1 filled | 0 | 0 calls | — |
| 08-29 `33250362574` | 0 filled | 0 | 0 of 2 calls | **3 s of 30 min**, and 8 s of 25 |

Both steps green all four mornings, both `continue-on-error`, and the mail said nothing.
**Three causes, and the account was not one of them.**

### 1. The cooldown parked the FREE rungs, not the expensive one

One `_jd_attempted` date per url suppressed the native JSON GET, the plain GET and the Unlocker
alike for seven days. The todo was **13 of 13 parked on 08-27, 20 of 21 on 08-28, 18 of 20 on
08-29** — a 30-minute step doing three seconds of work and reporting a healthy zero.

A native GET is 0.24–1.02 s and a plain GET about a second; only the paid rung is worth a week.
`run_backfill(free_rungs_ignore_cooldown=True)` now walks a cooled row on the free rungs with
`bd=None` and counts it `paid_cooldown`. **The matched driver is unchanged** (default `False`),
because there the cooldown is not what is starving it.

The stamp thereby stops being a skip and becomes an **ordering** key — which is exactly what
makes a budgeted pass resume where it stopped instead of re-walking the same prefix every night.

I also wrote a `jd-all-cooling` alarm for this shape and then **deleted it**:
`test_alarm_for_speaks_for_every_zero_ish_run_and_stays_quiet_on_a_healthy_one` pins
`alarm_for(todo=17, cooldown=17) == ""` with the reasoning that an alarm which fires every
morning gets trained away. That decision is older and it is right; the defect was the cooldown
parking the free rungs, and it is fixed where it lives rather than reported on top of.

### 2. The Unlocker had never once rendered JavaScript

`Unlocker.__call__` posted `{zone, url, format: "raw"}` and nothing else, while
`validate_bd.py`'s own docstring says "the Unlocker renders JS". It does not unless asked.
Measured against the live account on HiBob's job page:

| call | bytes | text | carries the posting |
|---|---|---|---|
| `format: raw` (what we always sent) | 1,342 | **7 chars** | no |
| `"render": true` | **63,293** | 4,929 | **yes** |
| header `x-unblock-render: true` | 1,342 | 7 | no |
| `render + data_format: markdown` | 8 | 7 | no |

So every paid body this layer ever got back from a JavaScript site was the same shell the free
rung already had — which is precisely what `bd-shell` had been recording, unread, all along.
A page the free rung reads as a shell now renders on the first paid call.

**Rendering is slow, and wall clock is the real constraint** (credits are unlimited this month).
On the first archive pass, **nineteen consecutive rendered calls timed out at 90 s each** — 28
minutes for nothing — before `Unlocker`'s `failing-after-20` breaker opened and the remaining
candidates reported `bd-unavailable`. That is the breaker working, but one slow class of page
should not be able to end the paid rung for a whole run, so renders now carry their own budget
(`RENDER_CAP` = 60). Past it a shell is **skipped, not bought raw** — an unrendered credit on a
JS page returns the shell we already have — and the refusal is `bd-render-capped`, **transient**,
because it is our budget speaking and not a verdict about the page. Parking those for a week
would put the one class of page rendering is FOR permanently out of reach. Filed as **432**:
the cap is a guess and wants re-measuring after a week of real runs.

A host that returns `host_breaker` (3) empty bodies in one run is parked for the rest of it
(`bd-parked`) — Shopify has 31 cards on one SPA, Nebius 46.

### 3. The title gate decided what to KEEP

`_todo` now returns two pools. The TITLE pool is what the classifier could accept; the ARCHIVE
pool is every other Israel-passing card. Every card lands in exactly one counted bucket and the
driver asserts the sum. Measured on the committed cache:

```
1,718 cards -> 28 title, 1,204 archive
447 refused as listing pages · 27 duplicate urls · 3 chrome · 0 dropped by the Israel filter
```

The title pool runs **first with the whole budget** — it is what the mail is made of, and the
archive is forty times its size, so ordering alone would starve it. The archive walks
`(oldest attempt, rank within company, label)`: a round robin that reaches every employer's
first card before any employer's second, because 287 of these cards are Comeet and a budget
that stops halfway must not have enriched two employers and none of the other 32.

**A listing page is refused in `_todo`, not in the ladder.** `run_backfill` stamps whatever it
walks, so a search url reaching the loop is parked for a week as if it had been read — and on
08-26 `careers.dhl.com/search-results?keywords=Israel` bought a credit. A native rung outranks
the url rule, so a Comeet or HiBob address is a posting whatever its path looks like.

**The Israel gate takes positive evidence only**, and I got that wrong first: `is_israel_job`
answers False for a card with no location at all, and my first version dropped those. A test
written for another purpose (`..._keeps_fetched_text_when_the_loop_dies`, whose fixture cards
carry no location) caught it. This driver fetches text; it does not judge relevance, and
`run.py` re-applies the real filter afterwards. `scrape_dropped_israel` is a **canary** — 0 of
2,141 today — not a filter.

## What the pass produced

One local run, `JD_ENRICH_BD_CAP=1000 JD_ENRICH_TIME_BUDGET_MIN=90`, from a worktree armed with
`secrets.env` (a worktree has no credentials by default, and an unarmed one returns a convincing
mass-zero):

```
=== JD enrichment: 21 filled (1 via Bright Data), 7 unfetchable, 0 in cooldown, 46 BD spent [failing-after-20]
=== archive: 942 filled (936 free, 6 paid), 36 failed, 0 left for tomorrow,
             760 remaining thin, 34.8 min used, one lap 1.0 days
=== Bright Data: 46 calls (8 rendered), 22 bodies, 15 of them with no posting in them, 7 filled
```

**963 descriptions, and the lap CLOSED** — nothing was left for tomorrow, inside 35 of the 90
minutes. Verified against the committed cache rather than the log:

| | at `4bca457` | after |
|---|---|---|
| cards carrying a real job description (`looks_like_jd`) | 432 | **1,381** (+949) |
| still thin | 1,558 | **760** |
| comeet.com cards with a description | 11 of 298 | **278 of 298** |
| careers.hibob.com cards | 1 of 20 | **18 of 20** |
| companies / cards | 391 / 2,141 | 391 / 2,141 (unchanged) |

The rung split over the whole run: **html 547 + 22 jsonld, native 387, bd 7**. The free rungs did
99.3 % of the work, which is the answer to "why was the paid rung producing nothing": it was
never the rung that mattered here — it was starved *and* blind, and the free rungs were parked
behind the same stamp.

**Comeet was the single largest fixable host and the text was never ours.** All 287 empty cards
sit behind `ats_platform=scrape` registry rows across 34 companies, so `fetch_comeet` — the one
path that asks for `details=true` and reads it — never runs on them. The per-job `_comeet_read`
rung already matched 284 addresses; the archive pool simply reached them. The durable fix is
`registry` converting those rows (**430**, with the company list).

## The two red tests were both CENSUS tests over cron-written files

That is the finding, not the two data points.

**`test_native_url_is_derived_from_the_public_url_alone`** looped over the live `companies.csv`
asserting "tenant == host label on every Workday row" — false of Workday in general. The
self-heal cron wrote one legitimate row (Ribbon Communications, host `vhr-genband`, cxs tenant
`vhr_genband`, `759ba36`) and master went red for every lane, with the cheapest route to green
being to park a row that had verified 1/1 IL. Now `_registry_wd_tenant` reads the tenant from
the company's own registry `api_url` **when that row is a Workday board on the same host**, and
falls back to the label. The same-host guard is load-bearing: it is what keeps a Workday 404
authoritative. Three fixture tests replace the loop (label default, registry override, and an
other-host row that may NOT rewrite an address). **398 closed.**

**`test_every_open_role_in_the_ledger_carries_a_job_description`** read
`cloud_state/roles.jsonl`, which the 05:00 digest commits. HiBob | Senior Business Analyst
arrived on 08-29 with `desc_len 0` and turned the suite red on data no push had touched. The
rule is now `jdfill.naked_open_roles(rows, texts)` with a synthetic-row test; the live count
already exists as `matched_actionable` in the enrich stamp, and the jd-text row of HANDOFF's
morning checks is where a number can be answered. **No soft/warning variant**: nothing in CI
runs on the data's schedule, and a warning carries no grep-able string.

And HiBob now has a rung — `/api/job-ad/<uuid>/application-form`, which answers **401 without a
same-host `Referer`** and 200 with one. It is the only rung that needs a header, so
`plain_fetch` grew an additive `headers=` and `_NATIVE_HEADERS` maps platform → header builder.
It is deliberately **not authoritative**: the endpoint was found by tracing the page, and a path
rename would 404 for every posting at once — a terminal 404 never comes due again, so that would
retire every HiBob role for ever.

## Two alarms the layer could not raise

**A dead JD-quality tier was silent.** 08-29: `matched_llm_candidates=7`, `matched_llm_calls=7`,
`matched_llm_unavailable=7` — the model was down for the whole run — and the bold `Stages:` line
said nothing, because `alarm_for` reads `run_backfill`'s counter and has never seen the quality
counter. The cheap rule standing IS the documented fallback; a fallback the mail cannot tell
from a success is the failure this layer keeps producing. Now
`matched:jd-quality-unavailable(7 of 7 candidates: llm-auth7)`.

**A refused re-clean crashed the driver.** `enrich_matched_jd.py:405` appended to `alarms`
twenty lines before `alarms` was bound: the loudest thing the layer can say raised
`UnboundLocalError`, `main` stamped `matched:crash:UnboundLocalError`, and the step is
`continue-on-error` — so a store full of login walls was a green morning. `alarms = []` is
hoisted and the ledger clause now appends rather than rebinds; the test drives `main()` through
the real refusal and asserts **both** clauses survive.

`*_bd_ok` also read as success when it is not: it counts BODIES the account returned, and on
08-29 both were the shell. `*_bd_shell` is new, `bd_ok - bd_shell` is what the credits bought.

## The cloud step

`.github/workflows/jd-archive.yml`, `30 1 * * *`, `timeout-minutes: 170`, work budget
`JD_ENRICH_TIME_BUDGET_MIN=90`, **no `continue-on-error`**.

**`concurrency: repo-state` is the load-bearing line, not the hour.** `daily-digest.yml` is
alone in its own group, so this job cannot delay the mail — and it writes `scraped_cache.json`,
which `scrape-refresh.yml` also writes, so sharing the group is what serialises the two. The
hour is nearly irrelevant: GitHub dispatched scrape-refresh at 04:44, 07:49, 05:41, 00:39 and
00:38 on its last five runs, and retry-unreachable at 09:14 on 08-29. So the pass assumes
nothing about running before the digest or after yesterday's run finished — it is idempotent
within a day and resumable across days by construction, and both are tested.

The timeout is a circuit breaker and the budget is deliberately smaller: `repo-state` already
carries scrape-refresh (78 min observed), retry-unreachable (50), auto-expand twice daily,
firmographics, listing-hunt, triage-dark and a 330-minute Sunday audit. Reserving 150 minutes of
a serialised group on an estimate is how a queue starves. **The first lap actually took 34.8
minutes**, so 90 has real headroom; raise it on the measured number (`scrape_archive_minutes`),
never on a guess.

## Spent

* **Bright Data: 50 credits** — 46 in the archive pass (8 rendered, 22 bodies, 15 empty, 7
  fills) and 4 in the render measurement above. Every one bought a number that is written down.
* **Claude: 0 calls.** The quality tier was not exercised against the live model.
* Wall clock: 34.8 minutes of the 90-minute budget for a lap that closed.

## One thing I did NOT commit, on purpose

`cloud_state/pipeline_stages.json`. My local pass wrote an `enrich` stamp carrying all the
numbers above — and dated **2026-08-30**, because `run_backfill` stamps `dt.date.today()`,
which is the LOCAL date, and the run crossed local midnight while UTC was still 08-29.
Committing it would have put a future-dated stamp with `scrape_ran=1` into cloud state, and
`record_enrich` would then have treated tomorrow's 05:00 cloud drivers as a same-day
re-dispatch and SUMMED their flows onto mine: the mail would have reported ~960 fills and
nobody could have said what the cloud produced. The cache itself is the work product and is
committed; the stamp is a measurement of a local run and stays local. (The cards' own
`_jd_attempted` stamps are fine — 1,232 of them read `2026-08-29`, because the loop ran before
local midnight; only the closing `record_enrich` call fell the other side of it.)

## NOT finished

* **428–432** are filed, with numbers: the discovery cache has no archive pass (1,678 reachable
  LinkedIn guest pages, and whether they are worth keeping is a real question after the 08-28
  sign-in-form incident); `_jd_attempted` never reaches the ledger's `jd_attempted`; the 34
  Comeet `scrape` rows; `_carry_jd` cannot carry a failure count so the cache has no backoff
  ladder; and the render cap is a guess.
* **374** (the scrape driver has neither `_reclean` nor `_quality_pass`) and **155**'s inline
  half are untouched — 374 properly means **112**, the two drivers being one driver twice, and
  this session made them less alike, not more.
* `HANDOFF.md` was **3,184 words against a 3,200 cap before I touched it**, so the contract's own
  minimum for a lane (one morning-check row + one line, 83 words here) cannot fit. I archived
  the one FULLY answered row (registry, 08-29, `PASS on the 20:00 slot`) to
  `docs/morning-checks.md` verbatim — the destination that file designates — to land at 3,199.
  The next lane will hit the same wall: it is `docs`'s to resolve, and I have not filed it as a
  backlog item because it is a property of the container rather than a defect in it.
