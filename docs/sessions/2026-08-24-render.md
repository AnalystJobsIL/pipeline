# 2026-08-24 → 2026-08-25 — `render` lane: the split, the card, the `Render:` line

Spec: `ARCHITECTURE.md §7d`. Guards: `tests/test_units.py`, the `# lane: render` block.
Rehearsal: `tests/rehearse_render.py` (`--golden` / `--cards` / `--real` / `--full`).
Open items: `docs/BACKLOG.md` 142–146, 149–150 (`render`), 147–148 (`docs`). Every number here was produced by a command run this
session; the command is beside it or in §7d / `docs/TAGGING.md`.

## What was wrong (measured before any edit)

- `pipeline/digest.py` was 1,444 lines at HEAD `60fae33` (the working tree carried the roles lane's hunks on top): ~330 of JD text extraction, three renderers, and a
  row loop that derived and rendered in one place. A change to how a location reads meant
  editing inside an HTML f-string.
- `build_digest`'s `.html`/`.txt` outputs are read by **nothing** (all 10 workflows, every
  `.py` — `grep -rn "digest-" --include=*.py --include=*.yml`): only `subject` reaches the
  payload, which `mark_sent.py` never reads. Every lane still wrote each mail line three times
  (the roles lane's uncommitted diff: one line, three hunks).
- `build_board_html` dropped roles with a mangled or >100-char title with no trace
  (`60fae33:pipeline/digest.py:733`); the footer's count was post-filter while
  `stats["board_count"]` was pre-filter.
- The markdown was built before the board (`60fae33:pipeline/run.py:386` vs `:399`), so a board-render failure
  could not reach the mail.
- The renderer never saw the role ledger: `reposts`, `closed_on`, `attribution.claimed_by`
  were recomputed or unavailable (BACKLOG 137).
- Four vocabularies were copies: `_STAGE_LABEL` lacked `private-enterprise` (44 records
  rendered the raw enum — 2 board cards, the same 2 in the email, and 1 archive card on 2026-08-25) and carried three
  keys the researcher cannot emit; `_ABOUT_JUNK` vs `company_info._JUNK_OUT` (each missed
  something the other had); three seniority regexes disagreeing on a bare "Analytics Lead"
  and knowing no Hebrew; `_LOC_CANON` covering 34 of the 121 + 68 place tokens `israel.py`
  knows (the store rendered `Raanana` beside `Ra'anana`, `ראשון לציון` beside `Rishon
  LeZion`, `On Site`, `Office`, `Center`, four "District" labels).
- `_md_esc` did not escape `<`; the email blurb was emitted with no escaping at all.
- Docs: the board legend said "~55-term lexicon" (98); `docs/TAGGING.md` listed six old
  task-group names (the code has eight) and quoted percentages nobody could re-derive;
  `HANDOFF.md` was at 250/250.

## What changed, per file

- **`pipeline/jdtext.py`** (new, ~590 lines, stdlib + the lexicon for one check): the extraction layer moved
  verbatim by anchor-based slicing (no retyping), plus: the EEO-footer rejection inside
  `_req_header_match`'s candidate loop; `_LOC_GROUPS` / `_LOC_REGIONS` / `_LOC_NOISE` and a
  `_norm_location` that tries a city part, then a region part, then a city glued inside the
  string, then an unknown town as written; `_display_company` (was nested in the board
  renderer). `_short`, `_role_snippet`, `_ROLE_MARKER` deleted — dead, zero references (the
  one deletion).
- **`pipeline/rolecard.py`** (new, ~460 lines after two waves): `build` (bare card first, then fill; never
  raises), `_from_ledger` (also-listed-as, re-post dates, archive-only closed-on),
  `cross_check` (shared-board via a real tenant key, display-collision, blurb-names-other —
  count only), `report`, `firmo_facts` with a total `_STAGE_LABEL`, the blurb gate as
  `_JUNK_OUT ∪ unable-to-confirm`, `sen_canon` over the classifier's and the lexicon's
  regexes.
- **`pipeline/digest.py`** (now ~1,030 lines): renderers over cards; `render_all` (board and
  archive first, the markdown last with `render_issues`; a renderer that raises ships a
  placeholder and a bold line); `build_markdown(..., ledger=, render_issues=)`,
  `build_board_html(..., ledger=, report=)`; `_md_blurb` / `_md_alarm` (strip what opens a
  link/tag/span, neutralise `@`); `<` added to `_MD_META`; the legend prints `len(SKILLS)`;
  `_html_audit` tolerant of a bare `html.escape`; `_firmo_facts` re-exported for tests.
- **`pipeline/run.py`** (`infra`, approved out-of-lane): the four `build_*` calls and the
  file writes became one `render_all` call + `::warning::render` lines + `summary["render"]`;
  a failed board/archive is not written; a failed email hands `mark_sent` no roles (wave 2).
  The genuinely new lines are ~14; the count cannot be re-derived from `git diff` while the
  roles lane's hunks share the file.
- **`tests/rehearse_render.py`** (new): the golden (a digest.py snapshot loaded as a member
  of the `pipeline` package vs the tree, six products, byte-equality + a tag-split diff), the
  card dump with a field-level diff against a saved dump, the live scoped run, the full
  scratch run that restores the four tree files it rewrites.
- **`tests/test_units.py`**: 13 assertions under `# lane: render`, 21 after wave 1's 8 pins, with wave 2's assertions added inside them.
- **Docs**: `ARCHITECTURE.md §7d` (new), `docs/TAGGING.md` rewritten against the code,
  `docs/MODULES.md` (+2 modules via `docs/gen_modules.py`'s `PIPELINE` dict — the diff was
  exactly the four lines), `docs/BACKLOG.md` 99/100/106 closed, 119/137 partly closed,
  142–148 filed, `HANDOFF.md` item 5 (the stale "board/UI work in a parallel session" note
  about these very files) replaced by this lane's line.

## Rejected alternatives (from the design critique, before writing code)

- **Reading the ledger's `tags` snapshot for rendering** — it stores only
  skills/family/track/years/degree/ai; the board needs the section extractors anyway for
  tasks/soft/ai_req, so it saves one `roleprofile.extract` and creates two vocabularies (the
  archive would render last month's beside today's board). A record, not a cache.
- **Adopting `seniority._REQ_HEADER`'s lookbehinds** — `(?<!the )` kills "The Requirements:"
  and `(?![,.;])` kills "Requirements." — the footer is rejected in the candidate loop instead.
- **Mutating `stats` from a renderer** to carry the render line — the payload's `summary`
  is an infra contract read by tests; `render_issues=` is a kwarg and `summary["render"]` is
  set by run.py, deliberately.
- **Unifying the three audit blocks** — `tests/fixtures/company_intel/mutations.json` pins
  the source text of two of those lines; deferred behind BACKLOG 142 (retire `build_digest`).
- **Dropping a blurb that names another employer** — acquirers and customers are named
  legitimately; counted instead.
- **`roles.tenant_slug` for the shared-board check** — it is the second URL segment, which
  for a Greenhouse job url is the posting id; the first real-data run grouped eight unrelated
  companies under the LinkedIn host. `rolecard._tenant` (BACKLOG 143).

## Defects the rehearsal found before any reviewer saw it

1. The first split used line numbers from a read taken before the roles lane's hunks landed
   in `digest.py`; the rebuild put `_firmo_facts` in the wrong file and cut the board
   docstring. Anchor-based slicing; the golden caught it (`SyntaxError`, then 6/6).
2. `_md_blurb` stripped parentheses from prose ("(like scaffolding, …)"); only `` ` [ ] < > * _ ``
   can break the mail. The golden's line-14 diff showed it.
3. `Tel Aviv District, Israel` rendered "Tel Aviv" because the glued-city substring search
   ran before the region check; a district-only address now reads "Tel Aviv area".
4. The EEO guard first looked 160 characters ahead and rejected a real header whose *section
   ended* with the footer; it now looks at the first fragment only.
5. Hyphenated place tokens (`tel-aviv`, `ramat-gan`, `תל-אביב`) were not indexed — the
   totality test over `israel.py` caught nine.
6. The email alarm line was `_md_esc`-ed into `role\(s\)`; alarms get `_md_alarm` instead.
7. `_bullets` dropped any requirement under 8 characters — a bare "Python" or "Excel" bullet
   was junk. The first fix (keep it when it contains a signal word) measured badly on the
   store: split leftovers "At least", "Fluent", "Strong" came back and displaced real bullets
   from the 12-cap (a soft-skill chip vanished). Final rule: a short fragment survives only
   when it IS a lexicon skill name (`_is_skill_name`).
8. `_looks_like_header` called any two-word fragment starting with a capital a header, so
   "Team player" never reached the soft-skill classifier; the first relaxation let "At least"
   / "You have" / "Product analytics" through. Final rule: a two-word fragment that is a
   skill or matches a soft-skill regex is a bullet; everything else keeps the old rule. The
   card-dump diff against the step-5 dump is now exactly: 3 hyphenated locations relabelled
   (`Tel-Aviv`, `Petah-Tikva`) and one card gaining "Team player" (+ its chip) —
   `tests/rehearse_render.py --cards --cards-golden <step-5 dump>` → `5 cards differ`.

## Numbers

| | |
|---|---|
| golden, pure move | 6/6 products byte-identical (57 board · 51 archive · 17 email roles) — baseline = the working-tree `digest.py` snapshotted before the split (HEAD `60fae33` + the roles lane's three `Roles:` hunks); against bare HEAD the `Roles:` line alone differs |
| golden, after the behaviour changes | `latest.md` +8/−6 lines, `index.html` +39/−35 tag-lines, `archive.html` +80/−32 (the documented command, against `60fae33`) — every line one of: stage label, location label, `Closed on`, the `Render:` line, the rejoined bullets, the roles lane's `Roles:` line |
| card model | 108 cards · 0 degraded · cross-check `['title-twin Bounce/Bounce AI', 'title-twin Port/Port.io']` on the committed store (`[]` before the title-twin detector) |
| seniority vocabulary swap | 2 of 111 titles changed (both "… Analytics Lead" → Lead+) |
| EEO guard | 0 of 111 changed |
| locations | 16 of 108 cards relabelled, 12 distinct pairs (list in §7d) — the first count written here, 9, was wrong: the wave-1 docs attacker re-derived it |
| live scoped run (`--real`, Fiverr/Wix/Lightricks) | `- **Render:** board 58 cards · archive 50 cards · email 0 cards`; paths reconcile; `git status` unchanged |
| tomorrow's email (`--full`, unscoped, 862 companies, live fetch, no LLM/BD) | scanned 862 · failed 3 · board 135 · archive 27 · email 20 · `- **Render:** board 135 cards · archive 27 cards · email 20 cards` · board rows rendered 135 = `board_count` 135 · Decision paths reconcile · nothing from `render` on `Needs a look` |
| suite | green except the pre-existing BACKLOG 130 failure (scraper, date-dependent); `check_invariants` OK with its 2 informational warnings |

## Adversarial waves

**Wave 1 — four Opus attackers, read-only, each with repro commands; then fixes, pins, and
re-measurement.** Confirmed = reproduced by a pasted command.

| lens | confirmed | fixed + pinned | filed | accepted / refuted |
|---|---|---|---|---|
| wrong company | 3 HIGH · 5 MED · 4 LOW | H1 title-twin detector (Port/Port.io, Bounce/Bounce AI — both on the shipped board, no guard fired); H2 `same_employer` (a name and its prefix-spelling are one employer: Kornit Digital/kornit); H3 real tenant key + platform-host cap (23 Lever rows were one "shared board"); M4 host-tenant ATSs key on the host; M5 aggregator judged on the host, `comeet.co` too; M6 display-collision on the names as written; M7 blurb tokens fall back to the whole key, word-bounded; M8 no unanchored "X is a …" blurb (an agency's client); L9 also-listed-as by identity; L10 one `render:` prefix; L11 the email cross-checked; L12 duplicate `_display_company` | — | refuted: HTML/markdown injection through names, footer injection, a different role's ledger record, blurb/facts fan-out across identity groups, `_tenant` normalisation, Scopio/Sckipio missed |
| injection / escaping | 2 HIGH · 6 MED · 5 LOW | H1 `_bare` outside the guard (an int title took all three products down) → every field coerced, a last-resort card; H2 whitespace inside a url → rejected; M3 every `stats` line neutralised (`_md_line`, `<` only where it opens a tag so `A<-B` survives); M4 `report` filled only once the page exists; M5 mangled titles hidden from the mail too; M6 `_md_esc` one line; M7 chips one line, typed; M8 `\\` escaped/stripped; L9 `_MANGLED_TITLE` learns `\n`, ` / `, ` > `, "– Apply now"; L10 badge regex prefix de-quadratic'd; L11 location input/output capped; L12 LinkedIn label by host; L13 chip types | — | refuted: board/archive HTML injection, inline-script injection, `esc(_safe_url)` order, scheme bypasses, entity corruption, ReDoS elsewhere |
| regression / mail contract | 2 HIGH · 1 MED · 4 LOW | H1 a failed product is NOT written (yesterday's board survives); H2 the legacy audits see the `Render:` line (`stats` copy, no mutation); M3 payload = the mail's line; L4 issues capped at 6; L5 the bullet rules named and pinned; L6 `--full` restores bytes, never `git checkout --`; L7 `"\\@"` | — | refuted: unattributed golden lines (none), other lanes' lines (byte-identical), payload/mark_sent, scoped runs, `ledger.records` shape, caps/first_scan, importers, performance (2.5 s per `render_all`) |
| docs truth | 12 WRONG · 2 UNVERIFIABLE · 34 TRUE | every WRONG corrected in place (106 un-closed then closed properly, 16 not 9 locations, 3 not 10 stage cards, no Hebrew title in the store, four files not three, the imports column, the mail grammar as a schema, the golden command with its revision, `render_all`'s failure semantics); AGENT_BRIEF's render row (docs lane's file, one line, disclosed) | 147 (duplicate BACKLOG numbers), 148 (the brief's roles paragraph) | unverifiable and left so: "11 genuinely new run.py lines" (the diff mixes the roles lane's hunks); the `--real`/`--full` figures (live runs, artefacts under `out/`) |

Legibility (the two-minute test): five of six questions answered within one hop of §7d /
TAGGING; "how do I prove a change didn't regress" needed the code until the golden command
named its revision — fixed.

**Wave 2 — three Opus confirmers.**

| confirmer | verdict | what it found | what changed |
|---|---|---|---|
| re-attack + mutation | CONDITIONAL NO-GO → GO | **HIGH R1**: `", ".join(_capped(issues, 6))` — `_capped` returns a string, so the mail's `Render:` line and the payload came out as `t, i, t, l, e, …` on every run with a cross-check issue, i.e. tomorrow (reproduced on the store and in the live `--real` run); 11/12 mutations killed, the survivor (`firmo_facts` accepting a list `sector`) pinned; `_safe_url` invisible-character bypass (U+200B…) → printable ASCII only (all 973 real urls are); `same_employer` wrong both ways on real names (Spear UAV / SpearUAV, Cisco / Splunk (Cisco) false; Aleph / Aleph Farms true) → space-stripped, `X (Parent)`, `_SITE_WORDS`; `blurb-names-other` firing on "global" → common-word stoplist; Workable's `/company/<hash>/` one tenant for every employer → `company` is plumbing; breezy.hr / applytojob.com host-tenanted; uncapped alarm lists → 3 per product, de-duplicated. Refuted: 55/58 of its own repros pass; 111/111 real apply links survive `_safe_url`; every real stats shape survives `_md_line` | R1 fixed + pinned (`board 2 cards, title-twin Meta/Meta Israel`), every MED above fixed + pinned, 149 filed |
| docs truth (second pass) | NO-GO → corrected | 9 wrong claims survived wave 1's corrections, four load-bearing: `roles.tenant_slug` "returns `io`" (it returns the posting id — wrong in three files), "three shapes" (four), the failure semantics contradicting themselves (a failed *email* stub IS written), "12 distinct pairs" listing 14 (two never changed); plus 1,451/1,444, 13/21, `[]`/two title-twins, step 7/6 in the brief, `roles._winner` → `Ledger._winner`, "one check" → two, the golden command's expected result today; 34 claims TRUE. Legibility: 5 of 8 questions answered within one hop; "how do I prove no regression" and "what do I check tomorrow" needed the code / HANDOFF | every listed line corrected in place; the golden's comment now says what it prints against `60fae33` today; the morning check is in §7d's table and HANDOFF |
| tomorrow's run + commit hygiene | NO-GO → GO on two conditions | R1 (above); **commit order**: a render-only commit without `pipeline/roles.py` was BUILT in a scratch worktree — `import pipeline.run` passes but `render_all` fails on the function-local `from .roles import _PLUMBING` on every product with a card, silently, forever (`board_ok False`, `email_ok False`); committing first would drag the roles lane's whole unpushed feature. Also: on a failed email `mark_sent` still burned the day's roles (fixed: `email_jobs = []`); Python 3.11 clean under `-W error`; LF endings; no path leaks; `render_all` 1.23 s; the Persist/publish steps keep yesterday's board when nothing was written | the roles import made tolerant (`_ROLES_PLUMBING` fallback) and pinned by the order-(c) rehearsal no longer failing; `mark_sent` guard; `r` → `rendered`; the staging plan is order (b): after the roles lane commits, whole-file stages for everything except `HANDOFF.md` / `docs/BACKLOG.md` (HEAD-based blobs) |

GO from all three lenses once (1) R1 is fixed — done — and (2) the roles lane has committed.

## What was spent

Bright Data 0 · pipeline Claude 0 (every run `--no-llm`, keys stripped) · SerpApi 0 ·
Opus sessions: 1 design critic + 4 attackers + 3 confirmers.

## NOT finished

`docs/BACKLOG.md` 142 (retire `build_digest`, `infra` + `company-intel`), 143
(`roles.tenant_slug`), 144 (AppSec Labs / AppSec blurb crossing, `company-intel`), 145
(`matched.seniority` never written), 146 (tests reaching private `digest` names). The
morning check for 2026-08-26 is in `HANDOFF.md`.

## Commit status

Pushed as `1458314` on top of the roles lane's `ea0f0b8`, in the order wave 2 required (roles
first: a render-only commit was built in a scratch worktree and shown to fail `render_all`
on every product, silently). The commit was assembled in a detached worktree — HEAD plus
render's whole files, and HEAD plus render's lines only for `HANDOFF.md` / `docs/BACKLOG.md`
(the shared checkout's staged copies would have deleted the roles lane's items 151–152) —
and every gate re-run there against today's cloud state: suite exit 0 (BACKLOG 130
deselected), `check_docs` 0/0, `check_invariants` OK, `--real` → `- **Render:** board 81
cards, title-twin Port/Port.io · archive 46 cards · email 6 cards`. The shared checkout was
left untouched because another session was writing its index at the time (two `cloud_state`
binaries unmerged there — not render's): its staged render files equal `origin/master` now,
so `git restore --staged` on them and a `git pull --rebase` reconciles it.

### As written in HANDOFF.md, 2026-08-25 (`render`)

*Moved here verbatim on 2026-08-27 by the `docs` lane, because a whole session
written as one 1,657-character line defeated HANDOFF.md-s cap.
sha256(first 16) of the line as it stood: `b41a96926d93b741`.*

5. **2026-08-25 `render` (the split, the card, the `Render:` line — 1 Opus design critic + 4 attackers + 3 confirmers):** `digest.py` (1,444 lines at `60fae33`, derived and rendered in one loop) is now `jdtext.py` (text→structure, stdlib only) → `rolecard.py` (one card per role from the row + its ledger record; never raises; `cross_check` names shared-board / title-twin / display-collision / blurb-names-other) → `digest.py` (rendering + escaping only; `render_all` is run.py's one call, board and archive BEFORE the mail). Pure move proven byte-identical on 6 products, then each change with its enumerated diff (`tests/rehearse_render.py --golden/--cards/--real/--full`); a failed board/archive is not written (yesterday's page stays), a failed email ships a stub and burns nothing; two adversarial waves (4 attackers + 3 confirmers): 7 HIGH fixed and pinned, the last one (`_capped` joined into characters) found by wave 2 on the live store. The mail carries `- **Render:** board N cards[, hidden/degraded/shared-board] · archive N · email N`; alarms above the fold; `also listed as` / re-posted / archive `closed on` from the ledger; stage label total (BACKLOG 99), one blurb gate (100), one seniority vocabulary and every `israel.py` place labelled (119), `<`/`@`/blurb escaping in the mail. **Out of lane, approved:** `run.py` render block (one `render_all` call, `summary["render"]`). NOT finished: 142–146; morning check 2026-08-26: `- **Render:** board N cards` must reconcile with the board's row count, nothing on `Needs a look` from `render`. Record: `docs/sessions/2026-08-24-render.md`; spec ARCHITECTURE §7d; `docs/TAGGING.md` re-verified.

