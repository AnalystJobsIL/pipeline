# 2026-08-31 — roles: five defects in the public dataset, one judge

The operator measured the PUBLISHED https://analystjobsil.github.io/board/roles.csv (155
rows; the repo copy was 161 and one run ahead) wrong in five ways and gave the lane one
verdict criterion: the published file after the next digest run. This session's changes,
re-derivations, and what could not be verified — every number checked against the tree or a
live read this morning.

## The five, and what landed

### 1. The description text was not in the published artefact set

Chosen: publish `roles_text.jsonl` itself (573,923 B, 169 records, join `role_id`), not a
`roles_full.csv` — the full arithmetic in
`docs/decisions/2026-08-31-roles-text-artefact.md` (short form: the CSV re-diffs nearly
every line daily on `last_seen`, so a joined full-text CSV re-pays the forbidden history
cost every day; the text file only changes when a description does). Code: `build_meta`
reads `ROLES_TEXT_PAGES_URL` into `description_text.download_url` /
`published_on_pages`, mirroring the CSV's own flag; the exact workflow diff is sharpened
into `498@infra`. **Until infra applies 498 nothing is automatic** — today the text is
reachable via the meta's `raw_url` (public repo, raw address), and the meta says exactly
that about itself.

### 2. Rows whose "description" is a search snippet

Measured 11 of 161 fail `jdfill.looks_like_jd` (8 open, 3 closed — the spawn brief's 9
undercounted the two ~160-char Hebrew rows, which fail on length; the marker families do
carry Hebrew) + 2 rows with no text. Policy: **MARK, never exclude**
(`docs/decisions/2026-08-31-snippet-rows.md`) — new column
`description_quality ∈ {jd, snippet, none}` (empty = could not measure), judged at export
from the sha1-matched text, meta `description_text.quality` block with its own identity
(`jd+snippet+none+unmeasured == rows`), and a `weak text N (S snippet, K none)` clause on
the mail's dataset line while any remain. Local re-derive on a copy of the real store:
**jd 148 · snippet 11 · none 2 · unmeasured 0, holds: true**.

### 3. Six rows rendered the company as a URL slug

`company` now renders through `rolecard.display_name` — the SAME guarded resolver the board
and mail use (junk/NFKC/homoglyph filters + impersonation guard; never the raw
firmographics field) — falling back to the registry name; `company_registry` (new column)
keeps the join key. 54 → 56 columns. Supersedes `504`'s additive-only shape on the
operator's word; the deliberate 1-day-old contract break, the full-union victim-set rule
(CSV may only diverge toward the honest slug) and the residuals are in
`docs/decisions/2026-08-31-company-column-shows-the-brand.md`. Local re-derive: withfaye →
**Faye**, helfy → **Helfy** (both rows), comblack → Comblack (rows leave via the recruiter
purge anyway), **finbounce stays finbounce** (guard refusal while the real Bounce AI row
exists — correct), **entrypoint stays entrypoint** (no display evidence exists;
`512@company-intel` filed; the honest residual this session cannot reach).

### 4. Wrong rows still published

The retraction machinery (built 08-30) needed no repair — re-verified end to end in code:
`Retractions` loads outside `_guard`, `_record_run` stamps before `rid in onboard`,
`build_rows` excludes every non-open/closed status. It has simply never run against the
committed store (ledger holds 0 `withdrawn`; the on-disk meta is the pre-retraction
schema). The 2 Comcast lines were already committed; Jobgether/comblack×2/Nogamy/Shavit go
via the recruiter purge predicate on the next run.

The DUPLICATE pairs could not go that way and got the real fix (`488` CLOSED):

- `checkout com|fraud data analyst` vs `checkout|fraud data analyst` — one Ashby posting
  under two url spellings (`/application`) and two seen-id prefixes; never even
  pair-tested (`_groups` buckets on exact sid and exact raw url).
- `bounce ai|data analyst` vs `finbounce|data analyst senior` — the IDENTICAL comeet
  posting page under two titles (the employer retitled it); the titles-agree arm blocked
  collapse forever. A retraction line canNOT fix either: a line binds to the posting url
  and would withdraw the GOOD row at the same address.

Fix: `same_posting` now compares on `rolecard._posting_key` (reused lazily as `roles._pk`
so render and ledger agree on what a posting is; a local copy would drift), a `pk` bucket
in `_groups`, and ONE narrow titles bypass — identical non-empty posting key + id-shaped
tail + titles equal once bare seniority words (`senior/sr/junior/jr`) are stripped.
Refusals pinned: Meta's listing url, a strong id alone, a digit-less prose page, "Data
Analyst, Growth". `sweep_store` supersedes both stored pairs at the next open; winners
traced (`Checkout.com` on source rank, `Bounce AI` on `names_in_url`, keeping its own
title). Plus the `489` retraction line for `bounce|data analyst` (LinkedIn url unique to
that record — proven by test to touch nothing else).

### 5. An aggregator copy beat the employer's own board as canonical

**Why Zipher regressed, found before fixing** (the task's precondition): the 170→2,021 fix
(`ae6eeae`, the cache-only sibling rung) was data whose donor the SAME morning's scheduled
scrape-cache refresh (`fc57789`) retitled and blanked; the committed store has held the
Indeed url + 170 chars continuously since 08-25. Two live defects remained and both are
fixed, or it regresses again:

1. `merge_duplicates`: a description-less own-board card is `_inherited` and the donor pool
   excluded it, so its ADDRESS could never ship. Now `deep` draws from all members — the
   origin gate (`_is_posting_page` → `_same_origin`), not the flag, keeps competitor cards
   out — and an inherited copy donates its URL, never its text. Zipher both orders: url →
   `zipher.ai/careers/data-analyst/`, text stays the 170-char snippet (now marked
   `snippet`, and fillable by `jd-text` from the role's OWN address — the self-healing
   direction). Same shape covers LTX (greenhouse row added 08-30 14:24, never yet in a
   digest; `fetch_greenhouse` does fetch descriptions, so it may also self-fix directly).
2. `upsert_matched` overwrote `url` unconditionally in both branches — the regression
   mechanism itself. Now ONE downgrade is refused: a stored non-aggregator url replaced by
   an aggregator one (board→board and agg→agg still overwrite). The same ratchet in
   `roles.reconcile` (the second write path; a scoped run or restore can make the
   aggregator side the newer one). The stale "both branches overwrite unconditionally"
   docstring note corrected.

**Navan is not fixable in this lane and its verification bullet will fail**: its own board
(navan.com/careers) yields 0 cards (JS-rendered; `261@scraper`) — with no own member there
is nothing to promote. Stated here so nobody reads the Navan row as this fix regressing.

## Guards added

13 tests (`tests/test_units.py`, "roles, 2026-08-31" block): url donation both orders +
textless canonical + no-origins inert; the upsert ratchet's four cases; the reconcile
ratchet's three; both duplicate pairs (same_posting + the pk bucket + the sweep + winner
titles); the bypass refusals; the 489 line touches only its row; quality marks not
excludes; unmeasured is empty not guessed; meta/mail agreement + the quality identity; the
text Pages flag end-to-end; the brand cell + registry join key; the guard refusal
direction. Suite: **1623 passed, 13 skipped, 0 failed** locally before the wave.

4 hand-written `tests/mutations.json` records — `pipeline/store.py` had ZERO (the derived
coverage check globs root writers only): deep-gate widened to root, own-pool inherited-gate
removal, upsert-ratchet inversion, same_posting exact-widening. All four verified to apply
and be killed on the committed tree (`tools/mutate.py --id …`; the harness archives HEAD,
so they read FAIL/stale until the commit lands).

## Not finished / not verifiable yet

- The published-file proof waits on today's digest (had NOT fired as of ~08:30Z; typically
  +5–12h late). The five curl checks are in HANDOFF's morning-check row; expected honest
  residuals: `entrypoint` slug (512), Navan url (261), text on Pages (498).
- `498@infra` (the publish copy loop) — filed with the exact diff; not automatic until
  applied.
- The firmographics NAME-join class behind 489 (`132`/`133`) — untouched.
