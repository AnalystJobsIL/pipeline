# 2026-09-16 — `infra`: the evidence archive on the account's Save Page Now API

One item: wire `archive_evidence.py` (the first step of `jd-archive.yml`, 12:30 UTC) to the
archive.org account the operator opened today. The decision record
(`docs/decisions/2026-09-16-archive-authenticated.md`) carries the measurement, the three
corrections to the spawn prompt and the re-scoped `620`; this is the session's ledger.
**Bright Data credits spent: 0** — this path has no paid rung. Archive captures made: 13
jobs, 4 named, on the one hand run.

## 1. What shipped (`9bae94a` code + tests + ledger lines; the docs in the next commit)

- **Two rungs behind one seam.** `submit(url, timeout, auth)` dispatches: keyed →
  `_submit_job` (`POST /save`, form body `url=`, `Authorization: LOW a:s`) then `_poll`
  (`GET /save/status/<job_id>` every `POLL_S` 10 s, bounded by the clock AND a count so a
  stubbed sleep cannot spin, the first read after one wait); no keys → the 09-04 GET,
  verbatim as `_submit_anon`. Every request kind goes through `_open`, so tests stub one
  function. `_json_call` gives both rungs the same two families (a 429 `throttled`, a 5xx
  `server`, a 4xx `http`, a network error `net:<class>`; a POST timeout is
  `net:TimeoutError`, never `pending`).
- **`STATUS_EXT`**: the archive's `error:<word>` vocabulary onto the ledger's classes by the
  09-15 rule; an unknown word is `server`. `err` stays a bare class; `status_ext` (and
  `job_id`) ride on the line only when set, so an anonymous-rung line is the 09-04 shape byte
  for byte. `read_ledger` carries `job_id` on a `pending`; the verify loop asks
  `/save/status/<job_id>` first and the availability API only for a line with no id.
- **The probe** (`_probe`, before `plan_batch` — the cap it lowers is the one the plan
  reads): `workers = min(WAYBACK_WORKERS, available)`, `day = min(cap, what the account has
  left)`, nothing left is `stop=daily-limit` before any POST; 401/403 or no keys → the
  anonymous rung with `unauthenticated: <why>` in the alarm, printed; a 5xx on the probe is
  not the keys failing. The same read after the day prints `(+K this run)`.
- **`Report.add_alarm`** composes clauses `; `-joined (the old `if not rep.alarm` let the
  first suppress `zero-produce`); `auth` / `jobs` / `polls` in the stamp; `Report.tally` is
  the one family counter (`record` and the verify loop both use it).
- **Secrets**: `_auth_headers` is the only reader; `_redact` on the crash line; `main()`
  loads `secrets.env` through `secretsenv.load(ROOT)` (not `run()`: a test's `run()` must
  never load the operator's file); `tests/conftest.py` holds the two names EMPTY beside the
  Bright Data pair.
- **Found by the hand run and shipped in the same commit**: the account's 429 with no
  `Retry-After` is a lagging slot count, not the per-IP block — `SLOT_WAIT_S` 20 s in the
  thread, one more ask, the day's pause (90 s) only on a second in a row.
- `jd-archive.yml`: the two secrets in the step `env:`; caps 250 / 40 / 60 / 400, budget
  60 min, pace 9 s, job ceiling 150 s, workers 6 (a ceiling); step timeout 70, job 180.
- Docs: ARCHITECTURE §4 row (and its stale "100 postings" — now the `wayback_day_cap`
  Fact reads the workflow), §5 stamp and ledger rows, *Reading an authenticated night*;
  `RUN_LOG.md` two rows; `gen_modules.py` → `MODULES.md`; `AGENT_BRIEF.md` 12 facts / 22
  sites; `620` re-scoped in place.

## 2. What the design review found before the code was written (one Plan agent)

Thirteen defects in the plan as drafted, all folded: secrets loaded in `run()` would have
armed every test from the operator's file; the probe placed after `plan_batch` lowered a cap
nothing read; a poll 429 handed to the pool as `throttled` would re-POST a running job; a
POST timeout as `pending` would route to the availability API and then to `unverified`, a
refusal; a poll bounded by the clock alone spins under a stubbed sleep; `workers = available`
made the env dead; the alarm's first clause suppressed `zero-produce`; two mutation records'
`find` strings went stale on the lines the change touched; `line()` is asserted byte-exact
by two tests; a `remaining ≤ 0` that zeroed `caps.day` alone still planned boards; `BODY_CLASSES`
had no `cached` prose; the doc's POST form is `/save` with the url in the body.

## 3. Tests and gates

Nine new `test_wayback_*` (POST→poll names the capture; the `status_ext` families in a
run — five `blocked-url` park a host, two `capture-location-error` are retried and park
nothing; the probe bounds workers and the day before the plan; no keys / 401 / 503 / a 401
mid-run; keys never in stdout, ledger, stamp or summary, the crash line redacted; a job past
the ceiling is `pending` with its id and resolved next run, an `error` end is its class and
never `unverified`; prose instead of a job; the keyed 429; polls are not requests and the
gate fronts the POST). Existing tests: zero edits — `_wb_quiet` gained `real_auth=False`,
which patches `Auth.from_env` to a bare `Auth()` so the anonymous rung runs without the
clause the keyless test environment would otherwise put on every run.

| gate | reading |
|---|---|
| `python -m pytest tests/test_units.py -k wayback` | **28 passed** |
| `python -m pytest` (worktree, before the docs commit) | **2,048 passed, 13 skipped, 1 failed** — the one is `test_docs_are_consistent_with_the_code` on the then-missing decision record |
| `python -m pytest` on `cc21e96` and again on `dfb1384` | **2,050 passed, 13 skipped, 0 failed** (254 s / 268 s) |
| `python check_invariants.py` | 2,450 rows, 1,414 active, 0 orphans |
| `python docs/check_docs.py` | 0 error(s), 0 warning(s) over 138 documents |
| **CI `35123005923` on `dfb1384`** (`event: push`) | **`success`, 16 of 16 jobs** — guard, guard-kill, six rehearsals, eight mutation shards |
| the evening fix (`cf2b775`): `-k wayback` 28 passed; `python -m pytest` **2,050 passed, 13 skipped, 0 failed**; two new records **killed**; **CI `35135060150` on `e591d15`** | **`success`, 16 of 16 jobs** |
| `python tools/guard_kill.py --base origin/master` | 9 new tests, **KILLS 9, CANNOT-FAIL 0** |
| `python tools/mutate.py --id …` × 15 (13 new records, 2 re-aimed), `--skip-baseline` on a green tree | **15 of 15 killed, every killer behavioural** — after one survivor: `wayback-ceiling-drops-job-id` lived because every status read in the fixture answered with the job's id, so the initial result's id carried nothing; the test now has a job whose reads FAIL (a 502), the case the design names, and the record dies (`90813f8`) |

## 4. Clause 4

**Deleted or unified.** `submit`'s exception handling became `_exc_result`, shared by both
rungs; the three family increments in `record` became `Report.tally`, shared with the verify
loop; the `daily-limit` alarm assignment became `add_alarm`; the run-wide `if not rep.alarm`.

**Extended, not duplicated.** `classify` and `BODY_CLASSES` read the POST's prose (one new
entry, `cached`); `_retry` grew the keyed 429 branch instead of a sibling; `read_ledger`
grew one key; `_wb_quiet` grew a flag; `Fact("wayback_day_cap")` follows `jdfill_bd_cap`
with its own workflow reader.

**Rejected, on the number.** Drain-in-three-nights (295 min of sends a night at the
archive's 7/min); `capture_all` (a 404 capture marks the address done for ever); a batch
status endpoint (39 reads for 13 jobs under a 70,000 allowance, unmeasured); `workers =
available − 1` (the slot wait costs 20 s a hit, a slot a third of the night — the first
night's `throttled` decides); `no-access` as an auth failure (the document: the target's
403); a Fact per cap (one, the one that had drifted).

**Made harder.** `auth` on `run()` and `_Pool`; `real_auth` on `_wb_quiet`; `job_id` in
hand-built ledger states that read it; `STATUS_EXT` to extend; two more names conftest
holds empty; a §4 row the `wayback_day_cap` Fact now refuses to let drift.

## 4b. The first scheduled night (run `35126457407`, `event: schedule`, `dfb1384`, 17:09–18:13Z)

It fired 40 minutes after the push, on the new code, and it is the 09-17 row's answer — a
**FAIL** with an exact reading. `[wayback] authenticated: 3 slots, 13 of 30000 captures used
today; 3 workers, day cap 250`; 250 + 40 planned; one `archive-down` pause after 61 requests
(`net 15`); the line: `submitted 44, failed 47, backlog 6107, boards 7 (of 599 due), verified
0, throttled 0, requests 154, captured 37, net 43, server 0, refused 4, jobs 56, polls 211`.
The 98 ledger lines it committed (`c0e1bbf`): **37 captured** (33 `il.linkedin.com`, 4
careers pages), **43 `net:TimeoutError`**, **14 `pending`, every one with a `job_id`**, 4
`il.indeed.com` `error:bad-request`; 14 board lines. No `unauthenticated`, no `zero-produce`.

The 43 were my number: `_submit_job` gave the POST a 30-s socket timeout because it answered
in a second from this machine — on the runner the archive holds the POST until a slot frees,
and the cut-off turned a queued job into `net:TimeoutError`, a 15-s retry and a second POST
for the same url (which is also what fed the archive-down pause: 15 in a row at 17:34).
The after-the-day account line was absent: the re-read failed and the code said nothing.

**Shipped the same evening:** the POST waits the job's ceiling (`WAYBACK_TIMEOUT_S`, raised
150 → 240 in the workflow: 14 of 56 jobs were still running at 150 s); the re-read failure
prints; a test pins the POST's timeout and the printed line; two more mutation records
(`wayback-post-cut-off-at-30s`, `wayback-after-read-silent`). The 09-18 row is the proof.

## 5. Not finished

- **The unattended proof**: the two 09-18 rows in `HANDOFF.md`. The first night (4b above)
  ran on the new code and FAILED on a number of my own; tomorrow's run is the one that
  proves the fix.
- **`620`** is re-scoped, not closed: a week of keyed nights, then the per-host capture
  rate (the item's Check command) and the operator's call on Indeed / `www.linkedin.com`,
  which refused 9 of 9 on the first slice.
- The two anonymous-rung constants nobody reads on the keyed rung any more
  (`WAYBACK_TIMEOUT_S` as a socket timeout, `throttle_wait_s` 300) stay for the fallback.
