# 2026-08-31 — `registry` — the red gate, and a drain that could not report its own death

Lane: `registry` (one session at a time). Branch `registry-0831` off `bf42cae`.
Sections owned: `ARCHITECTURE.md` §2 and §3.

## What I was asked, and what was actually true

Four tasks. Two of the four briefs were wrong about their own subject, and finding that out
was most of the work.

### Numbers re-derived at 14:20Z (the brief's, and what the tree says)

| | brief / 05:00 digest stamp | re-derived here | why they differ |
|---|---|---|---|
| queue `owed` | 369 | **65** selectable (`queue_state.py`, `QRS.targets()`) | that run's dispose step retired names *after* composing its own stamp |
| `new_intake` | 169 | 65 | same |
| intel backlog | 28 (`intel` stamp) | **22** | the 10:17 firmo cron drained it after the digest |
| BACKLOG 513 list | "22 employers, Nestlé/TransUnion/Trivago/אסם" | **21 inactive rows + 4 with no row** | see below |
| `tests.yml` | "red 4 runs" | **red 5 runs**, one job each time | a fifth push landed while I read |

`registry_health.py`: 2102 rows · 1102 active · 1000 parked · 0 owned by nothing.

## Task 1 — BACKLOG 514: the CI gate (`rehearse (worst, seed 1)`, red 5 consecutive runs)

**The filed title is wrong in both halves, and the fix it asked for would not have worked.**
514 says a cron "OVERWROTE a notes cell" and asks for the write path to go through
`pipeline/notes.py` and the lost verdicts restored. Reproduced byte-for-byte from the
shipped code (the run's headSha `d6408ca` is identical to origin/master for every file
involved):

1. **Nothing overwrote anything.** `bd_rescue.py:343` rebuilt `Israel Opera`'s row and its
   87-char paid verdict took the cell over `notes.py`'s 220 cap, so `append` evicted the two
   **unprotected** history segments (`queue-hunt`, `listing-hunt`) — through `notes.py`,
   exactly as designed. The `token` column gaining a copy of the url is `bd_rescue`'s row
   rebuild and is cosmetic: no pool or invariant reads `r[2]` on a scrape row, and 376 rows
   already carry it.
2. **The red is the opposite direction and involves neither evicted segment.** Retry's *new*
   segment `retry 2026-08-31: scanned; no open Israel roles now` **added** the row to
   `validate_empty`'s pool (membership is that literal substring, `validate_empty.py:42`),
   and retry's own `replace_own("retry", …)` deletes the phrase's sole carrier the next
   night. A one-night pool membership, which `rehearse_registry.py:354` correctly calls a
   loss — `validate_empty` has no `_OWN_STAMP` exemption, and should not have one: the tool
   that re-checks empties is not the tool that writes the emptiness.

**The rule that was missing** (now `ARCHITECTURE.md` §2, "a fact another pool selects on"):
the existing rule forbids a *pool* standing on a token inside another tool's segment
(BACKLOG 320). The mirror binds the *writer*, and nothing stated it: when your own segment
is the only carrier of a fact another pool selects on, `replace_own` deletes that fact every
night you re-stamp. You break no rule — it is your own segment — and the row silently leaves
a pool. §2 also mis-filed `validate_empty` under "fact pools ... cannot erode"; corrected to
a hybrid, which is how it eroded.

**The fix.** `retry_unreachable._fold_empty` carries the dated fact forward:
`retry <today>: still unreachable; no open Israel roles <date0>`. Two true dated facts,
never a claim of a fresh scan; `date0` is re-extracted each night so it cannot accumulate;
the moment a retry reaches the page the `empty` branch writes its own fresh phrase and the
fold state dissolves.

**The guard beside it is not optional, and this is the part a first pass would have
shipped broken.** The folded segment is ~33 chars longer, and `replace_own` DELETES before
it appends. `notes.append` drops a newcomer whole on a saturated all-protected cell — a
guarantee written for a tool that only ADDS — so the pair can delete a fact and then fail to
write its replacement. Measured against the real registry: 19 rows carry the phrase in their
own retry segment; the fold fits on 18; **`Syte`** (208 chars, 4 of 4 segments protected)
would lose *both* the phrase and this tool's stamp. `_keep_selectors` compares the candidate
cell against the one it holds and keeps the old note if a selector went missing — costing
one saturated row tonight's date, which is the trade `notes.append` already makes. Writing
the short unfolded segment instead is **not** a fallback: on a Syte-shape without a second
membership token it deletes the phrase's only carrier just the same.

**Row repair — attempted, then REFUSED, and that is the more useful answer.** I did restore
`listing-hunt 2026-08-29` via `notes.append` (153 → 218) and shipped it in the first commit.
An adversarial pass then killed it on three counts, all reproduced: (a) at 218/220 the
restore is the cell's ONLY unprotected segment, so the next stamp of any kind evicts it —
simulated against three different next stamps; (b) `probe_candidates._wake_note` strips
`^(listing-hunt|crack-walled|probe-woken)` segments **by design**, so restoring an 08-29
hunt verdict reinstates precisely what the row's own 08-31 `probe-woken` exists to clear;
(c) attempting `queue-hunt` as well returns the cell to 153, because `append` evicts the
restore to make room and then drops the newcomer too — the second restore COSTS the first.
So the row was reverted to exactly what the cron wrote, and **`rehearse (worst, seed 1)` is
green on that note**: the fold fixed the gate, the repair was never load-bearing. Three
verdicts were evicted across those two nights, not two, and the one worth having is the one
that cannot come back: bd_rescue's PAID `scanned via brightdata; roles-text present but no
resolvable board`. `514` asked for a restore; the honest answer is the measurement of why
there is no room for one.

**Evidence.** `rehearse --nights 14 --policy worst --seed 1`: `validate_empty` 65 → 64 on
night 1 before, flat at **65 across all 14 nights** after, 0 orphans throughout. Both new
guards were proved able to fail by reverting each half of the fix in source: no-fold kills
both tests, no-drop-guard kills the guard test. Both mutation records report `killed`.

## Task 4 — the drain's alarm could not see the failure it names

The `out/` fix (`queue_resolve_search.py:500`, `df803a1`) **is** on origin/master and carries
a test; the cloud key is armed (repo secrets set 08-20, and today's digest bought
`unlock_calls=86` from Actions). So the remaining work was the stamp — and the stamp was
blinder than the brief said:

**A disarmed key never reaches the IDLE alarm at all.** With no key
`deep_validate.google_via_unlocker` returns `[]` in silence → `search_one` calls it
`no-search-results` → `queue_state.ingest` records a dated attempt. A fully disarmed night
therefore writes ~112 confident refusals, `searched_recently` reads like a working drain,
the IDLE branch (`selectable and not searched`) never fires, and `tried_within(…, 14)` locks
every one of those names out for a fortnight on a measurement nothing made. That is CLAUDE.md
rule 2 with a paid rung on the other side of it.

Both discriminators were already on disk and unread:

* **`queue_resolve_search._refuse_to_run_disarmed`** — refuses before `ranked_targets()`, so
  nothing is selected and nothing recorded; unrecorded names stay never-searched and sort
  first tomorrow. Reads `DEEP_BD_SEARCH_CAP` exactly as `deep_validate` does, because cap 0
  short-circuits before the key is looked at and is indistinguishable downstream.
* **`queue_pipeline._drain_liveness`** — `empty_search_share` published every night
  (healthy **0.5 %**: 7 of 1,463 attempts 08-29..31; live reading today **1.2 %**; disarmed
  100 %), `queue drain BOUGHT NOTHING` at ≥90 % over ≥10, and when nothing was searched,
  `cloud_state/bd_spend.jsonl` splits "bought and died" from "never started" — the four dead
  shards of 08-30 each left a `credits:1` line, and a keyless shard leaves none because it
  never spends. A partial cap exhaustion lands at 25-50 % and is deliberately *below* the
  alarm: visible in the share, no wolf cried.

All four states were driven end-to-end and print distinct strings; a healthy night stays
silent.

**Cost this bought.** My preflight broke two of my own lane's offline drain tests, which
drive `main()` with a stubbed `search_one` in a keyless environment. They now declare the
rung armed with a dummy key (`tests/conftest.py` bans the transport, so nothing can be
bought). That is a real new obligation on any future test that drives `main()`, and it is
recorded in §2 rather than left to be rediscovered.

## Task 3 — the intel queue: 22 → 18, and one of the 22 was never a company

Re-derived at 14:20Z with the `company-intel` row's own one-liner: **22** (the 05:00 mail's
`intel` stamp said 28; the 10:17 firmo cron drained it in between). Every name was probed —
its board fetched, and Hebrew / Israel / German token counts and the page title read — and
three were retired **as a state** (`active=false` + a `verdicts.TERMINAL` token through
`notes.py`, never a deletion: the merge layer rescues origin's deletions, `458@infra`). None
of the three is in `research_companies.json`, so no `queue_disposition` entry was owed; a
re-discovery would meet the row and stop at `already-a-row`.

| retired | token | the evidence, from the page itself |
|---|---|---|
| **Ecommerce Guide** | `redundant` | self-describes as an aggregator: *"openings pulled daily from verified brand career sites, deduplicated"* — **3,675 roles across 1,484 companies in 71 countries**, top companies Gymshark, Klaviyo, Colgate-Palmolive |
| **Konsortium Ziviler Friedensdienst** | `redundant` | a German consortium job board whose table is `Land / Stellenangebot / ZFD-Akteur` — the third column names the HIRING org (GIZ, pbi, AGIAMONDO), and the single Israel-region posting is **Pro Peace's**. CLAUDE.md rule 5 exactly: Israel jobs on a page are not that page-owner's jobs |

My first version of that ZFD segment was 18 characters too long and `notes.append` paid for
it by evicting `queue-drain 2026-08-29 hunt; 1/1 IL` — the row's own record of the bad
activation, which is exactly the evidence a reader would want. Caught by diffing segments
before/after; the segment was shortened and rewritten so **all four changed rows lose
nothing** (`segments lost: NONE` on every one).
| **Rockerbox** | `duplicate of` | `rockerbox.com/careers` **301-redirects to `doubleverify.com/company/careers`** (acquired), and `DoubleVerify` is a separate ACTIVE greenhouse row — two active rows, one board. Found by the adversarial pass, not by me |
| **Peak Innovation** | `recruiter` | its own careers page: *"העסקה במודל מיקור חוץ (Outsourcing)"*, *"אנו מתמחים ... עד להעסקת העובדים במודל של מיקור חוץ"*, placements at *"חברות גלובליות ותאגידים ... במשק הישראלי"*, agency codes `JB-1620` / `JB-1608`. `ARCHITECTURE.md` §1 already named it as the agency `is_recruiter` cannot see by name; now the row says so |

**18 remain, and one of them is not a backlog item at all**: `Discovery` is the pipeline's
own `platform=discovery` pseudo-row. `research_firmographics` excludes it by platform, but
the `AGENT_BRIEF.md` one-liner every lane quotes does not — so the published number has been
**one too high** for as long as that row has existed. Honest reading: **17 researchable
names**, from 21 (22 less the artefact) at the start.

**What I did NOT do, and why it is not a silent gap.** I retired only what the page itself
proved. Six of the survivors are plainly foreign-headquartered (`Snke` — Brainlab's German
spinoff, `Rockerbox` — NYC, `Jove`, `ImagineArt`, `Happy Mammoth` = *Happy Koala LLC*,
`Abakus Center` — no Hebrew, German tokens) and a foreign HQ is **not** grounds for
retirement in this registry: Nestlé and NVIDIA are in scope for their Israeli hiring. The
real question for those rows is narrower and is a `registry` follow-up, not an intel one:
each was activated on a **1-of-N** Israel count (`Jove` 122/1, `ImagineArt` 33/1,
`Finagra` 9/2), which is the shape a location *query* produces rather than a walked board
(the Comcast class, `487`). Judging them needs the board walked, which is task 2's method
and did not fit this session. Retiring them on suspicion would have been the mass-zero
mistake with a nicer number attached.

## Task 2 — BACKLOG 513: three names settled, and the item's own list corrected

Re-derived: **21 inactive rows + 4 names with no row** (`icon plc`, `majestic labs`,
`mccann`, `viber`), not "22 employers". Four of the item's named faces do not survive
contact — `Trivago` has neither a row nor a discovery card; `Viber` is the ACTIVE row
`Rakuten Viber`; `McCANN` is two existing rows; `אסם` is ACTIVE on a Facebook URL. All of
that is now in `513`'s own text so the next session does not re-derive it.

**TransUnion — settled, and the existing park was already right.** Its stored URL is a
location QUERY (`?locationCountry=Israel`), which is never evidence either way, so I WALKED
the board instead (08-28's rule): `searchText=""`, paged to exhaustion —
**236 of 236 postings, 0 Israel roles by `pipeline.israel.is_israel_job`, and not one
`locationsText` containing "Israel"**. **No note was written**, deliberately: the row
already says `listing-hunt 2026-08-29: no IL listing`, which the walk confirms, and
`verdicts.TOKENS` has no "walked the board" token. Inventing one for a single row, or
restating a verdict the row already carries, would only risk evicting a real segment under
the 220 cap. The finding lives in `513` with the command.

**Prologic LTD — the row's own triage verdict is a false agency read, and I nearly repeated
it.** Its `<title>` markets "השמה הייטק" (hi-tech *placement*) and its note carries
`page-empty (LLM confirms no open roles (Page appears to be a recruitment/j…))`, so it
looked like a clean `recruiter` retirement. It is not: the page is a Bynet-group IT
infrastructure and cyber integrator (SolarWinds, Ivanti, SecPod, One Identity partner
webinars). `השמה` appears **once**, in marketing copy. A real Israeli employer, left parked,
now recorded so nobody retires it on the title.

**Then the render rung settled five of them for 5 Bright Data credits** — the sanctioned
rung 4, `python deep_validate.py --only "…"`, run from the shared checkout because a
worktree has no credentials:

| name | verdict | what shipped |
|---|---|---|
| **Glassbox Ltd.** | `greenhouse:glassboxltd`, **5 jobs / 3 Israel** | I ACTIVATED it and a guard took it back — see below. Now `alias-of Glassbox` |
| Diageo | `workday:diageo/Diageo_Careers`, **0 jobs** | stamped, still parked — and its own `listing-hunt 2026-08-28: verified 10 IL` was deliberately kept beside it, because the contradiction is the finding |
| Nestlé, Wipro Limited | `successfactors` | stamped. WIRE status: the fetcher exists, only the tenant needs cracking |
| Oak | no ATS in the rendered DOM | **no stamp written** — the only one worth adding would have evicted `another company's board`, which is the better fact about a row pointing at Opera Group's shared Teamtailor |

**And the guard that caught me.** I activated Glassbox Ltd. on `activation_verdict` = `ok`
and real Petah Tikva postings — then `test_no_two_active_rows_scan_the_same_board` failed:
an ACTIVE row named **`Glassbox`** already reads that byte-identical board. The company was
covered the whole time; `Glassbox Ltd.` is a NAME twin, exactly the `Viber`/`Rakuten Viber`
shape I had written up two hours earlier and did not think to apply to my own write. The
identity gate cannot see this — it answers "is this board this company's", and the honest
answer was yes. Re-filed as `alias-of Glassbox 2026-08-31: identical board URL (BACKLOG
133)`. **Grep `api_url` before activating a name that looks new**; the board is the
identity, not the name. Indeed-only inactive rows: **21, unchanged**. Novo Nordisk was inside its 30-day deep-validate
cooldown and was not re-read. Two of the four stamps had to be shortened or dropped after
diffing segments before and after: the 220-char cap took `verified 10 IL` from Diageo and
the identity finding from Oak on the first attempt. **Every row I touched this session ends
with `segments lost: NONE`.**

## The adversarial waves — what they broke that I had already shipped

Two Opus passes ran against the diff, one on the code and one on the data. Between them
they returned fifteen findings; **eight changed what shipped**, and the most important one
says my first fix was net-harmful on four rows.

### The code pass: carrying a fact forward COSTS the cell

**The fold made rows impossible to RETIRE, which is worse than the re-check it was
protecting.** The folded segment contains `no open israel roles`, so
`notes._PROTECTED_EXTRA` protects it — where the plain `still unreachable` segment it
replaces was evictable — and it is ~33 characters longer. `notes.append` refuses to evict a
protected segment and drops the newcomer instead, so a cell that fills with protected facts
stops accepting the one verdict that ends a row's life. Measured over the 26 rows of the
02:30 pool: a `domain-dead` stamp lands on **26** after a plain night and on **20** after my
fold. That is the exact bug class `pipeline/notes.py` was written against, introduced by a
fix for a sibling of it.

The rewrite: `_keep_selectors` takes a LIST of candidate segments and writes the first that
costs the row nothing, measured against the real predicates rather than a proxy —
`validate_empty`'s membership, this tool's own pool (and `bd_rescue`'s), and
`_can_still_be_retired` (a 48-char reserve, the width of `scan_dead_domains`'s
`domain-dead <date> (conn-dead (TimeoutError))`, the longest terminal segment any NIGHTLY
writer emits). `_fold_empty` now offers three forms — with `still`, without it, and without
`date0` — because on a saturated cell the fact outranks the adverb and the phrase outranks
the date. **Result over the same 26 rows: `validate_empty` 22 of 22 held, a terminal verdict
lands on 26 of 26, and 23 still get tonight's date** (up from 20 terminal-capable).

The same pass killed my guard's *design*, not just its numbers: it compared the literal
phrase before and after, which is a proxy for pool membership, and it gets `Syte` wrong —
Syte's membership is held by `empty-but-suspect`, not by the phrase, so the proxy froze a
row that had nothing to lose. Ask the predicate; it is the rule.

Three more, all fixed and each with a guard that fails without the fix:

* **The preflight checked two conditions; the rung has four.** `bd_rescue.unlock_status`
  reads `os.environ["BRIGHTDATA_ZONE"]` OUTSIDE its own `try`, so a missing zone RAISES per
  name — ~112 `search-error` attempts, no credit, preflight green — and `BD_RUN_CAP=0` (a
  workflow-level knob) short-circuits before the key is read. Both are the poisoning the
  preflight exists to refuse.
* **`queue drain BOUGHT NOTHING` could not fire on a single bad night.** The window is two
  days wide (the stamp runs past midnight), so a healthy 100 followed by a disarmed 100
  averages to 50 % and clears any threshold worth having. The share is now computed **per
  night, worst night wins**.
* **`_recent_qrs_spend` could raise**, contradicting its own docstring: `int(r["credits"])`
  sat outside the `try`, so one `"credits": "abc"` line would have deleted the entire
  `queue:` line from the mail.

### And the harness caught the guard I had not guarded

Re-anchoring the mutation records onto the rewritten code turned one of them from a formality
into a finding: `retry-fold-writes-over-a-saturated-cell` — which now removes the
`validate_empty` check from `_keep_selectors` — **SURVIVED**, green suite. My two tests
exercise rows where a fold fits, so the plain fallback is never reached and that check never
decides anything. The row that separates the three checks is one where every FOLD is refused
for headroom but the short plain segment fits: there and only there does the pool check
choose between writing a stamp that silently retires the row from `validate_empty` and
writing nothing. I had to search for that shape to build the fixture
(`test_the_guard_would_rather_write_nothing_than_drop_the_row_out_of_validate_empty`), and
it asserts the shape itself so the fixture cannot drift into meaninglessness. All three
records now report `killed`.

### The data pass: four rows

The data pass returned seven findings and **four of them changed what shipped**:

1. **Rockerbox was four lines from a row I had just edited and I walked past it.**
   `rockerbox.com/careers` **301-redirects to `doubleverify.com/company/careers`**, and
   `DoubleVerify` is a separate ACTIVE greenhouse row — two active rows publishing one board,
   the identity class this lane exists for. Verified myself, then retired
   `duplicate of DoubleVerify` with the redirect as the evidence. It was also sitting in the
   intel backlog I was measuring, so the number I would have reported was wrong.
2. **The Israel Opera repair was self-undoing** — see Task 1. Reverted.
3. **A terminal registry verdict reaches nothing downstream** (`518`): `_never_ours` has
   three sources and all three are negative for `Peak Innovation`, so parking it `recruiter`
   leaves its role filed `closed` where `purged` is the correct filing. My retirement is
   right and its consequence does not happen. Filed rather than hot-fixed — no live exposure.
4. **My first ZFD segment was 18 characters too long** and `notes.append` paid for it by
   evicting `queue-drain 2026-08-29 hunt; 1/1 IL`, the row's own record of the bad
   activation. Shortened and rewritten; all changed rows now lose **nothing**
   (`segments lost: NONE` on every one).

Two more were filed as they stand: `519` (`is_aggregator` does not know `ecommerceguide.com`,
a site that describes itself as one) and `520` (two of the drain's `N/M IL` activations are
false against the live boards — `Nascompany` is 5 jobs all Singapore against `1/1 IL`,
`Snke` renders `1 - 0 of 0` against `10/10 IL`).

I also caught two accuracy defects in my own first commit while re-reading it: a comment
citing a `retry ` word boundary the regex does not contain, and "four consecutive runs"
where the streak is **six** (33381636676 → 33396868611, checked job-by-job; `514` said four).

## Not finished

- **Task 2 (BACKLOG 513)** — not started beyond re-derivation. The 513 list
  was re-derived and four of its named faces dissolve on inspection: *Viber* is the active
  row `Rakuten Viber` (workday, reading 0/0 — a WALK-vs-query case), *McCANN* is two
  existing rows, *Trivago* has neither a row nor a discovery card, and *אסם* is ACTIVE
  pointed at a **Facebook URL**, duplicating Nestlé. `Nestle` and `Nestlé` are two parked
  rows on one URL (invariant check B compares exact names, so it is blind to the accent).
- `515` (a cron's `[skip ci]` means a cron-broken pool is only ever caught on another
  lane's push — `infra`'s file, with the proposal) and `516` (`bd_rescue`'s row rebuild:
  cosmetic on a scrape row, would downgrade a native one — diff filed, not taken, because
  churn there touches a nightly paid cron).

## The commit that closed `514` did not run CI, for the reason `514` is about

Worth its own heading because it cost this session its verdict and will cost the next one
too. `20b0e03`'s message explained backlog item `515` and **quoted the CI-skip marker
verbatim** while doing so. GitHub honours that marker anywhere in the message, body
included: `actions/runs?head_sha=20b0e03…` → `total_count 0`, no check-runs, nothing queued
or in progress, while its parent `bf42cae` had 1. So a commit whose entire subject was
un-redding the CI gate silently skipped the gate. **Never write the literal marker in a
commit message, even to describe it** — spell it `skip-ci`. The recovery is the next commit
with a clean message, which runs CI over both; that is the run quoted in `HANDOFF.md`.

## Clause 4

- **Deleted / unified:** the single `drain_alarm` string that listed three causes it could
  not distinguish is gone, replaced by three that each name one. No net deletion of code:
  `git diff --stat` is dominated by `+`, and I say so plainly — the two defects were missing
  guards, not duplicated ones.
- **Extended, not duplicated:** `_note`/`pipeline.notes.append` (the fold writes *through*
  the append-log, never around it); `_drain_liveness` gained two helpers rather than a
  second stamp; the preflight reads `DEEP_BD_SEARCH_CAP` with `deep_validate`'s own parse
  instead of inventing a second one. I declined to touch shared `pipeline/notes.py`.
- **Rejected, with the number:** a `rehearse_registry` exemption for "the row is still in
  another pool" (it would excuse *any* loss while a row sits in the retry pool — most
  triaged rows — reopening the class BACKLOG 320 closed); a new protected stamp class (edits
  shared plumbing and inflates cells already at the cap); shipping `VALIDATE_EMPTY_SIGNALS`
  to go green (it *activates* rows, 28 → ~113, staged behind one Sunday's evidence — using
  it to fix CI is promotion risk as scope creep); falling back to the short segment when the
  fold does not fit (measured: still loses the phrase's only carrier).
- **Would the next session find this?** `grep -n "no open israel roles" *.py pipeline/*.py`
  hits `validate_empty.py`'s selector and `retry_unreachable.py`'s fold and its guard, in
  that order; §2's new subsection is titled with the phrase a reader would search for.
- **What I made harder:** three things, named. (1) A test that drives `queue_resolve_search.main()`
  must now declare the rung armed. (2) `_keep_selectors` means this tool can decline to
  stamp a row — a reader tracing "why is there no retry line tonight" needs to know that.
  (3) One new stamp field, `empty_search_share`, that the mail now carries every night.
