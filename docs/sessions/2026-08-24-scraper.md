# 2026-08-24 — `scraper` lane: error is not empty, and the refresh in four processes

> Append-only, wave-structured; later waves win. Every count is a snapshot — re-derive with
> the command shown next to it before trusting it.

**Scope.** Files written: `scrape_universal.py`, `refresh_scrape_cache.py`, `cache_new_rows.py`
(now a shim), `tests/test_units.py` (+`tests/fake_scrape_worker.py`,
`tests/fixtures/scrape/{arm,xtend}.json`), `ARCHITECTURE.md` §0 env list, §1 tier-2 paragraph,
§5 two table cells, §5a scrape paragraph, `docs/BACKLOG.md` 84–94, this record, one `HANDOFF.md`
line. **Two out-of-lane edits, approved by the operator before they were made:** the bare
`Stamp the 'collect' stage` step deleted from `.github/workflows/scrape-refresh.yml`, and
`scrape rotted` registered in `merge_csv_rows._TOOL`. **Linter-forced, docs-lane text:** the
continue-on-error ratio sentence (26/78 → 25/77) in `ARCHITECTURE.md`, `CLAUDE.md` and
`docs/AGENT_BRIEF.md` (deleting the step changed both numbers; `docs/check_docs.py` refuses the
old sentence).

**Spent, locally.** Bright Data: 0 (`SCRAPE_VIA_UNLOCKER` unset in every local command; note
the cloud run has it on, as it did before this session). Claude tokens from the pipeline: 0
(`SCRAPE_LLM` unset for every refresh; `pipeline.run(use_llm=False)` in the rehearsal). Opus
attacker sessions: 10 (wave 1: 4, wave 2: 3, wave 3: 3) — the tally at the end. SerpApi: not
used.

## Baseline (re-derived before any edit, `git rev-parse --short HEAD` = f74c983 → 429b78c)

| fact | value | how |
|---|---|---|
| active `scrape` rows | **425** (doc said 412) | `python -c "import csv;print(sum(1 for r in csv.DictReader(open('companies.csv',encoding='utf-8')) if r['active']=='true' and r['ats_platform']=='scrape'))"` |
| rows failing `looks_like_a_job_listing_page` | **0 of 425** (doc said "ten of 417") | same loop with the gate |
| `scraped_cache.json` | 228 keys, 1,225 jobs; 271 with description, 50 with `posted_date`; 2 keys are non-scrape rows (`autods`, `kueez`) | python count |
| `cloud_state/scrape_rot.json` | **207 entries, 207 `empty`, 0 `error`** | python count |
| last cloud refresh (run 32677334301, 00:39→02:30 UTC) | **428** processed (the first draft said 433 — five stray env lines matched the parse), 228 with jobs = the 228 cache keys, 200 zero, **0 ERROR**, **111.6 min sequential**, median 11.4 s, mean 15.6 s, max 368 s (Ford) | `gh run view 32677334301 --log`, timestamps diffed per company line; re-derived by wave-2 attacker B |
| `collect` stamp | `{}` — no counts reach the mail | `cloud_state/pipeline_stages.json` |
| test suite | 297 cases (187 units + 110 registry), green | `python -m pytest -q --co` |
| `cache_new_rows.py` | run by no workflow; refresh always follows the 19:00 hunt (`repo-state` group); its 163 "missing" rows are all `empty` in rot | grep of `.github/workflows`, python count |

Zero errors across 428 arbitrary websites is not a measurement of the web; it is the proof that
the error path could not fire.

## The defects

### 1. Error was indistinguishable from empty
`scrape()` caught every navigation exception at the old line 251 and returned `[]`; the main
document's HTTP status was never read. So `refresh_scrape_cache`'s `jobs is None` branch —
carry-forward, `error` rot, parking — was dead code, and a 403 night deleted a company's jobs
from the cache while the product read "no openings".
**Fix.** `scrape_universal` is split into `_render(url)` (Playwright only; returns a `Rendered`
bundle with `http_status` and an `error` code) and `_extract(...)` (pure; the five strategies,
first hit wins). `scrape_result()` returns `ScrapeResult(jobs, status ∈ ok/empty/error, error,
strategy, rescued, …)`; `scrape()` is its list-only wrapper and still never raises (`resolve_deep`
calls it bare; `tests/test_registry.py` monkeypatches it with a two-argument lambda).
**Pinned by** `test_scrape_result_tells_a_broken_page_from_an_empty_board` (20 rows),
`test_scrape_never_raises_and_scrape_result_reports_the_failure`,
`test_refresh_carries_jobs_forward_on_error_but_never_on_empty`.

### 2. Sequential, and one company unbounded
111.6 min a night for 428 rows; Ford took 368 s (45 s goto + 12 s network-idle + 25 position pages × 12 s).
**Fix.** `refresh_scrape_cache` runs a `spawn` process pool (`SCRAPE_WORKERS`, default 4), one
Playwright per process, recycled by hand every `workers × 25` rows; a per-company `Deadline`
(`SCRAPE_COMPANY_BUDGET_S`, 150 s) clamps every network wait; the position-page timeout is 8 s.
Bookkeeping happens in the parent in registry order, so the pool and the inline path produce
byte-identical output (`test_refresh_pool_and_inline_paths_build_the_same_cache`).
CPython's `max_tasks_per_child` was tried first and **hung the full rehearsal at exactly
4 × 25 = 100 rows** (0 Chromium processes, log frozen for four minutes) — hence the manual
chunking (`test_refresh_pool_runs_a_module_level_worker_under_spawn` proves recycling).

### 3. Nothing about the scrape reached the email
The workflow stamped `collect` bare, `if: always()`, after the script; `stages.stamp()` replaces
the entry, so even a script that stamped counts would have lost them. `scrape_rot.json` was read
by nothing; `fetch_scrape` cannot fail; `health.py` exempts scrape rows.
**Fix.** The script stamps `collect` itself on every exit path with `rows scraped with_jobs empty
no_il errors carried unprocessed parked workers minutes` and an `alarm` token only when something
is wrong; the digest already prints `stages.summary()` verbatim. The workflow step is gone (a
crashed script no longer reports "collect: TODAY"). Pinned by
`test_refresh_stamps_collect_with_counts_the_digest_renders` (all three renderers) and
`test_no_workflow_step_restamps_collect_after_the_refresh`.

### 4. Non-atomic writes → `pipeline.atomic.write_json` (`test_atomic_write_json_leaves_no_partial_file`).

### 5. No side-effect-free local mode
Any local run parked rows in the real registry. **Fix.** `--only/--limit/--only-missing/--shard`
scope a run and scoped runs write nothing (`--apply` merges hits into the cache, additively);
`--dry-run` for a full read-only rehearsal; `SCRAPE_CACHE_OUT` / `SCRAPE_ROT_OUT` /
`SCRAPE_STAGES_OUT` redirect the three outputs. `test_refresh_scoped_and_dry_runs_write_nothing`.

### 6. A deliberate behaviour change, measured
The old extractor ran strategy 2 (DOM links) even after strategy 1 had succeeded. On 23 captured
real pages that added **18 entries, all run-together card blobs**: Nebius 1 (same URL as a clean
twin), VAST Data 1 (a United States role stamped `"EngineerIsrael"`), Port.io 16 — 6 US roles
(Palo Alto/Boston/Austin) mislabelled `"Editor Tel Aviv - Israel"` and 10 mangled twins of the
10 clean Tel Aviv roles the structured pass returns (different URL/`job_id`, so `merge_key` did
not collapse them: the old parse published **two board cards per Port.io posting**, one with a
corrupt title). **No real, distinct Israeli posting is dropped by the change** — re-derived by
wave-2 attacker C, correcting this record's first draft, which said "14 Palo Alto roles" and
"two real Tel Aviv roles lost". The new extractor stops at the first strategy that yields.
`test_scrape_strategies_stop_at_the_first_one_that_finds_jobs`.

## The A/B — old parse vs new parse on identical render payloads
Before the first edit, the pre-refactor file was frozen as a shim (render split from parse
mechanically at the old line 255, `urlopen` calls routed through an injectable `fetch`), 23
active URLs were rendered **once** with it (structured / DOM / cards / links strategies, four
empties, two 403-walled, three Hebrew), every plain fetch recorded, and the old output saved.
`ab_diff.py` replays the new `_extract` on the same payloads (scratchpad, not committed):

    TOTAL lost 18 gained 0 — strategies {'structured': 3, 'cards': 5, 'links': 4, 'dom': 2, '': 9}

The 18 are defect 6, nothing else. Only the two DOM-strategy payloads were small enough to
commit (`tests/fixtures/scrape/arm.json` 41 KB, `xtend.json` 32 KB); the other strategies are
covered by synthetic fixtures in `test_units.py`. **Unverified in the repo:** equivalence on real
structured/cards/links pages rests on this diff, which lives in the scratchpad.

## Wave 1 — four attackers (Opus, read-only), 16 findings: 13 fixed below, 3 filed with their owners
Lens A (error/empty), B (concurrency), C (registry side-effects), D (the mail path). Every
finding below was reproduced by me with the attacker's snippet before it was fixed.

| # | finding | fix | pinned by |
|---|---|---|---|
| A1/C1 | a rot streak kept `since` when it flipped `empty→error`: a row honestly empty for 60 days would be parked on its first transient timeout — 184 real rows one error from deactivation on 2026-08-30 | a flip starts a new streak; parking counts **observed** nights (`n`), so a budget-skipped night does not advance the clock | `test_refresh_a_streak_restarts_when_its_kind_changes_and_counts_observed_nights` |
| C2 | the rot entry survived a park, so a row listing-hunt re-activated was re-parked on its first error (`error 200d`) | rot cleared when a row is parked | in the carry test (`"Acme" not in rot`) |
| C4 | `parked=N` stamped on mass-failure / shrink nights that parked nothing | `parked` computed after the decision | shrink test asserts `parked == 0` |
| A2 | goto timed out after the first XHR page landed → 2 jobs, `rescued=True`, **overwrote a 30-role entry** with no error | a rescued result smaller than yesterday's is an error night (`partial:…`, carried) | `test_refresh_a_partial_rescue_keeps_yesterdays_fuller_list` |
| A3 | a function-valued `window.__data` is marshalled as `None` and crashed the parse into `internal:TypeError` | non-strings skipped; the in-page JS only pushes objects | in `test_scrape_recovers_a_job_object_…` |
| A4 | HTTP-200 walls read as "no roles" — Akamai's and Nokia's captured pages are literally `<title>Access Denied</title>` with a 200 | `_blocked_by()` markers (Akamai, Cloudflare, PerimeterX, Incapsula, DataDome, Distil) → `error block:<vendor>` unless plain HTTP got a readable page | 4 rows in the classification table |
| A5 | a 200 with nothing captured at all was "empty" | `error render:blank` | classification table |
| A6 | "37 roles found, 0 in Israel" was byte-identical to "nothing on the page" | `no_il` in the stamp, `found` in the rot entry | `test_refresh_records_roles_found_but_none_in_israel` |
| A7 | the plain-200 rescue applied to `goto:` failures but not `render:` ones | one rule for any render error | classification table |
| B1 | a worker that never returns blocked executor exit and the atexit join: one hung Chromium turned a finished night into a 330-min killed job with nothing committed | stall watchdog (`STALL_S` = 3 × company budget): terminate the chunk's children, report the rows in flight as `hang:` errors, next chunk on a fresh pool; `main()` ends with `os._exit` after flushing | `test_refresh_a_stuck_worker_costs_its_rows_not_the_night` (real spawn pool) |
| B2 | the shrink guard counted carried-unprocessed entries, so a budget-starved night masked a mass-empty | measured over companies that had jobs yesterday **and were processed** | second half of `test_refresh_shrink_abort_…` |
| D3 | `unprocessed-N` alarmed on any N | only above 5% of rows | — |
| — | a redirected full local run still wrote the tracked stamp | `SCRAPE_STAGES_OUT` | — |

Not fixed, filed with the owner: **C3** a rot-parked row carrying `dark-triage: page-empty` is
excluded from the hunt pool (BACKLOG 84, `registry`); **D1/D2** nothing reads `alarm` and
`require("collect", 1)` is silent at exactly one day (BACKLOG 85, `infra`, hook text given);
**B (cosmetic)** `.tmp_*` files from a hard kill are not gitignored (infra).

Attacks that found nothing, so they are not re-run: no duplicate `company_name` in the
registry (exact, case- or whitespace-insensitive); `_note_replace` cannot delete another
tool's segment; the park note is in `check_invariants`' and `listing_hunt`'s pools and in
`verdicts.TOKENS`; `_park` re-reads and skips rows deactivated mid-run; `tools/mutate.py` still
classifies the file as a park-only writer; no value that can reach the stamp contains a space,
`|`, `=`, `*`, `<` or a company name; the three renderers print `%` and `+` literally;
`scrape-refresh.yml`'s conflict path restores only the two files it owns.

## The rehearsal — tested as if it ran in tomorrow's email (16:20–16:30 UTC, 0 spend)

1. `SCRAPE_CACHE_OUT=<scratch> python refresh_scrape_cache.py --only "<25 names>" --apply
   --workers 4` — ten structured/cards/links boards, three Eightfold/Phenom, four known-empty,
   four walled, Ford, three Hebrew/.co.il. Result: `rows=25 scraped=25 with_jobs=14 empty=7
   no_il=0 errors=4 carried=0 … minutes=2`, errors `Akamai (http:403)`, `Ford
   (goto:TimeoutError)`, `Blender (http:404)`, `Fugu (http:404)` — the two 404s are dead
   listing URLs that will be parked after 7 error nights and handed to the registry pools,
   which is the designed path. The tree was byte-identical before and after (only the
   scratch cache changed; `--only` never writes the registry, rot or stamp).
2. Diff of the scratch cache against the committed one, 25 names: `TOTAL lost 68 gained 30`,
   of which Arm 23→2 (the frozen old extractor also gets 2 today — the site, not the code),
   Elbit 21/22 and Google 4/4 are listings that rotated since the cloud run, Port.io 29→10 is
   defect 6 (6 wrong-country entries, 12 mangled twins) **plus one real DOM-only role,
   "Security Operations & Automation", that the structured pass does not see — the one
   known coverage cost of first-hit-wins on a Comeet embed, BACKLOG 88**. Descriptions
   carried forward: 27 → 28; `posted_date`: 46 → 46 (the announced-date rule found no new
   dates on these pages — the feature is honest, not productive, yet).
3. `rehearse_run.py` (scratchpad) pre-seeds `pipeline.fetchers._SCRAPE_CACHE` with the
   scratch cache, points `stages.PATH` at a scratch copy of today's stamps with the full run's
   real `collect` stamp, disables secrets/JD/LLM, and runs `pipeline.run.run(only=<25>,
   out_dir=<scratch>, db_path=<scratch>)`. The rendered audit, verbatim from the `.md`:

       - Stage order: repair: never run | collect: 2026-08-24 (TODAY) carried=6 empty=160 errors=48 minutes=37 no_il=0 parked=0 rows=425 scraped=425 unprocessed=0 with_jobs=217 workers=4 | expand: 2026-08-24 (TODAY) | enrich: … | publish: … board=61 email=6 …

   and the same substring in the `.txt` and (escaped) `.html`. With a mass-failure stamp the
   line reads `collect: 2026-08-24 (TODAY) alarm=mass-failure-errors-98% carried=222 …` in
   all three. `git status` afterwards: unchanged.

## The regression verdict (the live old-vs-new comparison)
Every company the full run lost or shrank against the cache — 23 lost, plus Arm, Aqua
Security, Freightos, Anecdotes, Amazon Israel — was rendered **again, now, with the frozen
pre-refactor extractor and with the new one** (`live_compare.py`, scratchpad): **28 of 28
identical counts** (23 × 0/0; Arm 2/2; Aqua 1/1; Freightos 6/6 via `structured+dom`;
Anecdotes 1/1; Amazon 9/9). The 1,225 → 1,178 movement is the sites, the IP and the day.

## The full-scale timing run (local, `--dry-run`, 4 workers, 15:30–16:08 UTC)

`python refresh_scrape_cache.py --dry-run --workers 4` over all 425 active scrape rows,
`SCRAPE_VIA_UNLOCKER`/`SCRAPE_LLM` unset (0 spend), nothing written:

    === collect: rows=425 scraped=425 with_jobs=217 empty=160 errors=48 carried=6 unprocessed=0
        parked=0 workers=4 minutes=37 | strategies {'links': 81, 'structured': 35, 'cards': 57, 'dom': 44}

(this run predates the `no_il` key, which wave 1 added the same evening — a line from the
current code carries it, see the rehearsal)

**37 minutes against 111.6 sequential in the cloud** (median 17 s per company, p95 37 s, max
103 s — Ford, which took 368 s, now ends in 66 s as `goto:TimeoutError`). Errors by kind:
24 `block:cloudflare`, 9 `http:404`, 8 `http:403`, 4 `block:incapsula`, 1 `http:503`, 1
`goto:TimeoutError`, 1 `goto:Error` — 11% of rows, under the 20% mass-failure guard; six of
them had jobs in the cache and were carried. Against the committed cache (228 companies, 226
processed): 23 companies that had jobs came back with none, 25 new companies produced jobs,
25 shrank, 19 grew; 1,225 → 1,178 jobs. The 23 + the largest shrinks were re-rendered with the
**frozen pre-refactor extractor and the new one side by side** to separate the site/IP/day
from the refactor — result in the next section. Caveats: a residential IP (the runner's
datacenter IP sees more walls, not fewer); the LLM tier off (the runner's `with_jobs` may be a
few higher); the unlocker path untested locally.

The first attempt of this run **hung at exactly 100 rows** — CPython's `max_tasks_per_child`
— and a legacy rot entry (`last=today`, no `n`) crashed the rehearsal's second attempt with
`KeyError: 'n'`; both would have hit the 2026-08-25 cloud run. Both are fixed and pinned.

## Wave 2 — three attackers (A: `SCRAPE_ASSUME_IL`, C: regression diff, D: re-attack of wave 1)

| # | finding | fix | pinned by |
|---|---|---|---|
| C1/A3 | **my own bug**: the `\b` in `_CARD_DATE` went through a non-raw string and became a literal backspace byte, so only the unanchored "N days ago" branch was live | the two bytes replaced; a date must be announced (`Posted …`) | `test_posted_date_is_read_from_the_card_and_never_from_its_neighbour` |
| C2/A1/A2 | that live branch stamped the NEXT card's (or the footer's) age on a role; `run.py` prefers an ISO date over `first_seen`, so the role would never enter the 48-hour email | no date from the DOM strategy at all (its context is four ancestors' text run together, unattributable); cards read only their own bounded span, cut at the Apply button; unannounced ages are ignored | same test |
| C3 | a leadership array in page state (`{"name": "Dana Levi", "role": "VP Marketing", …}`) passed the title test through `role`, won as "structured", and hid the real board | a `name` that is not a role beside a `role`/`title` that is = a person, not a posting | `test_scrape_a_leadership_array_is_not_a_board` |
| A4 | a page-state blob with one or two "featured" postings suppressed a DOM-rendered board (synthetic; not seen on the 23 pages) | fewer than 3 structured hits fall through to the DOM pass and union with it (`structured+dom`) | in `test_scrape_strategies_stop_at_the_first_one_that_finds_jobs` |
| C4 | this record's first draft mis-stated the Port.io loss ("14 Palo Alto roles", "two real roles lost") | corrected: 6 US-titled, 10 mangled twins, none lost | — |
| D1 | **my own regression**: the partial-rescue rule compared tonight's read with the list it had carried itself, so a healthy 403-walled company rescued in full every night would be deactivated 7 nights after one role closed | a rescue with no browser error is a complete read; a browser-failed partial is held back `PARTIAL_MAX_NIGHTS` (2) and then believed | `test_refresh_a_rescued_full_read_is_believed_and_a_partial_one_converges` |
| D3 | `challenge-platform` (Cloudflare's JS-detections script on ordinary 200 pages) and `_Incapsula_Resource` read as walls | markers anchored to titles/denial text; those two removed | `test_scrape_a_cloudflare_protected_page_is_not_a_wall` |
| D4 | the shrink guard's 20-row floor re-opened the budget-starved hole; the fix was unpinned (reverting it survived the test) | floor 5 over processed companies that had jobs; a 10-of-25 case that only the processed view catches | shrink test, third case |
| D5 | `_kill_children` ran after `shutdown()`, which clears the executor's process table: it terminated nothing | children taken before shutdown | `test_refresh_the_stall_watchdog_really_terminates_the_stuck_child` (asserts the child is dead) |
| D6/D7 | `parked=0` under mass failure and the same-day `n` de-dup were unpinned | asserted with parkable rows / a same-day second run | mass-failure test, streak test |

Accepted, not fixed: **D2** a browser-failed partial read for a company with no cached history
is cached as the truth — there is nothing to compare it with; it self-corrects the next full
night. Held on the ten `SCRAPE_ASSUME_IL` payloads: title/location acceptance is byte-identical
old vs new (a nav menu with an Israeli footer yields 8 junk "roles" in **both** — the URL gate
`looks_like_a_job_listing_page` refuses all nine junk URLs, as documented), and on the 23 real
captures the new parse gained 0 rows under the flag. Every `seen_id` the new parse emits already
existed in the old output — nothing `sent`/`matched` is orphaned or re-sent.

## Wave 3 — three attackers (A: cold read, B: the operator's morning, C: mutation survival on the commit)

**A — cold read, 6 findings.** (1) `merge_json_cache.merge` cannot express a deletion: on a
push-conflict night every company the refresh dropped (empty, expired carry, parked) comes
back with yesterday's jobs — pre-existing, `infra`'s file, filed as BACKLOG 95 with the rule
to add. (2) The company budget expiring *inside* strategy 4 truncated a list and nothing
flagged it — fixed: a truncated pass sets `deadline:links`, so the refresh treats a smaller
result as a partial read (`test_scrape_a_deadline_inside_the_position_pages_is_a_partial_read`).
(3) A persistent mass-failure freezes the cache with no ageing — documented as the bound it
is; a budget cut stranded the same registry tail nightly — the processing order now rotates
by the day (`test_refresh_rotates_the_processing_order_by_day`). (4) `rot.pop` ran before
`_park`, so a failed registry write erased seven nights of evidence — the registry is written
first (`test_refresh_a_failed_registry_write_keeps_the_streak_that_justified_it`); and
`SCRAPE_CSV_OUT` joins the redirects. (5) The stall loop could stamp `hang:` on a future that
completed in the race window — `done()` is checked. (6) The workflow comment sized the budget
against "~850 rows" — corrected to 425.

**B — the operator's morning, 25 findings, all documentation.** The worst: §5a's example
`collect:` line was a sketch the code cannot emit (no `no_il`, counts that do not reconcile)
and no doc gave the arithmetic to check a line with. Fixed in §0/§1/§5a/§5b: the line is the
literal re-render of the rehearsal; the reconciliation rules; what a crash night and a
mass-failure night actually commit; the cloud run's `SCRAPE_LLM`/`SCRAPE_VIA_UNLOCKER`
against the local measurement; `SCRAPE_VIA_UNLOCKER` as a spend switch in the env list;
`parked=0` expected until ~2026-08-31; `carried` defined; the rot fields in the §5b runbook;
the retracted Port.io numbers in BACKLOG 88 and the dedup/pointer errors in BACKLOG 93 (found
by wave-2 B as well); this record's own "verbatim" quote that predated `no_il`, and its count
of the sequential cloud run (433 → 428: five stray env lines matched my parse).

**C — mutation survival**, 24 one-token mutations against the commit: 19 killed, 5 survived — all five coverage gaps, no code defect (a 4xx row without a body; no case in the 20–96% mass-failure band; a stale closure that made the shrink test's third block re-run the second's outcomes; the stall loop's `done()` branch unreached; the unprocessed alarm's 5% floor). Each is pinned with the assertion the attacker named. Three further predicted survivors (`_extract`'s 3 s plain-fetch gate, `_readable`'s 2,000-byte floor, `_LINK_PAGES_PER_PREFIX`) are filed as BACKLOG 96.

## After the waves — the hook (BACKLOG 85, closed with the operator's approval)
`stages.alarms("collect")` returns the lines the digest must say — the stamp is older than
today, or it carries `alarm=` — `pipeline/run.py` prints each as `::warning::stage …` and
puts them in `summary["stage_alarms"]`; the three renderers print `- **Stages:** …` beside
`- **Registry:** …`. Pinned by `test_a_stale_or_alarmed_collect_stamp_reaches_the_mail`.
Shared plumbing touched: `pipeline/stages.py` (+1 function), `pipeline/run.py` (+5 lines),
`pipeline/digest.py` (+6 lines) — every lane imports these; the change is additive.

## Tally
Ten Opus attacker sessions (4 + 3 + 3). Code defects fixed and pinned by a behavioural test:
wave 1 — 13; wave 2 — 9; wave 3 — 4. Filed with their owners, not fixed: 5 (BACKLOG 84, 85,
95, the `.tmp_` gitignore, the digest workflow's wholesale restore). Accepted with a reason:
2 (a partial read for a company with no history; a wall that answers with a readable plain
page). Doc corrections: ~30. Unit cases: 187 → 247 on the committed tree (+110 registry cases untouched).

## What the reviewers found SOUND — do not "fix" these
- `scrape()` returning `[]` on error for the other lanes: they all treat an exception as `[]`
  already; the distinction is opt-in via `scrape_result()`.
- The mass-failure branch writing `old ∪ successes`: it never shrinks, so the shrink guard
  cannot fire on top of it, by construction.
- Nokia reading as `empty`: the render hit Akamai's wall but plain HTTP got the real 234 KB
  page with no listing on it — the plain page decides, as designed.

## What I did NOT do, and whose it is
See BACKLOG 84–94. In one line each: hunt-pool exclusion of rot-parked page-empty rows
(`registry`); the `alarm` → `::warning::` hook (`infra`); `fetch_scrape` cache-path override
(`ats-fetch`); retiring the `cache_new_rows.py` shim (`docs`); `_loc_from_ctx` title bleed and
strategy-2 card quality (`scraper`, next); the two unmeasured scraper costs (`scraper`);
per-job strategy provenance (`scraper`, deferred); a warning-first invariant on the stamp
(`infra`); mutation records (`registry`); stale numbers in the diagram / brief / HANDOFF
(`docs`); the digest workflow's wholesale `cloud_state` restore (`infra`).
