# 2026-08-30 — `scraper`: the query stamped its location on the cards, and four floors hid real boards

**Lane:** `scraper`. **Spec:** ARCHITECTURE.md §1 item 2, §5a. **Base:** `deb030c`.
**Worktree:** `.claude/worktrees/scraper-0830b`, branch `scraper-0830b`.
**Files written:** `scrape_universal.py`, `refresh_scrape_cache.py`, `scraped_cache.json`
(one merged lakeFS entry), `tests/test_units.py` (the 2026-08-30 scraper block + three
retargeted older tests + two regenerated dom fixtures — only `_loc_src` added, values
byte-identical), `ARCHITECTURE.md` (§1 item 2, §5a, two dated cross-references in §2/§7c
whose claims my change made stale), `docs/BACKLOG.md`, `HANDOFF.md` (one line + one
morning-checks row), `bd_rescue.py` (**cross-lane, the debt rule**: 468's filed three-line
diff applied, copy deleted — lanes `scraper`→`registry`), this file.
**Nothing out of lane otherwise:** `companies.csv`, `pipeline/*`, the workflows, `store.py`,
`health.py`, `fetchers.py`, `israel.py` untouched.

**Mode: dry for every measurement; armed only for the lakeFS rescue** (`AJIL_SECRETS`
pointing at the operator's file — never copied — `SCRAPE_VIA_UNLOCKER=1 BD_RUN_CAP=10`).
**Spent: 0 Bright Data credits** (the rescue's plain reads sufficed; `unlock_calls=0` on
every run), **0 pipeline LLM calls** (`SCRAPE_LLM` never set locally), 0 SerpApi. Subagent
tokens: 3 Explore + 2 Plan + 3 Opus wave-1 attackers + 1 Opus wave-2 confirmer.
`python -m pipeline.bd_budget` at 21:54Z: 11,933 MTD, no ceiling until 2026-09-01.

## Morning checks — none were owed

The Morning-checks table had no `scraper` row. One is added, due 2026-08-31 (the first
unattended run of the new counters).

## The brief, re-derived — six of its numbers were wrong

| brief said | measured (origin/master `deb030c`) |
|---|---|
| 761 postings `location=="Israel"` | **700** (716 case-insensitive) |
| 6 provably wrong, URL-path floor | **73 provably non-Israel** (own title/url, zero IL evidence; 39 by url path alone); 142 with description signals; **479 of 700 carry no evidence either way**; 221 carry some |
| 23 active query-URL rows | **43** by the repo's own `audit_query_urls.has_location_query()`; ledger: 21 honoured / 19 unverifiable / 20 parked ("60 before parks" was right) |
| Meta's url is the offices[0] listing | its cached posting carries a real `/profile/job_details/` url |
| 26 open scraper items | **30** by the index (154 and 245 parsed open while substantively closed since 08-26) |
| `scrape_rot.json` has no reader (154) | `health.record` reads it since 08-26 (`pipeline/health.py:225`) |

Confirmed: 635 shared-url postings / 94 urls exact; 11 published bare-"Israel" rows;
Modellama pair (variant closed 08-27 — history); Sunflower = two real openings (distinct
comeet uids); rot 273 entries; 228's four roles all still missing.

## What was wrong, and what changed (six commits)

1. **`debfc07` — error ≠ empty.** `_classify` booked an ip-refused render "empty" on any
   readable plain 200 — lakeFS's WAF decoy sat 5 nights as `why: empty` beside
   `error: http:403` (with Nokia and Schneider Electric) while `health.overnight_verdict`
   had the fetch-error verdict waiting on the word "error". Now an ip-shaped refusal
   proves empty only when the plain page SHOWS a jobs section (`_plain_proves_empty`),
   with a belt in `_apply_result`; `_ip_shaped` moved to `scrape_universal` so the two
   cannot drift. Seam contract written into §5a (four invariants); the reader half filed
   as `507@ats-fetch` (renumbered from 503 at rebase — attacker C's F1) with tonight's census (**21** error-uncached rows, not the brief's
   32-by-another-count). 468 applied to `bd_rescue.py`; 245 and 154 closed as already-done.
2. **`f059fde` — the headline: the listing URL is never a location.** `_page_is_il`'s URL
   arm DELETED — not query-stripped: 33 rows are PATH-scoped (`/search-jobs/Israel`,
   `careers.arm.com/location/israel-jobs/`) and path-only would have re-shipped 22 of the
   73 provable-foreign (Arm 17, Intuit 3, Sanofi 2). A card nothing placed is refused at
   the ONE refusal point (`_Adder._judge`, which the fabricated "Israel" had been
   defeating) and counted; `_clean_loc` stops fabricating; the listing-url fallback is
   query-stripped (`_bare`); a foreign tail writes `country_code="XX"` (the gate's
   authoritative NO — the path echo `_bare` cannot strip); `_loc_src` ∈ own/group/assumed
   on every job, `"query"` unproducible; `_Board.flush` (evidence-based, Pecan/VAST) kept,
   tagged `group`; the LLM spend gate stays open on scoped pages (`_url_scoped_il`).
   Alarms: `fabricated-loc-N` (any unprovenanced bare "Israel" in tonight's fresh reads —
   zero by construction, so any N is a regression), levels `loc_unknown=N` and
   `legacy_loc=N`. Decisions argued: emit-empty-and-refuse beat trusting the registry's
   `honoured` ledger (30-day induction window; hermeticity; measured protection ≤1 genuine
   posting across the 21 honoured boards — 17 of their 18 stamps carry own evidence) and
   beat stamp-with-provenance (known-untrue values other lanes read). NO cache scrub: 255
   of the 700 sit on 13 inactive rows and evaporate at the next full rebuild; 445 re-
   extract honestly at 00:00; carried residue is counted (`legacy_loc`), not mutated.
   The hunts' `SCRAPE_ASSUME_IL="0"` disarm was being read as ON (truthiness) — fixed at
   the one read site. 242 closed as already-applied (measured: importing all three
   modules sets nothing); 462 + 496's scraper half closed, eightfold half re-filed `504`.
3. **`391e9b2` — a comeet slug settles the title tail (235).** Re-measured before fixing:
   **118 of 295** sluggable cached titles carried a chip tail (not 31 of 50), the slug
   named the clean title in ALL 118, the type-word splitter cleaned 0. `_comeet_slug_cut`
   cuts only a title that strictly EXTENDS its slug — Legit Security's nine neighbour-url
   cards are never renamed — and the residue is judged as the card's own place (`, ST`
   reads as a US state code by shape, IL excepted: Comeet spells Israel that way).
   Bonus measured on replay: Utila's three run-together widget titles
   (`Senior DevOps EngineerEngineeringTel AvivApply`) became three real Tel Aviv roles.
   `merge_key` untouched — the STOP condition was not met. 497 closed (`html.unescape` in
   `_judge`). Vocabulary added: exactly Texas/California/Philippines (247 annotated).
4. **`59ca9e7` — an address is taken from what NAMES the role (434).** The heading-inside-
   anchor card shape (`<h3><a href>`) matched NO pattern — Google Israel's 20 cards sat
   url-less for it (corpus: 20→0); a window anchor whose text or slug names the title is
   preferred; `resolve` matches slugs when link text is chrome; byte-nearest survives as a
   fallback REFUSED when its slug names a different role (Legit's class → 0 on the
   corpus). Still-weak readings are stamped `_own_url: false` at write time (jd-text's
   `_jd_shared_page` keeps its fetch-time meaning); `ownless=N` + `ownless-up-A-to-B`
   jump alarm (`_uncached_base` grew a `key` parameter instead of a copy; each anchor
   carries its own rows). Genuinely link-less, measured on the corpus: **5 of 81 boards /
   60 postings** (Elbit 23, VAST held-group 17, Plus500 15, Aleph Farms 3, Magic
   Software 2).
5. **`37963ee` + `26f4676` — what 228's four boards taught the ladder.** All four
   re-derived live: **nsKnox** — its registry url IS the position page; a gated one-
   `_Board` rung reads it (`1 via links`). **GenCell** — Wix wraps heading text in spans,
   `[^<]` matched none of its 12 roles; pattern fixed, tags stripped; the board prints no
   locations, so all are honestly refused and counted (`loc_unknown=11`) — placing them is
   the registry's call, not a stamp. **Predicta Med** — 2 roles, below the ≥3 junk-gate
   floor, which stays. **lakeFS** — behind THREE separate mechanisms: the decoy 403
   (commit 1 reclassified it), the 3-link floor (a short same-site prefix with ROLE-worded
   slugs now opens), and a dropped multi-region label — `Location: Remote – US East
   Coast, Europe or Israel` now ships as the role's own claim of Israeli eligibility (the
   symmetric half of the label-settles rule; foreign-only labels still settle foreign).
   Director RECOVERED, `1 via links`, merged; 0 credits — the plain page had it all along.
   Found on the way: the unlocker rung, built for walls, fired only when plain HTTP
   returned NOTHING — a decoy 200 kept it idle; it now buys the credit on a decoy, and an
   accepted proven-empty drops its refusal code so `_classify` and the belt cannot
   disagree (they did, live, on the first rescue attempt).

## Replay (81 captured bundles, hermetic, origin/master vs branch)

`same 271 · lost-real 0 · lost-stamped 28 (the intended honest drops, all on scoped
boards) · retitled 28 (slug cuts + unescapes) · gained 16 (Utila 3 real roles among them)
· promoted 20 · url-less 72 → 53`. Pecan 6/6 and VAST unchanged. The two C2A Security
"losses" are corrections: roles titled `based in Germany` / `based in East US` had been
proximity-lent `Jerusalem, Israel` by the old extractor.

## Clause 1 — the number

Postings whose location came from the query rather than the posting: **700 stamped, 479
with no evidence, 73 provably wrong → mechanism deleted; expected after the first full
cron: fabricated 0 (alarmed), ~305 evidence-backed stamps surviving, the rest honest
refusals.** Measured after the push by the morning-checks row (below) — until that cron
runs, the cache still holds the old stamps and this lane reports the mechanism closed,
not the cache clean.

## Clause 2 — it keeps moving without a session

Workflow: `scrape-refresh.yml`, cadence `0 0 * * *` (exists, unattended for weeks).
Alarms, in the mail's `Stages:` line via the existing `_alarm`→`stages.stamp` channel (no
workflow change needed): `fabricated-loc-N`, `ownless-up-A-to-B`; levels `loc_unknown`,
`legacy_loc`, `ownless` on the collect stamp. First unattended proof: the 00:00 UTC run
after this push — quoted in HANDOFF's morning-checks row when it lands, not before.

## Clause 4 — evidence

- **Deleted/unified:** `git diff --numstat origin/master...HEAD` → **+1100 −182**.
  Deleted: `_page_is_il`'s URL arm, `_clean_loc`'s fabrication, `_from_cards`' second
  refusal path (the `continue`), `refresh`'s `_ip_shaped` copy, `bd_rescue._load_secrets`'
  body (468). The + is dominated by tests (+438) and docs.
- **Extended, not duplicated:** `_Adder._judge` (the one refusal point, now the counter),
  `_split_title_tail`'s channel (`place_hint`), `_title_in`/`_match_weak` (the one
  matcher, reused for slugs), `_uncached_base(key)`/`_uncached_grew`/`_next_base` (one
  ratchet for two counters), `_parse_position_page`/`_Board` (the one-page rung),
  `_plain_proves_empty` (shared by `_classify`, the belt, and the decoy-unlock gate),
  `pipeline.secretsenv.load`, `_never_ran` (245 closed as already-done, not re-built).
- **Rejected, with the number that killed it:** query-stripping `_page_is_il` (22 of 73
  provable-foreign came via paths); trusting `query_filter.json` `honoured` (≤1 genuine
  posting protected, 30-day induction window); a cache scrub (the operator banned post-
  processing; 255/700 evaporate free); `_TAIL_TYPE` vocabulary growth (the slug cut needs
  none); lowering the cards floor for Predicta (2 bare titles are exactly what the junk
  gate exists for); an LLM url-copying schema (fabrication risk; resolve+slugs got there
  from evidence).
- **Would the next session find it?** `grep -n "location" scrape_universal.py` lands on
  `_loc_src` and the `_judge` counter; `grep -rn "fabricated-loc"` lands on `_alarm` and
  §5a; `grep -rn "query"` in ARCHITECTURE lands on the §2 class with the dated closure.
- **What got harder (count: 8 new names):** cache fields `_loc_src`, `_own_url`; stamp
  keys `loc_unknown`, `legacy_loc`, `ownless`, `ownless_base`, `ownless_rows_base`;
  alarm tokens `fabricated-loc-N`, `ownless-up-A-to-B`. Every one is documented in §5a's
  key list; none joins a sum invariant.

## Left for others / NOT finished

- `507@ats-fetch` (rot reader for the freshness verdict), `508@ats-fetch` (eightfold
  query stamp — `fetchers.py`), `500@roles` (the 11 published bare-Israel rows; the two
  Comcast rows still read `status: open` in `roles.csv` — retraction not yet propagated),
  468's two remaining copies (`bd_employees.py`, `pipeline/jdfill.py`).
- GenCell: 12 readable roles, no locations printed — registry/hunt decision.
- Predicta Med: 2 roles below the cards floor — wants a native or hunt path.
- Cypago-class dom-grid misassignment (221) unchanged — 3 corpus postings.
- The cache-wide post-cron numbers land with the 08-31 morning check.

## The morning-check one-liner (referenced by the HANDOFF row)

```
python -c "import json;s=json.load(open('scraped_cache.json',encoding='utf-8'));print(sum(1 for v in s.values() if isinstance(v,list) for j in v if j.get('location')=='Israel' and j.get('_loc_src') not in ('own','group','assumed')))"
```
Expected: equal to the collect stamp's `legacy_loc`, ratcheting to 0 over the following
nights; any count inside tonight's FRESH reads instead trips `fabricated-loc-N`.

## Triage of the 24 items left open (dispositions, no attempts)

- **208** superseded in spirit by `507@ats-fetch` (filed tonight with the 21-row census) —
  stays open until the reader lands. **228, 434**: dated progress notes in their bodies;
  228 closes when the 08-31 cron shows lakeFS+nsKnox cached, 434 when the cache-wide
  shared-url count re-measures.
- **243** half-answered: the 81-bundle harness was found alive in the 08-26 session
  scratchpad and drove tonight's acceptance (lost-real 0); strategy-4 spend is still
  invisible to it. **244** unchanged (the early-return keeps `worst` unset). **247**
  annotated (shape over vocabulary; comeet needs none again).
- **221/248** unchanged and now the largest remaining location-wrongness class (Cypago's 3
  on the corpus). **220** unchanged (`links:` carry, no ceiling). **363** partly softened:
  an ip-refused "empty" now needs `_plain_proves_empty`.
- **89/90/219** measurement items — 90's ask (per-job strategy provenance) is half-served
  by `_loc_src`; noted there is appetite for `_via`-per-job if a reader appears.
- **80/261/262/377** per-board renderer/SPA items — untouched, no new evidence.
- **265/431** `_carry_jd` items — untouched; tonight's mass title/url migration is the
  test they were waiting for; check the 08-31 digest's jd-fill line.
- **345** untouched (106 signal-less 200s). **356** untouched (guard-shape item).
- **410** untouched. **447** (Xpend exact-duplicate postings) — likely the shared-url
  fallback class; re-measure after the cron before touching.

## Wave 1 (three Opus attackers) — what they found, what was fixed, what was accepted

Attacker A, all verified with reproductions:
- **F1 (ship-blocker, FIXED):** the multi-region label fallback accepted Israel as PROSE
  ("New York, NY — we are an Israel-based company") and direct-added two foreign roles as
  Israeli, stamped `own`, invisible to every counter. `_LIST_ISRAEL` now requires Israel
  as a list MEMBER at the label's end; a prose counter-case is pinned in the guard.
- **F3 (FIXED):** embed-handoff jobs carried no `_loc_src`, so `fabricated-loc` would have
  false-fired on every handoff whose board says "Israel" (`embeds_won=2` live). The
  handoff loop stamps `own` — the board STATED it.
- **F4 (FIXED):** `locless` never forgot a title a later strategy placed — dom's bare
  reading inflated `loc_unknown` for cards the cards pass shipped. `discard` on append.
- **F2 (ACCEPTED, documented):** a 1-2-page group extends the existing Pecan gamble
  (prose-Israel, nothing foreign anywhere → group-Israel) to small boards. The
  demonstrated leak was F1's label door, now shut; a page with a foreign LABEL was
  already refused (`foreign` + `page_foreign`). Information-theoretically a US-remote
  page naming nothing foreign is indistinguishable from Pecan's shape at ANY group size —
  the ≥3 floor never protected against it either.
- **F5 (quoted here as the honest cost):** up to **392 bare-"Israel" postings across
  37-42 scoped boards** (Hunter Douglas 116, Shopify 33, ASML 25, adidas 19, Arm 18,
  Teradyne 17, AWS 12…) go dark on the first cron unless their card text names a place —
  an upper bound; the survivors are `loc_unknown`'s level to report nightly. No shrink
  guard fires (37 of ~406 `had`), empties never park, rows stay active.
- **F6 (accepted):** `_bare` maps 4 query-ROUTED registry urls (Neopharm-class
  `?page_id=`) to their homepage as a FALLBACK address; none has a cache entry today.
- Held under attack: dual-listed tails (0 of 2,483 cached cards have the shape; XX never
  wrongly written — all 29 newly-refused cached jobs verified genuinely foreign),
  `_bare`'s downstream (job_id/seen_id unchanged, 0 `is_job_url` flips), the "0" disarm,
  ASSUME leak paths, the ownless anchor bootstrap.

Attacker B (arithmetic/rot/carry), all verified:
- **F1 (HIGH, FIXED):** `ownless` summed over `written` with listings from `rows` — 17
  retired cache keys (264 postings) inflated it, and every abort night fired a false
  `ownless-up` and burned the anchor. Now summed over the registry's rows, like
  `_uncached`. **F4 (FIXED):** on an abort exit nothing written is tonight's read, so
  everything written counts as `legacy_loc`. **F2/F3** were attacker A's F3/F4 (embed
  `_loc_src`, locless discard) — one fix each, both attackers confirmed independently.
- **F5 (doc, FIXED):** the `cache-unreadable` exit stamps none of the derived keys — §5a
  now says so instead of over-promising "every exit".
- Declared on their measurement: the first-cron coverage event is 137–392 bare-"Israel"
  postings over 24–42 scoped boards (bounds differ by predicate; both under every guard —
  `loc_unknown` is the nightly report); the decoy-unlock rung's genuinely NEW spend class
  is **3 rows/night ≈ 90 requests/month** (Nokia, Schneider, lakeFS); `468`'s application
  means a worktree with `AJIL_SECRETS` set ARMS `bd_rescue` spenders — the old "worktrees
  silently disarm" safety now requires the operator NOT to export it; the committed
  lakeFS entry's title carries the site's "lakeFS Careers:" og-prefix (roles' title-twin
  logic will meet it), and the LLM tier would refuse the same multi-region string the
  links tier ships — two tiers, opposite verdicts, noted not resolved.
- Held under attack: §5a arithmetic on all four exits; belt→rot shapes and
  `overnight_verdict` on both new shapes; `_carry_jd` across the 118-title migration
  (113 descriptions carried, 0 lost, twins safe); the anchors' first-night bootstrap;
  the decoy rung with the flag unset (zero change); scraped_cache merge semantics (the
  lakeFS key survives; on a text-level push conflict take ORIGIN's file and re-add it).

Attacker C (gates/boundaries), all verified:
- **F2 (HIGH, FIXED):** `_comeet_slug_cut` fired on bare prefix-extension — "Data Analyst
  Team Lead" over a `data-analyst` slug was RENAMED and "Data Analyst 2" merged into its
  sibling (38 cached postings, Exodigo's six city-distinct roles collapsing to one): the
  fork bug inverted into a merge. The cut now refuses a residue that is neither a
  recognised place nor pure chips; both counter-cases pinned in the guard.
- **F3 (HIGH, FIXED):** linked headings arrive from a second finditer pass, so a mixed
  group had unsorted positions, an empty ctx, and a real Israeli card refused into
  `loc_unknown`. Sorted.
- **F1 (HIGH, resolved at rebase):** 503/504 were already taken on origin/master (`next`
  reads the merge-base, structurally blind to numbers master consumed after the fork) —
  the two filed items renumbered to the post-rebase `next` before the push.
- **F6 (FIXED):** a `, ST` tail wrote no hard country negative, so a link-less `Austin,
  TX` card on an `/israel-jobs/` path listing still passed the gate on the url text —
  `_US_STATE_TAIL` joins the XX predicate.
- **F7 (FIXED):** the lakeFS entry shipped the page's og-chrome ("lakeFS Careers: …") as
  its public title — a leading "…Careers:" prefix is the site's, stripped; the cache
  entry regenerated clean. The LLM-tier-vs-label divergence on multi-region strings is
  intentional and now documented: a LABEL is the role's own claim, a model-copied
  location string is not.
- **F4/F5 (FIXED):** §1's "never renamed" sentence now names the residue guard (the real
  mechanism), the XX sentence carries its full predicate, and every date claim says
  2026-08-30 / "the first cron on the 08-30 scraper commits" — no more mixed tenses
  about unpushed work.
- Gates quoted by the attacker: guard-kill KILLS 20/20 (none vacuous), docs 0 errors,
  backlog exit 0, invariants OK, full suite exit 0, debt gate clean for bd_rescue.py,
  lane boundaries clean (the 468 diff the only cross-lane file).
