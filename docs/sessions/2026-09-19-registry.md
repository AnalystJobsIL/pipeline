# `registry`, 2026-09-19 — five folds, one row reading a JS shell, and a census that was reporting its own answers

Branch `registry-2026-09-19`, rebased onto `origin/master` (`9931b14`). Nine commits, in order. Every
number below was measured on this tree, on a live page or on a run log — never recalled. The
planner's readings are marked where mine differ from them, because two of them did.

**Spend: 0 Bright Data credits** (`PAGE_UNLOCK_BUDGET=0 BD_RUN_CAP=0 JD_BD=0` on every
command that could reach the unlocker, and every gate call was passed an `html=` page — the
2026-09-18 session's 2 credits went on a bare "just checking the gate" one-liner against a JS
page). **4 seam reads** (`board_verify.verify`, `route: plain` on all four), from the SHARED
checkout, budget 4.

---

## 1. The three gate wrappers take the row's token (`a044451`)

**What was wrong.** `activation_ok`, `ok_to_write` and `identity_ok` took no `token`, so
`checkable_token("", api)` fell through to `_slug_candidates(...)[-1]` — `'il'` for
`career.adamtotal.co.il` — and `board_vouches` answered **False** on a DECLARED row, for
"carrying a tenant it did not declare". `not-ours`, before any page is read, on a company's
own board. `621`'s gate note, filed by the session that found it and could not fix it there.

**The class, measured.** **1 of the 14 declared-tenant rows.** For the other 13 the empty
token and the row's own token agree, because `checkable_token` recovers a Workday or Eightfold
tenant from the subdomain and greenhouse's from the path; only a QUERY-STRING tenant is
invisible to every url parse. All four ATS activators had the row's column 2 in scope and
dropped it — `auto_expand._row_for_ats` (`tok`), `retry_unreachable._row_for` (`tok`),
`bd_rescue` (`tok`), `wayback_rescue` (`r[1]`) — and **two of them were already handing that
same token to `embedded_board_ok` on the adjacent line**, which is what makes the omission a
defect rather than a design.

**The change is a forwarded argument, nothing more.** `token=""` on the three wrappers, passed
to the `activation_verdict` / `write_verdict` kwarg that already existed; `token=<the row's>`
at the four call sites. Measured on the Harel row shape with the page in hand (0 credits): all
three wrappers **True** on `token="harel"`, **False** on `""` (the documented refusal is
unchanged — a caller that drops column 2 still cannot activate a declared query-tenant row),
**False** on `"clal"` (the declaration refuses in both directions), and `n_jobs=0` still False.

**Untouched on purpose.** `listing_hunt`'s `identity_ok(name, url)` calls:
`tests/test_registry.py:1367` is a source-string COUNT of exactly that spelling, the hunt never
touches an active row, and 0 rows move. `GATE_CALLERS` is derived from callee NAMES, so a new
kwarg changes nothing there. The scrape-branch calls (`auto_expand:560`,
`retry_unreachable:255`) pass no token and need none — an ordinary host is decided by
`is_foreign` and the page.

**Rejected, with the number.** Teaching `_slug_candidates`/`checkable_token` the
`?token=…-<tenant>` form: 1 host, 1 row, and it would also move `company_identity.verdict()`
for every future adamtotal row while the fetcher's own belt (`fetch_adamtotal` raises before
its first GET unless column 2 equals the address's trailing label) already refuses drift.
Returning `None` for an empty token on a declared row: a WIDENING — a hunt could then activate
Harel onto any adamtotal tenant, the 09-13 `not_domains` incident in reverse.

**The gate found the cost of the change before CI did.** Four call sites moving made **21
existing mutation records STALE** — `test_no_mutation_record_goes_stale_unnoticed` names each
with `0 matches`, and a record that matches nothing guards nothing. Every one is **re-aimed,
not flagged**: same class, same `why`, same mutant semantics, with the token argument the call
now carries. `bd_rescue`'s call went back onto ONE line for the same reason — a wrapped call is
an anchor two records have to spell identically.

Four NEW records, each proved `1 killed, 0 SURVIVING, 0 coverage gap(s)` by its own
`tools/mutate.py --id` run, and **the killer of each is named because "4 killed" is not the
claim**: `activation-ok-token-dropped`, `write-ok-token-dropped` and `identity-ok-token-dropped`
were each killed by `test_the_three_gate_wrappers_forward_the_rows_token`, classified
`behavioural`. The call-site record `auto-expand-ats-token-dropped` came back
**`killed by test_the_mutation_coverage_dem (direct)`** — the catalogue's own coverage test,
which reds because the mutant moves a source key it derives. A meta-test is not a behavioural
kill, so that was checked by hand rather than accepted: the mutant applied, `-k
ats_activator_hands` alone reds (`1 failed`), reverted. `mutate` reports the FIRST failing test
it finds; both killers are now in the record's `killers` list so the next reader is not told the
meta-test is the guard.

---

## 2. `Discount Bank` was scraping a JS shell in front of its own board (`6f1b750`)

The planner and the `jd-text` planner disagreed about this row and **`jd-text` was right**: it
was `ats_platform=scrape` on `hcmUI/CandidateExperience/he/sites/CX_3001/requisitions`, the
human Oracle page — a JS shell — not `oraclehcm` on the list API. One bank's whole board was
being read as a page, and the held role record `discount bank|אנליסט.ית אשראי…` had a
163-character stub for a description.

Repointed to `oraclehcm` / token `''` /
`recruitingCEJobRequisitions?onlyData=true&finder=findReqs;siteNumber=CX_3001`. Measured
through the production fetcher from the shared checkout, 0 credits: **67 jobs, 67 Israel**
(`israel.is_israel_job` on Hebrew place strings — `country_code` is unset on this platform, so
the IL count is the vocabulary's and it read every one), and **`/job/5108` is among the cards**,
titled exactly `אנליסט.ית אשראי ראש.ת צוות- ענף אנליזת אשראי וחיתום- חטיבה עסקית`, `ישראל` —
the posting `jd-text`'s 09-22 row waits on. Four more analyst-ish titles on the same board.

The gate admits on the PAGE, as it did for the six existing `oraclehcm` rows: `is_foreign`
False, `tenant_is_this_company` **False** (the Oracle pod `ehsb` is opaque — the documented
22-active-row class), `board_vouches` None for `''` and for `'ehsb'`,
`activation_verdict(…, 67, html=<the human page, 101,375 chars, naming the bank in both
scripts>)` = **`ok`**. **No `identity_facts` declaration**: `tenants ("ehsb",)` changes no
behaviour today and would put a vendor pod in a table of company facts (the orchestrator's
ruling, and the measurement agrees).

`--explain` after: `oraclehcm`, `is_walled True`, every pool False. One gap found and filed:
`human_board_url` has no `oraclehcm` mapping, so it returns the API endpoint to itself and the
`activation_verdict` fallback can never read a human page for any of the seven rows.

**No registry write for the two Hebrew twins.** Both have carried `alias-of Discount Bank`
since 2026-09-01 and 2026-08-28 — what is missing is the `ALIASES` half, so **the fold has been
inert for eighteen days**. That is the `571` shape, and it was found by looking for the
declarations rather than for the parks.

---

## 3. Phoenix, Pagaya, Ram Aderet (`8315687`)

Three folds, each with the two-names-ONE-BOARD argument the 2026-09-11 rule asks for.

**`Phoenix Financial` → `הפניקס`.** The crowning evidence was in the ROLES ledger, not on a
page: the Latin row's three LinkedIn records carry the insurer's own requisition ids in their
titles (`Business Analyst - 50400095`, `Digital Analytics Team Lead - 50061453`,
`Marketing Data Analyst - 50400118`) and `www.fnx.co.il/career/open-positions` carries **all
three** today (free GET, 512,363 chars, ids verified individually). Same requisition ids on one
board. The cell was at **219 of 220** characters, so the append evicted the unprotected
`listing-hunt` and `wrong-url` segments and kept `dark-triage` — accepted, because the 09-18
NOT-THEIRS read of `arizonafinancial.org` is durable in `board_verify.json` either way.

One caveat on the SURVIVOR, stated because it is the row the fold now feeds:
`cloud_state/scrape_rot.json['הפניקס']` reads `{since: 2026-09-18, why: empty, n: 1, found: 0,
http: 200}` against an all-time baseline of **10**. One night is a blip, never a regression
(`health.REGRESSION_NIGHTS` = 2), and the page itself is fine — today's free GET is 512,363
characters and carries all three requisition ids — so this is an EXTRACTION miss on a JS board
(`scraper`'s class), not a reason to reverse the direction: `Phoenix Financial` has no address
at all, and the requisition ids are on `fnx.co.il`. If `n` reaches 2 the mail will announce it,
and that is the right place for it.

**`Pagaya` → `Pagayais`**, and it was not on `622`'s list at all: two greenhouse boards of one
employer, **both ACTIVE**. `pagayais` **8 jobs / 8 Israel**, every one `Tel Aviv, Israel`
(publishing `BI Team Lead`); `pagaya` **14 jobs / 0 Israel**, twelve `New York, NY (Pagaya US
New York)`. The IL board decides the direction — the Trivago shape. Address **kept**: its own
US board is evidence, not a wrong write. 0 role records either side.

**`רם אדרת | Ram Aderet` → `Ram Aderet Engineering`**, also new, and the mechanism is the
reason it had never been seen: two ACTIVE rows on one SITE with different paths and different
`identity_key`s, which every clause of `check_invariants.shared_boards` passes. The direction
is forced twice over — the Hebrew name contains ` | `, the notes separator, so `notes.split`
and `verdicts.alias_target` can never parse it as a survivor.

The planner proposed crowning `Ram Aderet Engineering` as it stood, at the cost of one card.
The orchestrator refused that and was right: **`/careers/252` scrapes 5 cards and is itself the
sixth posting** (`מנהל/ת עבודה`) on `/ram-aderet-group/careers`, which scrapes **6** — measured
live through the production `scrape_universal`, 0 credits. So the survivor was REPOINTED first,
then the Hebrew row parked. One seam read of the group page under the Latin name: `ok`,
`employer_named: "קבוצת רם אדרת / Ram Aderet"`, *"Ram Aderet Engineering is one of the group's
divisions"*. **Coverage traded: 0** — the card was being lost BEFORE this commit, not by it.

Checked on each of the three: `verdicts.alias_target` parses the survivor **exactly** as its
row's `company_name` cell, `registry_health --explain` reads `terminal True` with every pool
False, `identity_key(survivor)` is a fixed point with **exactly ONE ACTIVE row** on it
(`roles._alias_fold_target`'s precondition), and `check_invariants --strict` green.

---

## 4. `Matrix IT` was reading Matrix Technologies of Ohio (`04f32af`)

Three registry rows carry `Matrix` and **all three read DIFFERENT boards**, so no pair of them
can ever have the crowning argument — `622`'s Matrix line is not a fold, it is one wrong write.

* `מטריקס` → `matrixdna.ai/career/analytics-bi/` (Matrix DNA, 3/3 IL). Untouched.
* `Matrix` → comeet `19.00F`: **23 jobs / 0 Israel**, USA / Toronto / Jersey City / Mumbai /
  Japan, `company_name` "Matrix" on 23 of 23. The group's own non-Israeli arm, exactly the
  shape of Pagaya's US board. **Untouched, deliberately** — 0 IL either way.
* `Matrix IT` → `matrix-technologies-inc.breezy.hr`: **46 jobs / 0 Israel**, ten of them
  `Maumee, United States`. One seam read answers **`NOT-THEIRS`**, *"belongs to Matrix
  Technologies"*.

Parked through `python queue_pipeline.py --park "Matrix IT" --apply`, so the verdict cited is
the LEDGER's and not a hand-written one, and **declared in the same commit**:
`identity_facts.DECLARED["Matrix IT"]` `not_tenants ("matrix-technologies-inc",)`. The
declaration is the point — a note segment is not durable memory (`Sckipio`'s `url-cleared`
segment was evicted by four routine stamps) — and the row stays in the 19:00 hunt pool, which
is where it belongs: the Israeli integrator's own board is still unfound.

Checked after: `board_vouches` **False**, `embedded_board_ok` **False**,
`activation_verdict(…, 46, html=…)` **`not-ours`** WITH a page in hand,
`identity_facts.validate` clean on the live rows. Column 2 keeps the tenant **on purpose**: the
declaration now refuses it in both directions, so it is evidence of what was wrong rather than
an address a pool could act on. (The 09-18 session cleared Phoenix's token because that one was
an *aggregator url*, a different class.) **Coverage traded: 0 IL.**

**The declaration cost three things I did not plan for, and the suite named each** (`853db15`,
which completes this commit — `04f32af` … `430749b` do not pass the suite on their own, said
here because a bisect will find it):

1. `_NEGATIVE_IDENTITY` in `tests/test_registry.py` is the SUITE's memory of every recorded
   wrong write and `DECLARED` is the code's; they must agree, so a declaration with no incident
   line is half a record.
2. **The registry snapshot had to be re-cut**, `2026-09-13 → 2026-09-19`.
   `identity_facts.validate` refuses a `not_tenants` token equal to the row's own LIVE ACTIVE
   token — *declaring the incident forces the park* — but the test that runs it reads a dated
   snapshot (`tests/live_state.SNAPSHOTS`, infra 09-13), so **a park and its declaration cannot
   land in one commit against a six-day-old photo**. One line, and the 09-13 file stays on disk:
   a dated snapshot is evidence, not a slot. 2,383 → 2,495 rows, so nine tests across three
   lanes now read a newer population — the full suite is the judgement, and it is green.
3. **A guard that asked a proxy string instead of the predicate.**
   `test_a_recorded_wrong_write_is_neither_declared_nor_admitted` exempted
   `"greenhouse" in api or "comeet" in api` from its `tenant_is_this_company` clause — the two
   PATH platforms that happened to be in the table. `matrix-technologies-inc.breezy.hr` is a
   third case the string list could not express: breezy is subdomain-SHAPED but **absent from
   `identity_gate._SUBDOMAIN_TENANT_HOST`**, so the primitive scopes out before it reaches the
   `not_tenants` clause. The exemption now computes that predicate; `Riskified`/novartis and
   `Bancor`/bancorpbank stay in scope and still assert False. Nothing is unsafe —
   `board_vouches` reads `not_tenants` unconditionally and answers False — but a declaration
   that does not reach one of the four primitives is worth an item, and **patching the pattern
   was REJECTED on the measurement**: 5 breezy rows, 2 active; adding `breezy\.hr` flips
   `PLAYSTUDIOS` from True to False in the primitive while `board_vouches` stays `None`, so the
   gate does not move but `check_invariants` C3b's warn list and `--explain` both change their
   answer for a row nobody has complained about. `643`.

**`IBI Investment House` was the other candidate and is NOT folded.** The ledger had no verdict
under the Latin name, so one seam read was spent on `ibi.co.il/career/` for it:
**`UNVERIFIABLE`** — `route: plain`, 33,144 chars, 9,000 visible, `page_kind: error`, *"a wall
or an empty shell we could not read"*. A park needs a verdict and this is the absence of one;
`UNVERIFIABLE` may not stand in for a refusal or for a vouch, which is `board_verify.refuses`'
own cut. The row is left in the hunt pool, where `_active_twin` refuses any activation onto the
twin's board, so nothing is at risk while the answer is missing.

---

## 4b. Last night's Harel activation had been REVERTED by a cron, and nothing noticed (`f2de9a6`)

Found by accident, while re-deriving the "1 of 14 declared-tenant rows" claim for §1: the
script printed **0** rows where the empty token and the row's own token disagree, which should
have been impossible — and the reason is that `Harel Insurance & Finance` was not on the
`adamtotal` board any more. Both Harel rows were parked. **83 Israeli postings, six of them
analyst titles, had been out of coverage since 00:25Z**, and `roles._alias_fold_target` could
not fire for the employer at all.

The mechanism, off the commit times:

| | |
|---|---|
| 2026-09-18 **21:23:18Z** | the 19:00 `listing-hunt` run `35396505564` STARTS and snapshots `companies.csv` — the Harel row is parked on `www.harel-group.co.il/careers`, which really does scrape 0 cards |
| 2026-09-19 **00:08:04Z** | `91b9676` (registry) pushes the activation: `adamtotal` / `harel` / the token url / `active=true` |
| 2026-09-19 **00:25:14Z** | the hunt commits `7f960cd … (row-merged)` on top of `5dbec81`, which already held the activation, stamping `listing-hunt 2026-09-18: no IL listing; monitored candidate` on its own stale copy of the row |

Of the **two** rows `91b9676` wrote, **one SURVIVED and one was REVERTED**, and the reverted one
is exactly the row the hunt had also touched (`הראל ביטוח ופיננסים`'s `alias-of` park is still
there). So the merge is per ROW and the last writer for a row wins. It is not a bug in the
hunt's logic — its read was true of the row it read — nor in the merge's row keying. It is the
missing THIRD input: OURS is compared against ORIGIN with no BASE, so *changed from a
three-hour-old snapshot* and *changed authoritatively* are indistinguishable. Every `registry`
session that pushes between 19:00 and 00:30 UTC is exposed, and silently: `check_invariants`
passes either way, because a parked row carrying a pool token is a legal row.

Re-measured before re-writing, 0 credits: **83 jobs, 83 `is_israel_job`, 83 `country_code` IL**
through the production `fetch_adamtotal`, `board_vouches('harel')` True, `activation_verdict`
`ok` on the token alone, six analyst titles. The note's `chrome-verified 2026-09-18 … identity
gate refuses` segment was evicted to seat the stamp — superseded twice over — and `dark-triage`
was asserted to survive. `644` carries the diff for `infra`; **this commit is a hand-drain and
it will happen again the next night a session pushes inside a cron's window.**

## 5. `Group19` is the employer the board names (`4e65320`)

`https://www.group19.org.il/career` is the GROUP's page and its 8 listings belong to several
companies inside it, which is how a public-sector Power BI `Data Analyst` came to publish under
a UAV/defence profile (`633`, filed by `company-intel` on 09-18).

**Split by crowning the GROUP, not a subsidiary**, on the board's own evidence: no subsidiary
page lists jobs (`/data` and `/tech` carry 0 posting links, `/finance` 1), and one seam read of
`/career` under the NEW name answers `ok`, `employer_named: "Group19"`, *"Group19's own careers
page on its Israeli domain (.org.il), titled `קריירה | Group19`"*. New ACTIVE row `Group19`
takes the board (`identity_ok` True offline, `verdict` `match`); `Group19 Tech` is parked
`alias-of Group19 2026-09-19` with the subsidiary fact in the note — `/dataanalyst-1` names
**`Group19 Data` 11 times** against `Group19 Tech` **8** (navigation only) and puts the job in
`אופקים`.

**The one thing I nearly got wrong, and the rule that caught it.** My own scrape of
`/career` read **0 cards**, `status=empty`, http 200, `llm_calls=0`. Taken as a measurement
that would have said the new row reads an empty board. It is a broken run, not a measurement:
the same url scrapes **the same count under BOTH names** (0 and 0 from this checkout, which
lacks the rung this Wix page needs), while production's `health_baseline` and yesterday's
`scraped_cache.json` both read **8** — including the `Data Analyst` card with a 658-character
description. What the two-name test settles is the only thing at issue: **only the ROW name
moved**. The 00:00 UTC `scrape-refresh` re-scrapes every active scrape row, so the new row is
cached before the 09-20 digest.

**Rejected**: a RENAME (`459` — `company_name` is the join key for the firmographics, the roles
ledger, `matched` and the board); a second ACTIVE row `Group19 Data` on the same url
(`audit_empty_rows.active_twin` refuses two active rows on one board, and the scraper would then
give it all 8 cards); parking `Group19 Tech` `wrong-url` (the ledger reads that board `ok` FOR
the row and one live analyst card would drop). The note cost: the append evicted
`queue-hunt 2026-08-29: … monitored candidate` to seat the alias at 200 of 220 — free, because
`alias-of` is terminal and no pool reads the row again.

---

## 6. The census stopped reporting its own answers (`430749b`)

Two arms of `registry_health`'s bare report, both still report-only.

**`cross_script_twins` drops a pair the registry has already ruled on.** Three of the six it
printed on 2026-09-18 were folded already — Harel that day, both Discount pairs since 08-28 and
09-01 — so a reader had to re-derive which were live, every time. Two shapes, and **the second
is the one my first cut missed**: one row naming the other, and two rows folded onto the same
THIRD row (`בנק דיסקונט` and `Discount Bank בנק דיסקונט` are each `alias-of Discount Bank` and
neither names the other; I measured 3 where the plan predicted 3, saw which pair survived, and
added the clause). It reads `verdicts.alias_target` — the predicate a fold is WRITTEN with,
never a substring — so **no other park silences a pair**: a row parked `wrong-url` stays a live
candidate, which is exactly what `Phoenix Financial` was for six days.

Measured with the shipped function: **6** (old code, origin's rows) **→ 3** (new code, same
rows) **→ 2** after today's folds — the two `מטריקס` pairs, correctly unresolved.

**`site_twins(rows)` is new**: ACTIVE **non-ATS** rows sharing `board_verify.site(host)` under
two `identity_key`s with no alias between them. `check_invariants.shared_boards` cannot see them
and is right not to try — its key is `identity_key` + the EXACT path, which is what catches
`JPMorgan Chase` beside `JPMorganChase` on one url. **5 on origin's rows, 4 after the Ram
Aderet fold**, and the other three are worth a session each:

| site | rows |
|---|---|
| `massivit3d.com` | `Massivit 3D` (`/career/`) · `Massivit 3D Printing Technologies Ltd.` (`/job-openings/`) |
| `xtra-mile.co` | two paths, one agency |
| `amazon.jobs` | **FOUR** rows — and `AWS` and `Amazon Web Services (AWS)` read the SAME `business_categories/amazon-web-services` path under different query strings, which is why the exact key misses them |
| `siemens.com` | `Siemens Digital Industries Software` (`?location=Israel`) · `Siemens Israel` (`/locations/isr/jobs/`) — two location filters of one board |

A hit is a QUESTION, never a verdict: one employer on two paths of its own site (fold or
repoint), or a group and a subsidiary that legitimately hold two rows. Reuses
`board_verify.site` (the ledger's own registrable fold, moved there on 09-18 so there is one
copy) and `check_invariants.shared_boards` in the test as the negative control, with the
one-identity-one-path pair as its positive control.

**No mutation records for these two**: `registry_health` is read-only and `tools/mutate.py`'s
coverage demand is over the registry WRITERS. Both arms carry a behavioural test whose own
arms are the positive controls (the same rows without the fold are candidates again; a
`wrong-url` park is not a fold; an alias naming a THIRD row silences nothing).

---

## 7. The cross-lane half, written down because a park without it is inert

**`company-intel` owns `firmographics.ALIASES`**, and the 2026-09-11 rule is that the park and
the declaration land the same day. Seven keys, computed with `identity_key` on this tree — every
value is a fixed point of it and none of the seven is present today:

```python
ALIASES["phoenix financial"]                          = "הפניקס"
ALIASES["pagaya"]                                     = "pagayais"
ALIASES["רם אדרת ram aderet"]                          = "ram aderet engineering"
ALIASES["group19 tech"]                               = "group19"
ALIASES["בנק דיסקונט"]                                 = "discount bank"
ALIASES["discount bank בנק דיסקונט"]                    = "discount bank"
ALIASES["clal insurance finance כלל ביטוח ופיננסים"]     = "clal insurance and finance"
```

Plus a `Group19` RECORD bought evidence-first from the group's own site: `firmographics` fills
only a survivor's EMPTIES and "a survivor with NO record folds nothing", so without it the UAV
record on `Group19 Tech` is orphaned rather than folded. The sector the seam CONFIRMED for
`Group19 Tech` on 09-18 stays true — it is Group19 Tech's, and it no longer sits on the row that
publishes the analyst.

**`roles`** — what the next digest should print, from the ledger as it stands:

| survivor ← parked | records that move |
|---|---|
| `הפניקס` ← `Phoenix Financial` | 3: `Digital Analytics Team Lead - 50061453` (open), `Business Analyst - 50400095` and `Marketing Data Analyst - 50400118` (closed) |
| `Group19` ← `Group19 Tech` | 1: `Data Analyst` (open) |
| `Discount Bank` ← `בנק דיסקונט` | 1 withdrawn record whose title is character-for-character the survivor's OPEN one — a same-title twin fold, not a rename |
| `Pagayais` ← `Pagaya` | 0 — expect `left N record(s) in place` |
| `Ram Aderet Engineering` ← `רם אדרת \| Ram Aderet` | 0 either side |

**`jd-text`**: the survivor for the held `discount bank|אנליסט.ית אשראי…` stub is `Discount
Bank`, and its own address is now `.../sites/CX_3001/job/5108` on the `oraclehcm` board — the
fetcher writes it per card. **`ats-fetch`**: nothing; the fetcher's belt already matches
column 2.

## 8. What I made harder for the next lane

* **`site_twins` is a fourth twin predicate**, beside `shared_boards`, `active_twin` and
  `cross_script_twins`. It is the only one keyed on the SITE and the only one that is non-ATS
  only. A fifth should extend it, not join it.
* **`Group19` is a row no cron created.** Nothing else in the registry was born this way today,
  and its `scraped_cache.json` key does not exist until the 00:00 refresh runs. A session
  reading the board on 09-19 will not find it.
* **`Matrix IT` keeps a tenant in column 2 that the gate now refuses.** The declaration is the
  memory; a reader who only reads the row will think the address is live.
* **Two morning rows come due on 09-20 and one of them is an N/A by construction.** The wrapper
  change is pure functions: no unattended run can produce a number for it, and the row says so
  rather than leaving a date nobody can answer.
* **`registry_health`'s bare report is four lines longer.** It was already long.
* **The registry snapshot every suite reads is now dated 09-19.** Nine tests across three lanes
  read a population that grew by 112 rows in one commit. The 09-13 file is still on disk, so a
  test that wants the older photo can name it — but nothing points at it any more.
* **Four new backlog items** (`641`, `642`, `643`, `644`), each with its measurement, and `622`
  is still open on one clause: `Matrix IT`'s real board. They were filed as `636`-`639` and
  RENUMBERED at the rebase: `jd-text` filed `636`-`640` in the same hour, from a base that
  computed the same `max+1` — the collision the backlog's own header warns about, and
  `backlog.py check` refused it, which is the gate working. **The commit messages of `853db15`
  and `f2de9a6` still cite the pre-rebase numbers**, which is the one thing a renumber cannot
  reach. `jd-text`'s `636` is the SAME finding as this session's `Discount Bank` repoint,
  measured from the other end, and is marked closed in its own body.
* **`644` is a hazard every future `registry` session inherits**, and the only mitigation this
  session can offer is a sentence: do not push `companies.csv` between 19:00 and 00:30 UTC
  without re-checking the rows afterwards, because the 19:00 hunt commits from a snapshot taken
  three hours earlier and its copy of a row wins.
