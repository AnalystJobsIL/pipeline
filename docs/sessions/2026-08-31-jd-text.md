# 2026-08-31 — jd-text: Indeed is readable, and the description field comes clean

The task: every published role passes `looks_like_jd` or carries a written structural
reason; Indeed — where some roles exist and nowhere else — "must end with a win". It did:
**90 of the 92 cached Indeed postings fill** through a rung that did not exist this morning,
and the matched defect set went **14 → 11 by calibration alone, → 4 structural after the
next enrich run** fills the Indeed rows.

## The experiments, in the order run (all from the shared checkout root, ARMED-PROOF first)

Every paid process opened with `secretsenv.load(ROOT)` + an asserted
`bd_budget.spent_this_month()` read (`ARMED mtd=12289`) and a 1-credit positive control
(the SERP `discovery_daily.indeed_search` reads nightly — `window.mosaic.providerData`
present). Artifacts: the session scratchpad (`e1_serp_vjk.json`, `e1b_confirm.json`,
`yield_92.json`, `sweep_texts.json`, raw bodies).

| avenue | result | credits |
|---|---|---|
| E0 wayback CDX on the exact `viewjob?jk=` urls | 0 snapshots on 3, HTTPError on 2 (Hebrew-name urls) — dead, free | 0 |
| E1 Unlocker RAW, `jobs?vjk=<jk>` alone | page answers (443 KB) but the pane never opens — our jk absent; `extract_jd` on it returns the SAME 3,028 chars of page furniture for three different jks (the trap the pane parser's `jobKey` anchor exists for) | 5 |
| E1 Unlocker RAW, `jobs?q=<company>&l=Israel&vjk=<jk>` | **the pane opens keyed to OUR jk**: `window._initialData.autoOpenTwoPaneViewjobResponse.body.jobKey == vjk`, `sanitizedJobDescription` = the employer's full HTML. אסם 971 chars, TransUnion 6,670, Similarweb 5,610, דיסקונט 1,355; one transient `reject_authwall` (Prologic, passed on retry) | 6 |
| E1b confirm: `q=a` (throwaway query) | pane still opens keyed to the vjk → the rung needs no company name; Prologic 306 chars real ad | 3 |
| E2 rendered viewjob | **not run** — E1 hit the 4-of-5 bar first; raw is 1 credit vs 2 and off the 45 s render clock | 0 |
| E3 dataset collect-by-URL | **not run** — held as fallback, never needed | 0 |
| sweep: all 92 cached postings through the winner | see below | 87 |
| diagnosis: Ashley + Questar LinkedIn refetch | full postings ARE on the guest pages — under the wall (below) | 2 |

**Credits actually spent: 104** (of the 150 declared; `cloud_state/bd_spend.jsonl` carries
the per-process lines: 11 + 2 + 1 + 87 + 2, +1 control inside the first 11). All spent
inside August's unlimited window.

## The 92-card yield, re-derivable

`yield_92.json` (scratchpad; per-jk rows quoted below) — form
`il.indeed.com/jobs?q=a&l=Israel&vjk=<jk>`, raw, no per-card retry, `BD_RUN_CAP=95`.
Buckets at fetch time under the OLD marker bar: `pass 70 · marker_poor 7 · expired 14 ·
fetch_error 1`. Under the SHIPPED bar (the new requirement-idiom families), re-scored from
the captured texts: **90 of 92 carry 300+ chars passing `looks_like_jd`** (re-scored under the TIGHTENED bar: still 90 — Prologic's 306-char דרוש/ה+ניסיון ad passes it) — 76 of the 78
still live at source, and all 14 EXPIRED postings, whose pane still answers with the text
(an archived role's description is still this lane's mandate). Residue: `one פתרונות
טכנולוגיים` (1,773 chars, one family — the honest marker-poor leftover) and one
`fetch_error`. **0 auth-walls in 92** — the 2026-08-26 verdict is comprehensively
falsified.

## What shipped (the rung and the two parser fixes)

1. **`indeed.com` left `_UNFILLABLE` for `paid_only`** (`pipeline/jdfill.py`): free rungs
   keep the byte-identical `auth-walled` refusal and never spend the doomed plain GET;
   `fetch_jd`'s paid rung buys `indeed_fetch_url(jk)` and parses via `_from_paid_body` →
   `_indeed_jd` (pane parser, `jobKey == jk` anchored, `ok-indeed` reason; on a jk-keyed
   body it is the ONLY parser — `extract_jd`/ld+json on a SERP read other jobs).
   `JDFILL_INDEED_CAP` (8) bounds the inline nightly re-spend (240/month ceiling, 4.8 % of
   the September pool); the matched driver needs no twin — its failures stamp and ride
   7/14/28. `enrich_matched_jd`: `_address` prefers a FREE own-board sibling over the paid
   canonical (Zipher rule preserved), and `--dry-run` now builds no Unlocker at all (it
   could buy, now that Indeed rows pass the gate).
2. **The marker bar learned the requirement idioms — twice**: the first draft flipped 10
   real postings and an adversarial wave passed six synthesized junk texts through it
   (cookie banner, FAQ, benefits prose — the idiom-pair-as-two-families defect and the
   `take advantage` verb). Shipped: idioms FOLD to one family (`_REQ_IDIOM`), `advantage`
   guards against the marketing verb, `must have`/`nice to have` dropped (1 corpus flip vs
   3 junk classes). **8 flips, all real posting text; every synthetic refused.** Full
   ledger of rejected candidates: the decision record.
3. **`_after_the_wall`**: `extract_jd` recovers the posting on a WALL-FIRST page (LinkedIn
   guest shape: sign-in block ×6 above the posting). 0 activations on 1,478 stored bodies;
   2,395 and 4,931 chars recovered on the two live wall-first pages.

## The published rows — 11 stated by the operator, 15 re-derived at 05:xx, and where each landed

The operator's (b) hypothesis ("nav-bar/cookie-banner captures — a parsing defect") was
half right: none are nav-bar CAPTURES (`furniture_at` = None on all five), but two parsing
defects were real — the marker bar and the wall-first cut.

| row | disposition |
|---|---|
| Compie 1168 · ONE datAI 800 · Modellama-research 742 | **fixed by the marker bar alone** — complete real postings, one family each; they pass in place, no fetch |
| TransUnion 163 · אסם 162 · Nestlé 160 | **the rung fills them** (sample round proved the text: 6,670 / 971 / same posting as אסם under a second jk); land in the store at the next `enrich_matched_jd` run — they head its queue (`jd_tries` 0) |
| Navan 172 · Zipher 170 · LTX 166 | the concurrent `roles` session's own-board repoint; hands off |
| Ashley 1800 · Questar 1800 | not fossils-forever after all: the [:1800] truncation is real (mid-sentence tails, `first_seen` 08-20) but the LIVE guest pages still carry the full postings and `_after_the_wall` now reads them — refetch on their next due date replaces the fossil |
| Mobileye 770 | **structurally unfillable: `gone`** — 404 at Mobileye's own Lever board, terminal by design; the 770-char capture stands |
| Taboola 0 | **structurally unfillable: `gone`** — 404 at Taboola's own Greenhouse board (gh_jid 8035268), `jd_tries` 3 |
| Bylith 0 | **structurally unfillable at this address: `not-a-job-url`** — all 9 Bylith cards share the one listing url `www.bylith.com/careers`; the fix is a per-role ADDRESS (`scraper`/`registry`); no credit spent on a listing page |
| Meta 0 | superseded — out of scope by status |

Matched defect count: **15 at 05:xx (11 by the operator's morning read) → 11 after the
calibration landed → 4 structural-with-reasons** (Mobileye, Taboola, Bylith + whichever of
Ashley/Questar's next fetch precedes the digest) once the next enrich run fills the three
Indeed rows and roles' repoint covers its three. The roles lane's export policy can exclude
on the reasons above.

## The nightly refusal classes (task item c)

| class | disposition |
|---|---|
| ~28/night `indeed auth-walled` | **the rung** — heals via the inline filler (`JDFILL_INDEED_CAP` 8) + the matched driver; alarm: `inline jd-fill: the Indeed cap bound at N` on the mail's `Stages:` line |
| ~6/night listing-page urls | structural: `not-a-job-url` — a listing address cannot carry ONE posting's JD; the address is `scraper`/`registry` work. Per-card reason persistence (backlog 443's jd-text half) needs `run_backfill`'s `save(item, text, stamp)` contract widened to carry `jd.reason` — every test fake implements that shape, so the diff belongs to 443's own fix, not this session |
| ~4/night telegram js-shells | verdict stands: `secrethunter.io` stays in `_UNFILLABLE` (byte-identical 776-char shell 5/5); the per-process canary keeps it falsifiable — and Indeed's exit this session is the proof the canary system works |
| ~13/night retryable | by design: timeout/5xx/bd-* stamp ` transient` (retry tomorrow); definitive misses ride 7/14/28/30; `gone` is the only absorbing state |

## Morning checks

- The overdue 08-30 jd-text row: **FAIL** — `matched_llm_unavailable=16` (alarm
  `llm-auth9`) in the 2026-08-30 16:55Z enrich stamp; `archive_ran=1` PASS,
  `scrape_thin_remaining=300` PASS. The LLM seam was auth-refused on the runner (HANDOFF
  Open item 3), not a fetch problem.
- The LinkedIn no-markers 23→13 trend question: **pending** — today's digest had not fired
  by the session's end (last daily-digest run 33306411751, 08-30, `failure`); nothing was
  dispatched.

## What three adversarial opus waves found, and what changed

Wave A (money), B (fake evidence), C (vacuity/interlocks) ran on the diff before landing.
Fixed in this commit, each with a pinned test:

* **`--dry-run` crashed the matched driver** (A/B/C, reproduced): my own dry-run guard left
  `bd.used` reads outside it — `getattr` shapes now; the dry-run path is exercised
  end-to-end and asserts NO Unlocker is even constructed.
* **A keyless indeed url bought the walled viewjob page** outside the Indeed cap (A):
  `fetch_jd` refuses it before the credit.
* **A free-only pass stamped 7/14/28 verdicts on evidence nobody collected** (B): three
  production paths run `bd=None` (the archived pass, the dead-ledger fallback, the
  cooldown pass — which RE-stamped daily so the paid rung's turn never came). A keyed
  indeed miss with no paid rung is TRANSIENT now; only a BOUGHT miss backs off.
* **Cap-blocked/disarmed indeed cards were booked as failed fetches** (B): they are
  refusals again (`unfillable`), so `jd-fill: 0/28` + a false "every fetch failed" alarm
  cannot recur; the cap-0 off-switch no longer alarms every morning.
* **The SERP address was pinned against itself** (C, full-suite mutant: reverting
  `indeed_fetch_url` to the walled page stayed green): the literal URL is asserted now.
* **Six of nine idiom families were revert-safe and the junk synthetics passed** (C): the
  tightened bar above, every family exercised.
* **The `_address` test could not fail** (B M13/C): end-to-end now — sibling fills free,
  `bd.used == 0`, and the dry-run Unlocker guard has its kill (B M14).
* **A rail mark in `_WALL_MARKS` went unnoticed** (B M19): a rail-trap fixture whose
  candidate would pass kills it.
* **`_indeed_jd`'s anchor was a zero-tolerance literal** (C): one minifier space would
  have silently killed every fill — a regex now; and a snippet-sized pane is pinned as
  no-fill (B M18).
* **The "0 of 17 wall bodies" claim was weak evidence** (C): the corpus cannot test wall
  safety (cleaned 08-28); the claim now rests on the synthetics, and says so.
* Doc claims falsified by the rung and still standing were fixed: BACKLOG 261 annotated,
  181's closure annotated, ARCHITECTURE's Zipher "refused before a byte" and the
  `auth-walled 17` residue row, the 76-vs-90 contradiction.

Accepted with reasons (not fixed): the inline cap's 8 are largely the same front-ranked
cards until age-out — the cap is a ceiling on waste, not a drain schedule; the durable
fills are the matched driver's (stated in §7a). Same-night double-buy of a jk shared by a
matched row and a discovery card: ≤2 credits/night, bounded by both caps. `jdfill.Unlocker`
spend is still outside `BD_RUN_CAP`/`bd_spend.jsonl` — a pre-existing hole this rung now
sits in; the monthly ceiling IS consulted. A matched-driver rung-death is silent when
another bd fill lands the same run (`alarm_for` is driver-global) — the 09-01 morning check
is the tripwire; per-host alarm counters were declined (stamp-key discipline). The
jk↔employer binding is the discovery card's claim, unverified by the rung (rule 5 one level
up; אסם/Nestlé share one posting under two names — `shared-with-sibling` → `jd_quality`
catches byte-identical text the morning after). `_worked` counts free-only indeed misses
(disarmed runs only; gauge nuance). `enrich_scrape_jd` has no indeed gate — 0 indeed cards
in that cache, and if one arrives the rung fills it properly and stamps.

## Clause-4 answers

- **Deleted**: the `("indeed.com", None, "auth-walled")` `_UNFILLABLE` entry + its evidence
  comment; jdfill's own `load_secrets` body (backlog 468 applied — delegates to
  `pipeline/secretsenv`); the false "Indeed answers 401/403 to every client we own" claims
  in `enrich_matched_jd` and `ARCHITECTURE.md`.
- **Extended, not duplicated**: `fetch_jd`'s existing paid rung (the transform is 2 lines in
  it); `_from_body`'s parser-pair seam (`_from_paid_body` wraps it); `_JD_MARKERS` (no new
  regex machinery); `jd_body`/`furniture_at` (the wall path reuses both); `JDFiller`'s
  existing cap/alarm shape. Looked for an existing paid-fetch-with-transform seam first:
  `grep -n "def native_jd\|def unlock" pipeline/*.py bd_rescue.py` — `native_jd` is
  free-only, `bd_rescue.unlock_status` cannot render and is another lane's chokepoint.
- **Rejected on numbers**: the full table is in
  `docs/decisions/2026-08-31-indeed-paid-rung.md` — bypass-in-`fetch_jd` (dead code from
  all 3 callers + unbounded nightly respend), the dataset (`discover_new`: 5 days of
  `dataset_size: 0`; collect: never needed), rendered viewjob (never reached, 2× cost),
  SERP fallback parsing (3,028 identical furniture chars, 0/5 identity), a matched-driver
  cap twin (stamps already bound it at ~13/month), `you will` and three more marker
  families (9 junk flips), `_jd_why` this session (save-contract churn belongs to 443).
- **Made harder for the next lane**: one new env var (`JDFILL_INDEED_CAP`), one new paid
  URL shape to know about (`indeed_fetch_url`), two new jdfill public names (`paid_only`,
  `indeed_jk`), and the marker bar is one line longer to reason about. No new files, no new
  state keys, no workflow change.

## Per-jk sweep table (bucket at fetch time, OLD bar; chars as captured)

See `yield_92.json` for the machine-readable original; the 92 rows:

| jk | company | bucket | chars |
|---|---|---|---|
| 8018875cc3df2f8b | Diageo | expired | 8423 |
| 8eec28efd124a6d2 | Diageo | expired | 10650 |
| bf0e5c902dc82977 | Horizon Technologies | expired | 1996 |
| 95f72e6258c6baf3 | Horizon Technologies | expired | 911 |
| 528e42d83291fb32 | Horizon Technologies | expired | 1048 |
| 5db816409cfd619d | Jiga | expired | 2659 |
| 4d32b29fd9e9ab04 | Micron | expired | 6643 |
| bb9f874427de750e | Mobileye | expired | 3057 |
| 8997d11ab572109e | Ness Technologies | expired | 315 |
| 9784c063c918d237 | Oak | expired | 3685 |
| 706ba5229573907c | Software AG-SPL | expired | 591 |
| 842742fc66766a65 | Software AG-SPL | expired | 599 |
| fb88e22ffe13a0d7 | april | expired | 2331 |
| 5c5ec44a1d18d872 | מנורה מבטחים החזקות | expired | 533 |
| 2127de2e36428769 | Bright Data | fetch_error | — |
| fab474cbc6aaa1a2 | IBI בית השקעות | marker_poor | 673 |
| e442fbfa9c615588 | IBI בית השקעות | marker_poor | 1708 |
| 402811d2d75d9040 | Prologic LTD | marker_poor | 306 |
| 86c12111e78d4e90 | Prologic LTD | marker_poor | 642 |
| dd5ef2a55e8ed5b2 | one פתרונות טכנולוגיים | marker_poor | 1773 |
| 322e4bb63b374987 | בנק דיסקונט | marker_poor | 1355 |
| d1d0914ca9686718 | בנק דיסקונט | marker_poor | 1819 |
| 10816ea3a21bf4b4 | AppsFlyer | pass | 3538 |
| c2d27cf674f53756 | BioCatch | pass | 2391 |
| c2d27cf674f53756 | BioCatch | pass | 2391 |
| c74a199ff4ccf223 | Cellebrite | pass | 3259 |
| edb4d3e5fc208b85 | Cognyte | pass | 2575 |
| 61f95d45b31170fe | Cognyte | pass | 2321 |
| f2af190bb560cf4c | Discount Bank | pass | 1856 |
| 32825e0371ac30c1 | EPAM Systems, Inc. | pass | 2913 |
| 572efc1bf128884f | EPAM Systems, Inc. | pass | 2326 |
| dcd5d6f8646f6a11 | Experda | pass | 2792 |
| 6ab3c95da0a8cde1 | Factify | pass | 2474 |
| ffa2a7c4912b4475 | Fetcherr | pass | 3844 |
| 3869568f29c868f2 | Fetcherr | pass | 3167 |
| 1737a5adf4bedbba | GENPACT DS ISRAEL LTD | pass | 4829 |
| ed20f2d66546fad3 | GENPACT DS ISRAEL LTD | pass | 4762 |
| 6b15ec532ccefd75 | GENPACT DS ISRAEL LTD | pass | 4798 |
| c737d71dfbc56dea | GENPACT DS ISRAEL LTD | pass | 4459 |
| a1dd3b51ad60ae1f | Glassbox Ltd. | pass | 3914 |
| 1ade58295617e0cb | Google | pass | 4197 |
| 2e9ff86d0b88242e | Google | pass | 3601 |
| 6a68337408fbf6a9 | Google | pass | 3813 |
| f08cb38c2dce5a43 | HSBC Global Services Limited | pass | 4100 |
| c59c3d9d9095153e | Horizon Technologies | pass | 772 |
| 39dc0961e02bc310 | Horizon Technologies | pass | 2140 |
| 45a6e3309520fda6 | KELA | pass | 1716 |
| b17171f8cdb4b6c7 | LTX | pass | 4383 |
| decf762d0f51d42c | Leverate | pass | 4768 |
| a7e7b663a2a34532 | Liniad | pass | 962 |
| 214410b8267b785d | Lusha | pass | 2247 |
| c9a799177c12ad2b | MKS Inc. | pass | 4371 |
| 23822fc2327fb83c | McCANN | pass | 1839 |
| cdfdbe160f931730 | Mobileye | pass | 2932 |
| dbae954494267f1a | Mobileye | pass | 3541 |
| 736a52986835829a | Navan | pass | 3191 |
| 78aa486d917f901f | Ness Technologies | pass | 606 |
| c41c2dcdf96c8d07 | Nestlé | pass | 971 |
| ea710157834342d3 | Novo Nordisk | pass | 4784 |
| d27849b77f23249f | Palo Alto Networks | pass | 2664 |
| 495edaef6c3a2c16 | Palo Alto Networks | pass | 6577 |
| 8a414e7c7c7bfce6 | Palo Alto Networks | pass | 6744 |
| c214f2e04d4b6efd | Playtika Ltd | pass | 2841 |
| 4138145197a698c9 | Prologic LTD | pass | 1459 |
| a1771edc47cc4bdb | Questrade Financial Group | pass | 10722 |
| 0bf287d9c242ae6a | Recolabs | pass | 4389 |
| a8e07f1f5da330e1 | Salesforce | pass | 6271 |
| d071d2a551f34352 | SemiConductor Devices | pass | 1993 |
| 90313d0efe85ad58 | Similarweb | pass | 5610 |
| 8862d4c45a79c95d | Similarweb | pass | 4785 |
| 6ec245c41abb3f17 | Similarweb | pass | 5953 |
| 0a2987d759930357 | Similarweb | pass | 4743 |
| a7fea3de30517c74 | Software AG-SPL | pass | 576 |
| 2f0b1b8765e3af60 | Software AG-SPL | pass | 1111 |
| cf80ac0b9714bea0 | Speechify | pass | 3759 |
| d265f73a0f32c46f | Team8 | pass | 4002 |
| 7349c47d967eb4a7 | TransUnion | pass | 6670 |
| 054d5d076a2f3c47 | Trigo Retail | pass | 2671 |
| 9dc6c55ceb9ec95f | Upstream Security | pass | 2984 |
| daf0dcdfb64d558d | Viber | pass | 3431 |
| 3de10e0c4cb907aa | WalkMe | pass | 4738 |
| 0bc8696492501e41 | WalkMe | pass | 4106 |
| 592b64f4ea54308e | Webbing | pass | 3208 |
| 60ede7302cdeb34a | Wipro Limited | pass | 3273 |
| b081baf87df846ea | Zipher | pass | 2242 |
| 9cc271eace8493c2 | mPrest Systems | pass | 3665 |
| a7cd257b81d9ba1d | אסם | pass | 971 |
| 2fba6ec32b682d0b | בנק לאומי | pass | 1161 |
| fa8ee3b56e2e346e | בנק לאומי | pass | 1085 |
| 854da98ccaf04b12 | הפניקס | pass | 1233 |
| 85fea6b670598866 | הראל ביטוח ופיננסים | pass | 1117 |
| 3d0ab58013933278 | מטריקס | pass | 2083 |
