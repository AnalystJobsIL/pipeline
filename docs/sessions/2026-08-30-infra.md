# 2026-08-30 — `infra`: the suite was green, the gate was broken; a cancelled job names nothing; a cron that did not fire is a line in the mail

Base: `origin/master` @ `2019c3f` → rebased onto `e61dfd8` (roles' three commits). Clean
worktree `.claude/worktrees/infra-0830`, no `secrets.env`, `AJIL_SECRETS` unset (dry — nothing
here needs a credential).

**Spent: 0 Bright Data credits, 0 LLM calls, 0 workflow dispatches.** Every `gh` call was a
read. The one new recurring spend — the digest's 20-minute company-intel drain — is the
subscription token only, and 448 (Bright Data in the digest step) is HELD on the operator's
word until `jd-text` names the cap.

## 0. The numbers I was sent, re-derived

| claim in the brief | measured |
|---|---|
| no clean `tests.yml` verdict since 02:03Z | true — and two MORE cancellations landed while I read (`33295877346`, `33296276566`): eleven runs from 04:55Z, every `guard` job cancelled at `timeout-minutes: 10`; on `fd07897` steps 1–8 took 3m35s and the five seeded rehearsals were cut at 6m38s |
| mutation-gate 1 of 3 shards passing | true, on every run since 08-28 — and **none of the four survivors is a regression** (§2) |
| `_load_secrets` copied into ten modules (438) | **four** (`bd_rescue`, `bd_employees`, `pipeline/jdfill`, `pipeline/run`); seven root tools import `bd_rescue`'s (472) |
| several infra morning-check rows past due and re-dated | **none**: exactly two `infra` rows existed (due 09-10 and 09-11), both honestly `not yet due` |
| today's digest had not fired at 06:13Z | still not at 06:24Z (+84 min); the last three fired at +392, +734, +678 |
| company-intel's gap is 84 | 68 by identity key (their number; the fix is the drain in §5, not a second cron) |
| "master is red" | **the SUITE was green** — runner baseline at `2019c3f`: 1406 passed, 0 red, on all three shards. The GATE was broken. That makes item (1) the whole job and item (2) an adjudication |

`schedule_census --days 14` at 06:23Z: 71 due, 66 fired, 5 not seen, 1 isolated single-slot
drop — the 09-10 threshold (≥ 3) is not met and the recovery-cron decision is untouched.

## 1. `tests.yml`: a verdict, and a timeout that names what it killed (442, 435)

Rejected: raising the number (195's ruling, same shape). Rejected: five seeds inside `guard`
under one bigger timeout (a rehearsal would still hide a unit failure, and a cancel would
still name nothing). Built: **three jobs** — `guard` (suite, invariants, platform report;
3.5 min today under 10), `rehearse` (a six-job matrix: `worst` + `mixed` seeds 1–5, one
process each — `tests/rehearse_registry.py --seed` takes one int — 15 min each), and the
unchanged three-shard `mutation-gate`. And **every long step runs under `timeout
--signal=INT --kill-after=N <budget>m … || rc=$?` with a budget below its job's
`timeout-minutes`** (7/10, 12/15, 40/45): exit 124 becomes `::error::rehearsal (policy
mixed, seed 3) exceeded its 12-minute budget` and a FAILED step, never a cancelled job.
`test_every_long_tests_step_has_a_named_budget_below_its_job_timeout` pins both rules and the
matrix contents. 439 (`fetch-depth: 0` + `github.event.before`) was drafted here and
REVERTED by wave 1 -- see section 8; it lands with its docs half (467) or not at all.

## 2. The four surviving mutants — two equivalent, two records without killers; lane `registry`

| mutant | what it was | what I did |
|---|---|---|
| `hunt-queue-identity-remove` | equivalent (the same predicate gates the address seven lines later); registry had already adjudicated it in BACKLOG 368 and never flipped the flag | `must_be_killed_by_behavioural: false` + `equivalent_why`; the AST guard `test_both_hunt_write_branches_…` kills it (`mutate --id`: killed, static, 79 s) |
| `zero-park-when-skipped` | equivalent, born so in `4ed4cf8` — with `if evicts and not parks:` above, the `else:` runs unconditionally whenever `parks`, so dedenting the `if parks:` block is inert; its designed killer asserts the mutant's own behaviour | a flag flip is not enough for a mutant NO test can kill (it would still SURVIVE), so the record is **re-aimed** at the guard that line really carries: `if evicts and not parks:` → `if evicts:` (a proven wrong-url park skipped because the note is full). Killed by `test_a_zero_confirm_park_always_lands_in_a_re_check_pool` |
| `qdisp-retire-unhunted` | filed in `00062ef` with no ``Kills`` anywhere; the end-to-end fixture's empty drain let `no-evidence-to-read` catch the name before the deleted line | killer appended to `tests/test_units.py` (direct): a drain-only name is `no-hunt-attempt`. `mutate --id`: killed, subset 5, 19 s |
| `qdisp-prune-unrecorded` | its named killer asserts the record is WRITTEN; the deleted assert fires only on a pre-existing record with no `evidence` | killer appended (behavioural): a seeded `no-board` record without evidence makes `main --apply` raise instead of prune. `mutate --id`: killed, subset 5, 19 s |

No commit broke anything; the baseline-red exclusion (195/158) played no part (baseline
`1387 passed, 0 red`). The rule for the lane, filed as 470: `python tools/mutate.py --id
<record> --skip-baseline` in the commit that files the record.

## 3. The digest: the roles dataset beside the board; CI and cron health in the mail

- **Roles' filed diff applied** (their 453) with one change: the optional files
  (`archive.html`, `roles.csv`, `roles.csv.meta.json`, `funnel.csv`) are copied only when
  **non-empty** — an empty artefact warns and the board keeps its previous copy — and the
  three CSV paths have `SINGLE_WRITER` rows. `ROLES_PAGES_URL` is in the pipeline step's
  env so `roles.csv.meta.json` can stop hard-coding `published_on_pages: false` (roles' call).
  `test_board_publish_copies_only_non_empty_optional_files` runs the loop's shell for real.
- **444 applied with three corrections**: the `ci_health` step sits BEFORE the pipeline step
  (which reads the stamps) and before `persist` (which commits them — the filed diff had it
  after persist, where the stamp is never committed; my first draft had it after the
  pipeline, where the mail reads yesterday's), reads `--status completed` (an in-progress
  run has no conclusion), and one read of 200 runs gives `none` / `N+` instead of a streak
  capped at 50. `stages.alarms("ci", 1)` in `run.py`.
- **The cron watch** (item 4): `tests/schedule_census.py --alarm --stamp --days 3`, a mode of
  the lane's existing instrument (same slot expansion, same `cron_since`, same 720-min grace),
  run as a `continue-on-error` pre-step with `actions: read` and `fetch-depth: 0`. One clause
  per workflow with a dropped or past-grace slot; a `gh` failure is an alarm about the watch,
  never a false drop. Live at 06:3xZ from the worktree: `auto-expand: 08:00 on 08-28 not
  seen; daily-digest: 05:00 on 08-28 arrived +734 min late` over 10 workflows.
  `stages.alarms("cron", 1)` puts it on the `Stages:` line. The alarm, not the recovery.

## 4. Provenance, the loader, the seam, the stamp file

- **436**: `persist_state.run_provenance` appends `(<event> run <id>)` before `[skip ci]` in
  ONE place for all ten workflows; a local run's subject is untouched.
- **438**: `pipeline/secretsenv.py` — `AJIL_SECRETS=<path>`, else the MAIN checkout's file,
  else (a worktree) nothing and one stderr line. Deliberately not auto-discovering the main
  checkout's file from a worktree: that would arm every worktree with `BD_RUN_CAP` unset.
  `setdefault` only, so `tests/conftest.py`'s empty-string disarm still holds. `run.py`
  delegates; the three foreign copies are 468's three-line diff.
- **449** (shared plumbing, disclosed): `result`, else `<subtype>: <errors[0]>`, else the
  envelope's stdout. **451** (shared, disclosed): a stamp over a file that exists and does
  not parse is refused and the bytes are untouched.
- **440**: both occurrences of the personal handle are out of the tree (the docs lane's
  BACKLOG line too, operator-approved). It remains in the public repo's HISTORY from
  `c0f7635` (2026-08-22). The rewrite is planned, not run — see §7.

## 5. 450: not a second cron

Company-intel withdrew the 23:00 slot itself and the operator verified the arithmetic: the
gap is read at 05:00, the intake that creates it lands 19:00–22:20, and the 10:00 slot at
every observed lag finishes BEFORE that intake, so the morning number is structurally a day
stale; a 23:00 slot under the same lags beats the digest 2 mornings in 3 — odds, not a
mechanism. Built instead, on the operator's preference and my judgement of the bound: the
**same bounded drain inside `daily-digest.yml`, before the step that measures**, at
`--budget-min 20` (the whole job measured 27–32 min of a 285-min cap and the relay's real
cutoff is 23:39, so nothing is lost; what the number protects is the reader's clock — the
number I would not exceed on the mail's critical path is 30). The `10:17` cron keeps
`--budget-min 60` as the backlog backstop, plus the `if: failure()` stamp and node 22. The
`:17` minute tests BACKLOG 305 on the side. Proof is company-intel's number (morning check
09-07).

## 6. What I did NOT finish

- **448** — held for `jd-text`'s cap; two env lines, one follow-up commit.
- **467** `docs`: `check_docs.py` reading `AJIL_PUSH_BASE`. **468** registry/jd-text: the
  three remaining `_load_secrets` copies. **469** `docs`: the brief's ownership gap.
  **470** registry: the mutation-record rule. **471** infra: census exit code.
  **472**: the seven importers of `bd_rescue._load_secrets`.
- The two morning-check rows due 09-01 and the one due 09-07 carry the proof this session
  cannot produce: an unattended run.

## 7. 440 — the public-history rewrite (planned, NOT run)

The string is reachable via `git log -S` in `AnalystJobsIL/pipeline` from `c0f7635`. Window:
13:00–17:30 UTC on a day the orchestrator confirms every lane has pushed or parked and
`gh run list` shows nothing in progress. Steps: (1) `git clone --mirror` via the deploy key,
never the shared checkout; (2) `git filter-repo --replace-text` mapping the handle to
`someone`; verify `git log --all -S<handle>` → 0; (3) `git push --mirror --force` with the
deploy key; (4) every worktree `git rebase --onto origin/master <old-sha> <branch>` using
`filter-repo`'s `commit-map`; quoted SHAs in HANDOFF/BACKLOG/sessions go stale — keep the map
beside this note; (5) GitHub keeps force-pushed objects reachable by SHA until GC — ask
Support to purge; run records never printed the handle, so none need deleting (verify before
deleting anything, BACKLOG 324); (6) the private `backup` remote carries the identity by
design and is unaffected. Risks: six live worktrees; the relay hashes bytes, not SHAs (safe);
`check_unattended_proof` and `digest_watchdog.py` compare against `origin/master` (safe after
fetch).

## 8. Wave 1 — three Opus attackers on the branch before the push (findings → fixes)

| finding | fix |
|---|---|
| `timeout --kill-after` firing returns **137**, not 124, so the named error was dead code on the path it was written for | both codes are the budget; the message says which |
| the budget guard had six holes (the words in a comment, `python3`, two budgets summing past the job, a job with no timeout, a second long step without the handler, an `if:` skipping a job) | comments stripped, the budget must START the command, budgets summed per job, every job needs a timeout, the handler is per STEP, no `if:` in the file |
| `fetch-depth: 0` in `tests.yml` makes `test_ci_itself_…` red on a non-master ref, and nothing in the job consumed the history | reverted; 439 stays open with its shape recorded (the depth and the docs read land together) |
| `ci`/`cron` were stamped AFTER the pipeline step, so the mail carried yesterday's verdict | both steps moved before the pipeline (and before persist) |
| the digest's drain stamps `firmo` today, so `alarms("firmo", 2)` could never fire again; company-intel's label calls it the cron's | tried `1`; four company-intel tests pin the `2`, so the line keeps their value and says it is blind; 474 filed to company-intel with the decision |
| `ci_health` capped the streak at 50 and fabricated "50" from an empty list; a `gh` hiccup was a red step | one read of 200, `none` / `N+`, a `gh` failure is an honest `ci` stamp |
| `census()` looked for "the next slot" within the same day, so a run on time for the 08:00 was consumed by last night's dropped 20:00 and the 08:00 reported `not seen` | the timeline spans the window plus a day; test pins the shape |
| a BOM or an empty stamp file was "corrupt" and cost the run every stamp | `utf-8-sig`, empty = absent, `stamp()` returns a bool |
| `firmo_drain` had no inner budget and no failure stamp (the 450c hole, in the file that fixed it elsewhere); digest still on node 20 | inner `timeout 23m`, `alarm=step-failed(rc N)`, node 22 |
| `_kind` ran over raw stdout: `"duration_ms": 401` in the envelope read as an auth failure and opens the breaker on ONE strike | the fallback names the subtype and the envelope's key names; never raw stdout |
| `outcome --commit` (the failure notice) bypassed the provenance tag; a UI re-run keeps `schedule`; the tag could land in a body line; the audit line recorded the pre-tag message | outcome tagged; `attempt N` when `> 1`; subject line only; tag before the audit line |
| the loader read a BOM into the first key (Notepad's default) | `utf-8-sig` |
| `roles.csv.meta.json` on the board says it is not on the board; `ROLES_PAGES_URL` unread | 473 filed to roles |
| `AJIL_SECRETS` arms `pipeline.run` only; the Bright Data tools keep their copies | true and stated in the module; 468/472 |

Held under attack: the `-e` shell semantics of every new block, the heredoc extraction, the
matrix, the `[ -s ]` loop, the `_cli` parser on the alarm string, the `gh`-failure path of the
watch, the Sunday cron, the conftest disarm sentinel under `AJIL_SECRETS`, every `llm` pin
(49 tests), `rehearse_infra --conflict/--twice/--notice` with the provenance env set.

## 9. Verification (filled after the push)

- CI run on the landed commit: _pending_
