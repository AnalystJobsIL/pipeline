# 2026-08-31 (b) — roles: the identity fold, the twin collapse, the blocker column

The operator's standing correction — close gap CLASSES generically — applied to three
classes at once: one employer under two name strings (533, NVIDIA/NVIDIA AI), one posting
under two merge_keys of one company (the seen-id collision Stages alarm), and a weak text
with no stated reason (the loop `description_quality` opened this morning). Plus the LTX
prediction verified as mechanism. Every number below was re-derived this session from the
committed tree or a live read; the two adversarial waves ran against the committed diff.

## The numbers the spawn brief carried, corrected first

- **"~13 alias groups" is 3 in the role store** (helfy/Helfy, Meta/Meta Israel — already
  superseded, NVIDIA/NVIDIA AI). The 13 was BACKLOG 133's REGISTRY figure, which
  re-derives as **0 groups by api_url** and **11 same-identity groups on DIFFERENT
  boards** (Amazon/AWS class — deliberately separate scanner rows, out of scope).
- The store-side 13 that DOES exist is **13 seen_ids naming 2+ role_ids** — a different
  class (below), 4 pairs live today, all SAME-company.

## 1. The alias fold (533 CLOSED)

Seam: canonicalize the company STRING at intake (`roles.fold_company_aliases`, before
`classify_grouped` — one group, one LLM call) + `Ledger.fold_aliases` over stored
records. `merge_key` never moves. Full rationale + rejected alternatives with killing
numbers in `docs/decisions/2026-08-31-roles-alias-fold.md` (short: a pure key has nowhere
for an evidence gate — `identity_key("AppSec Labs")==identity_key("AppSec")`, two
employers, 144; resolve_claims-only cannot even SEE the pair — both postings aggregators,
0 shared evidence buckets; render-only prevented nothing on 08-31).

`_alias_fold_target`, the one gate: never a registry name (active or parked — Bounce AI
stays, the 489 direction); exactly one active identity match (11 ambiguous groups fold
onto neither); then casefold | DECLARED (`firmographics.ALIASES` key tested against the
plain-normalized name — the `values()` leak would have folded "NVIDIA Labs"; every alias
key pinned a fixed point of `_plain_norm`) | board (`store._same_origin`, inert on
aggregators). The `nvidia ai → nvidia` declaration is company-intel's `f62b472` (today
14:12Z) — this fold consumes it; company-intel messaged that ALIASES is now also a
role-record declaration table.

**Audit, committed store (193 records / 1,100 active): 7 candidates — 4 fold** (3
casefold field-repairs: Appcharge→AppCharge, GE HEALTHCARE→GE HealthCare, Helfy→helfy —
key-preserving since `_norm` lowercases; 1 declared supersede: NVIDIA AI→NVIDIA, same
title, seen_ids + sent mirror unioned so `filter_new` keeps seeing the 08-31 delivery),
**3 refused** (parked registry rows), **0 ambiguous, 0 no-twin**. A no-twin foldable is
LEFT in place (a role_id rename is a full-store migration; measured empty).

## 2. The twin collapse (the seen-id collision class REPAIRED, not just alarmed)

Mechanism found: a retitle mints a new merge_key and the id union puts the same seen_ids
on BOTH records — `filter_new` is `not any(is_sent)`, so one delivered twin suppresses
the other forever (HoneyBook `senior product analyst`: `emailed_on 2026-08-26` <
`first_seen 2026-08-27`, an inherited delivery), and the board carries a duplicate card.
`_groups`' cross-company arm refuses same-company pairs BY DESIGN, so the repair is a
sibling predicate with a HIGHER bar — `same_role_twin`: junior-pole vs senior-pole
refused absolutely; then ≥2 shared strong ids, or 1 shared id + identical non-empty
posting key, or 1 shared id + `_titles_agree`; ids borne by ≥3 of a company's records
demoted to weak first (F5 `workday:0` names four REAL postings — no live pair in the
store, forward defense only).

Two arms, because the live pairs are refreshed daily and can never go stale on their own:
`_resolve_claims` folds pairs both fetched today, testing with the STORED ids folded in
(today's cards alone share nothing) — winner by per-run `_source_rank` (the native
board's title is the employer's current one), TIE refuses the group; `sweep_store` folds
the pair whose stale half never returns — open beats closed, then later `last_seen`,
full tie refuses. Verified against all 5 same-company pairs: HoneyBook + Fetcherr fold at
run time (Fetcherr's board serves only "Data Analyst" now — live read of comeet 68.006;
the tableau title survives on a LinkedIn card, rank 2, so the arm has a clean winner;
if the card dies first, last_seen divergence hands it to the sweep), Modellama folds at
the sweep (reproduced on a store COPY: exactly `modellama|data analyst raanana full
time` → superseded, `twin_folds 1`, nothing else), Guardio + Percepto REFUSE (1 weak id,
titles disagree — the accepted cost is a duplicate archive row). `id_collisions` stays
run-scoped and textually unchanged; §7c now documents that it read `2` while the store
held 13, and why.

## 3. The blocker column (the exclusion path, per the operator's rule as written)

Contract agreed LIVE with jd-text (SendMessage, both directions) and the orchestrator:
- `description_blocker` (57th column, archive too) + meta `description_text.blocked`
  (counts by reason + the live policy); their 09-01 morning check greps these.
- Derived ONLY for `snippet`/`none` rows — GONE_MARK judges the ADDRESS, a donor-filled
  gone row is unmarked (jd-text's Mobileye caveat, pinned).
- Precedence: recorded `matched.jd_why` `structural:*` VERBATIM (their column ships
  tonight; `store._matched_cols_live` reads it defensively — the column is absent until
  the enrich driver's next run, window confirmed real by jd-text; `reconcile` carries it
  onto the record, sqlite's copy wins) → derived `gone` | `unfillable:<why>` |
  `listing-page`.
- Policy separable per the orchestrator's condition: `roles.BLOCKED_POLICY = "mark"`
  (the snippet-rows decision stands; addendum appended there); the exclude branch is
  BUILT and pinned — removes the row with its reason still counted, every meta identity
  whole (`reconciliation` gains `blocked_excluded`) — so the operator's row-by-row audit
  flips one constant, never the derivation.
- jd-text will NOT file the fenced BACKLOG diff (the carry is built here instead).

## 4. LTX — the prediction, verified as mechanism (no run to verify it as fact)

No digest since 11:29Z (33387229779), so "did tonight's run flip it" is unanswerable
until 09-01 ~05:00Z. What was verified instead: the live greenhouse API serves
`8644790002 | Commercial Operations Analyst | Jerusalem` — an EXACT merge_key title match
with the stored `ltx|commercial operations analyst` Indeed record — so the deep-donor
branch + the aggregator→board overwrite reach it the run the classifier admits the card
(the 08-31 non-flip was a first-fetch needing an LLM verdict). Morning-check row filed;
FAIL shape named (still `il.indeed.com/viewjob?jk=b17171f8cdb4b6c7` ⇒ investigate the
classify tier, not the ratchet).

## Guards and rehearsals

- 21 tests appended (`roles, 2026-08-31 (b)` block): the three fold gates + five
  refusals (registry-name/ambiguous incl. the case-twin-under-every-iteration-order arm,
  bare-strip, values()-leak + alias-key fixed-point drift wire, Bounce), sweep
  supersede-with-union + casefold-keeps-role_id + no-twin-left, twin arms + single-id /
  junk-id (F5 AND the Thales discriminating population) / junior-senior / tie refusals +
  filter_new invariance, blocker precedence/gating/derived reasons, jd_why
  reconcile + column-absence tolerance, meta blocked counts + BOTH policy branches with
  identities holding.
- 8 `tests/mutations.json` records (242→250), each anchored to this diff and named to
  its killing test; verified to apply-and-be-killed post-commit via `tools/mutate.py`.
- Rehearsals: happy/clobber/corrupt/massclose PASS; `--golden` PASS (differs from HEAD
  by exactly the claim collapse); `--audit-ids` unchanged (`1 problem: F5 workday:0` —
  311 not claimed).

## Out-of-lane touches, declared

`pipeline/run.py` outside the 484-691 block: the registry-facts block (~290, already
commented `lane: roles`) gains `_registry_names`/`_active_by_ident`/`_fold_resolver`;
the two `classify_grouped` call sites are wrapped (the fold must precede grouping or the
duplicate LLM call survives). `pipeline/firmographics.py`: ZERO edits (`ALIASES` imported
read-only). `tests/mutations.json` and `tests/test_units.py`: appends, per their shared
ownership.

## Not finished / left for others

- The Fetcherr fold is arm-dependent (see §2) — the 09-01 `retitle folds` count may be 1
  or 2; both are correct behavior, the morning check says which arm fired.
- `530@roles` (withdrawn means two things) — untouched, out of tonight's scope.
- `518@roles` (terminal registry verdict as a purge source) — untouched.
- jd-text's `jd_why` lands in the ARCHIVE csv too via the same build_rows path; nothing
  further owed there. `498@infra`, `512@company-intel`, `261@scraper` — unchanged.
