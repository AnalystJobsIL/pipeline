# 2026-08-30 — `docs` (CI): the suite I ran was not the suite CI ran

Base `1f6db41`. Worktree `.claude/worktrees/docs-0830b`. No Bright Data credits, no LLM
calls, no SerpApi, no dispatch, nothing written to `companies.csv`; `gh` read-only.

The morning's session shipped `check_tree_is_current` for "the file agents read is not the
file the linter checks". This is the same defect one level up, and it was **mine**: the three
guards I added to prove those checks work passed on every laptop and failed on every push.

## What was actually red, and whose it was

`tests.yml` has not been green in the last **100 runs** (`gh run list --workflow tests.yml
--limit 100` — no `success` in the window, which reaches back to 2026-08-26T20:24Z). Two
independent causes on the newest run (`33287065598`, sha `b4ac2a7a`):

| job | failure | whose |
|---|---|---|
| `guard` | `test_docs_are_consistent_with_the_code` — `coe_ratio` said 43, 92; the code said 44, 93 | `registry`'s `34f24b3` added a workflow; three of the four sites were updated with it and `docs/AGENT_BRIEF.md` was missed |
| `guard` | `test_a_checkout_left_behind_on_the_certified_docs_is_an_error`, `test_a_checkout_behind_only_on_code_never_goes_red`, `test_a_scheduled_workflow_change_must_name_the_morning_its_run_is_read` | **mine** |
| `mutation-gate` (0 and 1) | 3 surviving mutants — `qdisp-prune-unrecorded`, `qdisp-retire-unhunted` (M1/M3), `zero-park-when-skipped` | `registry`'s queue code; reported, not touched |

## Why my three failed, verified rather than assumed

The checks skip when `GITHUB_ACTIONS` is set — deliberately, because a runner's checkout is
one commit deep and "how far behind is this tree" has no answer there. My fixtures cleared
that variable from the **git subprocess environment** and never from the **pytest process the
check runs in**. So on a runner every one of them got an empty error list and asserted
against it.

Reproduced both ways before fixing: `GITHUB_ACTIONS=true python -m pytest -k "checkout_left
or checkout_behind or scheduled_workflow_change"` fails on this laptop exactly as it does on
the runner, and passes with the fix. That is the whole bug — a variable cleared in the wrong
process — and it is the same shape as everything else this week: **I verified in one
environment and reported for another.**

## The three guards now run in CI, and CI now checks my reasoning

`_not_ci` (a context manager in `tests/test_units.py`) clears the variable from the pytest
process for the duration of a check call. Three call sites use it.
`test_the_tree_check_is_silent_in_ci_and_never_fetches` deliberately does **not**: skipping in
CI is the design, and asserting on the skip is not the same as never exercising the check.

The *checks themselves* still skip on a runner, and that reason is now **observable and
verified rather than documented**:

* `_tree_skip_reason()` returns the reason in words. `python docs/check_docs.py --tree` prints
  `tree: no reading here - CI: the runner builds the pushed commit from a depth-1 checkout…`
  instead of a shrug.
* `test_ci_itself_confirms_why_the_tree_check_cannot_run_there` runs **only on a runner**. It
  asserts that either there is no `origin/master` ref or the history is one commit deep, and
  emits `CI checkout: shallow=… origin/master=… commits=…` as a **warning**, because pytest
  shows a passing test's stdout only under `-rP` — as a `print` the facts would have been
  visible exactly when they were not needed. If `actions/checkout` ever changes its
  defaults, the build says so instead of the documentation quietly becoming false. Verified
  it is not vacuous: handed a full clone while claiming to be CI it **fails**, reporting
  `shallow='false' origin/master='7ac03d08' commits='606'`.

**They are still local-only guards, and that is now written down rather than implied.** A
morning check due **2026-09-06** asks the question that matters: has any of the three actually
stopped a lane in a week? If not they are decoration, and the row says to file them as such.

## `coe_ratio`: the fix, and the design question underneath it

Fixed the number, then removed the class of failure. It is now a one-sided **floor** —
`coe_steps`, census class, `44+` at all four sites — reusing the machinery the census facts
already have.

Three options, and why this one:

* **Keep the exact pin and fix it each time.** Rejected on measurement, not taste. It broke
  within a day of being pinned, and when it broke it did not merely annoy: `Registry
  invariants` and the fourteen rehearsed nights are steps **below** `Unit guards` in the same
  `tests.yml` job, so a red suite **skips** them. A stale number in a brief switched off the
  registry gate — the exact failure the census floors were introduced to stop, which this
  file's own comments already argue at length.
* **Drop the number, print a command.** Rejected. The number *is* the rule — "a green
  workflow means nothing" — and a rule whose evidence is a command nobody runs is a rule
  nobody believes. The repo took this option for the API/page split because no decision turns
  on that number; a decision turns on this one every time an agent reads a green run.
* **A floor on the count.** Taken. Steps are added most weeks and a floor absorbs every
  addition. It fires only on a **collapse** — the case where `continue-on-error` became rare,
  the rule above it stopped being true, and a human must rewrite it that day.

The denominator leaves the pinned text: it was the half that moved most and the half no
reader acts on. `test_the_continue_on_error_number_is_a_floor_and_never_a_pin_again` pins the
*decision*, with the skipped-registry-gate argument in its docstring, so the next session that
wants to "tighten" it reads why first.

## The CI-truth clause

`docs/AGENT_BRIEF.md`'s definition of done and `CLAUDE.md`'s contract item 1 now say that
**`pytest` green means green in CI, on the commit you pushed** — `gh run list --workflow
tests.yml --limit 1`, the sha must be yours, the conclusion `success`, and **the run id goes
in the `HANDOFF.md` line**. A red run is yours to fix or revert whoever made it red; if it was
already red when you arrived, say so with the run id so the next session knows what it
inherited.

It is the sibling of yesterday's production clause. That one says an exit code is not a
result; this one says a result on your machine is not a result on the runner. Both exist
because 100 consecutive red runs went unmentioned while every lane reported a passing suite.

## What I could not make green, and why

* **The three `mutation-gate` mutants are still surviving** — `qdisp-prune-unrecorded`,
  `qdisp-retire-unhunted`, `zero-park-when-skipped`. They are `registry`'s queue code, in
  files this lane may not write. `tests.yml` cannot go green until that lane kills them, so
  the first genuinely green run will not be this one.
* Which means the CI-truth clause lands the same day it cannot yet be satisfied. That is
  said in the clause itself: report the run id and what you inherited, rather than claiming
  a green you do not have.

## What CI said about this commit

Run **33293548117** on `7ac03d08` — the first reading the new clause asks for:

| step | result |
|---|---|
| `Unit guards` (`python -m pytest`) | **success**, 102s (04:55:27→04:57:09Z) |
| `Registry invariants` | success |
| `Fourteen rehearsed nights` | success |
| `Five mixed-policy rehearsals` | **cancelled** at 05:05:33Z — the `guard` job hit `timeout-minutes: 10` |

So the three guards **do** now pass on a runner, and `Unit guards` is green in CI for the
first time in the 100-run window. `test_ci_itself_confirms_why_the_tree_check_cannot_run_there`
ran there (it is not skipped when `GITHUB_ACTIONS` is set) and passed, which is the
verification: on that runner there is genuinely no `origin/master` to be behind, or a
one-commit history. The documented reason is now confirmed by CI rather than asserted by me.

**The job is still not green, and the remaining cause is not mine and not fixable from this
lane.** The five seeded `rehearse_registry.py` runs no longer fit the 10-minute budget: steps
1–8 consume 3m13s and the rehearsals were cut off after 6m59s. `tests.yml` is `infra`'s file
and `tests/rehearse_registry.py` is `registry`'s, so raising the timeout or sharding the
seeds is theirs to do — the same shape as `docs/BACKLOG.md` 195, which rejected "just raise
the timeout" for the mutation gate and sharded it instead. Filed as `441@infra`.

## The runner corrected me

The reason I wrote for skipping in CI was "a depth-1 checkout has no ref to be behind". The
guard emitted what it actually found, on run **33294213125**:

    CI checkout: shallow='true' origin/master='61bbc99a' commits='1' reason_without_env=None

Half of it was wrong. `origin/master` **does** exist on a runner — it points at the very
commit being built — and `reason_without_env=None` says that with the variable removed the
check would run happily rather than bail out. The true reason is narrower and better:
**`origin/master` IS this commit, so `behind` is 0 by construction**, and a check that can
only ever say 0 is a check that says nothing.

`_tree_skip_reason()` says that now, with the run id beside it, and the guard asserts the
CONSEQUENCE — with the variable gone, `tree_state()` reports 0 behind — instead of the
symptom I guessed at. Verified not vacuous: on a full clone claiming to be CI it fails with
`the checkout is 1 commits behind origin/master`.

That is the exercise in one line. I had a plausible reason, wrote it into three places, and
it took a machine printing what it saw to show that half of it was false.

## Definition of done: the outcome is first now, and every lane has a number

The section was entirely process — tests green, docs updated, a HANDOFF line, an item filed,
and (since yesterday) an unattended run quoted. Every clause was about not breaking things.
A lane could satisfy all of them having drained nothing, and six lanes were optimising for
auditability because auditability was what the finish line asked for.

It now reads: **1. the number moved · 2. it keeps moving without a session · 3. nothing
broke** — with clause 3 labelled the price of admission, and *"reporting this section as the
outcome is the mistake the section exists to stop"*.

**Clause 2 is a delivery bar, not a wish.** The standard is not "the queue reached zero", it
is *"this flows automatically in git from now on"*. A lane whose number falls only while a
session runs has **not delivered**, however far it fell, and says those words. Three things
must be named — the **workflow**, its **cadence** (with the `event: schedule` run that proved
it), and the **alarm**, *where a human reads daily*. Any one missing **is** the lane's
remaining work. If the fix needs a workflow, that is `infra`'s file and the deliverable is
the **exact diff**, filed, plus the sentence *until infra applies it, nothing is automatic*.

### The numbers, re-derived here — not copied from the brief I was given

Measured 2026-08-30 05:1x–05:4x UTC from a clean worktree at `origin/master`:

| lane | number | reproduced? |
|---|---|---|
| `registry` | **259 owed of a 276-name intake queue** | exact |
| `company-intel` | **84 active rows with no sector** | exact |
| `classifier` | **191 roles on a superseded contract**, 60/run cap | exact |
| `ats-fetch` | **17 active rows whose newest posting is 12+ months old** | exact |
| `infra` | **5 of 71 scheduled slots not seen** in the fortnight (1 isolated drop) | exact |
| `roles` | **no public dataset; seniority empty on all 154** records | exact |
| `discovery` | **273 of 276** intake names already carry a disposition; **42** were retired conclusively and came back | derived — the brief said "names re-added", this is how many |
| `docs` | **60 consecutive non-green `tests.yml` runs** (100 at 04:00Z) | exact |
| `jd-text` | **223 attempted scrape cards still under 200 chars; 8 of 154 matched roles** | **the ~30/night figure could NOT be reproduced** |
| `scraper` | **33 cards on a shared JD page** | **the 6/night figure could NOT be reproduced** |

The last two are the ones worth saying out loud. `~30 a night that fail their fill (23
discovery-linkedin no-markers, 7 scrape shell)` and `6 postings a night whose url is a listing
page` are RUN counters: the newest committed stamp reads `matched_why=auth-walled3+no-markers2
scrape_why=bd-shell2` — **7**, not 30 — and the caches keep `_jd_attempted` but never the
REASON, so a card refused for `no-markers` is indistinguishable from one refused for
`auth-walled` the moment the run ends. Those two cells carry the stock they can measure and
say the flow is unmeasured; `443` asks for `_jd_why` on the card. **A made-up target is worse
than an admitted gap**, and a lane that cannot measure its own number after the push cannot
satisfy clause 1 either.

### My own lane, by the standard I just wrote

`docs`'s number is **master's CI red → green**, and it is **not delivered**. The workflow
exists (`tests.yml`, every push). The cadence is every push. **The alarm does not exist at
all** — which is precisely why 100 consecutive red runs went unmentioned while every lane
reported a passing suite. Nothing in the mail, the run log or any state file says the gate is
down.

So the lane's remaining work is the alarm, not the backlog around it, and the fix is two hunks
in `infra`'s files: a step in `daily-digest.yml` that reads `tests.yml`'s conclusion on master
and stamps `stages.stamp("ci", alarm=...)`, plus one line in `pipeline/run.py` adding
`stages.alarms("ci", 1)` to the list the mail renders — a stage that is not in that list is
invisible however loudly it stamps. Filed as **`444@infra`** with both hunks written out, and
verified as far as this lane can: `stages.alarms("ci", 1)` returns `['ci never ran']` today,
which is the correct reading until hunk 1 lands and goes quiet by itself on the first green
master. **Until `infra` applies it, nothing about CI health is automatic.**

## The verdict I do not have

Five pushes, five `tests.yml` runs, **no verdict on any of them**:

| run | commit | run conclusion | `Unit guards` within it |
|---|---|---|---|
| 33293548117 | `7ac03d0` | **cancelled** | success |
| 33294213125 | `61bbc99` | **cancelled** | success |
| 33294986316 | `fd07897` | **cancelled** | success |
| 33295877346 | `2344af7` | **cancelled** | success |
| 33297139619 | `ff4a927` | **guard cancelled** (06:32:47-06:43:03Z) | success |

The `guard` job dies on its own 10-minute timeout at step 9 (`442`), and the mutation-gate
shards carry `registry`'s surviving queue mutants (`435`). **A cancelled run names nothing** —
it did not judge this work and approve it, it stopped before it could judge anything — so
these commits are **UNVERIFIED IN CI**, and `HANDOFF.md` says that in those words.

What I can say is narrower than green: the step that would have judged this work,
`Unit guards` (`python -m pytest`), completed **success** inside every one of the four.
Quoting that as the verdict would be the same error as reporting a green suite from my own
terminal, which is the error this session exists to stop.

The substitute the orchestrator set — clean-worktree **counts** after the push, the run id
**and** its conclusion however unhelpful, and `UNVERIFIED IN CI` in `HANDOFF.md` — is now
clause 3 of the definition of done, permanently. It is narrower than "CI green", not looser:
it forbids both the false green and the silent omission.

## Infra's round, and the three checks of mine that never ran

**Infra corrected my `444` diff and it was right — I checked the code before agreeing.** I
placed the `ci_health` step before `Report the run's outcome`, which is *after* `Persist state
back to the repo`. `persist_state.py outcome --commit` commits a **fixed** list —
`cloud_state/last_run.json`, plus `digests/latest.md` when it writes a notice — from a fresh
worktree of origin/master, so my stamp would have been written and never committed: the alarm
would have been lost every night, silently, which is the failure it exists to prevent. Infra
also adds `--status completed` to the `gh run list` call; without it an in-progress run
returns a null conclusion, reads as `unknown`, and alarms daily, because the digest and
`tests.yml` overlap constantly. The item now carries infra's version and says both mistakes
were mine.

**`AJIL_PUSH_BASE` is implemented and tested, before infra's side landed.** `_baseline_ref()`
prefers `AJIL_PUSH_BASE` (`github.event.before`) over the merge-base, and
`check_morning_rows_survive` and `check_unattended_proof` both use it; an all-zero sha is
branch creation and skips, an unreachable sha skips, and without the variable CI behaves
exactly as before. `check_tree_is_current` deliberately does **not** take it: on a push build
HEAD *is* origin/master whatever the fetch depth, so `behind` is still 0 by construction.
Tested against a synthetic variable, so neither half waits on the other.

### Three of my own checks were dead code on master

Registering the checks I shipped, I found that **`check_unattended_proof`,
`check_no_home_paths` and `check_no_conflict_markers` are all defined on `origin/master` and
none of them is in `CHECKS`.** They have never run. The cause is banal and worth the sentence:
I registered them with `line.replace("check_x,", "check_x, check_y,")`, and `str.replace` does
nothing when its anchor is absent and reports nothing when it does nothing. Every other patch
in the session asserted its anchor; the registrations did not.

It is also, exactly, the defect this session exists to fix — a check that is green because it
never ran — shipped three times *inside* the work writing the guard against it. All four are
registered now (18 registered = 18 defined), and
`test_every_check_that_exists_is_registered_and_therefore_runs` fails the build the commit a
`check_*` appears without being called. That guard is the durable half; the three
registrations are the part that should never have needed doing.

### The three stale prose claims: my answer

Two of the three are counts, so the fact registry is the obvious tool, and **the measurement
says no**: the workflow count changed **8 times in 10 days** (6→7→8→9→10→9→10→11) — the
`coe_ratio` cadence exactly, and a pinned count that breaks takes the registry gate down with
it. A floor is no better, because `9+ of 10+` is not a claim about anything when the ratio is
the point. What is durable is the STRUCTURE each sentence is really asserting — *every
scheduled workflow shares `repo-state` except `daily-digest.yml`*, and *no workflow commits
state except through `persist_state.py`* — neither of which drifts when a workflow is added,
and both of which catch the real hazard (a new cron that forgets the group). Filed as `445`
with the measurement; not written today, because a structural check that is wrong is worse
than a number that is stale.

The third is the interesting one, and it defeats both obvious tools. `tests.yml` runs
`tools/mutate.py --all` is not a number, so the registry cannot hold it — and `--all` is still
a real flag of `mutate.py`, so a flag-existence check passes it too. What is false is the
**relationship** between the doc and the workflow, which is what the section-4 cron table
already checks one level up. `check_workflow_command_claims` holds every "`<workflow>` runs
`<command>`" sentence to that workflow's text, and found the live instance on its first run.
`docs/BACKLOG.md` is exempt for the same reason it is not a fact site: it quotes false claims
on purpose — including this check's own founding example — and a history that goes red for
describing history is a history somebody edits.

### Ownership, and two numbers corrected

`confirm_zero.py` and `queue_disposition.py` → **`registry`** (both write registry verdicts;
`discovery` owns the re-add that ignores the dispositions, `441`). `tools/mutate.py` →
**`infra`** (the harness `tests.yml` shards). **`tests/mutations.json` belongs to no lane and
is appended by all of them**, exactly as `tests/test_units.py` is — each record is a defeat
some lane's own code suffered. All four were unowned, which is how a catalogue with four
records that no longer matched any code went unnoticed.

`438` said `_load_secrets` is copied into **ten** modules; infra measured **four**
(`bd_rescue.py:46`, `bd_employees.py:41`, `pipeline/jdfill.py:89`, `pipeline/run.py:185`) and
the item now says four with the sites. The smaller number does not weaken it: four copies is
still four trees that cannot be armed.

One clause of `ARCHITECTURE.md` section 7 — `company-intel`'s section — was corrected in place,
because my own new check went red on it and a lane may not ship a red gate. Disclosed here and
in the commit; it is the single clause the check named, nothing else.

