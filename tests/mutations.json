[
 {
  "id": "crack-activate-narrow",
  "file": "crack_walled.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "if verdict.startswith(\"cracked\") and _wv != \"ok\":",
  "replace": "if verdict == \"cracked-scrape\" and _wv != \"ok\" and n_il < 0:",
  "why": "shipped defeat: the cracked-api/oraclehcm branch activates ungated while the source text still says ok_to_write. Found by a wave-8 reviewer with the suite at 224 passed.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "crack-activate-remove",
  "file": "crack_walled.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "if verdict.startswith(\"cracked\") and _wv != \"ok\":",
  "replace": "if False:",
  "why": "the activating branch with no gate at all",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "crack-oktowrite-invert",
  "file": "pipeline/identity_gate.py",
  "symbol": "activation_verdict",
  "class": "M2-gate-inversion",
  "find": "        if p is not None:\n            return \"ok\" if p else \"not-ours\"",
  "replace": "        if p is not None:\n            return \"ok\"",
  "why": "`is True` -> `is not None` at the point where a held page decides: inside the guard `v is not None` is vacuously true, so a page naming ANOTHER company (v is False) admits the board. Re-aimed twice: once when the separate html branch collapsed to one rule, again when the census ordering made the held page decide first (f1b28a8). Killed by census cell 1 (Cogniteam/Riskified).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-identity-invert",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "elif verdict == \"found\" and not _gate.identity_ok(name, url):",
  "replace": "elif verdict == \"found\" and _gate.identity_ok(name, url) is None:",
  "why": "shipped defeat: truthiness inverted, source token intact. A wave-8 reviewer reopened this with the suite green.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-activate-remove",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "elif verdict == \"found\" and not _gate.identity_ok(name, url):",
  "replace": "elif False:",
  "why": "the hunt's activating branch with no identity test",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-persist-remove",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "                        if not _gate.identity_ok(name, url):",
  "replace": "                        if False:",
  "why": "the nolisting branch persists fr[3]; refusing to ACTIVATE while still writing the ADDRESS only delays the same mistake 24h, because the fast path reads it",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-scope-narrow",
  "file": "pipeline/identity_gate.py",
  "symbol": "identity_ok",
  "class": "M3-branch-narrowing",
  "find": "    if host and ATS_HOST.search(host):",
  "replace": "    if host and ATS_HOST.match(host):",
  "why": "`.search` -> `.match` anchors the scope test at the string start, so every subdomain ATS host stops being treated as one",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "gap-activate-invert",
  "file": "repair_extract_gap.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "if il and not _gate.identity_ok(r[0], r[3]):",
  "replace": "if il and _gate.identity_ok(r[0], r[3]) is None:",
  "why": "shipped defeat: the sixth activating path, reopened with the suite at 227 passed",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "gap-activate-remove",
  "file": "repair_extract_gap.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "if il and not _gate.identity_ok(r[0], r[3]):",
  "replace": "if False:",
  "why": "runs 30 minutes before listing_hunt in the same job and forces SCRAPE_ASSUME_IL=1",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "gap-activate-narrow",
  "file": "repair_extract_gap.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "if il and not _gate.identity_ok(r[0], r[3]):",
  "replace": "if il and len(il) < 0 and not _gate.identity_ok(r[0], r[3]):",
  "why": "dead conjunct narrows the guard to nothing",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "deep-cand-fallback",
  "file": "deep_validate.py",
  "symbol": "apply_verdict",
  "class": "M2-gate-inversion",
  "find": "    _cand = api or \"\"",
  "replace": "    _cand = api or fr[3] or \"\"",
  "why": "the wave-7 bug, textually intact: falls back to the ROW'S OWN careers page, so identity is confirmed from the wrong page",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "deep-activate-remove",
  "file": "deep_validate.py",
  "symbol": "apply_verdict",
  "class": "M1-gate-removal",
  "find": "    if verdict == \"recovered\" and not (",
  "replace": "    if False and not (",
  "why": "deep_validate with no gate at all -- its state before wave 8",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "deep-scope-narrow",
  "file": "deep_validate.py",
  "symbol": "apply_verdict",
  "class": "M3-branch-narrowing",
  "find": "    if verdict == \"recovered\" and not (",
  "replace": "    if verdict == \"recovered-never\" and not (",
  "why": "narrows the guarded branch so the real verdict is unguarded",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "audit-tenant-invert",
  "file": "audit_empty_rows.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "        if _av != \"ok\":",
  "replace": "        if not (_av != \"ok\"):",
  "why": "an unreadable page becomes evidence; the Sunday audit SEARCHES for boards, so this is the highest-risk tool for it",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "audit-secondchance-remove",
  "file": "audit_empty_rows.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "        if _av != \"ok\":",
  "replace": "        if False and _av != \"ok\":",
  "why": "the audit's refusal branch removed entirely",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "repair-page-invert",
  "file": "repair_dead_urls.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "            names_us = _gate.page_names_company(name, u, html=html) is True",
  "replace": "            names_us = _gate.page_names_company(name, u, html=html) is not False",
  "why": "`is not False` admits None, so a page nobody could read is treated as ours -- SupPlant onto Workable's own front door",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "repair-accept-widen",
  "file": "repair_dead_urls.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "            if (v == \"match\" and whole_name) or names_us:",
  "replace": "            if True:",
  "why": "accepts any candidate the resolver produced",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "repair-narrow",
  "file": "repair_dead_urls.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "            if (v == \"match\" and whole_name) or names_us:",
  "replace": "            if (v == \"match\") or names_us:",
  "why": "drops the whole-name requirement: DiA Imaging Analytics strips to `dia` and matches dia.mil",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-activate-remove",
  "file": "bd_rescue.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "                if v and v[0] and not (_gate.activation_ok(name, api, v[0], html=html, token=tok)\n                                       and _gate.embedded_board_ok(name, tok, api)):",
  "replace": "                if v and v[0] and not (True\n                                       and _gate.embedded_board_ok(name, tok, api)):",
  "why": "bd_rescue's state before this refactor: it activated Bancor onto The Bancorp Bank's board",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-activate-invert",
  "file": "bd_rescue.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "                if v and v[0] and not (_gate.activation_ok(name, api, v[0], html=html, token=tok)\n                                       and _gate.embedded_board_ok(name, tok, api)):",
  "replace": "                if v and v[0] and not (_gate.activation_ok(name, api, v[0], html=html, token=tok) is None\n                                       and _gate.embedded_board_ok(name, tok, api)):",
  "why": "truthiness inverted with the token intact",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-activate-narrow",
  "file": "bd_rescue.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "                if v and v[0] and not (_gate.activation_ok(name, api, v[0], html=html, token=tok)\n                                       and _gate.embedded_board_ok(name, tok, api)):",
  "replace": "                if v and v[0] < 0 and not (_gate.activation_ok(name, api, v[0], html=html, token=tok)\n                                       and _gate.embedded_board_ok(name, tok, api)):",
  "why": "narrows the guard to an impossible job count",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wayback-activate-remove",
  "file": "wayback_rescue.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "        if r and not (_gate.activation_ok(name, r[2], r[3], html=r[5], token=r[1])\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "replace": "        if r and not (True\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "why": "resurrects an archived board with no identity check -- its state before this refactor",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wayback-activate-invert",
  "file": "wayback_rescue.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "        if r and not (_gate.activation_ok(name, r[2], r[3], html=r[5], token=r[1])\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "replace": "        if r and not (_gate.activation_ok(name, r[2], r[3], html=r[5], token=r[1]) is None\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "why": "truthiness inverted with the token intact",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wayback-activate-narrow",
  "file": "wayback_rescue.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "        if r and not (_gate.activation_ok(name, r[2], r[3], html=r[5], token=r[1])\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "replace": "        if r and r[4] < 0 and not (_gate.activation_ok(name, r[2], r[3], html=r[5], token=r[1])\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "why": "narrows the guard to an impossible job count",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-remove",
  "file": "validate_empty.py",
  "symbol": "check",
  "class": "M1-gate-removal",
  "find": "            if il > 0 and (_av != \"ok\" or not _gate.embedded_board_ok(name, tok, api)):",
  "replace": "            if il > 0 and (False or not _gate.embedded_board_ok(name, tok, api)):",
  "why": "promotes whatever board the page embeds -- its state before this refactor",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-invert",
  "file": "validate_empty.py",
  "symbol": "check",
  "class": "M2-gate-inversion",
  "find": "            if il > 0 and (_av != \"ok\" or not _gate.embedded_board_ok(name, tok, api)):",
  "replace": "            if il > 0 and (_av == \"ok\" or not _gate.embedded_board_ok(name, tok, api)):",
  "why": "truthiness inverted with the token intact",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-narrow",
  "file": "validate_empty.py",
  "symbol": "check",
  "class": "M3-branch-narrowing",
  "find": "            if il > 0 and (_av != \"ok\" or not _gate.embedded_board_ok(name, tok, api)):",
  "replace": "            if il < 0 and (_av != \"ok\" or not _gate.embedded_board_ok(name, tok, api)):",
  "why": "narrows the guard to an impossible Israel-job count",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-ats-remove",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M1-gate-removal",
  "find": "        if not _gate.activation_ok(nm, api, n_all, token=tok):",
  "replace": "        if False:",
  "why": "returns an active row straight from resolve()'s payload -- its state before this refactor",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-ats-invert",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M2-gate-inversion",
  "find": "        if not _gate.activation_ok(nm, api, n_all, token=tok):",
  "replace": "        if _gate.activation_ok(nm, api, n_all, token=tok) is None:",
  "why": "truthiness inverted with the token intact",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-ats-narrow",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M3-branch-narrowing",
  "find": "        if not _gate.activation_ok(nm, api, n_all, token=tok):",
  "replace": "        if n_all < 0 and not _gate.activation_ok(nm, api, n_all, token=tok):",
  "why": "narrows the guard to an impossible job count",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-ats-remove",
  "file": "auto_expand.py",
  "symbol": "_row_for_ats",
  "class": "M1-gate-removal",
  "find": "    if not _gate.activation_ok(nm, api, n_all, html=html, token=tok):",
  "replace": "    if False:",
  "why": "auto_expand runs TWICE daily and APPENDS new rows; this was its state before the refactor",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-ats-invert",
  "file": "auto_expand.py",
  "symbol": "_row_for_ats",
  "class": "M2-gate-inversion",
  "find": "    if not _gate.activation_ok(nm, api, n_all, html=html, token=tok):",
  "replace": "    if _gate.activation_ok(nm, api, n_all, html=html, token=tok) is None:",
  "why": "truthiness inverted with the token intact",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-ats-narrow",
  "file": "auto_expand.py",
  "symbol": "_row_for_ats",
  "class": "M3-branch-narrowing",
  "find": "    if not _gate.activation_ok(nm, api, n_all, html=html, token=tok):",
  "replace": "    if n_all < 0 and not _gate.activation_ok(nm, api, n_all, html=html, token=tok):",
  "why": "narrows the guard to an impossible job count",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "activation-njobs-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "activation_verdict",
  "class": "M4-dead-conjunct",
  "find": "    if not n_jobs:\n        return \"empty\"",
  "replace": "    if False:\n        return \"empty\"",
  "why": "a board that verifies with zero jobs is the empty-board shape, not a recovery",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "activation-listing-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "activation_verdict",
  "class": "M4-dead-conjunct",
  "find": "    if not looks_like_a_job_listing_page(api_url):\n        return \"not-listing\"\n    v = board_vouches(name, token, api_url)",
  "replace": "    v = board_vouches(name, token, api_url)",
  "why": "drops clause 3 of the activation rule; a nav menu scores like a board under SCRAPE_ASSUME_IL",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "walled-pool-note-only",
  "file": "pipeline/identity_gate.py",
  "symbol": "is_walled",
  "class": "M7-constant-drift",
  "find": "    return (\"unsupported ATS\" in (note or \"\")\n            or host_platform(row[3] if len(row) > 3 else \"\") is not None)",
  "replace": "    return \"unsupported ATS\" in (note or \"\")",
  "why": "the pool predicate becomes a string deep_validate owns and rewrites; one all-dark Saturday took it 25 -> 0 with every guard green",
  "must_be_killed_by_behavioural": false
 },
 {
  "id": "audit-ladder-serpapi-only",
  "file": "audit_empty_rows.py",
  "symbol": "serp",
  "class": "M6-fallback-removal",
  "find": "    urls = _serpapi(name, limit)",
  "replace": "    return _serpapi(name, limit)",
  "why": "SerpApi is exhausted until 2026-09-01, so a ladder with only that rung is a silent no-op that reports success",
  "must_be_killed_by_behavioural": false
 },
 {
  "id": "crack-oktowrite-callsite-invert",
  "file": "crack_walled.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "                        if _wv == \"ok\":",
  "replace": "                        if _wv is not None:",
  "why": "the novrfy branch persists fr[3]; `is not None` makes an unreadable page pass",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-found-narrow",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "            elif verdict == \"found\" and not _gate.identity_ok(name, url):",
  "replace": "            elif verdict == \"found-never\" and not _gate.identity_ok(name, url):",
  "why": "narrows the guarded branch so the real `found` verdict activates unguarded",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "apply-resolved-gate-drop",
  "file": "apply_resolved.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "                if _gate.is_foreign(name, api) or _gate.board_vouches(name, tok, api) is False:",
  "replace": "                if False:",
  "why": "apply_resolved runs at self-heal.yml 06:00 and re-points an ACTIVE row's api_url. Its write is a TUPLE target, which made it invisible to the derived enumeration until 2026-08-24.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "apply-resolved-gate-invert",
  "file": "apply_resolved.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "                if _gate.is_foreign(name, api) or _gate.board_vouches(name, tok, api) is False:",
  "replace": "                if _gate.is_foreign(name, api) or _gate.board_vouches(name, tok, api) is not False:",
  "why": "inverts the tenant veto: refuses the correct boards and admits the foreign ones",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "apply-resolved-gate-narrow",
  "file": "apply_resolved.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "                if _gate.is_foreign(name, api) or _gate.board_vouches(name, tok, api) is False:",
  "replace": "                if len(fields) >= 99 and (_gate.is_foreign(name, api) or _gate.board_vouches(name, tok, api) is False):",
  "why": "narrows the guard so no row is ever checked",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "page-strict-off",
  "file": "pipeline/identity_gate.py",
  "symbol": "page_names_company",
  "class": "M2-gate-inversion",
  "find": "    if page_mentions_company(name, html, strict=True):",
  "replace": "    if page_mentions_company(name, html, strict=False):",
  "why": "strict=True is the whole Bancor/Bancorp lesson: without it the name's words may appear scattered anywhere on the page.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "identity-foreign-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "identity_ok",
  "class": "M1-gate-removal",
  "find": "    if is_foreign(name, url):\n        return False\n    if host and ATS_HOST.search(host):",
  "replace": "    if False:\n        return False\n    if host and ATS_HOST.search(host):",
  "why": "On an ordinary careers domain -- the shape listing_hunt actually finds -- is_foreign is the ONLY gate identity_ok applies. Removing it re-opens FairFly onto fireflyspace.com.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "oktowrite-listing-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "write_verdict",
  "class": "M4-dead-conjunct",
  "find": "    if is_foreign(name, url) or not looks_like_a_job_listing_page(url):\n        return \"not-ours\" if is_foreign(name, url) else \"unreadable\"",
  "replace": "    if is_foreign(name, url):\n        return \"not-ours\"",
  "why": "activation_ok's copy of clause 3 has a mutation; ok_to_write's identical clause had none. Dropping it lets crack_walled persist an About/nav page as the row's api_url.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "identity-jobvite-open",
  "file": "pipeline/company_identity.py",
  "symbol": "ATS_HOST",
  "class": "M7-constant-drift",
  "find": "    r\"zohorecruit|myworkdaysite|paylocity|dayforcehcm|ripplingats|jobvite|taleo\\.net|\"",
  "replace": "    r\"zohorecruit|myworkdaysite|paylocity|dayforcehcm|ripplingats|\"",
  "why": "dropping jobvite/taleo from ATS_HOST re-opens docs/BACKLOG.md 42: is_foreign compares the company against the ATS VENDOR's domain again and refuses Varonis' and Radware's own boards -- the URLs crack_walled.listing_urls() builds itself. Re-aimed from the deleted identity_ok special branch when the closure landed. Re-anchored 2026-09-18 when `adamtotal` was appended to the pattern (the line the record held stopped being the last one).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "tenant-mismatch-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "tenant_is_this_company",
  "class": "M1-gate-removal",
  "find": "    if _verdict(name, url) == \"mismatch\":\n        return False",
  "replace": "    if False:\n        return False",
  "why": "the scoped mismatch veto is the sole refuser on Sight Sciences, Sight Diagnostics and Kubiya (38 -> 35 refused rows on the real registry).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "namestop-neutered",
  "file": "pipeline/identity_gate.py",
  "symbol": "page_names_company",
  "class": "M1-gate-removal",
  "find": "                    if w.lower() not in _NAME_STOP)",
  "replace": "                    if w.lower() not in set())",
  "why": "neuters the _NAME_STOP retry, so every row whose name carries a suffix the page omits is silently refused. R2 showed the only killer was an inspect.getsource assertion -- this record is what makes must_be_killed_by_behavioural do work.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-scrape-remove",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M1-gate-removal",
  "find": "        if not _gate.activation_ok(name, good_url, len(jobs2)):",
  "replace": "        if False:",
  "why": "the scrape exit of retry_unreachable's row builder. Its ats sibling three lines above had M1/M2/M3 and this had none, because coverage was counted per FILE."
 },
 {
  "id": "retry-scrape-invert",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M2-gate-inversion",
  "find": "        if not _gate.activation_ok(name, good_url, len(jobs2)):",
  "replace": "        if _gate.activation_ok(name, good_url, len(jobs2)):",
  "why": "deleting one token inverts the gate BOTH ways: activates Voiceitt, parks Pliops. 253 tests stayed green."
 },
 {
  "id": "retry-scrape-narrow",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M3-branch-narrowing",
  "find": "        if not _gate.activation_ok(name, good_url, len(jobs2)):",
  "replace": "        if len(jobs2) < 0 and not _gate.activation_ok(name, good_url, len(jobs2)):",
  "why": "narrows the scrape guard to an impossible job count"
 },
 {
  "id": "expand-scrape-remove",
  "file": "auto_expand.py",
  "symbol": "_row_for_scrape",
  "class": "M1-gate-removal",
  "find": "    if not _gate.activation_ok(name, good_url, len(jobs2)):",
  "replace": "    if False:",
  "why": "auto_expand runs 08:00 and 20:00 and APPENDS rows; the scrape branch had no mutation."
 },
 {
  "id": "expand-scrape-invert",
  "file": "auto_expand.py",
  "symbol": "_row_for_scrape",
  "class": "M2-gate-inversion",
  "find": "    if not _gate.activation_ok(name, good_url, len(jobs2)):",
  "replace": "    if _gate.activation_ok(name, good_url, len(jobs2)):",
  "why": "inverts the scrape gate in both directions"
 },
 {
  "id": "expand-scrape-narrow",
  "file": "auto_expand.py",
  "symbol": "_row_for_scrape",
  "class": "M3-branch-narrowing",
  "find": "    if not _gate.activation_ok(name, good_url, len(jobs2)):",
  "replace": "    if len(jobs2) < 0 and not _gate.activation_ok(name, good_url, len(jobs2)):",
  "why": "narrows the scrape guard to an impossible job count"
 },
 {
  "id": "crack-novrfy-remove",
  "file": "crack_walled.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "                        if _wv == \"ok\":",
  "replace": "                        if True:",
  "why": "the novrfy branch persists fr[3] without activating; listing_hunt's fast path activates on that address the next night."
 },
 {
  "id": "crack-novrfy-narrow",
  "file": "crack_walled.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "                        if _wv == \"ok\":",
  "replace": "                        if n_il < 0 and _wv == \"ok\":",
  "why": "narrows the novrfy guard so the address is never persisted (silent coverage loss direction)"
 },
 {
  "id": "crack-activate-invert",
  "file": "crack_walled.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "                    if verdict.startswith(\"cracked\") and _wv != \"ok\":",
  "replace": "                    if verdict.startswith(\"cracked\") and _wv is None:",
  "why": "ok_to_write returns a bool, so `is None` is always False -- the refusal branch never runs and every cracked verdict activates."
 },
 {
  "id": "hunt-persist-invert",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "                        if not _gate.identity_ok(name, url):",
  "replace": "                        if _gate.identity_ok(name, url) is None:",
  "why": "identity_ok returns a bool; `is None` is always False, so the nolisting branch persists every candidate address."
 },
 {
  "id": "hunt-persist-narrow",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "                        if not _gate.identity_ok(name, url):",
  "replace": "                        if n_il < 0 and not _gate.identity_ok(name, url):",
  "why": "narrows the persist guard to an impossible Israel-job count"
 },
 {
  "id": "tenant-filler-neutered",
  "file": "pipeline/identity_gate.py",
  "symbol": "tenant_is_this_company",
  "class": "M1-gate-removal",
  "find": "                         if w.lower() not in _NAME_FILLER))",
  "replace": "                         if w.lower() not in set()))",
  "why": "the filler-stripped core is the ONLY target a subdomain tenant matches for a name carrying a geographic or industry word. Neutering it flips 22 rows from admit to refuse (Qualcomm Israel, WINT Water Intelligence, Zebra Technologies), and on machine endpoints that refusal is final.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hostplatform-alias-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "host_platform",
  "class": "M7-constant-drift",
  "find": "    return _PLATFORM_ALIAS.get(m.group(1).lower(), m.group(1).lower())",
  "replace": "    return m.group(1).lower()",
  "why": "crack_walled._platform_of uses host_platform as the durable fallback when the note token has been erased (24 of 25 pool rows). Returning the raw host makes _HOST_PATTERNS miss, so crack_one skips the row silently.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "page-unlocker-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "page_names_company",
  "class": "M6-fallback-removal",
  "find": "    if (len(html) < 2000 and os.environ.get(\"BRIGHTDATA_API_KEY\")\n            and _UNLOCK_SPENT < _UNLOCK_BUDGET):",
  "replace": "    if (False and len(html) < 2000 and os.environ.get(\"BRIGHTDATA_API_KEY\")\n            and _UNLOCK_SPENT < _UNLOCK_BUDGET):",
  "why": "deletes the unlocker rung: a bot-walled page reads as no-evidence instead of being retried through the residential fetch. Re-aimed when the rung gained its paid-call budget (wave 6).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-refusal-token-drop",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M7-constant-drift",
  "find": "                    _note(note, \"retry: another company's board; no listing found\")]",
  "replace": "                    _note(note, \"retry: another company's board\")]",
  "why": "drops the hand-off token from the refusal note. The token-free version orphaned 9 named rows out of every pool at once, and 11 orphans block the digest commit via check_invariants bad().",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-refusal-token-drop",
  "file": "auto_expand.py",
  "symbol": "_row_for_ats",
  "class": "M7-constant-drift",
  "find": "                \"auto-expand: another company's board; no listing found\"]",
  "replace": "                \"auto-expand: another company's board\"]",
  "why": "same token rule for the sibling: a refusal that matches no pool orphans the appended row.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-ats-seed-leak",
  "file": "auto_expand.py",
  "symbol": "_row_for_ats",
  "class": "M7-constant-drift",
  "find": "        return [nm, \"scrape\", seed_url, seed_url, \"false\",\n                \"auto-expand: another company's board; no listing found\"]",
  "replace": "        return [nm, \"scrape\", api, api, \"false\",\n                \"auto-expand: another company's board; no listing found\"]",
  "why": "re-opens docs/BACKLOG.md 54: persisting the REFUSED board puts a foreign host into the row's address, and identity_gate.is_walled derives crack_walled's pool membership from that host.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-ats-transpose",
  "file": "auto_expand.py",
  "symbol": "_row_for_ats",
  "class": "M8-argument-transposition",
  "find": "if not _gate.activation_ok(nm, api, n_all, html=html, token=tok):",
  "replace": "if not _gate.activation_ok(api, nm, n_all, token=tok):",
  "why": "wave-3 R2: while activation_ok carried a dead `platform` parameter, transposing (name, platform) at four call sites left 258 tests green -- the identity decision silently ran on the platform string. The parameter is gone; the transposition that remains puts a URL in the name slot, every predicate fails, and the positive-control row goes false.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "page-readability-floor-drift",
  "file": "pipeline/identity_gate.py",
  "symbol": "page_names_company",
  "class": "M7-constant-drift",
  "find": "    if len(html) < 2000:\n        return None",
  "replace": "    if len(html) < 200:\n        return None",
  "why": "the floor is WHY undecidable pages are three-valued: under 2000 chars nothing on the page is evidence. Drifted to 200, a JS challenge shell 'answers' -- and it answers False, so the tenant clause that legitimately admits the 66 Workday machine endpoints is never reached. The census pin's cell 3 (900-char shell, affirmative tenant) goes red.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "unlocker-trigger-floor-drift",
  "file": "pipeline/identity_gate.py",
  "symbol": "page_names_company",
  "class": "M7-constant-drift",
  "find": "    if (len(html) < 2000 and os.environ.get(\"BRIGHTDATA_API_KEY\")\n            and _UNLOCK_SPENT < _UNLOCK_BUDGET):",
  "replace": "    if (len(html) < 200 and os.environ.get(\"BRIGHTDATA_API_KEY\")\n            and _UNLOCK_SPENT < _UNLOCK_BUDGET):",
  "why": "same constant, other consumer: drifted to 200 the unlocker retry fires only for near-empty pages, and every bot wall rendering a 200-2000 char challenge shell (the Bit class the rung exists for) silently reads as None -- a refusal at every caller. Killed by the unlocker-rung test now that its walled page is ~520 chars.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-embed-vouch-drop",
  "file": "validate_empty.py",
  "symbol": "check",
  "class": "M1-gate-removal",
  "find": "            if il > 0 and (_av != \"ok\" or not _gate.embedded_board_ok(name, tok, api)):",
  "replace": "            if il > 0 and _av != \"ok\":",
  "why": "drops the board-vouches-for-itself clause: a page naming THIS company then admits whatever board it embeds -- the wave-4 B1 (Cogniteam promoted onto Riskified's greenhouse off Cogniteam's own page).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-embed-vouch-drop",
  "file": "bd_rescue.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "                if v and v[0] and not (_gate.activation_ok(name, api, v[0], html=html, token=tok)\n                                       and _gate.embedded_board_ok(name, tok, api)):",
  "replace": "                if v and v[0] and not _gate.activation_ok(name, api, v[0], html=html, token=tok):",
  "why": "same drop at the 02:30 sibling: the unlocker page vouches for a board it merely embeds.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "embed-near-widen",
  "file": "pipeline/identity_gate.py",
  "symbol": "_tenant_near",
  "class": "M3-branch-narrowing",
  "find": "    return any(f and (f == t if min(len(f), len(t)) < 3\n                      else abs(len(f) - len(t)) <= 1 and (f in t or t in f))\n               for f in forms for t in targets)",
  "replace": "    return any(f and (f == t if min(len(f), len(t)) < 3\n                      else abs(len(f) - len(t)) <= 99 and (f in t or t in f))\n               for f in forms for t in targets)",
  "why": "widens near-equality to plain containment: `lili` is a substring of `elililly` and that promotion is a recorded incident. Re-aimed onto the short-form-equality expression (wave 6).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-ats-njobs-const",
  "file": "auto_expand.py",
  "symbol": "_row_for_ats",
  "class": "M8-argument-mis-passing",
  "find": "    if not _gate.activation_ok(nm, api, n_all, html=html, token=tok):",
  "replace": "    if not _gate.activation_ok(nm, api, 1, token=tok):",
  "why": "wave-4 R2: the count argument replaced with a truthy literal -- the gate's zero-jobs clause is intact in source and inert at this call site; a 0/0 board activates.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-scrape-njobs-const",
  "file": "auto_expand.py",
  "symbol": "_row_for_scrape",
  "class": "M8-argument-mis-passing",
  "find": "    if not _gate.activation_ok(name, good_url, len(jobs2)):",
  "replace": "    if not _gate.activation_ok(name, good_url, 1):",
  "why": "wave-4 R2: the count argument replaced with a truthy literal -- the gate's zero-jobs clause is intact in source and inert at this call site; a 0/0 board activates.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-ats-njobs-const",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M8-argument-mis-passing",
  "find": "        if not _gate.activation_ok(nm, api, n_all, token=tok):",
  "replace": "        if not _gate.activation_ok(nm, api, 1, token=tok):",
  "why": "wave-4 R2: the count argument replaced with a truthy literal -- the gate's zero-jobs clause is intact in source and inert at this call site; a 0/0 board activates.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-scrape-njobs-const",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M8-argument-mis-passing",
  "find": "        if not _gate.activation_ok(name, good_url, len(jobs2)):",
  "replace": "        if not _gate.activation_ok(name, good_url, 1):",
  "why": "wave-4 R2: the count argument replaced with a truthy literal -- the gate's zero-jobs clause is intact in source and inert at this call site; a 0/0 board activates.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wayback-njobs-const",
  "file": "wayback_rescue.py",
  "symbol": "main",
  "class": "M8-argument-mis-passing",
  "find": "        if r and not (_gate.activation_ok(name, r[2], r[3], html=r[5], token=r[1])\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "replace": "        if r and not (_gate.activation_ok(name, r[2], 1, html=r[5], token=r[1])\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "why": "wave-4 R2: the count argument replaced with a truthy literal -- the gate's zero-jobs clause is intact in source and inert at this call site; a 0/0 board activates.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-transpose",
  "file": "validate_empty.py",
  "symbol": "check",
  "class": "M8-argument-transposition",
  "find": "            _av = _gate.activation_verdict(name, api, il, html=html, token=tok) if il > 0 else \"empty\"",
  "replace": "            _av = _gate.activation_verdict(api, name, il, html=html, token=tok) if il > 0 else \"empty\"",
  "why": "wave-4 R2: name<->api_url transposed at a call site whose n_jobs slot is pre-guarded (a truthy-constant mutation is an equivalent mutant here); a URL in the name slot fails every predicate and the positive control goes red.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-activate-transpose",
  "file": "bd_rescue.py",
  "symbol": "main",
  "class": "M8-argument-transposition",
  "find": "                if v and v[0] and not (_gate.activation_ok(name, api, v[0], html=html, token=tok)",
  "replace": "                if v and v[0] and not (_gate.activation_ok(api, name, v[0], html=html, token=tok)",
  "why": "wave-4 R2: name<->api_url transposed at a call site whose n_jobs slot is pre-guarded (a truthy-constant mutation is an equivalent mutant here); a URL in the name slot fails every predicate and the positive control goes red.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "gate-page-transpose-html",
  "file": "pipeline/identity_gate.py",
  "symbol": "activation_verdict",
  "class": "M8-argument-transposition",
  "find": "        p = page_names_company(name, api_url, html=html)",
  "replace": "        p = page_names_company(api_url, name, html=html)",
  "why": "wave-4 R2: (name, api_url) transposed at the held-page call inside the gate itself. Every fixture's stub table keys on the first argument, so the stub missed, answered None, and the tenant clause supplied the expected verdict -- green, while every readable-page acceptance silently died.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "gate-page-transpose-tail",
  "file": "pipeline/identity_gate.py",
  "symbol": "activation_verdict",
  "class": "M8-argument-transposition",
  "find": "    p = page_names_company(name, human)",
  "replace": "    p = page_names_company(human, name)",
  "why": "wave-4 R2: same transposition at the fetch tail -- a Request built from a company name raises, html is empty, the answer is None, and the subdomain-mismatch population is refused wholesale.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-html-wrong-binding",
  "file": "bd_rescue.py",
  "symbol": "main",
  "class": "M8-argument-mis-passing",
  "find": "                if v and v[0] and not (_gate.activation_ok(name, api, v[0], html=html, token=tok)\n                                       and _gate.embedded_board_ok(name, tok, api)):",
  "replace": "                if v and v[0] and not (_gate.activation_ok(name, api, v[0], html=best_html, token=tok)\n                                       and _gate.embedded_board_ok(name, tok, api)):",
  "why": "wave-4 R2: the gate handed the LONGEST page seen across alts instead of the page the candidate was extracted from -- accept or refuse at random relative to the evidence. Survived while the fixture's single-alt constant-page population made the html argument informationless.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-pool-receiver-token-drop",
  "file": "listing_hunt.py",
  "symbol": "HUNT_POOL",
  "class": "M7-constant-drift",
  "find": "    r\"aggregator URL|no listing found|redirects to|scanned via brightdata|\"",
  "replace": "    r\"aggregator URL|redirects to|scanned via brightdata|\"",
  "why": "wave-4 R3: the receiver side of the wave-3 hand-off. Dropping this alternative emptied the hunt pool of 27 named rows (InnovoPro, LawGeex, FairFly, ...) with pytest, check_invariants and registry_health all green -- the senders were guarded, the receiver was not, and both mirrors were retyped copies.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-scrape-seed-leak",
  "file": "auto_expand.py",
  "symbol": "_row_for_scrape",
  "class": "M7-constant-drift",
  "find": "        return [name, \"scrape\", seed_url, seed_url, \"false\",\n                \"scraped page is not this company's; no listing found\"]",
  "replace": "        return [name, \"scrape\", good_url, good_url, \"false\",\n                \"scraped page is not this company's; no listing found\"]",
  "why": "wave-4 R1 (filed): the third builder kept the leak items 54 closed for the other two -- a refused ATS-hosted `good_url` in cols 2-3 joins crack_walled's pool via is_walled's host derivation.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-floor-down",
  "file": "validate_empty.py",
  "symbol": "check",
  "class": "M7-constant-drift",
  "find": "    if len(html) < 500:",
  "replace": "    if len(html) < 50:",
  "why": "wave-4 R2 (filed): the floor was killed only when drifted UP. Down at 50, a JS challenge shell 'answers' the textual scan and every walled row in the Sunday pool becomes a permanent false suspect.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wayback-embed-vouch-drop",
  "file": "wayback_rescue.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "        if r and not (_gate.activation_ok(name, r[2], r[3], html=r[5], token=r[1])\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "replace": "        if r and not (_gate.activation_ok(name, r[2], r[3], html=r[5], token=r[1])):",
  "why": "wave-5 R1: drops the board-vouches-for-itself clause at the third extract_ats caller; an archived page's foreign embed resurrects (Panoply onto Riskified's board).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wayback-html-drop",
  "file": "wayback_rescue.py",
  "symbol": "main",
  "class": "M8-argument-mis-passing",
  "find": "        if r and not (_gate.activation_ok(name, r[2], r[3], html=r[5], token=r[1])\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "replace": "        if r and not (_gate.activation_ok(name, r[2], r[3], html=\"\", token=r[1])\n                      and _gate.embedded_board_ok(name, r[1], r[2])):",
  "why": "wave-5 R1: drops the snapshot page from the gate -- the tenant clause (vacuous on path platforms) then decides, and a hijacked-domain snapshot with a name-matching embed resurrects.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "tenant-short-exact-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "_tenant_near",
  "class": "M3-branch-narrowing",
  "find": "    return any(f and (f == t if min(len(f), len(t)) < 3",
  "replace": "    return any(f and (f == t if min(len(f), len(t)) < 1",
  "why": "wave-6 R3: disables the short-form equality rule; the digit-stripped Comeet uid `f` is contained in `f5` and `hp` admits `hpe`. Also defangs the len>=2 target floor mutant R1 filed -- with equality required, a 1-char target admits only its exact token.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "embed-words-anchor-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "_EMBED_TOKEN_WORDS",
  "class": "M7-constant-drift",
  "find": "    r\"networks|network|city|medical|global|digital)$\")",
  "replace": "    r\"networks|network|city|medical|global|digital)\")",
  "why": "wave-6 R1 (filed): without the tail anchor, `techstars` strips its `tech` PREFIX to `stars` and matches the registry name `Stars` -- mid-token stripping manufactures identities.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "unlock-budget-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "page_names_company",
  "class": "M7-constant-drift",
  "find": "    if (len(html) < 2000 and os.environ.get(\"BRIGHTDATA_API_KEY\")\n            and _UNLOCK_SPENT < _UNLOCK_BUDGET):",
  "replace": "    if (len(html) < 2000 and os.environ.get(\"BRIGHTDATA_API_KEY\")\n            and True):",
  "why": "wave-6 R3: removes the paid-call budget from the unlocker rung -- BACKLOG 36's uncapped spend, armed the day the key landed on two Sunday cron steps.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "verdicts-terminal-drop-redundant",
  "file": "pipeline/verdicts.py",
  "symbol": "TERMINAL",
  "class": "M7-constant-drift",
  "find": "TERMINAL = (\"defunct\", \"domain-dead\", \"duplicate of\", \"redundant\", \"recruiter\", \"alias-of\")",
  "replace": "TERMINAL = (\"defunct\", \"domain-dead\", \"duplicate of\", \"recruiter\", \"alias-of\")",
  "why": "wave-6 R2: the unified list's ADDED tokens were unguarded; dropping `redundant` re-opens Marvell Israel (a deactivated scrape twin) into crack_walled's activating pool, publishing its roles under two rows.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "triage-target-match",
  "file": "triage_dark.py",
  "symbol": "in_triage_pool",
  "class": "M3-branch-narrowing",
  "find": "            and bool(TARGET_NOTES.search(r[5] or \"\"))",
  "replace": "            and bool(TARGET_NOTES.match(r[5] or \"\"))",
  "why": "wave-6 R2: same edit, 18:00 triage: 18 -> 0 selected, suite green.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "triage-skip-drop",
  "file": "triage_dark.py",
  "symbol": "in_triage_pool",
  "class": "M1-gate-removal",
  "find": "            and not SKIP_NOTES.search(r[5] or \"\"))",
  "replace": "            and True)",
  "why": "wave-6 R2 (filed): deleting the terminal exclusion admits 23 domain-dead/defunct rows to a notes writer whose 220-char trim eventually drops the terminal token itself.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "crack-pool-terminal-drop",
  "file": "crack_walled.py",
  "symbol": "in_crack_pool",
  "class": "M1-gate-removal",
  "find": "            and not is_terminal_row(r))",
  "replace": "            and not is_recruiter(r[0]))",
  "why": "wave-6 R2: the crack pool ACTIVATES; without the terminal exclusion the redundant twin and the alias rows re-enter it.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "tenant-short-exact-tighten",
  "file": "pipeline/identity_gate.py",
  "symbol": "_tenant_near",
  "class": "M3-branch-narrowing",
  "find": "    return any(f and (f == t if min(len(f), len(t)) < 3",
  "replace": "    return any(f and (f == t if min(len(f), len(t)) <= 3",
  "why": "wave-7 confirmation: the tightening direction of the short-form boundary -- at <= 3 Orbs' own 3-char ashby tenant refuses against its 4-char name, the one real row on the boundary.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "unlock-spent-increment-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "page_names_company",
  "class": "M7-constant-drift",
  "find": "            _UNLOCK_SPENT += 1",
  "replace": "            _UNLOCK_SPENT += 0",
  "why": "wave-7 confirmation: the counter IS the cap; with the increment gone the budget is inert and the Sunday spend is uncapped again.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "invariants-terminal-narrow",
  "file": "check_invariants.py",
  "symbol": "TERMINAL",
  "class": "M7-constant-drift",
  "find": "TERMINAL = _VERDICTS_TERM_RX.pattern",
  "replace": "TERMINAL = r\"defunct|domain-dead|alias-of\"",
  "why": "wave-7 confirmation: reverting the blocking gate's terminal set to the old private narrow copy was suite-green; it gates nine workflow commits.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "merge-inplace-revert",
  "file": "merge_csv_rows.py",
  "symbol": "merge",
  "class": "M7-constant-drift",
  "find": "    write_csv_rows(target_path, target)",
  "replace": "    open(target_path, \"w\", encoding=\"utf-8\", newline=\"\").write(\"\r\n\".join(\",\".join(r) for r in target) + \"\r\n\")",
  "why": "wave-7 confirmation: reverting the atomic write to an in-place truncation was suite-green; a runner kill mid-merge then commits a stump behind || true on nine recovery paths.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-pool-terminal-drop",
  "file": "listing_hunt.py",
  "symbol": "in_hunt_pool",
  "class": "M1-gate-removal",
  "find": "            and not is_terminal_row(r)   # terminal token OR an agency name",
  "replace": "            and not is_recruiter(r[0])   # agencies are never activated",
  "why": "the hunt ACTIVATES; without the terminal exclusion a deactivated redundant twin and every alias-of row re-enter it and get re-hunted onto a board already scanned. listing_hunt was the last tool spelling its own 3-token terminal list.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "facts-domain-drop",
  "file": "pipeline/company_identity.py",
  "symbol": "verdict",
  "class": "M1-gate-removal",
  "find": "    for parent in identity_facts.domains(cname):",
  "replace": "    for parent in ():",
  "why": "neuters the declared brand/parent domains (the old KNOWN_PARENT): UserWay on levelaccess.com becomes foreign again. That table had zero direct tests before this record.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "facts-tenant-veto-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "tenant_is_this_company",
  "class": "M1-gate-removal",
  "find": "    declared = identity_facts.tenants(name)\n    if declared:\n        if not labels:\n            return True                        # nothing checkable: unchanged\n        return any(identity_facts.normalize(l) in declared for l in labels)\n",
  "replace": "    declared = identity_facts.tenants(name)\n    if False:\n        if not labels:\n            return True                        # nothing checkable: unchanged\n        return any(identity_facts.normalize(l) in declared for l in labels)\n",
  "why": "removes the declared-tenant hook: a declaration can no longer override the string verdict (Itamar-shape stays refused) nor refuse a foreign tenant on a declared row.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "facts-tenant-scope-widen",
  "file": "pipeline/identity_gate.py",
  "symbol": "tenant_is_this_company",
  "class": "M3-branch-narrowing",
  "find": "    declared = identity_facts.tenants(name)\n    if declared:\n        if not labels:\n            return True                        # nothing checkable: unchanged\n        return any(identity_facts.normalize(l) in declared for l in labels)\n",
  "replace": "    declared = identity_facts.tenants(name)\n    if declared:\n        labels = list(_slug_candidates(urllib.parse.urlparse(url)))\n        if not labels:\n            return True                        # nothing checkable: unchanged\n        return any(identity_facts.normalize(l) in declared for l in labels)\n",
  "why": "matches the declared tenant against `_slug_candidates` -- host labels AND path segments in one list -- so `riskified` in the PATH of novartis.wd3 admits Novartis's Workday: the recorded incident, re-opened through the declarations table.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "facts-embed-token-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "embedded_board_ok",
  "class": "M1-gate-removal",
  "find": "    declared = identity_facts.tenants(name)\n    if declared:\n        return identity_facts.normalize((token or \"\").split(\"/\")[0]) in declared\n    return any(_tenant_near(c, targets) for c in _embed_token_forms(token))",
  "replace": "    declared = identity_facts.tenants(name)\n    if False:\n        return identity_facts.normalize((token or \"\").split(\"/\")[0]) in declared\n    return any(_tenant_near(c, targets) for c in _embed_token_forms(token))",
  "why": "removes the declared-token hook on path-tenant platforms: a declared row is judged by the heuristics again, so a foreign slug the near-match happens to like is admitted despite the declaration saying otherwise.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "roles-winner-drop-holder",
  "file": "pipeline/roles.py",
  "symbol": "_winner",
  "class": "M1-gate-removal",
  "find": "                    0 if _store.merge_key(j) in held else 1,",
  "replace": "                    0,",
  "why": "the claim winner's stability rule deleted with every roles test green: in every fixture the holder also carried the url's word or the shorter identity. Without it a two-claimant posting flips company names (and re-emails) on any tie-break edit. Found by the wave-1 mutation attacker, 2026-08-25.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "roles-mail-line-removed",
  "file": "pipeline/digest.py",
  "symbol": "build_markdown",
  "class": "M1-gate-removal",
  "find": "    if s.get(\"roles\"):\n        audit.append(\"- **Roles:** \" + \"; \".join(_md_line(x) for x in s[\"roles\"]))",
  "replace": "    if False:\n        audit.append(\"- **Roles:** \" + \"; \".join(_md_line(x) for x in s[\"roles\"]))",
  "why": "the whole role-record line could vanish from the daily email with the suite green: only tests/rehearse_roles.py read the markdown, and no workflow runs it. The SCHEDULING.md failure class: the mail stops saying what the run did and nothing notices. (re-aimed 2026-08-25 by registry: render commit 1458314 changed the line to _md_line(x); the record had 0 matches at HEAD.)",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "roles-record-run-looked-nowhere",
  "file": "pipeline/run.py",
  "symbol": "run",
  "class": "M8-argument-mis-passing",
  "find": "        scanned_ok={r[\"company_name\"] for r in rows}, failed=failed_names, paths=paths,",
  "replace": "        scanned_ok=set(), failed=failed_names, paths=paths,",
  "why": "the call site telling record_run where the run looked: with an empty set nothing ever closes on a scoped run and the ledger keeps dead roles forever, with pytest green until the end-to-end fixture test existed.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-note-base-drop",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M7-constant-drift",
  "find": "    return [name, \"scrape\", url, url, \"false\",\n            _keep_selectors(name, url, note, _fold_empty(note, today) + [plain])]",
  "replace": "    return [name, \"scrape\", url, url, \"false\", \"unreachable; could not scan\"]",
  "why": "the 2026-08-25 defect textually: a still-unreachable night rebuilt the cell from a literal and erased listing-hunt's and bd_rescue's segments on 9 rows nightly (b3d1d49). Re-anchored 2026-08-31 (b) onto the line the guard refactor moved it to.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-empt-keeps-unreachable",
  "file": "bd_rescue.py",
  "symbol": "main",
  "class": "M8-argument-mis-passing",
  "find": "        note = _note_replace(_reached_note(rows[rowi][5]), \"scanned via brightdata\",",
  "replace": "        note = _note_replace(rows[rowi][5], \"scanned via brightdata\",",
  "why": "the validated branch appended after the `unreachable` token instead of removing it, so retry_unreachable re-selected the row 90 s later and rewrote the cell (2026-08-25).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-activate-cell-overwrite",
  "file": "bd_rescue.py",
  "symbol": "main",
  "class": "M7-constant-drift",
  "find": "                                  _note_replace(_reached_note(rows[rowi][5]),\n                                                \"brightdata-rescued\",\n                                                f\"brightdata-rescued; {n_all}/{il} IL\")]",
  "replace": "                                  f\"brightdata-rescued; {n_all}/{il} IL\"]",
  "why": "the activation branch rebuilt the notes cell from an f-string (rule 3) while its own non-activating branches used replace_own.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wayback-activate-cell-overwrite",
  "file": "wayback_rescue.py",
  "symbol": "main",
  "class": "M7-constant-drift",
  "find": "                          _note_replace(_note_replace(rows[rowi][5], \"unreachable\", \"\"),\n                                        \"wayback-rescued\",\n                                        f\"wayback-rescued; {n_all}/{il} IL\")]",
  "replace": "                          f\"wayback-rescued; {n_all}/{il} IL\"]",
  "why": "same shape as bd-activate-cell-overwrite on the Sunday rescue.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-promote-overwrite",
  "file": "validate_empty.py",
  "symbol": "main",
  "class": "M7-constant-drift",
  "find": "            payload[5] = _note_replace(rows[rowi][5] if len(rows[rowi]) > 5 else \"\",\n                                       \"cross-validated\", payload[5])\n            rows[rowi] = payload",
  "replace": "            rows[rowi] = payload",
  "why": "the promote write replaced the whole row, erasing every other tool's segment on a Sunday activation.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-agg-seed-resolves",
  "file": "auto_expand.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "        if agg_seed:\n            # never rendered: the page is a posting on someone else's board (see module doc)",
  "replace": "        if False:\n            # never rendered: the page is a posting on someone else's board (see module doc)",
  "why": "the 2026-08-25 waste textually: every aggregator seed rendered for 17-25 s before the cap check, 76 wasted minutes a run.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-agg-parks-empty",
  "file": "auto_expand.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "                elif agg_seed:\n                    defer = \"llm-none\" if _llm.LAST[\"asked\"] else \"no-candidates\"",
  "replace": "                elif False:\n                    defer = \"llm-none\" if _llm.LAST[\"asked\"] else \"no-candidates\"",
  "why": "an aggregator seed the tier could not crack falls through to the `unreachable` row with the shell as its address -- the burying BACKLOG 177 measured at 28 rows.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-rotation-drop",
  "file": "auto_expand.py",
  "symbol": "main",
  "class": "M7-constant-drift",
  "find": "    todo.sort(key=lambda e: seen.get((e.get(\"name\") or \"\").strip(), \"\"))",
  "replace": "    pass",
  "why": "a budget without a rotation key re-walks the same prefix forever (ARCHITECTURE section 2).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-dupe-guard-drop",
  "file": "auto_expand.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "            if name.lower() in _names_now():      # re-read before the append (rule 4)",
  "replace": "            if False:      # re-read before the append (rule 4)",
  "why": "rule 4: `have` was a start-of-run snapshot; a concurrent writer's row got a twin.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-clear-token-drop",
  "file": "auto_expand.py",
  "symbol": "clear_agg_urls",
  "class": "M7-constant-drift",
  "find": "        r[5] = _note_append(note, f\"url-cleared {_today()}: {host} aggregator seed\")",
  "replace": "        r[5] = _note_append(note, f\"cleared {_today()}: {host} aggregator seed\")",
  "why": "a token no pool knows orphans the un-buried row (the verdict-string rule).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "llm-evidence-free-asks",
  "file": "resolve_llm.py",
  "symbol": "resolve_llm",
  "class": "M1-gate-removal",
  "find": "        if n_pages == 0:\n            # No page was read",
  "replace": "        if False:\n            # No page was read",
  "why": "20 evidence-free `claude -p` calls a day, 0/50 resolved (BACKLOG 177).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "llm-bd-cap-default",
  "file": "resolve_llm.py",
  "symbol": "_search_candidates",
  "class": "M7-constant-drift",
  "find": "    cap = int(os.environ.get(\"LLM_BD_SEARCH_CAP\", \"5\"))",
  "replace": "    cap = int(os.environ.get(\"LLM_BD_SEARCH_CAP\", \"150\"))",
  "why": "the paid rung's default is the operator's 4,500/month ceiling made concrete; 150 is deep_validate's per-process figure and would let one run spend 150 credits.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "extract-gap-terminal-drop",
  "file": "repair_extract_gap.py",
  "symbol": "in_extract_gap_pool",
  "class": "M5-population-widening",
  "find": "            and not is_terminal_row(r))",
  "replace": "            and True)",
  "why": "an activating pool without a terminal exclusion re-opens an alias-of row off its own extract-gap stamp (GenCell Energy, 2026-08-25).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "llm-resolver-model-drift",
  "file": "resolve_llm.py",
  "symbol": "_ask_claude",
  "class": "M7-constant-drift",
  "find": "                             model=os.environ.get(\"LLM_RESOLVE_MODEL\", \"sonnet\"),",
  "replace": "                             model=os.environ.get(\"LLM_RESOLVE_MODEL\", \"opus\"),",
  "why": "the resolver's model is a budget decision (the bare call ran the default, ~10x the cost per call); a drift back is silent in every log.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-terminal-drop",
  "file": "validate_empty.py",
  "symbol": "in_validate_empty_pool",
  "class": "M5-population-widening",
  "find": "    return (in_probe_pool(r)                 # parked, http, not aggregator/junk/terminal/agency",
  "replace": "    from pipeline.aggregators import is_aggregator as _agg\n    from pipeline.firmographics import looks_like_junk as _junk\n    return ((len(r) >= 6 and r[4] == \"false\" and (r[3] or \"\").startswith(\"http\")\n             and not _agg(r[3]) and not _junk(r[0]))",
  "why": "the Sunday cross-validation activates; without the terminal exclusion it re-activated three rows parked alias-of/redundant on 2026-08-25 (wave-1 pools attacker).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-tried-anchor",
  "file": "bd_rescue.py",
  "symbol": "main",
  "class": "M7-constant-drift",
  "find": "            mm = re.search(r\"bd-tried \\d{4}-\\d{2}-\\d{2} x(\\d+)\", note)",
  "replace": "            mm = re.search(r\"x(\\d+)$\", note)",
  "why": "a $-anchored counter never sees x3 once retry_unreachable appends after it: paid unlocks forever (wave-1 F4).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "llm-own-page-drop",
  "file": "resolve_llm.py",
  "symbol": "_verify",
  "class": "M1-gate-removal",
  "find": "    if not _own_page_names_token(name, token, api_url, pages, platform):",
  "replace": "    if False:",
  "why": "with search-sourced evidence, _slug_matches alone admitted Similarweb -> similartech and any Comeet uid (wave-1 write-path attacker).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "llm-embed-vouch-drop",
  "file": "resolve_llm.py",
  "symbol": "_verify",
  "class": "M1-gate-removal",
  "find": "    if platform != \"comeet\" and not _gate.embedded_board_ok(name, token, api_url):",
  "replace": "    if False:",
  "why": "a held own page can refuse a board it merely embeds (Cogniteam's page + a stale riskified embed).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-search-cap-drift",
  "file": "auto_expand.py",
  "symbol": "main",
  "class": "M7-constant-drift",
  "find": "    search_budget = int(os.environ.get(\"AUTO_EXPAND_SEARCH_CAP\", \"40\"))",
  "replace": "    search_budget = int(os.environ.get(\"AUTO_EXPAND_SEARCH_CAP\", \"400\"))",
  "why": "the search cap paces the queue and bounds DDG + unlock spend per run; it was unpinned (wave-1 F6).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "llm-calls-uncounted",
  "file": "auto_expand.py",
  "symbol": "main",
  "class": "M8-argument-mis-passing",
  "find": "                llm_budget -= _llm.LAST[\"calls\"]   # charge CALLS (retries included), not attempts",
  "replace": "                llm_budget -= 1   # charge CALLS (retries included), not attempts",
  "why": "resolve_llm retries once; charging one per attempt lets the retry double the spend the cap describes (wave-1 F6 near-miss).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-error-inverted",
  "file": "refresh_scrape_cache.py",
  "symbol": "_apply_result",
  "class": "M2-gate-inversion",
  "find": "    jobs = None if res[\"status\"] == \"error\" else res[\"jobs\"]   # ERROR != confirmed-empty",
  "replace": "    jobs = None if res[\"status\"] != \"error\" else res[\"jobs\"]   # ERROR != confirmed-empty",
  "why": "error and empty swapped: a 403 night reads as confirmed-empty and deletes a company's jobs (the 2026-08-24 scraper defect).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-carry-on-empty",
  "file": "refresh_scrape_cache.py",
  "symbol": "_apply_result",
  "class": "M5-population-widening",
  "find": "    jobs = None if res[\"status\"] == \"error\" else res[\"jobs\"]   # ERROR != confirmed-empty",
  "replace": "    jobs = None if res[\"status\"] == \"error\" or not res[\"jobs\"] else res[\"jobs\"]   # ERROR != confirmed-empty",
  "why": "an honest empty must not carry yesterday's jobs forward as if the page had broken.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-massfail-threshold",
  "file": "refresh_scrape_cache.py",
  "symbol": "MASS_FAILURE_PCT",
  "class": "M7-constant-drift",
  "find": "MASS_FAILURE_PCT = 20",
  "replace": "MASS_FAILURE_PCT = 80",
  "why": "a mass-failure night at 21-79% errors would commit its deletions silently.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-park-outside-error",
  "file": "refresh_scrape_cache.py",
  "symbol": "_apply_result",
  "class": "M5-population-widening",
  "find": "        days, _ = _rot_bump(rot, name, \"empty\", today, res)",
  "replace": "        days, _n = _rot_bump(rot, name, \"empty\", today, res)\n        if _n >= ROT_PARK_DAYS:\n            st.parked.append((name, f\"empty {days}d\"))",
  "why": "empty is not broken: only ERRORS park a row (ARCHITECTURE section 2).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-since-not-reset",
  "file": "refresh_scrape_cache.py",
  "symbol": "_rot_bump",
  "class": "M3-branch-narrowing",
  "find": "    if e is None or e.get(\"why\") != why or (shape and e.get(\"shape\") not in (None, shape)):",
  "replace": "    if e is None or (shape and e.get(\"shape\") not in (None, shape)):",
  "why": "flipping empty->error must start a NEW streak, or 60 honest empty days park a company on its first transient error.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "audit-deep-rung-drop",
  "file": "audit_empty_rows.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "    _deep_rung([n for n, _ in still] + [n for n, _ in unsupported], apply,",
  "replace": "    _deep_rung([], apply,",
  "why": "the Chromium rung IS the retired Saturday cron; dropping its input silently stops rendering every dark row (BACKLOG 6).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "audit-seen-path-drift",
  "file": "audit_empty_rows.py",
  "symbol": "AUDIT_SEEN",
  "class": "M7-constant-drift",
  "find": "AUDIT_SEEN = os.path.join(\"cloud_state\", \"audit_seen.json\")   # {name: last-audited date}",
  "replace": "AUDIT_SEEN = os.path.join(\"state\", \"audit_seen.json\")   # {name: last-audited date}",
  "why": "a gitignored rotation key is a stable sort on a constant in Actions: file order every Sunday (BACKLOG 38/164).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-suspect-drop",
  "file": "listing_hunt.py",
  "symbol": "actionable_mode",
  "class": "M1-gate-removal",
  "find": "    if ms and (not last or ms.group(1) > last):\n        return True",
  "replace": "    if False:\n        return True",
  "why": "a suspect verdict newer than the hunt's last one must re-arm the hunt (BACKLOG 65).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "apply-resolved-parked-scope",
  "file": "apply_resolved.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "                if _gate.is_foreign(name, api) or _gate.board_vouches(name, tok, api) is False:",
  "replace": "                if fields[4] == \"true\" and (_gate.is_foreign(name, api) or _gate.board_vouches(name, tok, api) is False):",
  "why": "the veto scoped to active rows re-pointed parked rows at foreign boards (BACKLOG 56).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-policy-retry",
  "file": "bd_rescue.py",
  "symbol": "skip_row",
  "class": "M1-gate-removal",
  "find": "    return bool(\"bd-policy\" in note\n                or (m2 and int(m2.group(2)) >= 3)\n                or not _QS.row_due(qstate, name, RUNG, CADENCE_DAYS, url=url))",
  "replace": "    return bool((m2 and int(m2.group(2)) >= 3)\n                or not _QS.row_due(qstate, name, RUNG, CADENCE_DAYS, url=url))",
  "why": "a policy-closed host retried nightly spends a credit for the same answer (BACKLOG 110).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "bd-error-code-drop",
  "file": "bd_rescue.py",
  "symbol": "unlock_status",
  "class": "M1-gate-removal",
  "find": "            err = r.headers.get(\"x-brd-error-code\") or \"\"       # 200 with a failure inside",
  "replace": "            err = \"\"       # 200 with a failure inside",
  "why": "the unlocker answers 200 with the failure in a header; ignoring it reads a wall as a page (BACKLOG 110).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "expand-corrupt-cache-write",
  "file": "auto_expand.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "        if _CACHE_OK[\"readable\"]:\n            with open(\"scraped_cache.json\", \"w\", encoding=\"utf-8\") as f:",
  "replace": "        if True:\n            with open(\"scraped_cache.json\", \"w\", encoding=\"utf-8\") as f:",
  "why": "writing {} over an unreadable cache deletes every company's cards (BACKLOG 156).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-corrupt-cache-write",
  "file": "retry_unreachable.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "        if cache_ok:\n            with open(\"scraped_cache.json\", \"w\", encoding=\"utf-8\") as f:",
  "replace": "        if True:\n            with open(\"scraped_cache.json\", \"w\", encoding=\"utf-8\") as f:",
  "why": "same rule in the 02:30 chain (BACKLOG 156).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "crack-eightfold-api-drop",
  "file": "crack_walled.py",
  "symbol": "listing_urls",
  "class": "M6-fallback-removal",
  "find": "        api = ([(\"eightfold\", f\"https://{host}/api/pcsx/search?domain={dom}\")] if dom",
  "replace": "        api = ([] if dom",
  "why": "a cracked Eightfold tenant must become an API row, not a nightly browser render (BACKLOG 77).",
  "must_be_killed_by_behavioural": false
 },
 {
  "id": "expand-slug-seed-drop",
  "file": "auto_expand.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "        if (agg_seed and e.get(\"slug\") and search_budget > 0\n                and os.environ.get(\"AUTO_EXPAND_SLUG_SEED\", \"0\") == \"1\"):",
  "replace": "        if False:",
  "why": "the slug is the one non-aggregator seed intake produces; ignoring it leaves 399 names on the LLM tier alone (BACKLOG 178).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "recruiter-boundary-remove",
  "file": "pipeline/verdicts.py",
  "symbol": "TERM_RX",
  "class": "M3-branch-narrowing",
  "find": "TERM_RX = re.compile(\"|\".join(r\"\\b\" + t + r\"\\b\" if t == \"recruiter\" else re.escape(t)",
  "replace": "TERM_RX = re.compile(\"|\".join(re.escape(t)",
  "why": "`SmartRecruiters` in a note made five rows terminal by substring, two of them parked in no pool (BACKLOG 72).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "terminal-row-name-remove",
  "file": "pipeline/verdicts.py",
  "symbol": "is_terminal_row",
  "class": "M1-gate-removal",
  "find": "    return is_terminal(r[5] if len(r) > 5 else \"\") or is_recruiter(r[0] if r else \"\")",
  "replace": "    return is_terminal(r[5] if len(r) > 5 else \"\")",
  "why": "agency-hood is decided by the NAME; without it nine agency-named parked rows count as coverage owed.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "terminal-keep-remove",
  "file": "pipeline/notes.py",
  "symbol": "append",
  "class": "M1-gate-removal",
  "find": "        victims = [i for i, p in enumerate(parts) if not protected(p)]",
  "replace": "        victims = list(range(len(parts)))",
  "why": "eviction must never take the alias-of/defunct segment that keeps a row out of every activating pool (19 rows at risk).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "merge-keep-remove",
  "file": "merge_csv_rows.py",
  "symbol": "_merge_notes",
  "class": "M1-gate-removal",
  "find": "        victims = [i for i in range(len(out) - 1, -1, -1) if not has_terminal(out[i])]",
  "replace": "        victims = [len(out) - 1]",
  "why": "the conflict path must honour the same protection or a conflict day undoes it.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "probe-pool-http-remove",
  "file": "probe_candidates.py",
  "symbol": "in_probe_pool",
  "class": "M1-gate-removal",
  "find": "            and (r[3] or \"\").startswith(\"http\")\n            and not is_aggregator(r[3])",
  "replace": "            and True\n            and not is_aggregator(r[3])",
  "why": "a url-cleared row has nothing to probe.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "probe-pool-aggregator-remove",
  "file": "probe_candidates.py",
  "symbol": "in_probe_pool",
  "class": "M1-gate-removal",
  "find": "            and not is_aggregator(r[3])\n            and not looks_like_junk(r[0])",
  "replace": "            and True\n            and not looks_like_junk(r[0])",
  "why": "a posting on someone else's board is not a candidate page.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "probe-pool-terminal-remove",
  "file": "probe_candidates.py",
  "symbol": "in_probe_pool",
  "class": "M1-gate-removal",
  "find": "            and not looks_like_junk(r[0])\n            and not is_terminal_row(r))",
  "replace": "            and not looks_like_junk(r[0])\n            and True)",
  "why": "an alias-of twin with an http address must not be probed and woken.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-pool-terminal-remove",
  "file": "retry_unreachable.py",
  "symbol": "in_retry_pool",
  "class": "M1-gate-removal",
  "find": "    if not (len(r) >= 6 and r[4] == \"false\" and url.startswith(\"http\")\n            and not is_terminal_row(r)):",
  "replace": "    if not (len(r) >= 6 and r[4] == \"false\" and url.startswith(\"http\")\n            and True):",
  "why": "the chain must never re-attempt a terminal row (BACKLOG 190).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-pool-narrow",
  "file": "retry_unreachable.py",
  "symbol": "in_retry_pool",
  "class": "M3-branch-narrowing",
  "find": "    if not _URL_DEAD.search(note):\n        return False",
  "replace": "    if True:\n        return False",
  "why": "BACKLOG 320: the `unreachable` token is written by ANOTHER tool -- it arrives inside `dark-triage <date>: url-dead (unreachable (dns/conn))` and triage rewrites its own segment nightly, so a pool standing on the word alone loses the row (`--policy worst` night 10, `mixed --seed 4` night 1). The durable stamp must still select.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-walled-remove",
  "file": "validate_empty.py",
  "symbol": "in_validate_empty_pool",
  "class": "M1-gate-removal",
  "find": "            and not _g.is_walled(r)          # crack_walled owns walled hosts",
  "replace": "            and True          # crack_walled owns walled hosts",
  "why": "a myworkdayjobs row is crack_walled's; cross-validating it over plain HTTP re-creates the wall verdicts.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "protect-unsupported-drop",
  "file": "pipeline/notes.py",
  "symbol": "_protected",
  "class": "M1-gate-removal",
  "find": "    return bool(_terminal_rx().search(seg) or _PROTECTED_EXTRA.search(seg))",
  "replace": "    return bool(_terminal_rx().search(seg))",
  "why": "the crack pool's membership fact lives in deep_validate's segment; evicting it emptied the pool 14 -> 9 in one rehearsed night (BACKLOG 27).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "probe-ever-drop",
  "file": "probe_candidates.py",
  "symbol": "main",
  "class": "M8-argument-mis-passing",
  "find": "        cur[\"ever\"] = (bool(_p.get(\"ever\")) or int(_p.get(\"il\") or 0) > 0 or int(_p.get(\"sig\") or 0) > 0\n                       or cur[\"il\"] > 0 or cur[\"sig\"] > 0)   # a pre-`ever` baseline counts too",
  "replace": "        cur[\"ever\"] = (cur[\"il\"] > 0 or cur[\"sig\"] > 0)   # a pre-`ever` baseline counts too",
  "why": "`ever` is set once and never cleared; recomputing it from tonight alone erased 64 validate_empty rows in one rehearsed quiet night.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "append-slice-restore",
  "file": "pipeline/notes.py",
  "symbol": "append",
  "class": "M1-gate-removal",
  "find": "        return SEP.join(parts)           # (a base already over the cap is left as it was: no slice)",
  "replace": "        return SEP.join(parts + [seg])[:cap]",
  "why": "wave-1 (2026-08-26): slicing the joined cell cut a protected segment mid-word or left `crack-walled <date>: ` dangling; check F then blocked the digest (mixed seed 1, night 5).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "append-fact-evict",
  "file": "pipeline/notes.py",
  "symbol": "append",
  "class": "M1-gate-removal",
  "find": "        victims = [i for i, p in enumerate(parts) if not protected(p)]",
  "replace": "        victims = ([i for i, p in enumerate(parts) if not protected(p)]\n                   or [i for i, p in enumerate(parts) if not _terminal_rx().search(p)])",
  "why": "letting a protected non-terminal fact yield evicted `no open Israel roles` on 12 real rows (validate_empty's selector); a protected segment is never evicted.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wake-strips-triage",
  "file": "probe_candidates.py",
  "symbol": "_wake_note",
  "class": "M3-branch-narrowing",
  "find": "_STALE_SEGMENT = re.compile(r\"^(listing-hunt|crack-walled|probe-woken)\\b\")",
  "replace": "_STALE_SEGMENT = re.compile(r\"^(listing-hunt|crack-walled|probe-woken|dark-triage)\\b\")",
  "why": "the wake deleted `dark-triage <date>: <mode>` -- 39/39 extract-gap rows and 115/228 triage rows left their pool on the first wake (wave-1, 2026-08-26).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wake-undated",
  "file": "probe_candidates.py",
  "symbol": "WAKE_STAMP",
  "class": "M8-argument-mis-passing",
  "find": "WAKE_STAMP = f\"probe-woken {TODAY}: re-hunt pending\"",
  "replace": "WAKE_STAMP = \"probe-woken: re-hunt pending\"",
  "why": "an undated wake cannot be compared with the triage stamp; the hunt's page-empty rule needs the date to know which is newer.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-keeps-wake",
  "file": "listing_hunt.py",
  "symbol": "_consume_wake",
  "class": "M1-gate-removal",
  "find": "    return SEP.join(p for p in split(note) if not p.lower().startswith(\"probe-woken\"))",
  "replace": "    return note",
  "why": "a `probe-woken` segment nothing removes is permanent: it skipped 6 rows in triage forever and stacks on every wake.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "page-empty-ignores-wake",
  "file": "listing_hunt.py",
  "symbol": "_triaged_page_empty",
  "class": "M1-gate-removal",
  "find": "    if w and (not w.group(1) or w.group(1) >= m.group(1)):\n        return False\n    return True",
  "replace": "    return True",
  "why": "the wake is the probe's whole purpose; a page-empty stamp that ignores it swallows every wake (105/105 in the original incident).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "triage-woken-exclusion-restore",
  "file": "triage_dark.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "               and (force or _needs_triage(r[5] or \"\"))]",
  "replace": "               and \"probe-woken\" not in (r[5] or \"\")\n               and (force or _needs_triage(r[5] or \"\"))]",
  "why": "the exclusion was permanent (nothing cleared the stamp): a woken row left triage's schedule forever while in_triage_pool still counted it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_registry.py::test_triage_does_not_consume_a_probe_wake_before_the_hunt_can_use_it"
  ]
 },
 {
  "id": "growth-line-drop",
  "file": "registry_health.py",
  "symbol": "pool_growth",
  "class": "M4-threshold-drift",
  "find": "        if before is not None and before >= 8 and now >= before * 1.5:",
  "replace": "        if before is not None and before >= 8 and now >= before * 15:",
  "why": "127 -> 228 re-baselined silently; two of the pools that grow activate rows.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "deep-key-rename",
  "file": "registry_health.py",
  "symbol": "pools",
  "class": "M8-argument-mis-passing",
  "find": "        \"deep_validate (Sun 04:00, the audit's Chromium rung)\":",
  "replace": "        \"deep_validate rung (Sun 04:00)\":",
  "why": "pool_floor keys on the label's head; renaming it skips that pool's floor until the next census.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "append-newest-first",
  "file": "pipeline/notes.py",
  "symbol": "append",
  "class": "M3-branch-narrowing",
  "find": "        parts.pop(victims[0])            # oldest UNPROTECTED first",
  "replace": "        parts.pop(victims[-1])           # oldest UNPROTECTED first",
  "why": "evicting the NEWEST unprotected segment inverts the module's one rule (attacker 2, B3).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "protected-mode-empty",
  "file": "pipeline/notes.py",
  "symbol": "_PROTECTED_EXTRA",
  "class": "M3-branch-narrowing",
  "find": "dark-triage \\d{4}-\\d{2}-\\d{2}: [a-z-]+|no open israel roles|",
  "replace": "dark-triage \\d{4}-\\d{2}-\\d{2}: [a-z-]*|no open israel roles|",
  "why": "a dangling `dark-triage <date>:` must stay evictable, or the corruption checks F/F2 exist for becomes permanent.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "harness-retention-off",
  "file": "tests/rehearse_registry.py",
  "symbol": "rehearse",
  "class": "M1-gate-removal",
  "find": "        if policy == \"worst\":\n",
  "replace": "        if policy == \"worst_\":\n",
  "why": "turns off the batch's headline evidence while `rehearsal OK` still prints (attacker 2, B3).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_registry.py::test_the_rehearsal_catches_a_cell_overwrite_and_bans_dns"
  ]
 },
 {
  "id": "merge-trim-ours-head",
  "file": "merge_csv_rows.py",
  "symbol": "_merge_notes",
  "class": "M3-branch-narrowing",
  "find": "        victims = [i for i in range(len(out) - 1, -1, -1) if not has_terminal(out[i])] or [len(out) - 1]",
  "replace": "        victims = [i for i in range(len(out)) if not has_terminal(out[i])] or [len(out) - 1]",
  "why": "the merge must trim the THEIRS tail (stale duplicates), never ours' head -- the lost update the comment says it prevents.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "merge-slice-restore",
  "file": "merge_csv_rows.py",
  "symbol": "_merge_notes",
  "class": "M1-gate-removal",
  "find": "        victims = [i for i in range(len(out) - 1, -1, -1) if not has_terminal(out[i])] or [len(out) - 1]\n        out.pop(victims[0])\n    return \" | \".join(out)",
  "replace": "        victims = [i for i in range(len(out) - 1, -1, -1) if not has_terminal(out[i])]\n        if not victims:\n            break\n        out.pop(victims[0])\n    return \" | \".join(out)[:cap]",
  "why": "the conflict path sliced a protected segment mid-word (`page-empt`) or left `dark-triage <date>: ` dangling, blocking the digest (attacker 2, R2).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "fact-floor-drift",
  "file": "check_invariants.py",
  "symbol": "main",
  "class": "M4-threshold-drift",
  "find": "    if fact_n < 50:",
  "replace": "    if fact_n < 5:",
  "why": "the fact-pool floor 10x weaker with every gate green (attacker 2, B3).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-arm-and",
  "file": "validate_empty.py",
  "symbol": "in_validate_empty_pool",
  "class": "M3-branch-narrowing",
  "find": "    empty_class = (\"no open israel roles\" in n or \"cross-validated\" in n or \"empty-but-suspect\" in n)",
  "replace": "    empty_class = (\"no open israel roles\" in n or \"cross-validated\" in n and \"empty-but-suspect\" in n)",
  "why": "narrows the token arm to rows carrying BOTH of validate_empty's own stamps -- none do (attacker 2, B3).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "protect-suspect-drop",
  "file": "pipeline/notes.py",
  "symbol": "_PROTECTED_EXTRA",
  "class": "M3-branch-narrowing",
  "find": "r\"empty-but-suspect|cross-validated|probe-woken\", _re.I)   # dated or not: older rows carry it bare",
  "replace": "r\"cross-validated|probe-woken\", _re.I)   # dated or not: older rows carry it bare",
  "why": "the Sunday deep stamp evicted Enzymit's `empty-but-suspect` and the row left the one pool that could re-check it (attacker 2, R4).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "harness-dns-open",
  "file": "tests/rehearse_registry.py",
  "symbol": "_forbid_sockets",
  "class": "M1-gate-removal",
  "find": "    socket.gethostbyname = boom           # type: ignore[assignment]\n",
  "replace": "",
  "why": "repair_dead_urls decides 'dead host' by DNS; an unbanned resolver makes the rehearsal depend on the network (attacker 2, R6).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_registry.py::test_the_rehearsal_catches_a_cell_overwrite_and_bans_dns"
  ]
 },
 {
  "id": "facts-not-tenants-drop",
  "file": "pipeline/identity_facts.py",
  "symbol": "not_tenants",
  "class": "M1-gate-removal",
  "find": "    return frozenset(_norm(t) for t in facts(name).get(\"not_tenants\", ()) if _norm(t))",
  "replace": "    return frozenset()",
  "why": "the negative declarations are the only thing refusing a path-tenant board without a page.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "facts-not-tenants-validate",
  "file": "pipeline/identity_facts.py",
  "symbol": "validate",
  "class": "M3-branch-narrowing",
  "find": "        if neg and row is not None and row[4] == \"true\":",
  "replace": "        if neg and row is not None and row[4] == \"maybe\":",
  "why": "declaring an incident must force the park; an active row on its own not_tenant is a contradiction the suite must see.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "vouch-neg-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "board_vouches",
  "class": "M1-gate-removal",
  "find": "    if (ntok and ntok in neg) or (label_tokens & neg):\n        return False",
  "replace": "    if False:\n        return False",
  "why": "a declared not_tenant must refuse before any string test.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "vouch-uid-true",
  "file": "pipeline/identity_gate.py",
  "symbol": "board_vouches",
  "class": "M3-branch-narrowing",
  "find": "    if not tok or _UID.fullmatch(tok):\n        return None                            # opaque: a uid vouches for nothing",
  "replace": "    if not tok or _UID.fullmatch(tok):\n        return True                            # opaque: a uid vouches for nothing",
  "why": "a Comeet uid carries no identity; True here is the old vacuity (BACKLOG 61).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "vouch-none-false",
  "file": "pipeline/identity_gate.py",
  "symbol": "board_vouches",
  "class": "M3-branch-narrowing",
  "find": "    if targets and any(_tenant_near(c, targets) for c in _embed_token_forms(tok)):\n        return True\n    return None",
  "replace": "    if targets and any(_tenant_near(c, targets) for c in _embed_token_forms(tok)):\n        return True\n    return False",
  "why": "a slug that merely fails near-equality is `cannot tell`, not refused: a veto here refused 81 of 460 active rows (BACKLOG 21).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "vouch-near-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "board_vouches",
  "class": "M1-gate-removal",
  "find": "    if targets and any(_tenant_near(c, targets) for c in _embed_token_forms(tok)):\n        return True\n    return None",
  "replace": "    return None",
  "why": "a near-equal tenant must vouch without a page, or every path-platform activation pays a GET.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "tenant-neg-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "tenant_is_this_company",
  "class": "M1-gate-removal",
  "find": "    if any(identity_facts.normalize(l) in identity_facts.not_tenants(name) for l in labels):\n        return False                           # a PROVEN other company's tenant",
  "replace": "    if False:\n        return False                           # a PROVEN other company's tenant",
  "why": "the subdomain gate must honour a negative declaration too (Bancor/bancorpbank).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "embed-neg-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "embedded_board_ok",
  "class": "M1-gate-removal",
  "find": "    if identity_facts.normalize((token or \"\").split(\"/\")[0]) in identity_facts.not_tenants(name):\n        return False                           # a PROVEN other company's board",
  "replace": "    if False:\n        return False                           # a PROVEN other company's board",
  "why": "the embed vouch must honour a negative declaration (Cogniteam/riskified).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "human-url-api-fallback",
  "file": "pipeline/identity_gate.py",
  "symbol": "human_board_url",
  "class": "M1-gate-removal",
  "find": "    (re.compile(r\"^https?://boards-api\\.greenhouse\\.io/v1/boards/([^/?#]+)\", re.I),\n     r\"https://job-boards.greenhouse.io/\\1\"),\n",
  "replace": "",
  "why": "without the human-page map the gate reads the greenhouse API endpoint (0 bytes) and every cannot-tell greenhouse row is deferred forever (BACKLOG 33).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "human-url-comeet-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "human_board_url",
  "class": "M1-gate-removal",
  "find": "    if _COMEET_API.match(u):\n        return _comeet_human_url(u)",
  "replace": "",
  "why": "Comeet is 121 active rows; without the learned human page every Comeet recovery is deferred forever.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "activation-unverified-admit",
  "file": "pipeline/identity_gate.py",
  "symbol": "activation_verdict",
  "class": "M3-branch-narrowing",
  "find": "    p = page_names_company(name, human)\n    if p is None:\n        return \"unverified\"",
  "replace": "    p = page_names_company(name, human)\n    if p is None:\n        return \"ok\"",
  "why": "an unreadable human page is no evidence; admitting it is the old vacuity with a GET on top.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "activation-human-page-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "activation_verdict",
  "class": "M8-argument-mis-passing",
  "find": "    human = human_board_url(api_url) or api_url",
  "replace": "    human = api_url",
  "why": "without the human-page read every cannot-tell row is deferred forever (coverage loss).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "activation-vouch-false-admit",
  "file": "pipeline/identity_gate.py",
  "symbol": "activation_verdict",
  "class": "M3-branch-narrowing",
  "find": "    v = board_vouches(name, token, api_url)\n    if v is False:\n        return \"not-ours\"                      # a declaration beats a page: Cogniteam's own",
  "replace": "    v = board_vouches(name, token, api_url)\n    if v is False:\n        return \"ok\"                            # a declaration beats a page: Cogniteam's own",
  "why": "a refused board must not activate.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "activation-unverified-stamp",
  "file": "deep_validate.py",
  "symbol": "apply_verdict",
  "class": "M8-argument-mis-passing",
  "find": "            }.get(_av, \"unverified (no readable page)\"))",
  "replace": "            }.get(_av, \"not this company's board\"))",
  "why": "an unverified board must not be stamped as proven foreign (BACKLOG 37).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "crack-unreadable-stamp",
  "file": "crack_walled.py",
  "symbol": "main",
  "class": "M8-argument-mis-passing",
  "find": "                            f\"crack-walled {TODAY}: \" + (\"not this company's board\" if _wv == \"not-ours\"\n                                                         else \"unverified (page unreadable)\"))",
  "replace": "                            f\"crack-walled {TODAY}: not this company's board\")",
  "why": "the primary refusal path must distinguish unreadable from proven foreign.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-unverified-stamp",
  "file": "validate_empty.py",
  "symbol": "check",
  "class": "M1-gate-removal",
  "find": "            if il > 0 and _av == \"unverified\":",
  "replace": "            if False:",
  "why": "an unverified board must not become a `suspect` note claiming it is not this company's.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "apply-resolved-vouch-drop",
  "file": "apply_resolved.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "                if _gate.is_foreign(name, api) or _gate.board_vouches(name, tok, api) is False:",
  "replace": "                if _gate.is_foreign(name, api):",
  "why": "the resolver's re-point must honour a negative declaration.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "c3b-warn-drop",
  "file": "check_invariants.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "        if unsure:\n            warn(f\"{len(unsure)} active path-platform rows whose tenant cannot vouch for the name \"",
  "replace": "        if False:\n            warn(f\"{len(unsure)} active path-platform rows whose tenant cannot vouch for the name \"",
  "why": "the hand-check list is the only visibility on active rows admitted by vacuity.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "triage-llm-schema-drop",
  "file": "triage_dark.py",
  "symbol": "llm_page_verdict",
  "class": "M8-argument-mis-passing",
  "find": "        d = llm.call_json(prompt, system=_SYSTEM, schema=_SCHEMA,",
  "replace": "        d = llm.call_json(prompt, system=_SYSTEM, schema={\"type\": \"object\"},",
  "why": "without its schema the page judge's answer is prose again (BACKLOG 117).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "triage-llm-unavailable-raise",
  "file": "triage_dark.py",
  "symbol": "llm_page_verdict",
  "class": "M1-gate-removal",
  "find": "    except llm.LLMUnavailable:\n        return None                       # no CLI / no auth / timeout: the regex verdict stands",
  "replace": "    except ValueError:\n        return None                       # no CLI / no auth / timeout: the regex verdict stands",
  "why": "an unavailable CLI must leave the regex verdict standing, not crash the 18:00 triage.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "audit-verdict-drop",
  "file": "audit_empty_rows.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "        _av = _gate.activation_verdict(name, api or \"\", n_all, token=tok or \"\")\n        if _av != \"ok\":",
  "replace": "        _av = _gate.activation_verdict(name, api or \"\", n_all, token=tok or \"\")\n        if _av == \"not-ours\":",
  "why": "an unverified board must not be written by the Sunday audit; only `ok` writes (confirmation wave R1).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "write-verdict-human-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "write_verdict",
  "class": "M8-argument-mis-passing",
  "find": "    target = url if html else (human_board_url(url) or url)",
  "replace": "    target = url",
  "why": "without the human-page redirect the write gate reads the API endpoint (0 bytes) and refuses every path-platform crack (confirmation wave R4).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "checkable-token-uid-drop",
  "file": "pipeline/identity_gate.py",
  "symbol": "checkable_token",
  "class": "M1-gate-removal",
  "find": "    m = _COMEET_API.match(api_url or \"\")\n    if m:\n        return m.group(1)                      # the uid IS the checkable thing (a negative can name it)",
  "replace": "",
  "why": "a declared negative uid (Sckipio/87.00C) must fire on the API form of the address (confirmation wave R7).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "vouch-nohost-true",
  "file": "pipeline/identity_gate.py",
  "symbol": "board_vouches",
  "class": "M3-branch-narrowing",
  "find": "    if not host:\n        return None                            # no address at all: nothing can vouch",
  "replace": "    if not host:\n        return True                            # no address at all: nothing can vouch",
  "why": "with no address the old code answered True and `_slug_matches` admitted every slug at its two-argument call sites (confirmation wave R1).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-deferred-drop",
  "file": "validate_empty.py",
  "symbol": "check",
  "class": "M8-argument-mis-passing",
  "find": "                return (\"deferred\", f\"{il} IL on {api[:50]} but nothing vouches for the board\")",
  "replace": "                return (\"confirmed\", None)",
  "why": "`confirmed` is counted and forgotten; a deferral must be its own verdict so the Sunday summary says how many (confirmation wave R2).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "validate-empty-suspect-guard-restore",
  "file": "validate_empty.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "            rows[rowi][5] = _new\n            _MODIFIED.add(name)",
  "replace": "            if \"no open israel roles\" in _new.lower():\n                rows[rowi][5] = _new\n                _MODIFIED.add(name)",
  "why": "the guard silently discarded the suspect finding on 67 of 93 signals-arm rows (BACKLOG 200); the token is protected now.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "triage-own-words-drop",
  "file": "triage_dark.py",
  "symbol": "_own_words",
  "class": "M1-gate-removal",
  "find": "    d = str(detail or \"\")\n    for tok, safe in _FOREIGN_TOKENS:",
  "replace": "    d = str(detail or \"\")\n    for tok, safe in ():",
  "why": "another tool's pool token inside triage's segment is rewritten away on the next re-triage (Bit, twice).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_registry.py::test_triage_never_writes_another_tools_pool_token_inside_its_own_segment"
  ]
 },
 {
  "id": "hunt-keep-address-ungated",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "            keep = q_url if (q_url and _gate.identity_ok(name, q_url)\n                             and not _ledger_refuses(name, q_url)) else \"\"",
  "replace": "            keep = q_url",
  "why": "refusing to ACTIVATE while still writing the ADDRESS only delays the mistake 24 hours -- this tool's fast path re-reads fr[3] the next night. QuantLR's best candidate was quantlab.com and FairFly's was fireflyspace.com.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-keep-address-invert",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "            keep = q_url if (q_url and _gate.identity_ok(name, q_url)\n                             and not _ledger_refuses(name, q_url)) else \"\"",
  "replace": "            keep = q_url if (q_url and not _gate.identity_ok(name, q_url)\n                             and not _ledger_refuses(name, q_url)) else \"\"",
  "why": "refusing to ACTIVATE while still writing the ADDRESS only delays the mistake 24 hours -- this tool's fast path re-reads fr[3] the next night. QuantLR's best candidate was quantlab.com (a US trading firm) and FairFly's was fireflyspace.com.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-keep-address-narrow",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "            keep = q_url if (q_url and _gate.identity_ok(name, q_url)\n                             and not _ledger_refuses(name, q_url)) else \"\"",
  "replace": "            keep = q_url if (q_url and _gate.identity_ok(name, q_url) is None\n                             and not _ledger_refuses(name, q_url)) else \"\"",
  "why": "refusing to ACTIVATE while still writing the ADDRESS only delays the mistake 24 hours -- this tool's fast path re-reads fr[3] the next night. QuantLR's best candidate was quantlab.com (a US trading firm) and FairFly's was fireflyspace.com.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-queue-identity-remove",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "            elif verdict == \"found\" and not _gate.identity_ok(name, q_url):",
  "replace": "            elif False:",
  "why": "the intake-queue arm ACTIVATES a row for a company that had none, off a URL a search engine returned -- the CyberArk->PANW / Lili->Eli Lilly shape. It carries its own copy of the gate because the AST completeness check does not follow calls.",
  "must_be_killed_by_behavioural": false,
  "equivalent_why": "EQUIVALENT MUTANT (BACKLOG 368, adjudicated by registry): the same predicate gates the address seven lines later (`keep = q_url if ... identity_ok`), so removing this branch changes no behaviour; the AST guard `test_every_registry_writer_consults_an_identity_predicate` is the RIGHT killer. Flag flipped by infra 2026-08-30 after the gate re-reported the adjudicated finding on every run since 08-28."
 },
 {
  "id": "hunt-queue-identity-invert",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M2-gate-inversion",
  "find": "            elif verdict == \"found\" and not _gate.identity_ok(name, q_url):",
  "replace": "            elif verdict == \"found\" and _gate.identity_ok(name, q_url) is None:",
  "why": "the intake-queue arm ACTIVATES a row for a company that had none, off a URL a search engine returned -- the CyberArk->PANW / Lili->Eli Lilly shape. It carries its own copy of the gate because the AST completeness check does not follow calls.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-queue-identity-narrow",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "            elif verdict == \"found\" and not _gate.identity_ok(name, q_url):",
  "replace": "            elif verdict == \"found-never\" and not _gate.identity_ok(name, q_url):",
  "why": "the intake-queue arm ACTIVATES a row for a company that had none, off a URL a search engine returned -- the CyberArk->PANW / Lili->Eli Lilly shape. It carries its own copy of the gate because the AST completeness check does not follow calls.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "apply-proposals-gate-remove",
  "file": "apply_proposals.py",
  "symbol": "_row_for",
  "class": "M1-gate-removal",
  "find": "    verdict = _gate.activation_verdict(name, api, n_il, html=held)",
  "replace": "    verdict = \"ok\"",
  "why": "the applier appends NEW ACTIVE rows from a proposal file walked hours earlier; with the gate gone every proposal with an Israel count activates, including another employer's board. This is the tool that writes ~100 rows in a batch.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "apply-proposals-gate-invert",
  "file": "apply_proposals.py",
  "symbol": "_row_for",
  "class": "M2-gate-inversion",
  "find": "    verdict = _gate.activation_verdict(name, api, n_il, html=held)",
  "replace": "    verdict = \"not-ours\" if _gate.activation_verdict(\n        name, api, n_il, html=held) != \"ok\" else \"empty\"",
  "why": "polarity flipped with the call intact: every board the gate admits is parked as someone else's and every board it refuses is kept. A test that only counts rows written passes this.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "apply-proposals-gate-narrow",
  "file": "apply_proposals.py",
  "symbol": "_row_for",
  "class": "M3-branch-narrowing",
  "find": "    verdict = _gate.activation_verdict(name, api, n_il, html=held)",
  "replace": "    verdict = _gate.activation_verdict(\n        name, api, n_il, html=held) if n_il < 0 else \"ok\"",
  "why": "narrows the gate to an impossible Israel-job count, so it is consulted on no real proposal while the call site still reads as present.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "zero-park-no-token",
  "file": "confirm_zero.py",
  "symbol": "_write",
  "class": "M1-gate-removal",
  "find": "        seg = (\"%s %s: %s; needs re-resolution\" % (MARKER, stamp, v[\"verdict\"]) if parks",
  "replace": "        seg = (\"%s %s: %s\" % (MARKER, stamp, v[\"verdict\"]) if parks",
  "why": "a `wrong-url` park that carries no verdicts.TOKENS token lands the row in NO re-check pool -- the zero audit parking a company into silence, which is the failure it exists to refuse.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "zero-park-when-skipped",
  "file": "confirm_zero.py",
  "symbol": "_write",
  "class": "M3-branch-narrowing",
  "find": "        if evicts and not parks:\n            skipped.append(r[0])",
  "replace": "        if evicts:\n            skipped.append(r[0])",
  "why": "a PROVEN park (a wrong-url verdict, the model naming another employer) must not be skipped because the row's note is saturated: skipping the note AND the park leaves a wrong address ACTIVE and re-audited nightly. RE-AIMED by infra 2026-08-30: the original record (dedenting the `if parks:` block out of the `else:`) was born equivalent in 4ed4cf8 -- with `and not parks` above it, the `else:` runs unconditionally whenever `parks`, so the nesting is inert and no test could kill it; it SURVIVED on every CI run since 08-29. This is the guard that line actually carries.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "zero-cond3-drop",
  "file": "confirm_zero.py",
  "symbol": "verdict_from",
  "class": "M1-gate-removal",
  "find": "    if not ev.get(\"cond3\"):\n        return ROUTING, False\n",
  "replace": "",
  "why": "condition (c) unenforced: a careers LANDING page that merely links to the board is judged as the board, and a model answering `states-none` about it manufactures a zero. `_JOBLINK` was dead code for the same reason.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "zero-cadence-drop",
  "file": "confirm_zero.py",
  "symbol": "in_zero_confirm_pool",
  "class": "M7-constant-drift",
  "find": "            and (FORCE or _ledger_stale(r[0], RECHECK_DAYS))",
  "replace": "            and True",
  "why": "without the cadence every run re-renders and re-judges every row already answered. It must read the LEDGER, not the note: the note write is best-effort, so a note-keyed cadence excluded nothing and the same 29 rows were re-audited in four consecutive batches.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "resolved-dup-board-drop",
  "file": "apply_resolved.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "                held_urls, held_boards = _boards_held(lines, name)\n                if _board_key(api) in held_urls or (\n                        plat and tok and (plat.lower(), tok.lower()) in held_boards):",
  "replace": "                held_urls, held_boards = _boards_held(lines, name)\n                if False:",
  "why": "the state that minted Unframe/Unframe AI on 2026-08-28: a re-resolve re-points an ACTIVE row onto a board another ACTIVE row already holds. check_invariants check B is blind (it counts NAMES, and by construction they differ) and roles._winner then publishes one row's roles under the other's name.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "resolved-dup-exact-only",
  "file": "apply_resolved.py",
  "symbol": "_board_key",
  "class": "M3-branch-narrowing",
  "find": "    q = urllib.parse.urlparse(u)\n    return q.netloc.replace(\"www.\", \"\") + q.path.rstrip(\"/\")",
  "replace": "    return u",
  "why": "comparing urls RAW misses the pair this check exists for: the hunt activated the row on the human page `job-boards.greenhouse.io/unframe` and the re-resolve wrote `boards-api.greenhouse.io/v1/boards/unframe/jobs`. Every url de-dup key in this lane normalises for that reason.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "applier-agg-endpoint-only",
  "file": "apply_proposals.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "        agg = next((u for u in trail[:2] if u and is_aggregator(u)), \"\")",
  "replace": "        agg = p.get(\"api_url\") if is_aggregator(p.get(\"api_url\") or \"\") else \"\"",
  "why": "a search rung resolves an aggregator's PAGE to the ATS board behind it, so the final api_url is an ATS host and no aggregator test can refuse it. Menora Mivtachim on jobkarov, one indirection on.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "zero-cond2-drop",
  "file": "confirm_zero.py",
  "symbol": "verdict_from",
  "class": "M1-gate-removal",
  "find": "    if ev.get(\"cond2\") == \"not-ours\":\n        return \"wrong-url\", False\n",
  "replace": "",
  "why": "condition (b) then rests on ONE LLM boolean, and `employer_named` is legally \"\" under the schema. Dolby Laboratories was CONFIRMED with cond2=not-ours on 2026-08-29.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "zero-followed-confirms",
  "file": "confirm_zero.py",
  "symbol": "verdict_from",
  "class": "M1-gate-removal",
  "find": "    if ev.get(\"followed_to\"):\n        return ROUTING, False\n",
  "replace": "",
  "why": "the one-hop follow fires exactly when a page shows no postings -- which is what a correctly empty filtered board looks like -- so confirming on the followed page turns `shows-none` into a zero. It took Dolby to a Germany jobs page and Nominal to a single posting.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "zero-errors-freeze",
  "file": "confirm_zero.py",
  "symbol": "_ledger_stale",
  "class": "M7-constant-drift",
  "find": "    kind = str(v.get(\"verdict\") or \"\").split(\":\")[0]\n    if kind == \"stripped\":\n        return True\n    if kind in _NOT_A_CADENCE:\n        days = min(days, ERROR_RECHECK_DAYS)\n",
  "replace": "",
  "why": "an ERROR verdict is a fact about our renderer; freezing the row for RECHECK_DAYS on it retires coverage. 34 rows were in that state when this was found.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "zero-routing-uncapped",
  "file": "confirm_zero.py",
  "symbol": "_write",
  "class": "M1-gate-removal",
  "find": "            if off >= MAX_DEACTIVATE:\n                capped.append(r[0])\n                continue\n            off += 1\n",
  "replace": "",
  "why": "routing was 83 of 139 ledger rows, the commonest disposition, and it was the one arm MAX_DEACTIVATE did not bound: one broken run could turn off an unbounded number of ACTIVE rows.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "queue-settled-prefix",
  "file": "queue_state.py",
  "symbol": "is_settled",
  "class": "M7-constant-drift",
  "find": "    return any(str(a.get(\"verdict\") or \"\") in TERMINAL\n               for a in (state.get(name) or {}).get(\"tried\") or [])",
  "replace": "    return any(any(str(a.get(\"verdict\") or \"\").startswith(t) for t in TERMINAL)\n               for a in (state.get(name) or {}).get(\"tried\") or [])",
  "why": "prefix matching made `resolved-domain` -- the company's own SITE, which is evidence and not a board -- settle 55 names that still had every later rung to run.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "queue-settled-newest",
  "file": "queue_state.py",
  "symbol": "is_settled",
  "class": "M1-gate-removal",
  "find": "    return any(str(a.get(\"verdict\") or \"\") in TERMINAL\n               for a in (state.get(name) or {}).get(\"tried\") or [])",
  "replace": "    return str((state.get(name) or {}).get(\"verdict\") or \"\") in TERMINAL",
  "why": "the log is not chronological: backfilling the drain after stamping `already-a-row` buried 64 of 65 settled names behind a later refusal, and the census said 2 where 66 were.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "ownsite-parked-admitted",
  "file": "auto_expand.py",
  "symbol": "_site_from_guess",
  "class": "M1-gate-removal",
  "find": "        from confirm_zero import _PARKED, _visible\n        if _PARKED.search(_visible(html)[:4000]):\n            return _no(\"parked-domain\")\n",
  "replace": "",
  "why": "a domain FOR SALE names the company by construction, so page_mentions_company cannot refuse it; 18 of 136 no-linkback names are parking pages, and a wrong own-domain corrupts the one check resolve_llm._verify has.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "qdisp-prune-unrecorded",
  "file": "queue_disposition.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "    for n in retire:                       # the assertion the whole file exists for\n        assert n in state and state[n].get(\"evidence\"), \"pruning %r with no record\" % n\n",
  "replace": "",
  "why": "pruning a name without a persisted verdict destroys the evidence and makes the claim 'this company has no findable board' in the same act -- the operator's rule 1 where the subject is a NAME.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "qdisp-no-floor",
  "file": "queue_disposition.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "    if len(retire) > MAX_PRUNE_FRACTION * len(names):",
  "replace": "    if False:",
  "why": "a run that retires the whole queue is a broken run, not a measurement (CLAUDE.md rule 2). Without the floor one bad LLM day empties the file.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "qdisp-retire-unhunted",
  "file": "queue_disposition.py",
  "symbol": "retirable",
  "class": "M3-branch-narrowing",
  "find": "    if name not in hunt:\n        return False, \"no-hunt-attempt\"                 # the deepest rung never ran\n",
  "replace": "",
  "why": "retiring a name the deepest rung never touched records 'nothing findable' about a company nobody looked for.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "applier-monitor-drop",
  "file": "apply_proposals.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "    props = [p for p in doc[\"proposals\"] if p.get(\"kind\") in (\"ats\", \"scrape\", \"monitor\")]",
  "replace": "    props = [p for p in doc[\"proposals\"] if p.get(\"kind\") in (\"ats\", \"scrape\")]",
  "why": "dropping the monitor kind writes NO row for a company whose careers page we found, which removes it from probe_candidates' DAILY pool -- the only mechanism that surfaces it the morning it posts -- and leaves a ~12-day queue cycle instead.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "applier-monitor-asserts-absence",
  "file": "apply_proposals.py",
  "symbol": "main",
  "class": "M3-branch-narrowing",
  "find": "            seg = \"queue-hunt %s: careers page documented; monitored candidate\" % today",
  "replace": "            seg = \"queue-hunt %s: no IL listing; monitored candidate\" % today",
  "why": "the monitor row exists to record an ADDRESS, never an absence; the old queue arm's note said `no IL listing` and that is the operator's rule violated once per name.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "recruiter-plural-drop",
  "file": "pipeline/recruiters.py",
  "symbol": "_KEYWORD",
  "class": "M7-constant-drift",
  "find": "    r\"\\b(recruit(ing|ment|ers?|x)?|staffing|headhunt(ing|ers?)?|manpower|\"",
  "replace": "    r\"\\b(recruit(ing|ment|x)?|staffing|headhunt(ing|ers?)?|manpower|\"",
  "why": "discovery 2026-08-30: reverting `ers?` re-admits `Sales Experts Executive Recruiters` (a live queue entry) to the names funnel, one auto_expand run from a registry row.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "dispose-rejudges-overruled",
  "file": "queue_pipeline.py",
  "symbol": "dispose",
  "class": "M1-gate-removal",
  "find": "\n            and not is_reopened(n, state)]",
  "replace": "]",
  "why": "`reopen` clears `raw_verdict`, so without this conjunct the judge re-admits every name a human overruled, writes `state[name]` wholesale and `overturned_from` is gone. At the guard_kill range base (bfdff0f) the todo filter was the stricter `n not in state`, so the guard could not fail there; this record is what makes it provable.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-query-url-stamped-il",
  "file": "audit_query_urls.py",
  "symbol": "il_jobs",
  "class": "M1-gate-removal",
  "find": "    if il and has_location_query(url) and not independent_il_evidence(url, jobs):\n        return []",
  "replace": "    if False:\n        return []",
  "why": "on a `?location=Israel` URL every card is stamped Israel by the scraper; without this gate the hunt activates Comcast on 14 US postings and undoes the audit's park the next night.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "query-audit-park-on-description",
  "file": "audit_query_urls.py",
  "symbol": "card_signal",
  "class": "M5-population-widening",
  "find": "    text = \" \".join(fields)\n    if _IL_PLACE.search(text) or _IL_SQUASHED.search(squashed):",
  "replace": "    fields.append(str(job.get(\"description\") or \"\")[:400])\n    text = \" \".join(fields)\n    if _IL_PLACE.search(text) or _IL_SQUASHED.search(squashed):",
  "why": "a description is JD boilerplate (ASML: 'China, Connecticut' on a filter it honours); reading it as the card's place parks honest rows.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "drain-targets-ignore-disposition",
  "file": "queue_resolve_search.py",
  "symbol": "targets",
  "class": "M1-gate-removal",
  "find": "        if QD.is_retired(n, disp, today):\n            continue",
  "replace": "        if False:\n            continue",
  "why": "174 names retired on evidence and re-added by intake would each buy a paid search the night their 14-day cadence lapsed (2026-09-12); the retirement is on disk and the drain must read it.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "catalog-window-over-fresh",
  "file": "pipeline/secrethunter.py",
  "symbol": "queue_entries",
  "class": "M3-branch-narrowing",
  "find": "        if slug not in today:",
  "replace": "        if False:",
  "why": "discovery 2026-08-30: without the catalog-slice gate the offer is the first N unqueued names, so the second run of a day sees a shifted list and queues the next N -- the 586-names-in-a-day shape the per-day cap exists to stop.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "catalog-retired-drop",
  "file": "pipeline/secrethunter.py",
  "symbol": "queue_entries",
  "class": "M3-branch-narrowing",
  "find": "        if keys & retired_keys:",
  "replace": "        if False and keys & retired_keys:",
  "why": "discovery 2026-08-30: dropping the retirement check re-offers every name the registry retired with evidence once per catalog rotation (BACKLOG 441: ~100 and ~48 such re-adds in the two 2026-08-30 cloud runs).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "merge-deep-gate-widened-to-root",
  "file": "pipeline/store.py",
  "symbol": "merge_duplicates",
  "class": "M1-gate-removal",
  "find": "deep = [m for m in members if _is_posting_page(m, origins) and m.get(\"url\")]",
  "replace": "deep = [m for m in members if _authoritative(m, origins) and m.get(\"url\")]",
  "why": "the 2026-08-31 url-donation widening (an _inherited board card may donate its ADDRESS) leans entirely on _is_posting_page: with the gate loosened to _authoritative, a board ROOT displaces a per-job card. Killed by test_a_board_root_address_never_displaces_a_per_job_card.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "merge-own-inherited-gate-removal",
  "file": "pipeline/store.py",
  "symbol": "merge_duplicates",
  "class": "M1-gate-removal",
  "find": "own = [m for m in members if _authoritative(m, origins) and not m.get(\"_inherited\")]",
  "replace": "own = [m for m in members if _authoritative(m, origins)]",
  "why": "the TEXT donor pool must keep excluding _inherited copies — their description is classify_grouped's copy of the group's longest, not something that posting carried; without the gate a copied text rides the board url it was copied onto. SURVIVED its first run; killed by test_a_copied_text_never_rides_the_board_url_it_was_copied_onto.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "upsert-url-ratchet-inversion",
  "file": "pipeline/store.py",
  "symbol": "upsert_matched",
  "class": "M2-gate-inversion",
  "find": "if old_url and (not str(new_url or \"\").strip()\n                            or (_is_aggregator_url(new_url)\n                                and not _is_aggregator_url(old_url))):",
  "replace": "if old_url:",
  "why": "the 2026-08-31 anti-regression ratchet refuses exactly TWO downgrades (board url -> aggregator url, and any stored url blanked); inverted to 'a stored url never changes', an employer moving hosts is pinned to a dead address forever. Killed by the host-move assertion in test_a_stored_board_url_is_never_regressed_to_an_aggregator_link.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "same-posting-exact-widening",
  "file": "pipeline/roles.py",
  "symbol": "same_posting",
  "class": "M2-gate-inversion",
  "find": "exact = bool(pa) and pa == pb",
  "replace": "exact = pa == pb",
  "why": "the posting-key arm added 2026-08-31 relies on '' meaning nobody's posting: without the bool() guard, any two aggregator or listing urls (both keyed '') become the same posting and the titles bypass opens cross-company. Killed by test_titles_are_never_bypassed_without_a_per_posting_id.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "merge-donation-over-any-canonical",
  "file": "pipeline/store.py",
  "symbol": "merge_duplicates",
  "class": "M1-gate-removal",
  "find": "elif deep and (_is_aggregator_url(out.get(\"url\"))\n                       or not str(out.get(\"url\") or \"\").strip()):",
  "replace": "elif deep:",
  "why": "the 2026-08-31 url donation may fire only over an aggregator (or url-less) canonical; with the gate gone, a competitor card that won the canonical sort is handed OUR board url and its JD ships under our own address (wave A finding 1, reproduced). Killed by test_a_competitor_canonical_is_never_handed_our_board_url.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "same-posting-tail-host-fallback",
  "file": "pipeline/roles.py",
  "symbol": "same_posting",
  "class": "M6-fallback-removal",
  "find": "tail = path_part.rsplit(\"/\", 1)[-1] if \"/\" in path_part else \"\"",
  "replace": "tail = path_part.rsplit(\"/\", 1)[-1]",
  "why": "a query-only posting key (careers.f5.com?jobId=...) has no path; rsplit then returns the HOST, 138 registry hosts carry a digit, and one digit in a brand name arms the titles bypass for a whole board (wave A finding 5, reproduced). Killed by test_a_query_only_posting_key_never_arms_the_titles_bypass.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retraction-url-key-host-only",
  "file": "pipeline/roles.py",
  "symbol": "_url_key",
  "class": "M5-population-widening",
  "find": "return host.lower() + (sep + rest.rstrip(\"/\") if sep else \"\")",
  "replace": "return host.lower()",
  "why": "a retraction url key collapsed to its host withdraws every record on that host at once - the x/998629 attacker shape (one partial key, many employers' postings) with the truncation on the matcher's side. Killed by test_the_bounce_wrong_employer_retraction_withdraws_the_linkedin_row_only.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-still-unreachable-drops-the-empty-fact",
  "file": "retry_unreachable.py",
  "symbol": "_row_for",
  "class": "M5-population-widening",
  "find": "            _keep_selectors(name, url, note, _fold_empty(note, today) + [plain])]",
  "replace": "            _keep_selectors(name, url, note, [plain])]",
  "why": "this tool OWNS the only carrier of `no open israel roles` on a row it scanned empty, and replace_own deletes its own segment before appending. Without the fold, the first unreachable night retires the row from validate_empty's pool - four consecutive tests.yml runs red on `rehearse (worst, seed 1)`, BACKLOG 514. Killed by test_a_still_unreachable_night_does_not_retire_the_row_from_validate_empty. Re-anchored 2026-08-31 (b): the fold is now a LIST of candidate forms; dropping it leaves only the plain segment, which is the original defect.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retry-fold-writes-over-a-saturated-cell",
  "file": "retry_unreachable.py",
  "symbol": "_keep_selectors",
  "class": "M6-fallback-removal",
  "find": "        if was_empty_pool and not _in_validate_empty(after):",
  "replace": "        if False:",
  "why": "notes.append drops a newcomer whole on a cell at the cap whose every segment is protected - a guarantee written for a tool that only ADDS. Paired with replace_own it deletes the segment and then fails to write the replacement, so the ~33-char-longer fold costs Syte (208 chars, 4/4 protected) both the phrase and this tool's stamp. Killed by test_the_fold_is_skipped_when_writing_it_would_cost_the_pool_it_protects. Re-anchored 2026-08-31 (b): the guard now asks the POOL PREDICATE, not the literal phrase (the proxy got Syte wrong). Removing the check is the same defeat.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "ci-display-name-impersonation-guard-off",
  "file": "pipeline/rolecard.py",
  "symbol": "display_name",
  "class": "M1-gate-removal",
  "find": "            if k != company and o and (o <= its or its <= o):\n                return \"\"",
  "replace": "            if False:\n                return \"\"",
  "why": "company-intel guard: this is what makes a DISPLAY_NAME_OVERRIDES entry safe to ship. With it off, `finbounce` renders `Bounce AI` beside the active `Bounce AI` row -- one company's name on another's card, the failure 487 names. It is also how the lane learned `Landacorp -> Landa` could never render (a second active row, `Landa Digital Printing`, holds that identity), 2026-08-31.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "ci-display-name-never-retracted",
  "file": "pipeline/firmographics.py",
  "symbol": "apply_display_names",
  "class": "M6-fallback-removal",
  "find": "        elif not rep[\"skipped\"] and DISPLAY_NAME_KEY in rec:\n            del rec[DISPLAY_NAME_KEY]\n            rep[\"removed\"] += 1",
  "replace": "        elif False:\n            del rec[DISPLAY_NAME_KEY]\n            rep[\"removed\"] += 1",
  "why": "company-intel guard: withdrawn evidence must RETRACT the name, and this branch is the only thing that can express it -- `merge`'s fill-forward carries a stale one forward for ever, which is how a cleared name kept coming back from the sqlite side (wave 1b, 2026-08-30).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "donor-selflink-string-compare",
  "file": "pipeline/jdfill.py",
  "symbol": "role_addresses_on",
  "class": "M4-guard-removal",
  "find": "        if _same_page(u):\n            continue                        # the listing page linking to itself",
  "replace": "        if u.rstrip(\"/\") == (page_url or \"\").rstrip(\"/\"):\n            continue                        # MUTANT: the full-URL compare this replaced",
  "why": "shipped defeat: the self-link skip was a full-URL string compare, so every query, fragment and trailing-slash variant of the listing page came back as this role's own address -- and a listing page is exactly what `is_job_url` refuses, so the row is handed back the address it is already stuck at. Found by wave C 2026-08-31; the fix shipped one commit before its test, which is why the test is catalogued rather than killing in range.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "wire-url-double-encodes",
  "file": "pipeline/jdfill.py",
  "symbol": "wire_url",
  "class": "M16-escaping",
  "find": "quote(parts.path, safe=\"/%:@&=+$,;~!*'()\")",
  "replace": "quote(parts.path, safe=\"/:@&=+$,;~!*'()\")",
  "why": "shipped defeat: with `%` out of the safe set, an address that mixes raw non-ASCII with an existing percent-escape is encoded twice (`%D7%90` -> `%25D7%2590`) and 404s. 0 such urls in the caches today, so this is the defensive case pinned deliberately. Found by wave C 2026-08-31; fix shipped one commit before its test.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "alias-fold-drops-the-evidence-gate",
  "file": "pipeline/roles.py",
  "symbol": "_alias_fold_target",
  "class": "M6-fallback-removal",
  "find": "    if _plain_norm(name) == _plain_norm(r):\n        return (r, \"casefold\")",
  "replace": "    if True:\n        return (r, \"casefold\")",
  "why": "identity_key equality alone folds AppSec Labs onto AppSec - two real employers (BACKLOG 144). The evidence ladder (casefold / declared / board) is the whole difference between the 533 fix and the 489 defect. Killed by test_alias_fold_refuses_bare_suffix_strip_equality.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "alias-fold-reads-alias-values-as-declarations",
  "file": "pipeline/roles.py",
  "symbol": "_alias_fold_target",
  "class": "M5-population-widening",
  "find": "    if ALIASES.get(_plain_norm(name)) == ident:",
  "replace": "    if ident in set(ALIASES.values()):",
  "why": "the values() test folds ANY name whose stem strips to a declared canonical - 'NVIDIA Labs' strips to 'nvidia' and would fold with no declaration at all. The declaration is the alias KEY matched against the plain-normalized name. Killed by test_alias_fold_declaration_is_the_alias_key_not_the_stripped_stem.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "alias-fold-accepts-an-ambiguous-identity",
  "file": "pipeline/roles.py",
  "symbol": "_alias_fold_target",
  "class": "M5-population-widening",
  "find": "    if len(targets) != 1:\n        return None",
  "replace": "    if not targets:\n        return None",
  "why": "an identity two active rows answer to (Amazon/AWS/Amazon Israel - 11 such groups) must fold onto neither; picking 'any one of them' attributes a role to whichever name set iteration serves. Killed by test_alias_fold_never_rewrites_a_registry_name (the Acme/acme case-twin arm, which folds under EVERY iteration order).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "twin-fold-single-id-arm",
  "file": "pipeline/roles.py",
  "symbol": "same_role_twin",
  "class": "M5-population-widening",
  "find": "    if len(platform_shared) >= 2:\n        return True",
  "replace": "    if len(platform_shared) >= 1:\n        return True",
  "why": "within one company a single shared id is the SpearUAV/listing-id shape: Guardio's developer and engineer share one comeet id and are two openings. Arm 1 requires two independent PLATFORM id spaces to agree. Killed by test_one_shared_id_alone_never_folds_twins. Re-anchored 2026-08-31 (b, post-wave): the arm now counts platform-issued ids only — wave A folded 7 real wrong pairs through the looser draft.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "twin-fold-counts-a-junk-id",
  "file": "pipeline/roles.py",
  "symbol": "_groups",
  "class": "M6-fallback-removal",
  "find": "            weak = {sid for (_c, sid), cnt in idc.items() if cnt >= 3}",
  "replace": "            weak = set()",
  "why": "F5's workday:0 and Thales' display-list ids are board fingerprints _is_id_shaped admits; without the population demotion, one junk id plus the location-glue titles arm folds two real postings of one company. Killed by test_an_id_borne_by_three_records_of_one_company_is_never_twin_evidence (the Thales arm).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "twin-winner-ignores-source",
  "file": "pipeline/roles.py",
  "symbol": "_twin_winner",
  "class": "M2-ordering",
  "find": "        ranked = sorted(idxs, key=lambda i: (_source_rank(jobs[i]), i))",
  "replace": "        ranked = sorted(idxs)",
  "why": "the winner must be the native board's card (the title the employer shows today), never whichever job happened to come first in the merged list - the stale LinkedIn title would rename the role on the public dataset. Killed by test_two_shared_strong_ids_fold_a_retitled_twin_and_keep_the_native_titled_job (the stale card is deliberately FIRST).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "alias-sweep-drops-the-sid-union",
  "file": "pipeline/roles.py",
  "symbol": "_fold_into_twin",
  "class": "M6-fallback-removal",
  "find": "        winner[\"seen_ids\"] = sorted((set(winner.get(\"seen_ids\") or [])\n                                     | set(loser.get(\"seen_ids\") or [])) - {\"\"})",
  "replace": "        winner[\"seen_ids\"] = sorted(set(winner.get(\"seen_ids\") or []) - {\"\"})",
  "why": "the loser's seen_ids are what filter_new consults: without the union the winner row does not carry the id the posting was DELIVERED under, and the role is re-emailed under the canonical name - the exact class resolve_claims' stored-id union already guards at run time. Re-anchored 2026-09-11: the alias sweep and the title sweep (585) now share one `_fold_into_twin` seam, so this one record covers both. Killed by test_alias_sweep_supersedes_the_stored_twin_and_unions_its_ids and by test_the_title_sweep_supersedes_into_a_live_twin_instead_of_overwriting_it.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "blocker-marks-a-jd-row",
  "file": "pipeline/roles.py",
  "symbol": "_blocker",
  "class": "M6-fallback-removal",
  "find": "    if dq not in (\"snippet\", \"none\"):\n        return \"\"",
  "replace": "    if dq == \"never\":\n        return \"\"",
  "why": "GONE_MARK is a verdict about the ADDRESS, not the text (jd-text's 2026-08-31 contract): a gone row that gained a donor copy has a complete description and must not ship marked blocked. The dq gate is what keeps the column about the text. Killed by test_a_blocker_never_marks_a_row_whose_text_passes.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "twin-reclaim-guard-removed",
  "file": "pipeline/roles.py",
  "symbol": "_resolve_claims",
  "class": "M6-fallback-removal",
  "find": "            if wc and wc == sup[k].get(\"company\"):\n                continue",
  "replace": "            if False:\n                continue",
  "why": "the morning a board closes a posting while its stale aggregator card lingers, every pre-guard reclaim condition holds for the twin key - reclaiming re-splits the role, and the at-rest sweep then crowns the stale title (open beats closed). Reclaim is for cross-company parking only. Killed by test_a_twin_superseded_record_never_reclaims_itself.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "twin-fold-counts-a-url-fallback-id",
  "file": "pipeline/roles.py",
  "symbol": "same_role_twin",
  "class": "M5-population-widening",
  "find": "    platform_shared = {s for s in shared\n                       if not s.partition(\":\")[2].lower().startswith(\"http\")}",
  "replace": "    platform_shared = set(shared)",
  "why": "a url-fallback `scrape:` sid is sidebar junk a board-wide mis-scrape stamps on several records at once (Nift holds five foreign LinkedIn urls on ONE record); counting two of them as two independent witnesses folds two real openings with no title test. Wave A, finding 6. Killed by test_the_arms_that_folded_seven_real_wrong_pairs_are_gone.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "twin-fold-pk-arm-without-the-titles-guard",
  "file": "pipeline/roles.py",
  "symbol": "same_role_twin",
  "class": "M5-population-widening",
  "find": "    return same_posting(a, b)",
  "replace": "    return same_posting(a, b) or _pk(a.get(\"url\")) == _pk(b.get(\"url\")) != \"\"",
  "why": "the first draft's bare identical-pk arm: the scraper's href ladder binds several cards to one url (85 (company,url) pairs carry 2+ titles), and it folded Legit Security's Senior Software Engineer into Head of Engineering — 7 wrong pairs in the live cache. same_posting's four-guard bypass is the whole difference. Wave A, findings 1-2. Killed by test_the_arms_that_folded_seven_real_wrong_pairs_are_gone.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "supersede-follows-no-chain",
  "file": "pipeline/roles.py",
  "symbol": "_supersede",
  "class": "M6-fallback-removal",
  "find": "        seen = {loser_key}\n        while True:\n            w = self.records.get(winner_key)\n            if w is None or w.get(\"status\") != \"superseded\":\n                break\n            nxt = w.get(\"superseded_by\") or \"\"\n            if not nxt or nxt in seen:\n                return\n            seen.add(winner_key)\n            winner_key = nxt",
  "replace": "        seen = {loser_key}",
  "why": "the at-rest sweep and the run-time arm elect winners by different rules; superseding A into a B whose superseded_by is A closes a CYCLE — both halves off the board, the email and roles.csv, `ledger N = store N` still green, no alarm. Wave B, finding 1. Killed by test_the_two_twin_arms_can_never_close_a_supersede_cycle.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "blocker-unguarded-against-a-bracket-url",
  "file": "pipeline/roles.py",
  "symbol": "_blocker",
  "class": "M6-fallback-removal",
  "find": "    except Exception:  # noqa: BLE001 — a mark is a nicety; the dataset is not\n        return \"\"",
  "replace": "    except ValueError:\n        raise",
  "why": "jdfill.unfillable -> urlsplit RAISES on a bracket-mangled netloc, and `[email protected]` is Cloudflare's obfuscation placeholder scrapers lift off careers pages; one such row rode export_dataset's catch and kept yesterday's roles.csv for the day. Wave B, finding 3. Killed by test_one_bracket_url_cannot_take_the_days_dataset_down.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "twin-winner-breaks-ties",
  "file": "pipeline/roles.py",
  "symbol": "_twin_winner",
  "class": "M6-fallback-removal",
  "find": "        if len(ranked) >= 2 and _source_rank(jobs[ranked[0]]) == _source_rank(jobs[ranked[1]]):\n            return None",
  "replace": "        if False:\n            return None",
  "why": "two equally-sourced live titles is not a retitle we can call - a guessed winner renames a role on the public dataset (the Fetcherr pair: two comeet slugs, both rank 0). Killed by test_twin_fold_tie_on_source_refuses.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "indeed-pane-key-unbound",
  "file": "pipeline/jdfill.py",
  "symbol": "_indeed_pane",
  "class": "M2-gate-inversion",
  "find": "    if not isinstance(pane, dict) or (pane.get(\"jobKey\") or \"\").lower() != str(jk or \"\").lower():\n        return None",
  "replace": "    if not isinstance(pane, dict):\n        return None",
  "why": "The SERP body we buy carries OUR jk's pane beside other jobs' cards. Drop the key equality and the parser takes whatever pane the page happens to open with -- the measured failure is 3,028 identical characters of page furniture returned for three different jks (2026-08-31 E1). Shipped 08-31 with no mutation record; added 09-01. Killed by test_a_bought_serp_whose_pane_is_not_our_job_fills_nothing.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "donor-echo-employer-half-free",
  "file": "pipeline/jdfill.py",
  "symbol": "doc_names_role",
  "class": "M1-gate-removal",
  "find": "    c_only = c_words - t_words\n    return (bool(c_only) and len(t_words & decl) >= min(2, len(t_words))\n            and bool(c_only & decl))",
  "replace": "    c_only = c_words - t_words\n    return len(t_words & decl) >= min(2, len(t_words))",
  "why": "The employer half of the cross-source admission gate, paid for by the TITLE: a declaration always contains the title's words, so `Bright Data` was confirmed by the `data` in `Data Analyst` and Fetcherr's posting published under Bright Data's name (the pair store._same_origin cites). Shipped 08-31 with no mutation record; added 09-01. Killed by test_the_employer_half_of_the_gate_cannot_be_paid_for_by_the_title.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "indeed-identity-gate-removal",
  "file": "pipeline/jdfill.py",
  "symbol": "fetch_jd",
  "class": "M1-gate-removal",
  "find": "            if jk and _pane_denies_role(decl, title, company):",
  "replace": "            if False:",
  "why": "The jk is the discovery card's CLAIM about which posting this is, and until 09-01 nothing checked it: the 08-31 rung verified jobKey == jk and stored whatever that pane served. Remove the gate and a card whose jk names another role buys that role's text under this row's name. Killed by test_a_bought_pane_that_declares_another_role_fills_nothing_and_spends_no_second_credit.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "pane-denial-needs-a-comparable-title",
  "file": "pipeline/jdfill.py",
  "symbol": "_pane_denies_role",
  "class": "M4-guard-removal",
  "find": "    if not (decl - _named_words(company)):\n        return False",
  "replace": "    if False:\n        return False",
  "why": "Re-aimed 2026-09-01 after an adversarial wave proved the original record EQUIVALENT: it deleted `if not decl` and the whole-declaration `_scripts` test, both of which the bilingual fix made redundant (an empty or cross-alphabet declaration is emptied by the per-word script filter and caught by the clause below) -- 0 behavioural differences over the gate's own 9 cases, so no test could ever kill it. THIS is the load-bearing guard: after the declaration is filtered to the title's alphabet, something in it must still be capable of BEING a title. Drop it and a bilingual pane whose Latin half is only the employer's name -- `'אנליסט/ית נתונים … Bank Leumi'` vs `Data Analyst` -- is denied for being written in Hebrew. Killed by test_the_pane_gate_reads_only_the_alphabet_the_title_is_written_in.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "jd-soup-window-widened",
  "file": "pipeline/jdfill.py",
  "symbol": "_is_markup_soup",
  "class": "M3-branch-narrowing",
  "find": "    return len(_MARKUP_SOUP.findall(str(text or \"\")[:_SOUP_HEAD])) >= _SOUP_HITS",
  "replace": "    return len(_MARKUP_SOUP.findall(str(text or \"\")[:3000])) >= _SOUP_HITS",
  "why": "The HEAD window is the rule, not the count. A real posting may END in serialization -- four of Fayrix's six cards carry a JSON form-field blob from offset ~2,500 and ten of Crossriver's fourteen cards a literal escape in ordinary prose -- so widening the window past the head vetoes real job descriptions to catch the same one soup document. Measured over all 4,684 stored CARDS (~1,860 with text) on 2026-09-01. Killed by test_serialized_markup_is_not_a_job_description_and_a_real_jd_with_a_json_tail_is.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "refuted-opens-ratchet-to-anything",
  "file": "enrich_matched_jd.py",
  "symbol": "_store_text",
  "class": "M4-guard-removal",
  "find": "    if refuted and looks_like_jd(text):",
  "replace": "    if refuted:",
  "why": "A refuted row's stored text defends nothing, but only a real job description may take its place: blanking `have` unconditionally makes every LONGER string win, including page furniture, so a row the tier disowned could be overwritten with a nav bar. Found by this function's own refusal test while it was being written (09-01). Killed by test_a_refuted_row_lets_a_shorter_true_posting_through_and_nothing_else_does.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "refuted-widens-to-every-suspicion",
  "file": "enrich_matched_jd.py",
  "symbol": "_quality_pass",
  "class": "M3-branch-narrowing",
  "find": "        if why == \"no-company-echo\" and klass == \"not-a-jd\":",
  "replace": "        if True:",
  "why": "Only the suspicion that asks WHOSE posting this is, and only a FRESH `not-a-jd`, may open the length ratchet. Widen it and two defects come back at once: a row the tier called merely INCOMPLETE loses its own posting to the next longer thing any rung returns (wave A's 08-31 donor-overwrite, from the other side), and a `partial` -- which `_QUALITY_SYSTEM` defines as this role's own posting, truncated -- is treated as a disownment. Killed by test_a_partial_verdict_never_refutes_because_partial_is_this_roles_own_posting and test_only_the_echo_suspicion_refutes_and_the_cheaper_ones_never_do.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "refuted-reads-a-cached-bool-as-a-class",
  "file": "enrich_matched_jd.py",
  "symbol": "_quality_pass",
  "class": "M4-guard-removal",
  "find": "        klass = \"\"                         # \"\" = this row's verdict came from the cache",
  "replace": "        klass = \"not-a-jd\"",
  "why": "`llm_cache.verdict` is a BOOL, so a cache hit cannot say whether the model answered `partial` or `not-a-jd`. Pretending it can lets a cached rejection -- possibly of this role's own truncated posting -- unlock the length ratchet for ever, with no call and no way to revisit it. It also hides that `verdict` is a loop-carried local, so reading it on the cached branch serves the PREVIOUS row's class. Killed by test_a_cached_rejection_never_refutes_because_the_column_cannot_say_which_no_it_was.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "refutation-outlives-the-fill",
  "file": "enrich_matched_jd.py",
  "symbol": "_donor_pass",
  "class": "M4-guard-removal",
  "find": "            refuted.discard(mkey)\n",
  "replace": "",
  "why": "A refutation must close the moment a rung fills the row. `cache` and `own-address` donors are gated on the URL (`_own_posting`), not on the document naming the role -- only a `copy` runs `doc_names_role` -- so a refutation left standing after a fill is a hole a LATER donor walks into: an adversarial wave reproduced a 333-character cache donor overwriting a row's own 705-character posting. Killed by test_a_refuted_row_stops_being_refuted_the_moment_a_rung_fills_it.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "pane-gate-compares-across-alphabets",
  "file": "pipeline/jdfill.py",
  "symbol": "_pane_denies_role",
  "class": "M3-branch-narrowing",
  "find": "    decl = {w for w in decl if _scripts({w}) & _scripts(t_words)}",
  "replace": "    decl = set(decl)",
  "why": "`declared_identity` CONCATENATES the pane's jobTitle and companyName, so a Hebrew posting at a Latin-named employer declares in BOTH scripts. Comparing the whole declaration then passes the script guard on the company's Latin words while the title is measured against Hebrew it can never match, and the row is denied for being bilingual -- on a board where 25 of 106 Indeed cards carry a Hebrew title. Killed by test_the_pane_gate_reads_only_the_alphabet_the_title_is_written_in.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "refuted-row-reaches-the-paid-rung",
  "file": "enrich_matched_jd.py",
  "symbol": "_donor_pass",
  "class": "M1-gate-removal",
  "find": "        row_bd = bd if (mkey in paid_keys and mkey not in refuted",
  "replace": "        row_bd = bd if (mkey in paid_keys",
  "why": "Before the refuted flag existed, a row whose text passed `looks_like_jd` was skipped BEFORE `_donor_candidates` and could not spend. A refuted row now walks the candidates, so without this clause it reaches the Unlocker: an adversarial wave measured 0 paid calls before and 1 (2 with the render retry) after, per refuted row per cooldown window, against the shared 25 cap. A row that already HAS text may work the free rungs; it may not preempt a row that has none. Killed by test_an_archived_role_reaches_the_free_rungs_and_not_the_paid_one, whose third leg passes a non-empty `refuted=` -- until 2026-09-02 no test in the suite did, which is why this record SURVIVED shard 6 of run 33534533355.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "pending-exclusion-back-to-blocker-only",
  "file": "pipeline/roles.py",
  "symbol": "build_rows",
  "class": "M1-gate-narrowed",
  "find": "        if weak and BLOCKED_POLICY == \"exclude\" and not archive:",
  "replace": "        if blocker and BLOCKED_POLICY == \"exclude\" and not archive:",
  "why": "the 2026-08-31 reading of the ruling, which published two PENDING weak rows on 09-01 (Madanes with no text, בנק דיסקונט with a snippet) to the csv, the board and the mail. The operator's words are 'no role in the UI and in the db without description' — a weak row leaves whether or not a STRUCTURAL blocker can be derived for it. Killed by test_a_pending_weak_row_leaves_the_dataset_and_returns_when_its_text_lands.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "pending-count-moved-inside-the-policy",
  "file": "pipeline/roles.py",
  "symbol": "build_rows",
  "class": "M6-fallback-removal",
  "find": "            counts[\"pending\"] += 1",
  "replace": "            counts[\"pending\"] += 0",
  "why": "the pending census is counted at DETECTION, not inside the exclude branch, so a `mark` day reports the same number it publishes. Zeroed, the meta's description_text.blocked says `pending: 0` while pending rows sit in the file — the silent-collapse shape wave B found in the quality block. Killed by test_the_meta_and_mail_agree_on_weak_text_and_the_quality_identity_holds.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "text-quality-collapses-unmeasured-into-none",
  "file": "pipeline/roles.py",
  "symbol": "text_quality",
  "class": "M1-gate-narrowed",
  "find": "    return \"none\" if dlen is _NO_LENGTH else \"\"",
  "replace": "    return \"none\"",
  "why": "a stored length with no text in hand means the caller cannot SEE the text (a frozen or corrupt roles_text.jsonl), not that there is none. Collapsed to `none`, every such row is weak and the exclude policy empties the board and the csv on a bad text day — the mass-zero failure class, arriving as a policy verdict. Killed by test_text_quality_separates_no_text_from_a_text_it_cannot_see.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "same-text-bucket-floor-removed",
  "file": "pipeline/roles.py",
  "symbol": "_text_key",
  "class": "M1-gate-narrowed",
  "find": "    if sha and isinstance(dlen, int) and dlen >= SAME_TEXT_MIN:",
  "replace": "    if sha and isinstance(dlen, int):",
  "why": "below SAME_TEXT_MIN an identical string is boilerplate two boards share ('Apply now'), not evidence of one posting: without the floor every short-stub pair across two employers becomes a fold candidate and one of two real roles is superseded. Killed by test_the_text_arm_needs_a_substantial_text_and_titles_that_match.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "same-text-arm-drops-the-titles-guard",
  "file": "pipeline/roles.py",
  "symbol": "_same_text_posting",
  "class": "M6-fallback-removal",
  "find": "    return ta == tb",
  "replace": "    return True",
  "why": "byte-identical text under two employers with DIFFERENT titles is two postings sharing a page, not one role. Without the guard the arm folds a careers page's whole card set across employers — the BACKLOG 370 shape, cross-company. Killed by test_the_text_arm_needs_a_substantial_text_and_titles_that_match.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "winner-ignores-what-the-posting-names",
  "file": "pipeline/roles.py",
  "symbol": "_winner",
  "class": "M6-fallback-removal",
  "find": "                    0 if _name_in_text(name, j) else 1,",
  "replace": "                    0,",
  "why": "with no url evidence, no incumbency and equal source ranks, the posting's own words are the only evidence there is: the אסם/Nestlé pair is decided here, and without this key it falls through to the caseless-name and shortest-identity keys and crowns a PARKED Nestlé over the live אסם the text names twice. Killed by test_the_winner_reads_the_posting_and_stops_calling_hebrew_a_lowercase_stub.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "winner-calls-a-caseless-name-a-lowercase-stub",
  "file": "pipeline/roles.py",
  "symbol": "_winner",
  "class": "M1-gate-narrowed",
  "find": "                    1 if (name == name.lower() and name != name.upper()) else 0,",
  "replace": "                    1 if name == name.lower() else 0,",
  "why": "Hebrew, Arabic and CJK have no case, so `name == name.lower()` is true of every name in them and the 'kornit' stub demotion fires on all of them — demoting אסם under Nestlé. The guard costs nothing where case exists (the kornit/Kornit Digital election is unchanged). Killed by test_the_winner_reads_the_posting_and_stops_calling_hebrew_a_lowercase_stub.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "name-evidence-vacuous-on-an-empty-token-set",
  "file": "pipeline/roles.py",
  "symbol": "_name_in_text",
  "class": "M6-fallback-removal",
  "find": "    if not toks:\n        return False",
  "replace": "    if not toks:\n        return True",
  "why": "`all()` over an empty set is True, so a name that tokenizes to nothing would get free evidence — the BACKLOG 510 shape (identity_ok vouching for a newspaper). Killed by test_name_evidence_refuses_an_empty_token_set_and_a_partial_match.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "email-gate-after-the-payload",
  "file": "pipeline/run.py",
  "symbol": "run",
  "class": "M1-gate-narrowed",
  "find": "    email_jobs = [j for j in email_jobs if _mailable(j)]",
  "replace": "    email_jobs = list(email_jobs)",
  "why": "the whole point of gating at SELECTION: `out/digest-<date>.json` is built from email_jobs and mark_sent burns every row in it, so a role hidden later (as a mangled title already was) is marked delivered and `filter_new` never offers it again. Killed by test_a_role_held_for_its_text_is_never_marked_sent_and_is_mailed_when_it_lands.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "board-gate-before-the-closure-snapshot",
  "file": "pipeline/run.py",
  "symbol": "run",
  "class": "M2-order-swap",
  "find": "    alive_jobs = list(board_jobs)             # the role record judges closure on THIS, not the capped page\n    # The same ruling on the board",
  "replace": "    board_jobs = [j for j in board_jobs if _publishable(j)]\n    alive_jobs = list(board_jobs)             # the role record judges closure on THIS, not the capped page\n    # The same ruling on the board",
  "why": "a role held for having no description is still OPEN. Gating before the closure snapshot tells the record it left its board: it is archived as filled, and 'reopens' the morning its text lands. Killed by test_holding_a_role_off_the_board_never_tells_the_record_it_closed.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "archive-is-what-was-rendered-not-what-is-alive",
  "file": "pipeline/run.py",
  "symbol": "run",
  "class": "M6-fallback-removal",
  "find": "    onboard = {(j[\"company\"], j[\"title\"]) for j in alive_jobs}   # = still open",
  "replace": "    onboard = {(j[\"company\"], j[\"title\"]) for j in board_jobs}   # = still open",
  "why": "the archive is `matched` minus what is ALIVE. Minus what was RENDERED, every role the board did not draw — one held for its text, one trimmed by the BOARD_MAX_ROLES backstop — is published on a page headed 'no longer on the employer's careers page' while it is live. Killed by test_a_role_with_no_usable_description_reaches_neither_the_board_nor_the_mail.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "readmit-without-the-held-stamp",
  "file": "pipeline/run.py",
  "symbol": "run",
  "class": "M1-gate-narrowed",
  "find": "        held = str((ledger.records.get(store.merge_key(j)) or {}).get(\"held_since\") or \"\")[:10]\n        return bool(held) and held >= _readmit_from",
  "replace": "        t = (getattr(ledger, \"text\", {}) or {}).get(store.merge_key(j))\n        upd = str((t or {}).get(\"updated\") or \"\")[:10]\n        return bool(upd) and upd >= _readmit_from",
  "why": "the re-offer exists for roles THIS gate withheld. Keyed on the text merely being fresh instead, it re-offers any alive unsent role whose description jd-text backfilled — quietly mailing the unsent backlog and rewriting what the 48h promise means (BACKLOG 310, deliberately still open). The golden rehearsal caught the loose version doing exactly that. Killed by test_a_role_held_for_its_text_is_never_marked_sent_and_is_mailed_when_it_lands.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "deep-empty-claim-unconditional",
  "file": "deep_validate.py",
  "symbol": "apply_verdict",
  "class": "M2-gate-inversion",
  "find": "                      or _gate.board_vouches(name, tok or \"\", _cand) is True",
  "replace": "                      or True",
  "why": "the 2026-09-01 defect restored: `empty` is reached before activation_verdict asks whose board it is, so a name-alike's board answering zero (Expedia for Experda, Prologis for Prologic) is recorded as THIS company having no roles -- the claim ARCHITECTURE section 2's first verdict rule forbids",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retraction-seen-id-arm-outranks-the-owner",
  "file": "pipeline/roles.py",
  "symbol": "match_all",
  "class": "M5-population-widening",
  "find": "                or (sid_keys & e[\"_urls\"]\n                    and not e.get(\"_owned\") and not e.get(\"role_id\"))]",
  "replace": "                or (sid_keys & e[\"_urls\"])]",
  "why": "a seen_id is not an address a record claims, it is every address a fetch ever bound to it. Without the owner check one stray id makes a retraction take a SECOND, live role: `percepto|senior product analyst` (open, accept, on the board) carries the own url of `percepto|data insights operations` as a `scrape:` id, and 1 of 39 lines took both records. Nothing alarms - `_hits` counts lines answered, never records taken. Killed by test_a_retraction_binds_to_the_record_that_owns_its_address_never_a_stray_seen_id.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "resolved-twin-left-in-the-merged-set",
  "file": "pipeline/roles.py",
  "symbol": "_resolve_claims",
  "class": "M4-guard-removal",
  "find": "                    for i in done:\n                        lo = merged[alive[i]]\n                        sids |= set(aug[i].get(\"seen_ids\") or [])\n                        srcs |= set(lo.get(\"sources\") or [lo.get(\"ats_platform\") or \"\"])\n                        drop.add(alive[i])",
  "replace": "                    for i in done:\n                        lo = merged[alive[i]]\n                        sids |= set(aug[i].get(\"seen_ids\") or [])\n                        srcs |= set(lo.get(\"sources\") or [lo.get(\"ats_platform\") or \"\"])",
  "why": "a fold recorded in the ledger that is not applied to this run's `merged` leaves two merge_keys behind one `filter_new` kill-switch: both are upserted, one can never be emailed, and `id_collisions` alarms every morning for a pair already resolved (HoneyBook, the 2026-09-02 mail). Killed by test_an_already_folded_twin_leaves_this_runs_merged_set_and_stops_colliding.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "resolved-twin-honours-a-verdict-from-outside-the-group",
  "file": "pipeline/roles.py",
  "symbol": "_resolve_claims",
  "class": "M1-gate-narrowed",
  "find": "                if len(live) == 1 and all(_terminus(gkeys[i]) == gkeys[live[0]]\n                                          for i in done):",
  "replace": "                if len(live) == 1:",
  "why": "applying a stored verdict is only safe where that verdict names a survivor IN this group. A `superseded_by` pointing elsewhere, taken as an election of whoever happens to be the group's one live member, drops a record into a winner the ledger never chose - the supersede CYCLE wave B found, with both halves off every product and `ledger N = store N` still green. Killed by test_a_stored_supersede_pointing_outside_the_group_is_still_refused.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retraction-ownership-reads-only-the-ledger",
  "file": "pipeline/roles.py",
  "symbol": "bind",
  "class": "M5-population-widening",
  "find": "        owned = {_url_key(r[\"url\"]) for r in list(records.values()) + list(extra or ())",
  "replace": "        owned = {_url_key(r[\"url\"]) for r in list(records.values())",
  "why": "`load()` DROPS a malformed ledger line and still returns `ok` while bad <= CORRUPT_FRAC, so a ledger-only ownership set fails OPEN: one bad line for the record that owns a retraction's address re-arms the stray-id arm and withdraws the LIVE role, with status=ok, frozen=False and no alarm anywhere. Reproduced by an adversarial wave on this session's own diff before the push. Killed by test_a_dropped_ledger_line_cannot_re_arm_the_stray_id_arm.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retraction-role-id-line-still-reaches-by-seen-id",
  "file": "pipeline/roles.py",
  "symbol": "match_all",
  "class": "M1-gate-narrowed",
  "find": "                    and not e.get(\"_owned\") and not e.get(\"role_id\"))]",
  "replace": "                    and not e.get(\"_owned\"))]",
  "why": "a line carrying a `role_id` has already said which record it means, so the seen_ids arm can only ever ADD one its author did not name. This is the lock that holds where ownership cannot be established at all - an unbound Retractions, or a day both stores lost the owning record. Killed by test_a_role_id_line_never_reaches_for_a_record_it_did_not_name.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "resolved-twin-ignores-the-supersede-chain",
  "file": "pipeline/roles.py",
  "symbol": "_resolve_claims",
  "class": "M1-gate-narrowed",
  "find": "                if len(live) == 1 and all(_terminus(gkeys[i]) == gkeys[live[0]]\n                                          for i in done):",
  "replace": "                if len(live) == 1 and all((rows.get(gkeys[i]) or {}).get(\"superseded_by\")\n                                          == gkeys[live[0]] for i in done):",
  "why": "two folds on different days make a chain - A folded into B in August, B into C in September, and today only A and C are fetched. A direct `superseded_by == the survivor` test leaves A in `merged`, so both keys are upserted and the seen-id collision alarm fires for ever: the defect this branch exists to remove, one fold later. Killed by test_a_supersede_chain_reaches_the_survivor_of_a_twin_group.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "retraction-seen-id-fallback-deleted",
  "file": "pipeline/roles.py",
  "symbol": "match_all",
  "class": "M6-fallback-removal",
  "find": "                or (sid_keys & e[\"_urls\"]\n                    and not e.get(\"_owned\") and not e.get(\"role_id\"))]",
  "replace": "                or False]",
  "why": "the id arm is NARROWED by 545, never deleted: a posting whose record has since been re-keyed - a re-scrape that moved the card to a new address - is named by nothing except the `seen_id` that remembers where we fetched it, and the retraction would silently stop catching it. `tools/guard_kill.py` named the preservation test CANNOT-FAIL without this record, which is the honest reading: a test that asserts UNCHANGED behaviour is verified by a mutation, not by a revert. Killed by test_a_retraction_no_record_owns_still_matches_by_its_seen_id.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "anon-employer-substring",
  "file": "pipeline/recruiters.py",
  "symbol": "is_anonymous_employer",
  "class": "M1-gate-narrowed",
  "find": "    return bool(n) and bool(_ANON_ANY.search(n)) and bool(_ANON_ONLY.match(n))",
  "replace": "    return bool(n) and bool(_ANON_ANY.search(n))",
  "why": "drops the WHOLE-NAME half and leaves the substring half, which is the shape that makes this class dangerous: `Confidential Computing Inc`, `Stealth Security Ltd` and `Anonymous Analytics Ltd` are real-company names carrying an anonymiser word, and refusing them mutes a real employer at the cache write AND the names funnel in one step. Killed by test_an_anonymised_employer_name_is_refused_whole_name_not_by_substring.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "anon-employer-drop",
  "file": "pipeline/recruiters.py",
  "symbol": "_is_recruiter_name",
  "class": "M6-fallback-removal",
  "find": "    if is_anonymous_employer(n):\n        return True\n",
  "replace": "",
  "why": "removes the branch that carries the rule to every EXISTING caller -- the read-side gate in fetchers.fetch_discovery, auto_expand and the queue drain, not only the two intake sites. The same commit deleted `confidential` and `confidential careers` from `_CONFIRMED` because this branch subsumes them, so dropping it silently un-refuses two names that had been refused since 2026-08-17. Killed by test_an_anonymised_employer_name_is_refused_whole_name_not_by_substring.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "desc-appeal-removed",
  "file": "pipeline/seniority.py",
  "symbol": "_desc_appealed",
  "class": "M1-gate-removal",
  "find": "    if not looks_like_jd(d) or _desc_is_ml(d):\n        return False",
  "replace": "    if True:\n        return False",
  "why": "the description appeal is the only route `Zoll Medical | Business Operations, CMS` has to the LLM: its title carries no analytics word at all, so no phrase list reaches it and the gate rejects it deterministically with no call to disagree - silent exclusion, the failure class ARCHITECTURE.md section 8 is about. Killed by test_the_description_appeal_routes_the_third_measured_false_negative_to_the_llm.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_description_appeal_routes_the_third_measured_false_negative_to_the_llm"
  ]
 },
 {
  "id": "desc-appeal-promoted-to-strong",
  "file": "pipeline/seniority.py",
  "symbol": "_relevance",
  "class": "M2-gate-inversion",
  "find": "            or _GATE_APPEAL.search(title_l) or _desc_appealed(desc)):\n        return \"signal\"",
  "replace": "            or _GATE_APPEAL.search(title_l) or _desc_appealed(desc)):\n        return \"strong\" if _desc_appealed(desc) else \"signal\"",
  "why": "an appeal may only ever ASK. `strong` enables the strong+senior fast-accept and would admit a title UNREAD on nothing but the words its description happens to contain - the promotion `docs/decisions/2026-09-01-analytics-engineer-boundary.md` rejects by name. Killed by test_the_description_appeal_routes_the_third_measured_false_negative_to_the_llm.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_description_appeal_routes_the_third_measured_false_negative_to_the_llm"
  ]
 },
 {
  "id": "desc-appeal-escapes-the-no-llm-guard",
  "file": "pipeline/seniority.py",
  "symbol": "_gate_appealed",
  "class": "M4-guard-removal",
  "find": "    if not (_GATE_APPEAL.search(title_l) or _desc_appealed(desc)):",
  "replace": "    if not _GATE_APPEAL.search(title_l):",
  "why": "`_gate_appealed` is a hand-mirrored counterfactual of `_relevance` rather than a call to it, so an arm added there and forgotten here hands `_sig_accept_nollm` an accept the gate never earned. The appeal's whole evidence is that the posting mentions the right words, which is exactly what a breaker-open fallback would then treat as a verdict. Killed by test_a_row_the_description_appeal_rescued_is_never_accepted_without_the_llm.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_row_the_description_appeal_rescued_is_never_accepted_without_the_llm"
  ]
 },
 {
  "id": "desc-appeal-survives-a-shared-careers-page",
  "file": "pipeline/seniority.py",
  "symbol": "_classify",
  "class": "M4-guard-removal",
  "find": "        if shared and rel == \"signal\":\n            bare = _relevance(title_l, company_l)\n            if bare in (\"excluded\", \"none\"):\n                return {\"relevance\": bare, \"seniority\": sen, \"decision\": \"reject\",\n                        \"path\": \"keyword\", \"contract\": self.contract,\n                        \"reason\": (\"engineering/ML/non-data-analyst title\" if bare == \"excluded\"\n                                   else \"no analytics signal in title\")}\n        # The posting's own text places it outside Israel",
  "replace": "        # The posting's own text places it outside Israel",
  "why": "the gate reads the description eighteen lines before the shared-text guard can know it belongs to a different role. Without this, one careers PAGE stored as every posting's description demotes every refused title at that employer to the LLM on one blob - Get SAT had ten roles sharing 4,000 characters - and judging a posting on soup is what the 2026-09-02 session paid for twice (Prisma, Ballerine). Killed by test_a_shared_careers_page_takes_back_the_hearing_its_text_bought. Re-anchored 2026-09-11 (classifier): the keyword reject dict gained a `contract` key. Re-anchored 2026-09-13 (classifier): the geography head (566) now sits between this guard and the description test.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_shared_careers_page_takes_back_the_hearing_its_text_bought"
  ]
 },
 {
  "id": "desc-appeal-drops-the-tool-word",
  "file": "pipeline/seniority.py",
  "symbol": "_desc_appealed",
  "class": "M5-population-widening",
  "find": "    return bool(_DESC_APPEAL_PHRASE.search(d) and _DESC_APPEAL_OUTPUT.search(d)\n                and _DESC_APPEAL_TOOL.search(d))",
  "replace": "    return bool(_DESC_APPEAL_PHRASE.search(d) and _DESC_APPEAL_OUTPUT.search(d))",
  "why": "the conjunction IS the rule: without the tool word the appeal admits 52 cards against 23, measured 2026-09-03, and a posting that says `data analysis` and `report` without naming one tool is the shape every consultancy and every operations posting has. An adversarial wave applied exactly this and the whole suite stayed green. Killed by test_every_conjunct_of_the_description_appeal_has_a_posting_that_fails_it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_every_conjunct_of_the_description_appeal_has_a_posting_that_fails_it"
  ]
 },
 {
  "id": "desc-appeal-drops-the-output-word",
  "file": "pipeline/seniority.py",
  "symbol": "_desc_appealed",
  "class": "M5-population-widening",
  "find": "    return bool(_DESC_APPEAL_PHRASE.search(d) and _DESC_APPEAL_OUTPUT.search(d)\n                and _DESC_APPEAL_TOOL.search(d))",
  "replace": "    return bool(_DESC_APPEAL_PHRASE.search(d) and _DESC_APPEAL_TOOL.search(d))",
  "why": "same conjunction, the other arm: 28 cards against 23, and dropping it admits any engineering posting whose stack line says SQL beside the words `data analysis`. Also survived the first draft of the guards. Killed by test_every_conjunct_of_the_description_appeal_has_a_posting_that_fails_it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_every_conjunct_of_the_description_appeal_has_a_posting_that_fails_it"
  ]
 },
 {
  "id": "desc-appeal-floor-is-a-length",
  "file": "pipeline/seniority.py",
  "symbol": "_desc_appealed",
  "class": "M3-branch-narrowing",
  "find": "    if not looks_like_jd(d) or _desc_is_ml(d):",
  "replace": "    if len(d.strip()) < MIN_DESC or _desc_is_ml(d):",
  "why": "a nav bar and a cookie banner clear 300 characters -- `_classify` records that exact migration where `has_text` stopped being `len(raw) >= MIN_DESC` -- and under a length floor two of the cards this rule admitted were 4,000 characters of a careers site's own menu listing `Tableau, PowerBI, Qlik`. The floor is also what makes the rule cost no Bright Data: `enrich_scrape_jd.py` skips an already-filled card on `looks_like_jd` (:143) BEFORE the title gate (:173), so only a rule sharing that predicate can be free by construction. Killed by test_the_description_appeal_reads_a_posting_and_not_a_page.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_description_appeal_reads_a_posting_and_not_a_page"
  ]
 },
 {
  "id": "desc-appeal-un-fires-on-a-real-signal",
  "file": "pipeline/seniority.py",
  "symbol": "_classify",
  "class": "M2-gate-inversion",
  "find": "        if shared and rel == \"signal\":\n            bare = _relevance(title_l, company_l)\n            if bare in (\"excluded\", \"none\"):\n                return {\"relevance\": bare, \"seniority\": sen, \"decision\": \"reject\",",
  "replace": "        if shared and rel == \"signal\":\n            bare = \"none\"\n            if bare in (\"excluded\", \"none\"):\n                return {\"relevance\": bare, \"seniority\": sen, \"decision\": \"reject\",",
  "why": "the un-fire asks what the gate would have said with NO text, never `did the description arm fire`. Hard-coding the refusal takes the hearing away from a title that reached `signal` on the gate's own vocabulary, so a genuine analyst role at an employer whose scraper stored one careers page is rejected with no call to disagree -- silent exclusion. The first draft's guard could not catch this: it used a `strong` title, which never enters the branch. Killed by test_a_shared_careers_page_takes_back_the_hearing_its_text_bought.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_shared_careers_page_takes_back_the_hearing_its_text_bought"
  ]
 },
 {
  "id": "desc-appeal-backfill-loses-its-counters",
  "file": "pipeline/seniority.py",
  "symbol": "judge_backfill",
  "class": "M4-guard-removal",
  "find": "                self.backfill_keyword += 1\n                self.backfill_no += bool(published)\n",
  "replace": "",
  "why": "the backfill's un-fire is assembled by hand rather than through the `_reject` closure, so it has to move both counters itself. `backfill_no` is what the retraction alarm reads -- a published row judged NO needs a human to write a line -- and dropping it makes the alarm under-count silently, which is the class ARCHITECTURE.md section 8 is about. Killed by test_a_shared_careers_page_takes_back_the_hearing_its_text_bought.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_shared_careers_page_takes_back_the_hearing_its_text_bought"
  ]
 },
 {
  "id": "jd-head-skip-uses-the-classifier-regex-again",
  "file": "pipeline/jdfill.py",
  "symbol": "extract_jd",
  "class": "M5-population-widening",
  "find": "    rs = _HEAD_SKIP.search(text)\n",
  "replace": "    from .seniority import _ROLE_START\n    rs = _ROLE_START.search(text)\n",
  "why": "`seniority._ROLE_START` is the classifier's rough starting point for a keyword count, and its alternatives are prose: `as an? `, `you'?ll `, `in this role`, and `requirements?`. Used as a CUT it published descriptions beginning \"as a Great Place to Work\" and descriptions that open at Requirements with the responsibilities deleted -- 105 of 244 stored rows had a mid-line first hit. Killed by test_extract_jd_starts_at_a_section_heading_and_never_mid_sentence.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_extract_jd_starts_at_a_section_heading_and_never_mid_sentence"
  ]
 },
 {
  "id": "jd-tail-rail-loses-its-hebrew-twin",
  "file": "pipeline/jdfill.py",
  "symbol": "_PAGE_FURNITURE",
  "class": "M1-gate-narrowed",
  "find": "    r\"עבודות דומות|\"\n",
  "replace": "",
  "why": "the Hebrew-locale rail heading. Without it `furniture_at` returns None on every il.linkedin.com capture this board actually stores, which is how 49 of 177 published rows carried 25 other employers' job titles inside their own description. The English `similar jobs` never fires on those pages, and on the 2026-09-11 corpus the `Show more` pair always cuts first -- so the rail marker is the guard for a posting LinkedIn does not fold, where the rail is the first chrome on the page. Killed by test_the_rail_is_furniture_even_when_the_page_renders_no_show_more_button.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_rail_is_furniture_even_when_the_page_renders_no_show_more_button"
  ]
 },
 {
  "id": "jd-head-cut-moves-inside-jd-body",
  "file": "pipeline/jdfill.py",
  "symbol": "jd_body",
  "class": "M5-population-widening",
  "find": "    cut = furniture_at(text)\n    return (text or \"\") if cut is None else (text or \"\")[:cut].rstrip()",
  "replace": "    text = strip_head(text or \"\")\n    cut = furniture_at(text)\n    return (text or \"\") if cut is None else (text or \"\")[:cut].rstrip()",
  "why": "the placement decision, as a mutation. `jd_body` IS the semantics of `looks_like_jd`, and `roles.better_description` RETURNS `jd_body(...)` -- so a head rule here becomes a second head-SHORTENING path writing BOTH stores with none of `_reclean`'s floor or share ceiling, which is the shape wave 2 measured at 13 rows and 39,956 characters. Killed by test_the_page_header_is_cut_at_fetch_and_never_inside_jd_body.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_page_header_is_cut_at_fetch_and_never_inside_jd_body",
   "tests/test_units.py::test_a_head_repair_is_handed_back_unless_the_ledger_line_is_retracted",
   "tests/test_units.py::test_the_head_cut_is_not_wired_into_the_two_readers_that_must_not_have_it"
  ]
 },
 {
  "id": "jd-page-slice-guard-removed",
  "file": "pipeline/jdfill.py",
  "symbol": "looks_like_jd",
  "class": "M1-gate-removal",
  "find": "    if _is_markup_soup(text) or _is_listing_card(text) or page_slice(text):\n",
  "replace": "    if _is_markup_soup(text) or _is_listing_card(text):\n",
  "why": "`567`: a capture that begins mid-sentence AND ends on the cap is a slice of a page -- `gamida cell|senior business analyst commercial data analytics` is 6,000 characters carrying six field-sales bullets from another posting, and it turned a classifier verdict to NO. Without the guard it reads as a job description to every keyword rule. Killed by test_a_page_slice_is_not_a_job_description.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_page_slice_is_not_a_job_description"
  ]
 },
 {
  "id": "jd-refuted-column-ignored-on-the-way-in",
  "file": "enrich_matched_jd.py",
  "symbol": "_store_text",
  "class": "M1-gate-removal",
  "find": "    if disowned and jdfill.refute_key(text) in disowned:\n",
  "replace": "    if False and jdfill.refute_key(text) in disowned:\n",
  "why": "a text this row was told is not its posting, offered again by tomorrow's cache card, must lose whatever its length. Without this the repair of a wrong-but-longer description is handed straight back the next morning -- 572, measured on prisma photonics on 2026-09-02. Killed by test_a_refutation_outlives_the_run_that_reached_it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_refutation_outlives_the_run_that_reached_it"
  ]
 },
 {
  "id": "jd-refuted-column-does-not-open-the-ratchet",
  "file": "enrich_matched_jd.py",
  "symbol": "_store_text",
  "class": "M6-fallback-removal",
  "find": "    if disowned and jdfill.refute_key(have) in disowned:\n        refuted = True\n",
  "replace": "",
  "why": "the other half: what is STORED having been disowned is what lets the shorter true posting in. In-memory `refuted` only survives one run and only a FRESH model verdict feeds it, so without the column the row is stuck for ever with no alarm (572's cheaper half). Killed by test_a_refutation_outlives_the_run_that_reached_it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_refutation_outlives_the_run_that_reached_it"
  ]
 },
 {
  "id": "jd-refutation-read-below-the-quality-switch",
  "file": "enrich_matched_jd.py",
  "symbol": "_quality_pass",
  "class": "M3-branch-narrowing",
  "find": "    stored = _durably_refuted(conn, every)\n    if os.environ.get(\"JD_QUALITY\", \"1\") == \"0\":\n        c0 = Counter()\n        if stored:\n            c0[\"refuted_stored\"] = len(stored)\n        return set(stored), set(stored), c0",
  "replace": "    if os.environ.get(\"JD_QUALITY\", \"1\") == \"0\":\n        return set(), set(), Counter()\n    stored = _durably_refuted(conn, every)",
  "why": "the durable refutation needs no model, so reading it below the switch silences the channel in exactly the state where a repair matters most: a runner whose token is refused, or a pass run with the tier off. Killed by test_the_stored_refutation_is_read_with_the_tier_switched_off.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_stored_refutation_is_read_with_the_tier_switched_off"
  ]
 },
 {
  "id": "jd-mid-sentence-head-buys-a-model-call",
  "file": "enrich_matched_jd.py",
  "symbol": "_quality_pass",
  "class": "M6-fallback-removal",
  "find": "        if why == \"mid-sentence-head\":\n",
  "replace": "        if False:\n",
  "why": "a text that begins in the middle of a sentence is incomplete at a place we can see; there is nothing for a model to adjudicate and the call is waste. Without the branch the row buys a verdict that cannot change the answer. Killed by test_a_mid_sentence_head_reaches_the_todo_and_buys_no_model_call.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_mid_sentence_head_reaches_the_todo_and_buys_no_model_call"
  ]
 },
 {
  "id": "confirmed-loses-peak-innovation",
  "file": "pipeline/recruiters.py",
  "symbol": "_CONFIRMED",
  "class": "M7-constant-drift",
  "find": "    \"peak innovation\", \"peak tech innovation\",\n",
  "replace": "    \"peak tech innovation\",\n",
  "why": "classifier 2026-09-11: the registry parked Peak Innovation `recruiter 2026-08-31` and the name was not in _CONFIRMED, so the purge, the discovery drop and the row filter all let `peak innovation|data analyst` publish for a week (518@roles). Dropping the entry re-opens exactly that gap; the class test reads companies.csv's dated recruiter verdicts and demands is_recruiter() True for each. Killed by test_every_registry_recruiter_verdict_is_a_mechanism.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_every_registry_recruiter_verdict_is_a_mechanism"
  ]
 },
 {
  "id": "reject-map-overwrites-a-live-accept",
  "file": "pipeline/roles.py",
  "symbol": "_record_run",
  "class": "M1-gate-removal",
  "find": "            if rec is None or rid in by_key:\n                continue                      # never seen, or this run accepted it: live wins",
  "replace": "            if rec is None:\n                continue",
  "why": "543's reject map must never reach a role THIS RUN accepted. A mixed group (some copies rejected, one accepted) puts the same role_id in both the map and `merged`; without the `by_key` guard the reject is applied after the live stamp and the dataset contradicts the board the same run renders. Killed by test_record_run_stamps_the_runs_own_rejects_and_withdraws_every_rejected_row.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "reject-map-stamps-a-withdrawn-record",
  "file": "pipeline/roles.py",
  "symbol": "_record_run",
  "class": "M4-guard-removal",
  "find": "            if (rec.get(\"status\") or \"open\") not in (\"open\", \"closed\"):\n                continue\n            new_cls = _class_of(cls, self.live_contract)",
  "replace": "            new_cls = _class_of(cls, self.live_contract)",
  "why": "a withdrawn or purged record carries a HUMAN's standing verdict and a superseded one is not published at all. Stamping a machine reject over any of them overwrites an adjudication with a guess and re-counts rows that left the dataset days ago. Killed by test_record_run_stamps_the_runs_own_rejects_and_withdraws_every_rejected_row.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "class-contract-guessed-for-a-cache-hit",
  "file": "pipeline/roles.py",
  "symbol": "_class_of",
  "class": "M3-branch-narrowing",
  "find": "    if \"contract\" not in out and live_contract and out.get(\"path\") in (\"keyword\", \"llm\"):",
  "replace": "    if \"contract\" not in out and live_contract:",
  "why": "544's whole point is that the column must not LIE about provenance. An `llm_cache` verdict may have been made under any retired contract and only the classifier's seam knows which; stamping today's onto it publishes a stale verdict as a current one, which is exactly the reading the column was added to make impossible. Killed by test_the_class_cell_carries_the_contract_and_never_guesses_one.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "title-canon-cuts-any-trailing-name",
  "file": "pipeline/roles.py",
  "symbol": "_canon",
  "class": "M4-guard-removal",
  "find": "    m = _OWN_TAIL.match(t)\n    if m and _same_employer(m.group(2), company):",
  "replace": "    m = _OWN_TAIL.match(t)\n    if m:",
  "why": "the trailing-name rule may only cut the EMPLOYER's own name. Without the test it eats a real part of the role: `GTM Business Analyst - Marketing` becomes `GTM Business Analyst`, `BI Developer - Payments` loses its team, and the truncated title becomes half of merge_key - a new role_id for a posting we already publish. Killed by test_canonical_title_cuts_only_furniture_on_the_measured_population.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "title-canon-drops-the-first-segment",
  "file": "pipeline/roles.py",
  "symbol": "_canon",
  "class": "M3-branch-narrowing",
  "find": "        keep = [segs[0]] + [s for s in segs[1:] if not _furniture_only(s, location)]",
  "replace": "        keep = [s for s in segs if not _furniture_only(s, location)]",
  "why": "the first ` | ` segment is the role's NAME and is never a candidate for the furniture cut. Dropping that anchor lets a title whose first segment happens to read as furniture vanish entirely, and the record is then keyed on what is left. Killed by test_canonical_title_cuts_only_furniture_on_the_measured_population.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "title-rename-orphans-the-description",
  "file": "pipeline/roles.py",
  "symbol": "_rename_record",
  "class": "M6-fallback-removal",
  "find": "        t = self.text.pop(rid, None)\n        if t is not None:",
  "replace": "        t = self.text.get(rid)\n        if False:",
  "why": "roles_text.jsonl joins on role_id. A rename that leaves the text line under the OLD key orphans the description, and `flush`'s orphan prune then DELETES it - the role loses the text that took a paid fetch to get, and the export publishes it as description-less. RE-ANCHORED 2026-09-18 onto `_rename_record`, the body `_fold_titles` and `_fold_aliases` now share: the same mutant, one record, so the alias sweep's rename is covered by it too (a second record aimed at the same lines would be an equivalent mutant, not a second guard). Killed by test_the_title_sweep_renames_the_record_across_all_three_stores; also killed by test_alias_sweep_renames_a_foldable_record_with_no_twin.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "title-rename-not-declared-to-the-shrink-guard",
  "file": "pipeline/roles.py",
  "symbol": "flush",
  "class": "M6-fallback-removal",
  "find": "                dump(self.path, out, may_drop=set(self.renamed))",
  "replace": "                dump(self.path, out)",
  "why": "the shrink guard compares KEY SETS, so a rename - which keeps the record count identical - still loses a key and is refused. sqlite is rekeyed in the same seam, so a refused ledger write leaves the two stores split and yesterday's file standing, every morning. Killed by test_the_title_sweep_renames_the_record_across_all_three_stores.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "rekey-overwrites-an-existing-row",
  "file": "pipeline/store.py",
  "symbol": "rekey_matched",
  "class": "M4-guard-removal",
  "find": "        if self.conn.execute(\"SELECT 1 FROM matched WHERE mkey=?\", (new_mkey,)).fetchone():\n            return False",
  "replace": "        pass",
  "why": "a rename onto a key another role already owns destroys that role's row - its first_seen, its seen_ids and the delivery marks that stop it being emailed again. The refusal is what makes the caller fold into the twin instead, keeping both histories. Killed by test_rekey_matched_refuses_an_existing_key_and_repoints_superseded_by.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "page-closed-reads-the-whole-description",
  "file": "pipeline/roles.py",
  "symbol": "page_says_closed",
  "class": "M3-branch-narrowing",
  "find": "    return bool(_PAGE_CLOSED.search(str(desc or \"\")[:PAGE_CLOSED_WINDOW]))",
  "replace": "    return bool(_PAGE_CLOSED.search(str(desc or \"\")))",
  "why": "the marker is a verdict only where LinkedIn's chrome puts it, in the first few hundred characters. Searched over the whole text, a job description that merely says applications are no longer accepted by email closes a live posting on its own prose. Killed by test_page_says_closed_reads_the_chrome_marker_and_never_a_quote_in_the_body.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "page-closed-ignores-the-employers-own-board",
  "file": "pipeline/roles.py",
  "symbol": "page_closed",
  "class": "M4-guard-removal",
  "find": "    if not srcs or not all(str(s).startswith(\"discovery-\") for s in srcs):\n        return False",
  "replace": "    pass",
  "why": "a LinkedIn mirror is up to 21 days stale by construction, so it may only speak for a posting we know from nowhere else. Without this gate a stale mirror closes a role the employer's own board is still listing today - the board is the authority, which is the rule the whole registry rests on. Killed by test_page_closed_is_only_for_a_linkedin_row_with_no_board_of_its_own.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "page-closure-stamped-with-todays-date",
  "file": "pipeline/roles.py",
  "symbol": "_record_run",
  "class": "M3-branch-narrowing",
  "find": "                    rec[\"status\"], rec[\"closed_on\"] = \"closed\", why[1]",
  "replace": "                    rec[\"status\"], rec[\"closed_on\"] = \"closed\", run_date",
  "why": "`closed_on` is published and a reader takes it for the day the posting went away. These postings closed on the day the page we stored said so - often weeks earlier - and stamping today claims we watched it happen, in the one column a dataset user would use to measure how long roles stay open. Killed by test_record_run_closes_a_page_closed_row_on_the_day_its_text_was_captured.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "page-closed-row-is-upserted-anyway",
  "file": "pipeline/run.py",
  "symbol": "main",
  "class": "M4-guard-removal",
  "find": "        if store.merge_key(j) in _page_closed:\n            continue",
  "replace": "        if False:\n            continue",
  "why": "the discovery cache re-serves the card for up to 21 days. Upserting it puts the key back in `closed_keys()`'s reappearance path, so first_seen resets to today and a fresh episode is minted EVERY morning - a daily `reopened 1` for a posting that is gone, and a role that keeps re-entering the 48h email window. The skip lives in run.py's upsert loop, so no ledger-level test can reach it: the killer drives a whole `run.run` over a page-closed record whose card arrives again. SURVIVED on 2026-09-11 (infra's run 34622790874, shard 6) because this record shipped without one.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_page_closed_role_is_not_upserted_so_the_cache_cannot_reopen_it_daily"
  ]
 },
 {
  "id": "page-closed-re-closes-a-purged-record",
  "file": "pipeline/roles.py",
  "symbol": "_record_run",
  "class": "M4-guard-removal",
  "find": "            elif (prev_status not in RETRACTABLE and judged(rec.get(\"company\"))\n                    and page_closed(rec, rec)):",
  "replace": "            elif judged(rec.get(\"company\")) and page_closed(rec, rec):",
  "why": "`purged` (the company was never an employer) and `withdrawn` (a human ruled this posting out) are standing verdicts, and `closed` is a weaker, different claim - the posting was gone from the board. Without the guard the page arm overwrites one with the other: measured on the committed store, comblack|business intelligence developer is purged AND carries the marker, and the arm reported 5 closures where 4 are real. The purge branch above only reaches a row whose company is in THIS run's never_ours, so it cannot shield a purged record here. Killed by test_a_purged_or_withdrawn_record_is_never_re_closed_by_its_page.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hiring-call-leaves-a-fragment",
  "file": "pipeline/roles.py",
  "symbol": "_canon",
  "class": "M4-guard-removal",
  "find": "            if (rest and _store._norm(rest) and not _furniture_only(rest, location)\n                    and _WORD_START.match(rest)):",
  "replace": "            if rest and _store._norm(rest) and not _furniture_only(rest, location):",
  "why": "whatever a hiring call leaves behind must BEGIN a role name. Measured over the 6,368 cards in both caches: without this a board that spaces out the gender form leaves `/h mnhl /t ...` and a card whose whole title is the call leaves `- <category>`. A mangled title is worse than the blob it came from, because it becomes half of a role_id and a new record. Killed by test_a_hiring_call_never_leaves_a_fragment_of_itself_behind.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "page-closed-skips-the-scoped-guard",
  "file": "pipeline/roles.py",
  "symbol": "_record_run",
  "class": "M4-guard-removal",
  "find": "            elif (prev_status not in RETRACTABLE and judged(rec.get(\"company\"))\n                    and page_closed(rec, rec)):",
  "replace": "            elif prev_status not in RETRACTABLE and page_closed(rec, rec):",
  "why": "a role closes ONLY where the run actually looked, which every other arm of the ladder enforces through `judged`. The page arm's evidence is stored text, so the check looks redundant - but without it a scoped run closes roles at companies it never fetched: measured 4 closures from `--only \"Wix\"` against the real store, the exact command CLAUDE.md hands every agent as the harmless local run. A full digest is unaffected. Killed by test_a_scoped_run_never_closes_a_page_closed_role_it_did_not_look_at.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "jd-closed-line-dropped-at-capture",
  "file": "pipeline/jdfill.py",
  "symbol": "extract_jd",
  "class": "M6-fallback-removal",
  "find": "    return with_closed_line(full, text)[:DESC_MAX]\n",
  "replace": "    return text[:DESC_MAX]\n",
  "why": "a posting captured CLOSED loses `No longer accepting applications` with the rest of the header `strip_head` cuts, and then nothing stamps it: `roles.page_closed` has no text arm, no stamp and, on a first sighting, no ledger memory, so the role stays open for the 21 days the discovery cache re-serves the card (587). The job dict's `jd_why` reaches no reader in the same run, so the kept first line is the one in-lane carrier. Killed by test_a_fresh_capture_of_a_closed_page_keeps_the_closing_sentence_and_only_that.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_fresh_capture_of_a_closed_page_keeps_the_closing_sentence_and_only_that"
  ]
 },
 {
  "id": "jd-closed-line-prepended-before-the-head-skip",
  "file": "pipeline/jdfill.py",
  "symbol": "extract_jd",
  "class": "M2-order-swap",
  "find": "    rs = _HEAD_SKIP.search(text)\n    if rs and len(text) - rs.start(1) >= MIN_DESC:\n        text = text[rs.start(1):]\n    # LAST, after the head skip: prepended any earlier, `_HEAD_SKIP` cuts it off again.\n    return with_closed_line(full, text)[:DESC_MAX]\n",
  "replace": "    text = with_closed_line(full, text)\n    rs = _HEAD_SKIP.search(text)\n    if rs and len(text) - rs.start(1) >= MIN_DESC:\n        text = text[rs.start(1):]\n    return text[:DESC_MAX]\n",
  "why": "the order is the whole mechanism: a posting that opens with a company intro above `About the role` is trimmed to its first heading, and a closure line prepended before that trim is trimmed with the intro -- the fix present in the code and absent from every capture that has an intro. Killed by test_a_fresh_capture_of_a_closed_page_keeps_the_closing_sentence_and_only_that.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_fresh_capture_of_a_closed_page_keeps_the_closing_sentence_and_only_that"
  ]
 },
 {
  "id": "jd-fresh-capture-stamps-ok-over-a-closed-page",
  "file": "enrich_matched_jd.py",
  "symbol": "_capture_why",
  "class": "M4-guard-removal",
  "find": "    if closed_page_at(text) is None:\n        return default\n",
  "replace": "    if True:\n        return default\n",
  "why": "the canonical capture stamped every text `ok:canonical:<host>`, so the durable `closed-by-page` arm `roles.page_closed` reads was written only when a re-clean cut the sentence -- and a capture that KEEPS the sentence is never cut again. Killed by test_a_fresh_capture_of_a_closed_page_is_stamped_with_its_capture_date.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_fresh_capture_of_a_closed_page_is_stamped_with_its_capture_date"
  ]
 },
 {
  "id": "jd-closed-stamp-overwrites-a-verdict",
  "file": "enrich_matched_jd.py",
  "symbol": "_stamp_closed_pages",
  "class": "M4-guard-removal",
  "find": "_STAMP_FREE = (\"COALESCE(jd_why,'')='' OR COALESCE(jd_why,'') LIKE 'failed:%'\")\n",
  "replace": "_STAMP_FREE = (\"1=1\")\n",
  "why": "a `structural:` value is a blocker the published dataset quotes verbatim, and an `ok:` value is the fetch's own verdict; the stamp pass may only fill an EMPTY cell. Without the clause it rewrites every row carrying the sentence every night. Killed by test_rows_that_already_carry_the_closing_sentence_are_stamped_without_a_cut. Re-anchored 2026-09-18: the `WHERE … AND COALESCE(jd_why,'')=''` clause became the shared `_STAMP_FREE` predicate when `failed:` joined the empty cell as a value this writer may take. Widening it to `1=1` is the same defeat through the new spelling.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_rows_that_already_carry_the_closing_sentence_are_stamped_without_a_cut"
  ]
 },
 {
  "id": "jd-normalise-skips-the-digest-seam",
  "file": "pipeline/jdfill.py",
  "symbol": "JDFiller.maybe_fill",
  "class": "M6-fallback-removal",
  "find": "        self.normalise(job)\n        if not self.enabled:\n",
  "replace": "        if not self.enabled:\n",
  "why": "the 09-13 digest re-cleaned 7 rows (3,752 characters) and the commit after it held 0 of them shorter: the card the digest upserts carried the furniture and `upsert_matched` compares lengths. A store cut every night and re-lengthened every morning. Killed by test_the_digest_seam_no_longer_re_installs_the_furniture_every_morning.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_inline_filler_normalises_a_card_that_already_reads_as_a_jd",
   "tests/test_units.py::test_the_digest_seam_no_longer_re_installs_the_furniture_every_morning"
  ]
 },
 {
  "id": "jd-donor-text-installed-with-its-header",
  "file": "enrich_matched_jd.py",
  "symbol": "_store_text",
  "class": "M6-fallback-removal",
  "find": "    text = jdfill.reclean_text(text) or text\n",
  "replace": "",
  "why": "the matched driver's door: a cache card or donor copy installed with its header is cut by tonight's `_reclean` and handed back by the ledger's longer copy tomorrow -- the 09-11 oscillation, one donor away. Killed by test_a_donor_text_is_cut_on_the_way_into_matched.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_donor_text_is_cut_on_the_way_into_matched"
  ]
 },
 {
  "id": "jd-cache-reclean-cuts-what-the-scraper-wrote",
  "file": "enrich_scrape_jd.py",
  "symbol": "reclean_cache",
  "class": "M5-population-widening",
  "find": "            if not j.get(\"_jd_attempted\") or j.get(\"_jd_shared_page\"):\n",
  "replace": "            if j.get(\"_jd_shared_page\"):\n",
  "why": "21 of the 65 cache cards the cut would change on 09-13 were built by the scraper, and `refresh_scrape_cache._carry_jd` carries a description only onto an EMPTY rebuilt card -- so the nightly rebuild restores their furniture and the noon re-clean would cut it again, for ever, with a counter that never reaches 0. Killed by test_the_cache_reclean_shares_the_matched_rule_and_keeps_its_floor_and_ceiling.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_cache_reclean_shares_the_matched_rule_and_keeps_its_floor_and_ceiling"
  ]
 },
 {
  "id": "jd-cache-reclean-loses-its-ceiling",
  "file": "enrich_scrape_jd.py",
  "symbol": "reclean_cache",
  "class": "M4-guard-removal",
  "find": "    if share > RECLEAN_MAX_SHARE:\n        print(f\"::warning::cache reclean REFUSED",
  "replace": "    if False:\n        print(f\"::warning::cache reclean REFUSED",
  "why": "CLAUDE.md rule 2 for a store rewrite: a furniture rule that starts matching prose would rewrite the whole cache unattended at 12:30. The matched re-clean refuses above 15 %; the cache pass must refuse on the same number. Killed by test_the_cache_reclean_shares_the_matched_rule_and_keeps_its_floor_and_ceiling.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_cache_reclean_shares_the_matched_rule_and_keeps_its_floor_and_ceiling"
  ]
 },
 {
  "id": "jd-wrong-address-read-as-this-roles-posting",
  "file": "pipeline/jdfill.py",
  "symbol": "JDFiller.maybe_fill",
  "class": "M1-gate-removal",
  "find": "               or (\"wrong-address\" if address_names_another_role(url, title) else \"\"))\n",
  "replace": "               or \"\")\n",
  "why": "a Comeet link whose slug spells out a different role is the scraper pairing a title with its neighbour's href (7 Legit Security cards, 09-13); reading it fills the row with another role's posting, and the classifier printed one as a verdict it could not re-judge with no way to say why. Killed by test_the_inline_filler_names_why_a_job_has_no_description_this_run.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_inline_filler_names_why_a_job_has_no_description_this_run"
  ]
 },
 {
  "id": "facts-not-domains-drop",
  "file": "pipeline/identity_facts.py",
  "symbol": "not_domains",
  "class": "M1-gate-removal",
  "find": "    return tuple(facts(name).get(\"not_domains\", ()))",
  "replace": "    return ()",
  "why": "the negative host declarations are the only memory of a board_verify NOT-THEIRS read the 19:00 hunt consults: without them Mars Antennas re-activates on careers.mars.com (BACKLOG 596).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "facts-not-domains-loose-suffix",
  "file": "pipeline/identity_facts.py",
  "symbol": "host_matches",
  "class": "M5-population-widening",
  "find": "    return bool(h) and any(h == d or h.endswith(\".\" + d)",
  "replace": "    return bool(h) and any(h == d or h.endswith(d)",
  "why": "a bare suffix refuses xmars.com for a mars.com declaration -- another company's refusal written by this one's evidence.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "facts-not-domains-validate",
  "file": "pipeline/identity_facts.py",
  "symbol": "validate",
  "class": "M3-branch-narrowing",
  "find": "        if nd and row is not None and row[4] == \"true\":",
  "replace": "        if nd and row is not None and row[4] == \"maybe\":",
  "why": "declaring an incident must force the park, exactly as for not_tenants; an active row on its own not_domain is a contradiction the suite must see.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "verdict-not-domains-drop",
  "file": "pipeline/company_identity.py",
  "symbol": "verdict",
  "class": "M1-gate-removal",
  "find": "    if identity_facts.host_matches(host, identity_facts.not_domains(cname)):\n        return \"mismatch\"",
  "replace": "    if False:\n        return \"mismatch\"",
  "why": "the ONE consumer of not_domains: without it is_foreign stays False on a shared word and every ordinary-host gate admits the foreign board again.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "verify-scope-narrow",
  "file": "queue_pipeline.py",
  "symbol": "needs_verify",
  "class": "M3-branch-narrowing",
  "find": "    if not (is_monitor or is_queue_active or activated_by_a_rung(r)):",
  "replace": "    if not (is_monitor or is_queue_active):",
  "why": "a row a scraping rung ACTIVATED is never read by a model otherwise: 222 active scrape rows on 2026-09-13, Mars Antennas among them.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "verify-declared-drop",
  "file": "queue_pipeline.py",
  "symbol": "verify_existing",
  "class": "M4-guard-removal",
  "find": "        if v == BV.NOT_THEIRS and declared_ours(r[0], r[3]):",
  "replace": "        if False:",
  "why": "a declaration beats a page; without it a misread row flaps between a park and the hunt's re-activation every 30 days.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "park-arm-no-evidence",
  "file": "queue_pipeline.py",
  "symbol": "park_from_ledger",
  "class": "M4-guard-removal",
  "find": "        if not recs:\n            out[n] = \"refused: the ledger has no NOT-THEIRS / not-a-board / dead-url read\"\n            continue",
  "replace": "        if not recs:\n            recs = [{}]",
  "why": "a hand park must cite what board_verify read, never what a session believed.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "monitor-arm-no-ok",
  "file": "queue_pipeline.py",
  "symbol": "park_from_ledger",
  "class": "M4-guard-removal",
  "find": "        if not BV.is_ok(state, n, monitor):",
  "replace": "        if False:",
  "why": "a parked row's address is what listing_hunt's fast path activates on; only a url the ledger reads ok may be written there.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "ledger-contradicted-exact-url",
  "file": "pipeline/board_verify.py",
  "symbol": "_host_reads",
  "class": "M3-branch-narrowing",
  "find": "        if kn != n or site((urllib.parse.urlparse(ku).netloc or \"\").lower()) != dom:",
  "replace": "        if kn != n or ku != (url or \"\").strip().lower():",
  "why": "the ledger is keyed on the url a read happened on and the hunt re-finds a sibling path; an exact-url census misses Mars, the founding case.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "ledger-contradicted-bare-label",
  "file": "pipeline/board_verify.py",
  "symbol": "site",
  "class": "M5-population-widening",
  "find": "            return \".\".join(parts[i:])",
  "replace": "            return parts[i]",
  "why": "the bare label folds adscale.com (AdScale's own board) into adscale.tech (Adscale Tech): measured, an innocent row in the census.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "agg-hosts-drop-social",
  "file": "pipeline/aggregators.py",
  "symbol": "HOSTS",
  "class": "M7-constant-drift",
  "find": "    \"greylock.com/jobs\", \"facebook.com\", \"instagram.com\",",
  "replace": "",
  "why": "a VC portfolio page and a social page both name the company, so only the host list refuses them (Greylock Partners, אסם).",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "spend-armed-drop",
  "file": "bd_rescue.py",
  "symbol": "book",
  "class": "M4-guard-removal",
  "find": "        SPENT[\"armed\"] = True",
  "replace": "        pass",
  "why": "apply_proposals pops the key at import; every queue-drain shard since 2026-09-11 bought ~36 credits and wrote no ledger line.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "qrs-pace-optimistic",
  "file": "queue_resolve_search.py",
  "symbol": "SEC_PER_NAME",
  "class": "M7-constant-drift",
  "find": "SEC_PER_NAME = float(os.environ.get(\"QRS_SEC_PER_NAME\", \"58\") or 58)",
  "replace": "SEC_PER_NAME = float(os.environ.get(\"QRS_SEC_PER_NAME\", \"40\") or 40)",
  "why": "an optimistic pace selects more names than a shard's budget can score: 09-12/13 paid 6 searches it never read at 55.",
  "must_be_killed_by_behavioural": false
 },
 {
  "id": "qrs-cap-second-bound",
  "file": "queue_resolve_search.py",
  "symbol": "NIGHT_CAP",
  "class": "M7-constant-drift",
  "find": "NIGHT_CAP = 45",
  "replace": "NIGHT_CAP = 30",
  "why": "the cap is the second bound: with it at 30 no step length lifts capacity past 4 x 30 = 120, below the 155 the alarm counted.",
  "must_be_killed_by_behavioural": false
 },
 {
  "id": "monitor-identity-drop",
  "file": "queue_pipeline.py",
  "symbol": "park_from_ledger",
  "class": "M1-gate-removal",
  "find": "        if not _gate.identity_ok(n, monitor):",
  "replace": "        if False:",
  "why": "the monitor arm writes a row's address; without the identity gate a ledger ok outranks a declared negative.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "monitor-identity-invert",
  "file": "queue_pipeline.py",
  "symbol": "park_from_ledger",
  "class": "M2-gate-inversion",
  "find": "        if not _gate.identity_ok(n, monitor):",
  "replace": "        if _gate.identity_ok(n, monitor) is None:",
  "why": "truthiness inverted with the source token intact: the shape a wave-8 reviewer reopened in listing_hunt.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "monitor-identity-narrow",
  "file": "queue_pipeline.py",
  "symbol": "park_from_ledger",
  "class": "M3-branch-narrowing",
  "find": "        if not _gate.identity_ok(n, monitor):",
  "replace": "        if not _gate.identity_ok(n, monitor) and n == \"\":",
  "why": "a refusal branch that can never be taken is the gate removed with the call still in the source.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-rolish-english-only",
  "file": "scrape_universal.py",
  "symbol": "_rolish",
  "class": "M6-fallback-removal",
  "find": "    return bool(ROLE.search(t) or _ROLE_HE.search(t))",
  "replace": "    return bool(ROLE.search(t))",
  "why": "a Hebrew board's heading group is refused by the role-ratio gate when only `ROLE` (English) counts: Cal's 29 decoded titles matched 3 against a floor of 9 and the board read 0 (594). Killed by test_scrape_entity_encoded_hebrew_headings_are_all_read.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-hebrew-right-edge-drop",
  "file": "scrape_universal.py",
  "symbol": "_build_israel_loc",
  "class": "M3-branch-narrowing",
  "find": "    return re.compile(left + r\"(?:\" + \"|\".join(alts) + r\")(?-i:(?![a-z0-9א-ת]))\", re.I)",
  "replace": "    return re.compile(left + r\"(?:\" + \"|\".join(alts) + r\")(?-i:(?![a-z0-9]))\", re.I)",
  "why": "without a Hebrew right edge `אילת` matches inside `שאילתות` (\"queries\") and Cal's model-developer posting ships located Eilat, a plausible wrong city on the board and in the mail (594). Killed by test_scrape_a_hebrew_place_inside_a_word_is_not_a_place_but_a_proclitic_form_is.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-azor-noun-read-as-town",
  "file": "scrape_universal.py",
  "symbol": "_loc_from_ctx",
  "class": "M4-guard-removal",
  "find": "    hits = [h for h in ISRAEL_LOC.finditer(ctx) if _noun_reading(ctx, h) != \"\"]",
  "replace": "    hits = list(ISRAEL_LOC.finditer(ctx))",
  "why": "`אזור הצפון` / `לאזור האישי` are \"the north region\" / \"the personal area\"; read as places they stamp the town of Azor on the card (51 of the 58 cached `אזור` locations, 2 of Cal's 29). Killed by test_scrape_a_hebrew_place_inside_a_word_is_not_a_place_but_a_proclitic_form_is.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-vouch-without-page-token",
  "file": "scrape_universal.py",
  "symbol": "_page_is_il",
  "class": "M1-gate-removal",
  "find": "    return bool((vouched or flag) and ISRAEL_LOC.search(_html.unescape(page_html or \"\")))",
  "replace": "    return bool(vouched or (flag and ISRAEL_LOC.search(_html.unescape(page_html or \"\"))))",
  "why": "the row vouch must never stand alone: the page's own Israeli place is the half `_page_is_il`'s docstring says may never widen, and without it every locless card on any `.il` host, nav menus included, becomes an assumed Israeli posting. Killed by test_scrape_an_il_host_vouches_a_locless_card_only_with_a_page_token.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-dom-own-ignored",
  "file": "scrape_universal.py",
  "symbol": "_from_dom",
  "class": "M6-fallback-removal",
  "find": "        t = own if (len(own) >= 5 and _rolish(own) and not BAD_TITLE.match(own)) else d.get(\"title\", \"\")",
  "replace": "        t = d.get(\"title\", \"\")",
  "why": "the rendered anchor's textContent runs the board's filing chips into the title: `Splunk Dashboard Developer BACKEND גוש דן` on Logica-IT, read as title words by the classifier (584). Killed by test_scrape_a_dom_entry_prefers_its_own_text_over_the_anchors_label_blob.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-cards-labels-kept",
  "file": "scrape_universal.py",
  "symbol": "_from_cards",
  "class": "M6-fallback-removal",
  "find": "            text = own if (len(own) >= 5 and (_rolish(own) or not _rolish(raw))) else raw",
  "replace": "            text = raw",
  "why": "the served-HTML twin of the DOM `own` field: a heading's label-classed spans are filing chips, and kept they fork the title and the role's key (584). Killed by test_scrape_a_heading_group_drops_label_spans_and_keeps_wix_styling_spans.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-vouch-not-threaded",
  "file": "refresh_scrape_cache.py",
  "symbol": "_worker",
  "class": "M8-argument-mis-passing",
  "find": "        res = scrape_result(name, url, assume_il=il_host(url))",
  "replace": "        res = scrape_result(name, url)",
  "why": "the 00:00 refresh is the only reader that never vouched for a row: Cal read ~30 postings under the hunt's flag and 1 under the cron (594). Killed by test_refresh_worker_vouches_a_row_by_its_board_host.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-addressless-card-text-dropped",
  "file": "scrape_universal.py",
  "symbol": "_from_cards",
  "class": "M6-fallback-removal",
  "find": "                  date=_date_from_card(ctx[:400]), desc=own,",
  "replace": "                  date=_date_from_card(ctx[:400]),",
  "why": "a heading card with no address of its own has no page to fetch (the refresh counts the listing url as `not_job_url`), so its card window is the only copy of the posting's text: Cal's 29 postings, each a heading, its description and a modal CV button (594). Killed by test_scrape_a_card_with_no_address_of_its_own_keeps_its_own_text.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-assumed-card-names-no-role",
  "file": "scrape_universal.py",
  "symbol": "_from_cards",
  "class": "M1-gate-removal",
  "find": "            assume = (url_is_il and _role_named(t)\n                      and not (href and getattr(add, \"vouched_only\", False)))",
  "replace": "            assume = (url_is_il\n                      and not (href and getattr(add, \"vouched_only\", False)))",
  "why": "the row vouch places a card nothing else placed; without the title's own role noun it places whatever heading shares the group: Globalbit's `AI-First Engineering`, `Growth & Learning` and three more \"why join us\" bullets on its .co.il board (measured 2026-09-13, 6 admitted, 0 of them postings). Killed by test_scrape_an_il_host_vouches_a_locless_card_only_with_a_page_token.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-vouch-assumes-an-addressed-card",
  "file": "scrape_universal.py",
  "symbol": "_from_cards",
  "class": "M4-guard-removal",
  "find": "            assume = (url_is_il and _role_named(t)\n                      and not (href and getattr(add, \"vouched_only\", False)))",
  "replace": "            assume = url_is_il and _role_named(t)",
  "why": "an addressed card the host vouch assumes Israeli is a STRONG reading and ends the ladder before the position-link rung reads its page: BST Group read 1 posting from its page with its own place and description, and 6 heading cards with it, 5 of them only assumed and none described (replayed 2026-09-13). Killed by test_scrape_an_il_host_vouches_a_locless_card_only_with_a_page_token.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-hebrew-group-with-addresses-read",
  "file": "scrape_universal.py",
  "symbol": "_from_cards",
  "class": "M5-population-widening",
  "find": "        if hebrew and any(hrefs):",
  "replace": "        if False:",
  "why": "a heading group that only Hebrew role words admit, read with its postings' own addresses, ends the ladder before the position-link rung opens those postings: replayed 2026-09-13 over 33 .il boards, John Bryce 11 postings -> 1, Latet 22 -> 2, Chimes lost all 25 descriptions. Killed by test_scrape_a_hebrew_board_with_addresses_is_left_to_the_rungs_that_open_them.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "scrape-dom-hebrew-admission",
  "file": "scrape_universal.py",
  "symbol": "_from_dom",
  "class": "M5-population-widening",
  "find": "        if (ROLE.search(d.get(\"title\", \"\")) and not BAD_TITLE.match(t) and _POSTING_HREF.search(u2)",
  "replace": "        if (_rolish(t) and not BAD_TITLE.match(t) and _POSTING_HREF.search(u2)",
  "why": "a DOM card always carries its own address and ends the ladder, so admitting Hebrew-worded anchors there takes addressed Hebrew boards from the rung that opens each posting (the same 2026-09-13 replay). Killed by test_scrape_a_hebrew_board_with_addresses_is_left_to_the_rungs_that_open_them.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "geo-gate-veto-removed",
  "file": "pipeline/israel.py",
  "symbol": "is_israel_job",
  "class": "M1-gate-removal",
  "find": "    if stated_foreign_place(job):\n        VETOED[0] += 1\n        return False\n",
  "replace": "",
  "why": "the gate believed the feed's location over the posting's own words: Wiliot's widget copied `Israel` onto eight cards while each office sat glued to the title (`Data Solutions AnalystSan Mateo`), and they reached the classifier's residue. Measured over both caches on 2026-09-13: 36 of 6,691 cards refused, all 36 read and every one an office abroad. Killed by test_a_foreign_city_glued_to_the_title_is_not_an_israeli_posting.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "geo-head-removed-from-classify",
  "file": "pipeline/seniority.py",
  "symbol": "_classify",
  "class": "M1-gate-removal",
  "find": "        if place:\n            self.geo_rejected += 1\n            return {**base, \"decision\": \"reject\", \"path\": \"keyword\",\n",
  "replace": "        if False:\n            self.geo_rejected += 1\n            return {**base, \"decision\": \"reject\", \"path\": \"keyword\",\n",
  "why": "the gate at fetch time reads an aggregator's 160-character snippet; Diageo's `Location: 3 WTC (New York)` arrived with the fill, after the gate, and the seam judged it YES on scope and was right to, because nothing in LLM_RULES asks about geography. It was published until a hand retraction. Killed by test_the_classifier_head_refuses_a_posting_whose_own_text_places_it_abroad.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "backfill-reads-a-decision-as-judged",
  "file": "pipeline/roles.py",
  "symbol": "class_unjudged",
  "class": "M3-branch-narrowing",
  "find": "    return not cls.get(\"decision\") or not cls.get(\"contract\")\n",
  "replace": "    return not cls.get(\"decision\")\n",
  "why": "30 of 170 published rows carried a decision no contract stood behind (`rows_unknown`, 2026-09-12), every one closed, and the backfill - the only writer that reaches a closed record - read any decision as judged. Re-judged on 2026-09-13: 8 of 31 verdicts moved. Killed by test_a_decision_no_contract_stands_behind_is_owed_a_verdict.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "jdfill-unlocker-ignores-run-cap",
  "file": "pipeline/jdfill.py",
  "symbol": "Unlocker.__call__",
  "class": "M1-gate-removal",
  "find": "        if _run_cap_reached() is not None:        # the PROCESS cap, shared with bd_rescue (600a)",
  "replace": "        if False:",
  "why": "BACKLOG 600(a): the JD layer's Unlocker POSTs to api.brightdata.com itself, so BD_RUN_CAP=0 did not stop the digest's JD fill and the [bd-spend] line reported a cap that never applied to it (infra, 2026-09-13).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_shared_run_cap_binds_the_jd_layers_own_unlocker"
  ]
 },
 {
  "id": "wayback-net-parks-host",
  "file": "archive_evidence.py",
  "symbol": "_Pool.record",
  "class": "M1-gate-removal",
  "find": "            if fam in HOST_REFUSAL:                  # only the archive's word about THIS address parks it\n",
  "replace": "            if True:\n",
  "why": "2026-09-15: every `5 consecutive refusals, parked for today` from 09-04 to 09-15 was the archive's own failure (a connection error or a 5xx, 0 refusal lines in 768) charged to LinkedIn, Comeet and Indeed, four hosts a night; a park must read only the archive's word about THIS address (infra).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_an_archive_side_failure_never_parks_a_host_but_a_refusal_still_does"
  ]
 },
 {
  "id": "wayback-pause-never-stops",
  "file": "archive_evidence.py",
  "symbol": "PAUSE_EPISODES",
  "class": "M7-constant-drift",
  "find": "PAUSE_EPISODES = 2        # pauses a day, a 429 or an outage streak alike\n",
  "replace": "PAUSE_EPISODES = 99       # pauses a day, a 429 or an outage streak alike\n",
  "why": "the day must END after its two pauses: a dead archive would otherwise be asked in eights until the request cap, and the 30-minute budget is the only other bound (infra, 2026-09-15).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_the_archive_down_pauses_twice_then_ends_the_day_with_no_host_parked",
   "tests/test_units.py::test_wayback_a_second_throttle_episode_gets_its_own_wait_and_the_third_ends_the_day"
  ]
 },
 {
  "id": "wayback-second-429-ends-the-day",
  "file": "archive_evidence.py",
  "symbol": "PAUSE_EPISODES",
  "class": "M7-constant-drift",
  "find": "PAUSE_EPISODES = 2        # pauses a day, a 429 or an outage streak alike\n",
  "replace": "PAUSE_EPISODES = 1        # pauses a day, a 429 or an outage streak alike\n",
  "why": "`582`: one pause ended the archive day on five of twelve nights; the second episode with its own Retry-After is the item (infra, 2026-09-15).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_a_second_throttle_episode_gets_its_own_wait_and_the_third_ends_the_day"
  ]
 },
 {
  "id": "wayback-outage-after-drift",
  "file": "archive_evidence.py",
  "symbol": "OUTAGE_AFTER",
  "class": "M7-constant-drift",
  "find": "OUTAGE_AFTER = 8          # consecutive archive-side REQUESTS that open a pause",
  "replace": "OUTAGE_AFTER = 9          # consecutive archive-side REQUESTS that open a pause",
  "why": "the threshold was measured (fires on the five degraded nights of 09-04..09-15, on none of the seven others); a test that only says `pauses twice` cannot see it move (infra, 2026-09-15).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_the_archive_down_pauses_twice_then_ends_the_day_with_no_host_parked"
  ]
 },
 {
  "id": "wayback-boards-tail",
  "file": "archive_evidence.py",
  "symbol": "run",
  "class": "M1-gate-removal",
  "find": "        _Pool(interleave(postings, boards), ledger, out, rep, caps, started, budget_s, now, auth=auth).run()\n",
  "replace": "        _Pool(postings + boards, ledger, out, rep, caps, started, budget_s, now, auth=auth).run()\n",
  "why": "boards queued behind every posting read `boards 0 (of ~595 due)` on every night from 09-04 to 09-15 because the day ended first (infra, 2026-09-15).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_boards_go_out_one_in_six_so_an_early_end_still_captures_boards"
  ]
 },
 {
  "id": "wayback-archive-side-is-an-attempt",
  "file": "archive_evidence.py",
  "symbol": "eligible",
  "class": "M1-gate-removal",
  "find": "    return _days(today, s[\"last_at\"]) >= cooldown_days(s[\"last_err\"], s[\"refusals\"], s[\"attempts\"])\n",
  "replace": "    return _days(today, s[\"last_at\"]) >= cooldown_days(s[\"last_err\"], s[\"attempts\"], s[\"attempts\"])\n",
  "why": "the 30-day `tired` cooldown read every line as the address's attempt: 160 of 483 uncaptured urls had only archive-side lines on 09-15 and were two outage nights from retirement (infra, 2026-09-15).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_the_archives_failures_are_not_the_urls_attempts"
  ]
 },
 {
  "id": "wayback-timeout-is-a-capture",
  "file": "archive_evidence.py",
  "symbol": "run",
  "class": "M1-gate-removal",
  "find": "    if rep.captured == 0 and (postings or boards):\n",
  "replace": "    if rep.submitted == 0 and rep.boards == 0 and (postings or boards):\n",
  "why": "the old rule: `submitted` counts a 60-s timeout as accepted, so 09-12 (64 accepted, 0 named, 37 unverified three days later) never alarmed (infra, 2026-09-15).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_the_stamp_carries_the_split_and_captured_is_not_submitted"
  ]
 },
 {
  "id": "wayback-polls-count-as-requests",
  "file": "archive_evidence.py",
  "symbol": "_Pool._submit",
  "class": "M7-constant-drift",
  "find": "            self.rep.polls += res.polls\n",
  "replace": "            self.rep.polls += res.polls\n            self.rep.requests += res.polls\n",
  "why": "a status read is not a capture: `WAYBACK_REQ_CAP` bounds captures + verifications, and three reads a job would spend it three times over before the day's cap (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_polls_are_not_requests_and_the_pace_gate_fronts_the_post"
  ]
 },
 {
  "id": "wayback-unknown-status-ext-is-http",
  "file": "archive_evidence.py",
  "symbol": "_classify_job",
  "class": "M7-constant-drift",
  "find": "        return Result(http, \"\", STATUS_EXT.get(word.split(\":\", 1)[-1], \"server\"), job_id=jid, status_ext=word)\n",
  "replace": "        return Result(http, \"\", STATUS_EXT.get(word.split(\":\", 1)[-1], \"http\"), job_id=jid, status_ext=word)\n",
  "why": "a word the table does not know must be the archive's, never the address's: 09-15 showed every host park to date was an archive failure charged to the host that happened to be next (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_status_ext_words_land_on_the_two_families"
  ]
 },
 {
  "id": "wayback-ceiling-drops-job-id",
  "file": "archive_evidence.py",
  "symbol": "_poll",
  "class": "M8-argument-mis-passing",
  "find": "    n, last = 0, Result(200, \"\", \"pending\", job_id=job_id)\n",
  "replace": "    n, last = 0, Result(200, \"\", \"pending\")\n",
  "why": "a `pending` with no id goes to the availability API and, unanswered for three days, becomes `unverified` -- a refusal of the address -- when the archive could have answered exactly (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_a_job_past_the_ceiling_is_pending_with_its_id_and_resolved_next_run"
  ]
 },
 {
  "id": "wayback-probe-cap-is-documentation",
  "file": "archive_evidence.py",
  "symbol": "_probe",
  "class": "M1-gate-removal",
  "find": "            caps.day = min(caps.day, lim - used)\n",
  "replace": "            caps.day = caps.day\n",
  "why": "the day cap the account has left must be the one `plan_batch` reads, or it is documentation (the `577` defect again: a cap nothing enforced) (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_the_account_probe_sets_workers_and_the_day_before_the_plan"
  ]
 },
 {
  "id": "wayback-probe-after-plan",
  "file": "archive_evidence.py",
  "symbol": "run",
  "class": "M8-argument-mis-passing",
  "find": "    if not dry_run:                                  # BEFORE the plan: the cap it lowers is the one `plan_batch` reads\n        auth, used_before = _probe(auth, caps, rep)\n    postings, boards, rep.backlog, rep.boards_due = plan_batch(targets, ledger, today, caps)\n",
  "replace": "    postings, boards, rep.backlog, rep.boards_due = plan_batch(targets, ledger, today, caps)\n    if not dry_run:\n        auth, used_before = _probe(auth, caps, rep)\n",
  "why": "the probe lowers `caps.day`; read after `plan_batch` it lowers nothing the plan used (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_the_account_probe_sets_workers_and_the_day_before_the_plan"
  ]
 },
 {
  "id": "wayback-first-alarm-wins",
  "file": "archive_evidence.py",
  "symbol": "Report.add_alarm",
  "class": "M3-branch-narrowing",
  "find": "        if clause and clause not in self.alarm.split(\"; \"):\n            self.alarm = (self.alarm + \"; \" + clause) if self.alarm else clause\n",
  "replace": "        if clause and not self.alarm:\n            self.alarm = clause\n",
  "why": "the old `if not rep.alarm` let the first clause suppress `zero-produce`: an unauthenticated zero night must say both in the mail (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_no_keys_or_refused_keys_fall_back_anonymous_and_say_so"
  ]
 },
 {
  "id": "wayback-poll-429-reposts",
  "file": "archive_evidence.py",
  "symbol": "_poll",
  "class": "M6-fallback-removal",
  "find": "        if err is not None and err.err == \"throttled\" and err.retry_after:\n            _sleep(min(err.retry_after, max(0.0, deadline - time.monotonic())))\n",
  "replace": "        if err is not None and err.err == \"throttled\":\n            last = err\n            break\n",
  "why": "a 429 to a status READ handed to the pool as `throttled` re-POSTs a job that is still running: a duplicate capture, a second of the url's five a day, and a pause charged to a read (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_polls_are_not_requests_and_the_pace_gate_fronts_the_post"
  ]
 },
 {
  "id": "wayback-crash-print-unredacted",
  "file": "archive_evidence.py",
  "symbol": "main",
  "class": "M6-fallback-removal",
  "find": "        print(f\"[wayback] CRASHED: {type(e).__name__}: {_redact(str(e)[:200])}\", flush=True)\n",
  "replace": "        print(f\"[wayback] CRASHED: {type(e).__name__}: {str(e)[:200]}\", flush=True)\n",
  "why": "the crash line is the one print that carries free text; an exception that quotes a request would put the account key on a public run page (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_keys_never_reach_the_log_the_ledger_or_the_stamp"
  ]
 },
 {
  "id": "wayback-empty-status-ext-written",
  "file": "archive_evidence.py",
  "symbol": "Line.dumps",
  "class": "M5-population-widening",
  "find": "        d = {k: v for k, v in self.__dict__.items() if v or k not in (\"job_id\", \"status_ext\")}\n",
  "replace": "        d = dict(self.__dict__)\n",
  "why": "an anonymous-rung line must stay the 2026-09-04 shape byte for byte: the ledger is append-only, merged as whole lines, and 768 lines predate the two fields (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_run_writes_one_line_per_attempt_and_verifies_yesterdays_pending",
   "tests/test_units.py::test_wayback_keyed_post_then_poll_names_the_capture"
  ]
 },
 {
  "id": "wayback-unauthenticated-retried",
  "file": "archive_evidence.py",
  "symbol": "_Pool.work",
  "class": "M4-dead-conjunct",
  "find": "            if _family(res.err) in ARCHIVE_SIDE and res.status_ext != \"unauthenticated\":   # our keys refused: no second ask\n",
  "replace": "            if _family(res.err) in ARCHIVE_SIDE:\n",
  "why": "our keys refused is not the archive down: a second POST with the same keys is a second refusal, and a 15-s wait for it on every thread (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_no_keys_or_refused_keys_fall_back_anonymous_and_say_so"
  ]
 },
 {
  "id": "wayback-post-timeout-is-pending",
  "file": "archive_evidence.py",
  "symbol": "_json_call",
  "class": "M7-constant-drift",
  "find": "        return 0, None, _exc_result(e, \"net:TimeoutError\")\n",
  "replace": "        return 0, None, _exc_result(e, \"pending\")\n",
  "why": "the POST answers in a second; a silence there is the archive not answering, and a `pending` with no job goes to the availability API and then to `unverified`, a refusal of the address (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_polls_are_not_requests_and_the_pace_gate_fronts_the_post"
  ]
 },
 {
  "id": "wayback-job-status-skipped",
  "file": "archive_evidence.py",
  "symbol": "run",
  "class": "M6-fallback-removal",
  "find": "            if s.get(\"job_id\") and auth.keyed:\n",
  "replace": "            if False:\n",
  "why": "a pending line that names its job is resolved exactly by `/save/status/<job_id>`; the availability API is the anonymous rung's guess and needed three days and 206 `unverified` lines (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_a_job_past_the_ceiling_is_pending_with_its_id_and_resolved_next_run"
  ]
 },
 {
  "id": "wayback-keyed-429-pauses-the-day",
  "file": "archive_evidence.py",
  "symbol": "_Pool._retry",
  "class": "M1-gate-removal",
  "find": "                if keyed and not res.retry_after:\n                    wait, slot_wait = SLOT_WAIT_S, True\n                else:\n",
  "replace": "                if False:\n                    wait, slot_wait = SLOT_WAIT_S, True\n                else:\n",
  "why": "measured 2026-09-16: the account's 429 with no Retry-After is a lagging slot count, and the anonymous rung's 300-s pause answered it twice in a 15-minute run -- ten minutes paused for two free slots (infra).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_a_keyed_429_without_retry_after_is_a_slot_wait_not_a_pause"
  ]
 },
 {
  "id": "wayback-post-cut-off-at-30s",
  "file": "archive_evidence.py",
  "symbol": "_submit_job",
  "class": "M7-constant-drift",
  "find": "    status, data, err = _json_call(req, max(0.0, timeout / 2.0))\n    if status in (401, 403):\n",
  "replace": "    status, data, err = _json_call(req, 30.0)\n    if status in (401, 403):\n",
  "why": "the first scheduled night (2026-09-16, run 35126457407): 43 of 154 requests were `net:TimeoutError`, the POST cut off at 30 s while the archive held it for a slot -- each a 15-s retry and a second POST for the same url (infra). Re-anchored 2026-09-18: the POST's wait is now HALF the job's budget, not a constant and not the whole of it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_keyed_post_then_poll_names_the_capture"
  ]
 },
 {
  "id": "wayback-after-read-silent",
  "file": "archive_evidence.py",
  "symbol": "run",
  "class": "M6-fallback-removal",
  "find": "        else:                                        # never silent: the first night's line was simply absent\n            print(f\"[wayback] authenticated: the account could not be re-read after the day \"\n                  f\"({status or 'net'} {err.err if err else 'unreadable'})\", flush=True)\n",
  "replace": "        else:\n            pass\n",
  "why": "the first scheduled night's after-the-day account line was simply absent, and the morning row that compares its delta to `jobs` had nothing to read (infra, 2026-09-16).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_the_account_probe_sets_workers_and_the_day_before_the_plan"
  ]
 },
 {
  "id": "ledger-refuses-ignores-dates",
  "file": "pipeline/board_verify.py",
  "symbol": "refuses",
  "class": "M5-population-widening",
  "find": "    return bool(reads) and reads[-1].get(\"verdict\") == NOT_THEIRS",
  "replace": "    return any(v.get(\"verdict\") == NOT_THEIRS for v in reads)",
  "why": "a stale NO is not a NO: `Hillel Il` was read `ok` on hillel.org/careers on 09-15 and NOT-THEIRS on a BLOG url of the same host on 08-31, and a host-keyed count with no date rule reported it contradicted for three days. Newest read on that host wins, or the fold turns any historical misread into a permanent refusal.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "ledger-refuses-unverifiable-counts",
  "file": "pipeline/board_verify.py",
  "symbol": "_host_reads",
  "class": "M5-population-widening",
  "find": "        if (v or {}).get(\"verdict\") == UNVERIFIABLE:\n            continue",
  "replace": "        if False:\n            continue",
  "why": "UNVERIFIABLE means we failed to LOOK, never an answer -- `cached()` makes the same cut. Counted, a later 'could not read it' silently clears a real NOT-THEIRS and the hunt re-activates the row.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-ledger-veto-removed",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "            elif verdict == \"found\" and _ledger_refuses(name, url):\n                # ...and the same refusal when a MODEL already read this host and said so.\n                refused = \"ledger: another company's board\"",
  "replace": "            elif False:\n                refused = \"\"",
  "why": "the veto that was missing when the 19:00 hunt re-activated `Kima` onto careers.akima.com (refused 09-14) and `PayPlus` onto payplus.com (refused 09-15). `identity_gate` cannot see it -- it is pure by design and the ledger is a state file -- so the refusal has to bind in the WRITER.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "queue-lapse-suppression-swallows-an-unmeasured-stamp",
  "file": "queue_pipeline.py",
  "symbol": "stamp_queue",
  "class": "M5-population-widening",
  "find": "            _measured_lapse = _new is not None and _new == 0 and _sel is not None",
  "replace": "            _measured_lapse = not _new",
  "why": "'we could not measure it' is not 'it was zero': `_drain_liveness` swallows a broken queue file and leaves `new_intake` absent, and a suppression that reads an ABSENT number as a reason to stay quiet kills the alarm exactly when it matters.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "search-backoff-uncapped",
  "file": "queue_resolve_search.py",
  "symbol": "cadence_days",
  "class": "M5-population-widening",
  "find": "    return min(SEARCH_CADENCE_DAYS * (2 ** max(0, len(tries) - 1)), SEARCH_BACKOFF_CAP)",
  "replace": "    return SEARCH_CADENCE_DAYS * (2 ** max(0, len(tries) - 1))",
  "why": "the back-off must never become a retirement: 'no candidate was this company's live page' is not 'this company has no board' (operator rule 1). Uncapped, a name refused nine times waits 3,584 days.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "hunt-queue-ledger-address-ungated",
  "file": "listing_hunt.py",
  "symbol": "main",
  "class": "M1-gate-removal",
  "find": "            keep = q_url if (q_url and _gate.identity_ok(name, q_url)\n                             and not _ledger_refuses(name, q_url)) else \"\"",
  "replace": "            keep = q_url if (q_url and _gate.identity_ok(name, q_url)) else \"\"",
  "why": "the ADDRESS half of the ledger veto on the queue arm. Refusing to ACTIVATE while still storing the address only delays the mistake 24 hours -- this tool's own fast path re-reads fr[3] the next night, which is how a refused host comes back.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "backfill-ignores-a-reject-cell-under-a-retired-contract",
  "file": "pipeline/class_backfill.py",
  "symbol": "reject_owed",
  "class": "M6-fallback-removal",
  "find": "    if cls.get(\"contract\") != (contract or _seniority().CONTRACT):\n        return True\n",
  "replace": "    if False:\n        return True\n",
  "why": "2026-09-18 (classifier): the retired-contract arm is the only one that survives a contract bump -- a bump supersedes every cached verdict, so the `|jd` arm is empty for the mornings the drain takes to refill it. Without it the 11 published `reject` cells found that morning are stranded again, and from the same day a `reject` cell DELETES the row.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_published_reject_cell_is_owed_a_live_verdict_and_only_ever_upwards"
  ]
 },
 {
  "id": "refill-lets-an-accept-cell-become-a-reject",
  "file": "pipeline/roles.py",
  "symbol": "class_refillable",
  "class": "M4-guard-removed",
  "find": "    return (cls.get(\"decision\") == \"reject\" and new_cls.get(\"decision\") == \"accept\"\n            and bool(new_cls.get(\"contract\")))\n",
  "replace": "    return bool(new_cls.get(\"contract\"))\n",
  "why": "2026-09-18 (classifier): the second arm of the refill is ONE-WAY on purpose. Widening it to accept-to-reject would let a backlog pass take a live role off the board with no retraction line and no reader -- the channel for that is `roles_retractions.jsonl`, url-precise and reversible.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_published_reject_cell_is_owed_a_live_verdict_and_only_ever_upwards"
  ]
 },
 {
  "id": "a-decision-record-stops-outranking-the-seam-on-the-live-path",
  "file": "pipeline/seniority.py",
  "symbol": "_classify",
  "class": "M6-fallback-removal",
  "find": "        ruled = adjudicated(job)\n        if ruled:\n            self.adjudicated += 1\n            return {**base, \"decision\": ruled[\"decision\"], \"path\": ruled[\"path\"],\n                    \"reason\": ruled[\"reason\"]}\n        if rel == \"excluded\":\n",
  "replace": "        if rel == \"excluded\":\n",
  "why": "2026-09-18 (classifier): `ADJUDICATED` must be read at BOTH judging sites. `team8|briya medical data analyst` answers NO to the seam every time it is asked (NO/NO/NO on three fresh calls), and since that morning a `reject` cell removes the row -- so a live path that skips the map drops a posting the 2026-09-01 workplace record adjudicated IN, the morning it reopens.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_decision_record_outranks_the_seam_on_the_role_ids_it_names"
  ]
 },
 {
  "id": "unreachable-reason-prints-a-bare-question-mark-again",
  "file": "pipeline/seniority.py",
  "symbol": "_classify",
  "class": "M7-constant-drift",
  "find": "                    self.unreachable_why[\"shared text\" if shared\n                                         else (job.get(\"_jd_why\") or NO_JD_WHY)] += 1\n",
  "replace": "                    self.unreachable_why[\"shared text\" if shared\n                                         else (job.get(\"_jd_why\") or \"?\")] += 1\n",
  "why": "2026-09-18 (classifier): `classify N superseded verdicts CANNOT be re-judged (not-a-job-url 3, ? 1, wrong-address 1)` printed every morning and the `?` told a reader the reason had been LOST, when in fact jd-fill had never attempted the card. A grouped alarm whose largest bucket is punctuation is an alarm nobody can act on.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_unreachable_alarm_names_why_each_role_had_no_description"
  ]
 },
 {
  "id": "scrape-card-closed-anchor-counts",
  "file": "scrape_universal.py",
  "symbol": "_card_bounds",
  "class": "M1-gate-removal",
  "find": "    for m in _A_OPEN.finditer(win):\n        if \"</a>\" not in win[m.end():]:      # still open at the heading: it wraps the card\n            lo = base + m.start()\n",
  "replace": "    for m in _A_OPEN.finditer(win):\n        if True:\n            lo = base + m.start()\n",
  "why": "measured 2026-09-18: an anchor already CLOSED before the heading is the previous card's -- 54 cached cards across 10 boards carried a neighbour's url, Deloitte's `AI Engineer- R&D and Innovation Center` the Administrative Assistant's `df-070-en`. Dropping the open/closed test restores that (scraper).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_scrape_a_closed_anchor_before_the_heading_is_the_neighbours_not_the_cards"
  ]
 },
 {
  "id": "scrape-card-end-ignored",
  "file": "scrape_universal.py",
  "symbol": "_card_bounds",
  "class": "M7-constant-drift",
  "find": "    hi = max(pos, end)\n    base = max(0, pos - 600)\n",
  "replace": "    hi = pos + 1600\n    base = max(0, pos - 600)\n",
  "why": "the after-search must stop at the NEXT SIBLING heading; `pos + 1600` reaches past it, which is how a card with no link of its own took the next card's (2026-09-18, scraper).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_scrape_a_closed_anchor_before_the_heading_is_the_neighbours_not_the_cards"
  ]
 },
 {
  "id": "scrape-card-window-cut-inside-the-tag",
  "file": "scrape_universal.py",
  "symbol": "_card_bounds",
  "class": "M8-argument-mis-passing",
  "find": "    for m in _A_OPEN.finditer(win):\n        if \"</a>\" not in win[m.end():]:      # still open at the heading: it wraps the card\n            lo = base + m.start()\n    return lo, hi\n",
  "replace": "    for m in _A_OPEN.finditer(win):\n        if \"</a>\" not in win[m.end():]:      # still open at the heading: it wraps the card\n            lo = base + m.end() - 1\n    return lo, hi\n",
  "why": "the window is handed to `_card_anchor_for`, whose `_ANCHOR_RX` needs the whole `<a ...>...</a>`: a `lo` inside the open tag silently costs every `<a href=own><h3>T</h3></a>` card its address (Centrical 3 of 6, measured 2026-09-18, scraper).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_scrape_a_closed_anchor_before_the_heading_is_the_neighbours_not_the_cards"
  ]
 },
 {
  "id": "refresh-carry-keys-on-the-listing",
  "file": "refresh_scrape_cache.py",
  "symbol": "_addresses",
  "class": "M1-gate-removal",
  "find": "    if listing and not _is_own_address(j, listing):\n        url = \"\"\n",
  "replace": "    if False:\n        url = \"\"\n",
  "why": "measured 2026-09-18: a url-less card's url IS the listing, shared by every url-less card on the board -- keyed on it the carry handed one card's text to any new title (20 cards / 9 boards; Medison 1 -> 5 titles, one a night from 2026-08-31) (scraper).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_refresh_the_listing_url_is_not_a_name_a_carry_may_match_on"
  ]
 },
 {
  "id": "refresh-carried-twins-counts-nothing",
  "file": "refresh_scrape_cache.py",
  "symbol": "_carried_twins",
  "class": "M6-fallback-removal",
  "find": "    return sum(len(ts) for ts in by_desc.values() if len(set(ts)) >= 2)\n",
  "replace": "    return 0\n",
  "why": "the `carried_twins` token on the `collect:` stamp is the only thing that would say the listing-keyed carry is back; a counter that always reads 0 reports a clean night on the night it returns (scraper, 2026-09-18).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_refresh_the_listing_url_is_not_a_name_a_carry_may_match_on"
  ]
 },
 {
  "id": "scrape-position-anchor-at-the-title-copy",
  "file": "scrape_universal.py",
  "symbol": "_parse_position_page",
  "class": "M8-argument-mis-passing",
  "find": "    mb = _H1_RX.search(body)\n    at = len(re.sub(r\"<[^>]+>\", \" \", body[:mb.start()])) if mb else txt.find(title)\n",
  "replace": "    mb = None\n    at = len(re.sub(r\"<[^>]+>\", \" \", body[:mb.start()])) if mb else txt.find(title)\n",
  "why": "`txt.find(title)` is the `<title>` element's copy at byte 0, so the place nearest the anchor is whatever chrome lies between it and the h1: אסם's navbar slogan placed 10 of 12 postings in שדרות while their own pages say Petah Tikva and Modiin (unlocker, 2026-09-18) (scraper).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_scrape_a_position_pages_place_is_anchored_at_its_heading_not_at_the_title_copy"
  ]
 },
 {
  "id": "scrape-bare-country-counts-as-a-place",
  "file": "scrape_universal.py",
  "symbol": "_bare_country",
  "class": "M1-gate-removal",
  "find": "    return bool(loc) and len(re.sub(r\"(?i)israel|ישראל|[\\W_]+\", \"\", loc)) < 2\n",
  "replace": "    return False\n",
  "why": "the bare country is the absence of a place: with the anchor at the h1, a `<title>DevOps Engineer in Ramat Gan, Israel</title>` over a bare h1 puts `Israel` nearer the heading than the city, and 10 of the 27 readings that first moved lost their city that way (scraper, 2026-09-18).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_scrape_a_position_pages_place_is_anchored_at_its_heading_not_at_the_title_copy"
  ]
 },
 {
  "id": "scrape-store-door-skips-the-reclean",
  "file": "scrape_universal.py",
  "symbol": "_store_desc",
  "class": "M6-fallback-removal",
  "find": "    return reclean_text(text) or text\n",
  "replace": "    return text\n",
  "why": "BACKLOG 608(a): `reclean_cache` refuses any card without `_jd_attempted` and a scraper-built description never has one, so 19 cards over 8 boards carried 15,033 characters of page header and footer on 2026-09-18 (Amitim Pension Funds 9). This door is the only place the cut can happen (scraper).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_scrape_the_store_door_re_cleans_with_jd_texts_own_rule"
  ]
 },
 {
  "id": "reject-sweep-skips-a-closed-record",
  "file": "pipeline/roles.py",
  "symbol": "_withdraw_rejected",
  "class": "M3-branch-narrowing",
  "find": "            if st_ in (\"open\", \"closed\") and decision == \"reject\":",
  "replace": "            if st_ == \"open\" and decision == \"reject\":",
  "why": "every one of the 11 rows that shipped `class_decision=reject` on 2026-09-18 was CLOSED -- the cells were stamped days earlier, while the roles were still fetched, and the records then closed on the ordinary ladder. A sweep that reaches only open records leaves exactly the pile it was built for in the file, and would have looked correct on any morning with a live reject in it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_reject_cell_already_on_a_closed_record_is_withdrawn_on_the_next_run"
  ]
 },
 {
  "id": "reject-sweep-stamps-retracted-on",
  "file": "pipeline/roles.py",
  "symbol": "_withdraw_rejected",
  "class": "M7-constant-drift",
  "find": "                rec[\"withdrawn_by\"] = WITHDRAWN_BY_CLASSIFIER\n                rec[\"withdrawn_on\"] = run_date\n",
  "replace": "                rec[\"withdrawn_by\"] = WITHDRAWN_BY_CLASSIFIER\n                rec[\"withdrawn_on\"] = run_date\n                rec[\"retracted_on\"] = run_date\n",
  "why": "`retracted_on` is a retraction LINE's field. A record carrying one with no line behind it is LIFTED back onto the ordinary ladder by the lift arm the next morning, alarming `roles retraction lifted for N role(s)` -- so a machine withdrawal that wrote it would un-withdraw itself daily and blame a missing human line for it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_record_run_stamps_the_runs_own_rejects_and_withdraws_every_rejected_row"
  ]
 },
 {
  "id": "reject-sweep-is-not-reversible",
  "file": "pipeline/roles.py",
  "symbol": "_withdraw_rejected",
  "class": "M6-fallback-removal",
  "find": "                for k in _WITHDRAW_STAMPS:\n                    rec.pop(k, None)\n                if rid in onboard:",
  "replace": "                if rid in onboard:",
  "why": "the reversal is what lets the classifier re-admit a row with a VERDICT instead of a hand-written retraction line (the 2026-09-18 ruling). Leaving the three stamps on a record the sweep has just returned to open/closed publishes it in `roles.csv` AND in the meta's `removed` list at the same time -- a row that is both in the file and withdrawn from it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_machine_withdrawal_is_reversed_by_the_verdict_alone"
  ]
 },
 {
  "id": "export-guard-publishes-a-reject-cell",
  "file": "pipeline/roles.py",
  "symbol": "build_rows",
  "class": "M4-guard-removal",
  "find": "        if decision == \"reject\":\n            counts[\"reject_refused\"] += 1\n            continue\n        if not decision:\n            counts[\"unjudged_refused\"] += 1\n            continue\n",
  "replace": "",
  "why": "the tripwire above the mechanism: `_withdraw_rejected` should have emptied this class before a file was built, so a row reaching the export with a `reject` (or empty) cell means the sweep did not run -- a re-derive from an older ledger snapshot, or a bug. Without the guard that row is published exactly as the 11 rows of 2026-09-18 were, and nothing counts or alarms it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_export_refuses_a_leaked_reject_and_the_meta_still_reconciles"
  ]
 },
 {
  "id": "reject-map-counts-a-served-stale-no",
  "file": "pipeline/roles.py",
  "symbol": "reject_map",
  "class": "M5-population-widening",
  "find": "        if contract and (cls.get(\"contract\") or \"\") != contract:\n            continue\n",
  "replace": "",
  "why": "647: since 2026-09-18 a `reject` cell is a WITHDRAWAL, so the map must hold only the NOs this run made. `seniority._classify` also returns `reject` for a verdict it merely SERVED from the cache under a retired contract (254 of them on 2026-09-19, 40 unreachable for want of a description) -- without the contract check two rows left the public dataset on zero live votes (Amitim, whose live `|jd` row said YES the same morning; Menora, on a 09-14 `|bare` NO) and the backfill's own `accept` for them was then discarded by the `rid in class_rejects` skip.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_served_stale_no_is_not_this_runs_reject_and_the_backfill_accept_stands",
   "tests/test_units.py::test_a_served_stale_no_leaves_a_published_accept_cell_exactly_as_it_was"
  ]
 },
 {
  "id": "backfill-map-ignores-this-runs-rename",
  "file": "pipeline/roles.py",
  "symbol": "_current_id",
  "class": "M6-fallback-removal",
  "find": "        seen = {rid}\n        cur = rid\n        for _ in range(8):\n            nxt = self.renamed.get(cur)\n            if not nxt or nxt in seen:\n                break\n            cur = nxt\n            seen.add(cur)\n        return cur\n",
  "replace": "        return rid\n",
  "why": "647: the two verdict maps `record_run` applies are built BEFORE this run's at-rest folds -- `class_backfill.candidates` reads `ledger.records` at run.py:553 and `fold_aliases` renames at 636 -- so the identity function makes `records.get(old_key)` return None and the map entry is dropped SILENTLY, with no counter and no alarm. That is how `הפניקס|...50400095` kept a stale `reject` cell and was withdrawn from the dataset on a morning the backfill had judged it accept.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_backfill_map_follows_a_rename_this_same_run_made"
  ]
 },
 {
  "id": "rename-overwrites-an-occupied-key",
  "file": "pipeline/roles.py",
  "symbol": "_rename_record",
  "class": "M4-guard-removal",
  "find": "        if new_key in self.records:\n",
  "replace": "        if False:\n",
  "why": "sqlite refuses a rename onto an existing mkey, but a record the LEDGER holds and sqlite has lost (the rehydration case) slips past that check and is clobbered by the moved record -- a history deleted by a rename, which is the one thing this seam exists to avoid. Retention is the product.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_rename_refuses_rather_than_destroy_a_history"
  ]
 },
 {
  "id": "alias-no-twin-leaves-the-record",
  "file": "pipeline/roles.py",
  "symbol": "_fold_aliases",
  "class": "M3-branch-narrowing",
  "find": "            elif self._rename_record(rid, new_key, company=r):",
  "replace": "            elif False:",
  "why": "the retired behaviour (2026-09-18). Leaving the record means the canonical key arrives from intake every morning and mints a BRAND-NEW record with today's `first_seen`, while this one stops being fed and closes as if the posting had gone: one posting published twice, with a false closure and a false `new`. `_fold_titles` refuses that trap (585); the alias sweep used to walk into it.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_alias_sweep_renames_a_foldable_record_with_no_twin"
  ]
 },
 {
  "id": "twin-tie-break-ignores-the-description",
  "file": "pipeline/roles.py",
  "symbol": "_twin_winner_at_rest",
  "class": "M4-guard-removal",
  "find": "        if len(shas) == 1 and shas != {\"\"} and len(urls) == 1 and urls != {\"\"}:",
  "replace": "        if True:",
  "why": "the third key is licensed ONLY by one address AND one description -- the narrowest statement of `one posting` this store can make. Without that conjunction the tie-break crowns an incumbent over two genuinely different roles: measured on the committed store, `autods` carries `data analyst` beside `senior marketing analyst` under one greenhouse id, 1 of the 2 non-BioCatch platform-id groups, and folding it would delete a live opening from every product.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_twin_tie_on_two_different_postings_is_still_refused"
  ]
 },
 {
  "id": "jd-mirror-k-too-small",
  "file": "pipeline/jdfill.py",
  "symbol": "mirrored_nav",
  "class": "M5-population-widening",
  "find": "NAV_K = 8\n",
  "replace": "NAV_K = 3\n",
  "why": "`611`: eight tokens is where a repeated marker-free run stops being prose and becomes a menu. Measured over the 2,421 stored bodies, dropping the requirement that the run recur at all fires on 407 bodies and pushes 175 below `looks_like_jd`; at K=3 an ordinary posting that quotes its own values line in the intro and again in the closing paragraph loses both sections. Killed by test_the_mirror_rule_never_fires_on_a_posting_that_repeats_its_own_sentence.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_mirror_rule_never_fires_on_a_posting_that_repeats_its_own_sentence"
  ]
 },
 {
  "id": "jd-mirror-tail-left-in-the-body",
  "file": "pipeline/jdfill.py",
  "symbol": "furniture_at",
  "class": "M1-gate-removal",
  "find": "    cuts = [x for x in (m.start() if m else None, form_at(text),\n                        nav[1] if nav else None) if x is not None]\n",
  "replace": "    cuts = [x for x in (m.start() if m else None, form_at(text)) if x is not None]\n",
  "why": "`611`: an own-board careers page renders no login wall, no similar-jobs rail and no application form, so `_PAGE_FURNITURE` and `form_at` both answer None over it -- and both `אסם` rows published 62-70 % site chrome, the first marker word at 2,005 of 3,059. The footer is recognised by being the same block the page rendered above the posting. Without this the tail stays in `jd_body` and every reader sees it. Killed by test_a_nav_rendered_twice_brackets_the_posting_and_both_halves_go.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_nav_rendered_twice_brackets_the_posting_and_both_halves_go"
  ]
 },
 {
  "id": "jd-mirror-head-cut-eats-the-closing-line",
  "file": "pipeline/jdfill.py",
  "symbol": "strip_head",
  "class": "M6-fallback-removal",
  "find": "    body = with_closed_line(t, body)\n",
  "replace": "",
  "why": "`587` through the 2026-09-18 cut. The page's closure sentence is kept as the text's FIRST line, which is ABOVE the head menu -- so the mirror's head cut is the first rule in this module that can reach it (`_HEAD_FURNITURE`'s markers are the page's own header and are long gone by the time text is stored). Losing it re-blinds `roles.page_closed` on exactly the own-board rows `611` is about. Killed by test_the_mirrored_head_cut_gives_the_closing_sentence_back.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_mirrored_head_cut_gives_the_closing_sentence_back",
   "tests/test_units.py::test_the_cut_keeps_the_closing_sentence_and_the_stamp_pass_writes_the_verdict"
  ]
 },
 {
  "id": "jd-entities-left-undecoded",
  "file": "pipeline/jdfill.py",
  "symbol": "html_to_text",
  "class": "M6-fallback-removal",
  "find": "    h = _html_mod.unescape(h).replace(\"\\xa0\", \" \")\n",
  "replace": "    h = h.replace(\"&nbsp;\", \" \").replace(\"&amp;\", \"&\").replace(\"&#39;\", \"'\").replace(\"&quot;\", '\"')\n",
  "why": "The four spellings LinkedIn uses were decoded by hand and every numeric entity a company's own careers page renders was not: measured 2026-09-18, 26 of the 186 published rows carried a raw entity, `&#8211;` on 320 stored bodies and `&#8217;` on 153, and `html.unescape` changes 40 of 267 `matched` texts. Killed by test_entities_are_decoded_at_every_door_and_nowhere_else.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_entities_are_decoded_at_every_door_and_nowhere_else"
  ]
 },
 {
  "id": "jd-listing-card-reads-as-a-posting",
  "file": "pipeline/jdfill.py",
  "symbol": "looks_like_jd",
  "class": "M1-gate-removal",
  "find": "    if _is_markup_soup(text) or _is_listing_card(text) or page_slice(text):\n",
  "replace": "    if _is_markup_soup(text) or page_slice(text):\n",
  "why": "`google israel|research data scientist ii waze` published a 573-character card off Google's careers RESULTS page -- `corporate_fare Google place Tel Aviv, Israel bar_chart Early` plus the minimum qualifications -- with `url` still the results page. It cleared the bar on {qualification, experience}, so `maybe_fill` returned at its first line every morning and the seam bought a fresh verdict on a snippet. 21 of the 2,421 stored bodies are this shape and every one is a Google listing card. Killed by test_a_google_results_card_is_not_a_job_description.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_google_results_card_is_not_a_job_description"
  ]
 },
 {
  "id": "wayback-poll-gets-a-second-full-budget",
  "file": "archive_evidence.py",
  "symbol": "_submit_job",
  "class": "M7-constant-drift",
  "find": "        res = _poll(str(data[\"job_id\"]), auth, max(0.0, timeout - (time.monotonic() - t0)))\n",
  "replace": "        res = _poll(str(data[\"job_id\"]), auth, timeout)\n",
  "why": "the shape of the leak measured on 2026-09-17 (run 35251054872): the POST and the reads each got the whole `WAYBACK_TIMEOUT_S`, so 240 was a 480-s per-job ceiling -- 11,199 worker-seconds opened 34 jobs and named 15, and the per-job p90 from the ledger's `at` gaps was 305 s against a cap of 240 (infra).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_the_post_and_the_reads_share_one_job_budget"
  ]
 },
 {
  "id": "wayback-job-ignores-the-remaining-clock",
  "file": "archive_evidence.py",
  "symbol": "_Pool._submit",
  "class": "M1-gate-removal",
  "find": "        res = submit(t.url, self._job_budget(), self.auth)\n",
  "replace": "        res = submit(t.url, self.caps.timeout_s, self.auth)\n",
  "why": "2026-09-17: the night's last ledger line landed at 18:12:24Z, 62 minutes into a 60-minute budget, because a job started near the end still got the whole ceiling -- and a step that overruns its `timeout-minutes` is ABANDONED, not stopped (infra, 2026-09-12).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_a_job_never_outlives_the_days_remaining_clock"
  ]
 },
 {
  "id": "wayback-budget-not-divided-by-the-workers",
  "file": "archive_evidence.py",
  "symbol": "_Pool._job_budget",
  "class": "M7-constant-drift",
  "find": "        return min(self.caps.timeout_s, max(0.0, left) / max(1, self.caps.workers))\n",
  "replace": "        return min(self.caps.timeout_s, max(0.0, left))\n",
  "why": "the bound is the remaining clock over the WORKERS: three workers each taking the whole remainder is three times the clock that is left, which is how 09-17 ran to 62 minutes of a 60-minute budget (infra).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_a_job_never_outlives_the_days_remaining_clock"
  ]
 },
 {
  "id": "wayback-bad-request-back-to-a-week",
  "file": "archive_evidence.py",
  "symbol": "STATUS_EXT",
  "class": "M7-constant-drift",
  "find": "    \"bad-request\": \"excluded\",\n",
  "replace": "    \"bad-request\": \"http\",\n",
  "why": "measured 2026-09-18 over the whole ledger: 16 of 16 `error:bad-request` ends are `il.indeed.com/viewjob?jk=`, 0 of 23 keyed Indeed jobs ever captured, and a 7-day cooldown asked the archive the same impossible question every week (infra, 620).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_wayback_an_address_the_archive_cannot_request_waits_thirty_days"
  ]
 },
 {
  "id": "render-hidden-title-back-to-a-count",
  "file": "pipeline/rolecard.py",
  "symbol": "report",
  "class": "M6-fallback-removal",
  "find": "        alarms.append(f\"{len(hidden)} role(s) hidden — the title is a card blob: {named[:200]}\")\n",
  "replace": "        alarms.append(f\"{len(hidden)} role(s) hidden — the scraped title is a card blob, fix the scrape\")\n",
  "why": "614: the mail printed `1 role(s) hidden` every morning from 2026-09-12 and named nothing, so identifying the one role cost a session a re-derivation over the ledger and both caches -- and the alarm's own text sent that work to the wrong lane (infra, 2026-09-18).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_render_report_names_the_hidden_titles_not_a_count"
  ]
 },
 {
  "id": "render-blurb-me-check-ignores-aliases",
  "file": "pipeline/rolecard.py",
  "symbol": "cross_check",
  "class": "M4-guard-removal",
  "find": "        m = _toks(key) + [t for k, v in ALIASES.items() if v == key for t in _toks(k)]\n",
  "replace": "        m = _toks(key)\n",
  "why": "632: a blurb that names its company by the spelling identity_key folds into the registry row (`DoiT is a global technology company` on the row `doitintl`) read as naming somebody else, and that hit reached the published board on 2026-09-19 (render).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_blurb_names_other_reads_the_brand_and_refuses_a_two_word_noun"
  ]
 },
 {
  "id": "render-blurb-me-check-ignores-display-name",
  "file": "pipeline/rolecard.py",
  "symbol": "cross_check",
  "class": "M4-guard-removal",
  "find": "        if card.get(\"display_name\"):\n            m += _toks(identity_key(card[\"display_name\"]))\n",
  "replace": "",
  "why": "632: a company whose blurb names it by the brand company-intel evidenced (`display_name`, vetted and possibly revoked by block (b)) must excuse itself; the export is data and ALIASES is code, so a brand with no alias entry yet is the normal case (render, 2026-09-19).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_blurb_names_other_reads_the_brand_and_refuses_a_two_word_noun"
  ]
 },
 {
  "id": "render-blurb-two-word-victim-back-to-its-noun",
  "file": "pipeline/rolecard.py",
  "symbol": "cross_check",
  "class": "M1-gate-removal",
  "find": "        if len(toks) == 1 and (toks[0] in _COMMON_WORDS or len(toks[0]) < 5 or len(key.split()) > 1):\n",
  "replace": "        if len(toks) == 1 and (toks[0] in _COMMON_WORDS or len(toks[0]) < 5):\n",
  "why": "632: 4 of the 6 hits over all 188 cached blurbs were a two-word brand reduced to its second noun -- `Air Products`->`products`, `Poc System`->`system`, `Capital One`->`capital`, `REE Automotive`->`automotive`; the word is not the brand (render, 2026-09-19).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_blurb_names_other_reads_the_brand_and_refuses_a_two_word_noun"
  ]
 },
 {
  "id": "render-blurb-accuser-filtered-like-a-victim",
  "file": "pipeline/rolecard.py",
  "symbol": "cross_check",
  "class": "M3-branch-narrowing",
  "find": "        if not about or me not in mine:\n",
  "replace": "        if not about or me not in victims:\n",
  "why": "632: one token dict made the victim rule the accuser rule -- 632's own first diff silenced `Oak - Identity->Sckipio` (pinned since 2026-08-30) and hid the real `Discount Bank` duplicate: 1 hit where the split design measures 2 (render, 2026-09-19).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_blurb_names_other_reads_the_brand_and_refuses_a_two_word_noun",
   "tests/test_units.py::test_cross_check_names_five_wrong_company_shapes_and_only_those"
  ]
 },
 {
  "id": "member-rank-ignores-the-closure-line",
  "file": "pipeline/roles.py",
  "symbol": "_member_rank",
  "class": "M4-guard-removal",
  "find": "    return (closed_page_at(text) == 0, -len(text))",
  "replace": "    return (False, -len(text))",
  "why": "the retired `longest text wins` rule (607). A LinkedIn card's page chrome is reliably longer than a clean own-board JD, so `hibob|ai product data analyst` and `meta|data scientist product analytics` published the mirror's copy -- opening with the closure sentence -- for 21 days while their employers' own boards still listed them. The text a reader was shown said the opposite of the row's own status, and the seam judged on it. Measured: 4 groups change `best`.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_an_own_board_copy_outranks_a_longer_copy_whose_page_says_it_is_closed"
  ]
 },
 {
  "id": "member-rank-reads-the-closure-line-anywhere",
  "file": "pipeline/roles.py",
  "symbol": "_member_rank",
  "class": "M5-population-widening",
  "find": "    return (closed_page_at(text) == 0, -len(text))",
  "replace": "    return (closed_page_at(text) is not None, -len(text))",
  "why": "the rule reads a POSITION, not a phrase. `587` puts the page's own closure sentence at offset 0 of the capture and only there; a description that merely quotes it (`we are no longer accepting applications by email`, 2,000 characters into the body) is a sentence ABOUT the job, and demoting that copy would pick a shorter, worse text on evidence that says nothing about the posting. Widening this to `is not None` is the same over-reach `PAGE_CLOSED_WINDOW` exists to stop one layer up.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_an_own_board_copy_outranks_a_longer_copy_whose_page_says_it_is_closed"
  ]
 },
 {
  "id": "group-normalise-only-the-best-member",
  "file": "pipeline/roles.py",
  "symbol": "classify_grouped",
  "class": "M3-branch-narrowing",
  "find": "        for m in group:\n            jdfill.normalise(m)\n",
  "replace": "",
  "why": "607. `normalise` ran only inside `maybe_fill(best)`, so an un-normalised twin kept its longer furniture text; the inherit branch copies `best`'s text onto a member only when the member's is SHORTER, so the twin's furniture survived and whichever copy `merge_duplicates` made canonical carried it into `upsert_matched`. Measured on the 09-13 run: `matched_recleaned` 7 rows / 3,752 characters cut every night and re-lengthened every morning -- the 09-11 fixed-point defect through a third door.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_every_member_of_a_group_is_normalised_not_only_the_one_that_is_judged"
  ]
 },
 {
  "id": "text-less-member-keeps-no-fill-reason",
  "file": "pipeline/roles.py",
  "symbol": "classify_grouped",
  "class": "M6-fallback-removal",
  "find": "        if best.get(\"_jd_why\"):\n            for m in members:\n                if m is not best and not str(m.get(\"description\") or \"\").strip() \\\n                        and not m.get(\"_jd_why\"):\n                    m[\"_jd_why\"] = best[\"_jd_why\"]\n",
  "replace": "",
  "why": "607(c). `maybe_fill` runs once per group, on `best`, and names why it could not fill; a text-less sibling was therefore reasonless and the classifier printed `superseded verdicts CANNOT be re-judged (... ? 1)` about it on the 09-18 digest (`gong|senior data scientist - ai research & reliability`). An unnamed exit is indistinguishable from a bug in the plumbing, which is the whole reason the `_jd_why` channel exists.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_text_less_copy_inherits_the_reason_its_group_could_not_be_filled"
  ]
 },
 {
  "id": "failed-reason-never-reaches-the-blocker",
  "file": "pipeline/roles.py",
  "symbol": "_blocker",
  "class": "M4-guard-removal",
  "find": "        if why.startswith(\"failed:\"):",
  "replace": "        if False:",
  "why": "the operator's 2026-09-18 ruling (607): a row held back out of the dataset carries a written, counted reason or it has none at all. Without this arm the Phoenix row (`הפניקס|דאטה אנליסט ית`) and its `failed:shell:2026-09-18` is invisible and the row is counted as an anonymous `pending` -- 'nobody has got to it yet' -- when the truth is 'we tried, and the page is a JS shell'. jd-text owns the WRITE; this is the only READ.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_failed_fill_reason_reaches_the_public_blocker_but_never_outranks_gone"
  ]
 },
 {
  "id": "failed-reason-outranks-gone",
  "file": "pipeline/roles.py",
  "symbol": "_blocker",
  "class": "M2-order-swap",
  "find": "        if str(rec.get(\"jd_attempted\") or \"\").endswith(GONE_MARK):\n            return \"gone\"\n        if why.startswith(\"failed:\"):",
  "replace": "        if why.startswith(\"failed:\"):\n            return why\n        if str(rec.get(\"jd_attempted\") or \"\").endswith(GONE_MARK):\n            return \"gone\"\n        if False:",
  "why": "the ordering the operator ruled on and the reason it is cheap to get right TODAY: a row whose own board 404s is stamped `gone` on `jd_attempted` and the same definitive miss now ALSO earns `failed:gone:<date>` from the new writer, so the two arms compete. `gone` is the better published word and the vocabulary readers already have; `failed:` should only speak where nothing else can. Measured 2026-09-18: 3 records carry the `gone` stamp, 1 carries a `failed:` reason, and the sets do not intersect -- collision 0 rows, which will not stay true.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_failed_fill_reason_reaches_the_public_blocker_but_never_outranks_gone"
  ]
 },
 {
  "id": "adamtotal-token-belt-dropped",
  "file": "pipeline/fetchers.py",
  "symbol": "fetch_adamtotal",
  "class": "M4-guard-removal",
  "find": "    if (row.get(\"token\") or \"\").strip().lower() != tenant:",
  "replace": "    if False:",
  "why": "the only company-bearing string on an AdamTotal address is the `?token=<uuid>-<tenant>` label, and NO gate reads a query string: `identity_gate.board_vouches` judges registry column 2. Without this belt the two can drift apart and the row publishes another tenant's 83 postings under a name the gate vouched for -- the 2026-08-23 FairFly/fireflyspace shape, reached through the one field nothing else checks. It also raises BEFORE the first GET, so a mismatched row costs no request.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_an_adamtotal_row_whose_token_is_not_its_boards_tenant_is_refused"
  ]
 },
 {
  "id": "adamtotal-il-stamp-needs-a-place-cell",
  "file": "pipeline/fetchers.py",
  "symbol": "fetch_adamtotal",
  "class": "M1-gate-narrowed",
  "find": "            if not stated_foreign_place(job):",
  "replace": "            if job[\"location\"] and not stated_foreign_place(job):",
  "why": "the stamp is unconditional on purpose. Measured 2026-09-18 over the whole board: 83 of 83 cards are Israeli, 10 carry no `fa-map-marker-alt` span at all, and `israel.is_israel_job` recognises only 37 of the 83 Hebrew region names on its own -- so gating the stamp on the place cell drops those 10 rows outright and leaves the other 36 to a vocabulary that cannot read `גוש דן`. The veto that keeps it safe is the card's OWN foreign statement, not the presence of a cell.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_an_adamtotal_card_with_no_place_cell_is_still_an_israeli_posting"
  ]
 },
 {
  "id": "declared-tenant-labels-keep-the-vendors-own",
  "file": "pipeline/identity_facts.py",
  "symbol": "validate",
  "class": "M5-population-widening",
  "find": "                labels = _tenant_labels(host, plumbing, ats_host_rx)",
  "replace": "                labels = [_norm(l) for l in host.split(\".\")[:-2] if not plumbing(l)]",
  "why": "the retired expression (2026-09-18). `[:-2]` drops exactly a two-part suffix, so on `career.adamtotal.co.il` the VENDOR's own label enters the tenant population and a declaration for a tenant that lives in the query string refuses itself: `declared tenants ['harel'] match none of the board's subdomain labels ['adamtotal']`. A label the ATS pattern itself matches is the vendor, never a tenant. Measured on the committed registry: `validate` returns the same 0 problems either way and exactly 1 of 667 active ATS rows' label list differs (`Workday` on workday.wd5, undeclared), and the refusal still fires on a two-part-suffix board that is somebody else's.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_declared_tenant_is_checked_against_the_labels_left_of_the_vendors_own"
  ]
 },
 {
  "id": "regression-nights-back-to-one",
  "file": "pipeline/health.py",
  "symbol": "REGRESSION_NIGHTS",
  "class": "M7-constant-drift",
  "find": "REGRESSION_NIGHTS = 2",
  "replace": "REGRESSION_NIGHTS = 1",
  "why": "1 is last night's reading, which is what `regressed to zero` meant until 2026-09-18 and what made the count churn: over the fourteen nights to that morning 81 runs began inside the window and 41 of them (51%) lasted exactly one night, while 20 names entered the class two or more separate times -- IRP Systems announced 09-08, 09-11, 09-14 and 09-16, one night each, with `http 202 / found 0` in the rot file on all four. At 1 the mail announces a board on Tuesday and clears it on Wednesday, and a reader cannot tell a broken board from an interstitial.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_board_empty_for_one_night_only_is_neither_announced_nor_cleared"
  ]
 },
 {
  "id": "a-blip-that-vanished-reads-as-cleared",
  "file": "pipeline/health.py",
  "symbol": "mail_lines",
  "class": "M4-guard-removal",
  "find": "            if v.get(\"reason\") == \"regressed-to-zero\" and _nights_of(v) < REGRESSION_NIGHTS:\n                continue",
  "replace": "            if False:\n                continue",
  "why": "`cleared:` must mean a board recovered from something the mail SAID. A one-night regression is never announced, so announcing its disappearance is a recovery from nothing -- and it is the louder half of the churn: the 09-17 mail cleared 10 rows and 8 of them had been announced the night before (the same 8-of-9 shape on 09-09 and 8-of-10 on 09-15). The four other things this loop refuses to call a recovery are each there for the same reason.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_board_empty_for_one_night_only_is_neither_announced_nor_cleared"
  ]
 },
 {
  "id": "regression-streak-narrowed-to-scrape-rows",
  "file": "pipeline/health.py",
  "symbol": "_announce",
  "class": "M3-branch-narrowing",
  "find": "    if reason != \"regressed-to-zero\":",
  "replace": "    if reason != \"regressed-to-zero\" or (cur or {}).get(\"platform\") != \"scrape\":",
  "why": "the streak is a field on the stale entry precisely so it is platform-blind. 2 of the 20 names that entered the class more than once in the fortnight to 2026-09-18 are NATIVE -- DoubleVerify (greenhouse) and Swimm (comeet) -- and `scrape_rot.json`, the obvious place to read `n` from, has nothing to say about either. A scrape-only rule leaves both flapping and hides that the mechanism is not the scraper's.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_native_board_earns_the_streak_the_same_way_a_scrape_row_does"
  ]
 },
 {
  "id": "same-day-rewrite-counts-as-another-night",
  "file": "pipeline/health.py",
  "symbol": "_nights",
  "class": "M2-gate-inversion",
  "find": "    return n if str(old.get(\"last\") or \"\") == today.isoformat() else n + 1",
  "replace": "    return n + 1 if str(old.get(\"last\") or \"\") == today.isoformat() else n",
  "why": "two writers commit `stale.json` -- the digest and the Monday self-heal -- and 4 of the 18 snapshots in the fortnight to 2026-09-18 were a second write on a date already written. Counting a re-write as a night lets the pair reach REGRESSION_NIGHTS inside one morning, which announces as a two-night regression a board that was read empty once: the exact churn the threshold exists to stop, reintroduced through the clock.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_second_write_on_the_same_date_is_not_another_night"
  ]
 },
 {
  "id": "base-href-ignored",
  "file": "pipeline/jdfill.py",
  "symbol": "_page_links",
  "class": "M8-argument-mis-passing",
  "find": "absolute = urljoin(base_url, raw)",
  "replace": "absolute = urljoin(page_url, raw)",
  "why": "resolve relative hrefs against the PAGE instead of its declared <base href>. Measured live 2026-09-19 on Google's results page: this produces .../jobs/results/jobs/results/120188... , a doubled path the host answers 404 to, and role_addresses_on hands it back as this role's own address.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "cx-site-label-overrides-list",
  "file": "pipeline/jdfill.py",
  "symbol": "is_job_url",
  "class": "M4-guard-removal",
  "find": "for p in parts if not _SITE_LABEL.match(p)):",
  "replace": "for p in parts):",
  "why": "let an Oracle CE site label (CX_3001) prove a listing is a posting again. That is how Discount Bank's 67-requisition listing was fetched as one posting, read as an 80-character shell and stamped a definitive miss.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "comeet-query-key-dropped",
  "file": "pipeline/jdfill.py",
  "symbol": "is_job_url",
  "class": "M1-gate-narrowed",
  "find": "r\"(^|&)(jk|jobid|job_id|id|req|comeet)=[\\w.-]+\"",
  "replace": "r\"(^|&)(jk|jobid|job_id|id|req)=[\\w.-]+\"",
  "why": "drop the comeet uid from the posting-id query keys, so the one address a Comeet widget board gives a single position (?comeet=D7.C29) is refused as a search page before a byte is fetched.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "donor-skips-the-registry-listing",
  "file": "enrich_matched_jd.py",
  "symbol": "_donor_candidates",
  "class": "M6-fallback-removal",
  "find": "            if asked == 1 and body and not found:",
  "replace": "            if False:",
  "why": "never ask the company's registry listing when the row's own url named no posting. Measured 2026-09-19: google israel|research data scientist ii waze carries the BARE results page, which lists nothing; the registry url lists 20 postings including the Waze one, whose page answers with 3,761 characters of JD.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "cooled-rows-keep-bd",
  "file": "enrich_matched_jd.py",
  "symbol": "_run",
  "class": "M8-argument-mis-passing",
  "find": "                     probe_cell=probe_cell, free_rungs_ignore_cooldown=True,",
  "replace": "                     probe_cell=probe_cell,",
  "why": "put the FREE rungs back inside the 7/14/28 cooldown on the live pass, so a row stamped once is not walked again for a week and every free rung added since that stamp waits out the whole ladder. 4 of the 6 rows this lane could not fill on 2026-09-19 were in that state.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "render-rung-after-bd",
  "file": "pipeline/jdfill.py",
  "symbol": "fetch_jd",
  "class": "M3-branch-narrowing",
  "find": "    if renderer is not None and reason in (\"shell\", \"no-markers\") and _render_shaped(",
  "replace": "    if renderer is not None and bd is None and reason in (\"shell\", \"no-markers\") and _render_shaped(",
  "why": "make the FREE render run only when the paid rung is absent, i.e. put the credit ahead of the free rung on every row that may spend. Measured 2026-09-19: Phoenix renders to 1,672 characters with 3 marker families for 0 credits.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "render-capped-definitive",
  "file": "pipeline/jdfill.py",
  "symbol": "fetch_jd",
  "class": "M7-constant-drift",
  "find": "            reason, transient = \"render-capped\", True",
  "replace": "            reason, transient = \"render-capped\", False",
  "why": "park a page for 7/14/28 days because OUR render budget was spent, not because anything was read. That is the defect `bd-render-capped` was already fixed for: it puts the one class of page rendering exists for out of reach.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "comeet-by-name-without-ambiguity-veto",
  "file": "pipeline/jdfill.py",
  "symbol": "comeet_widget_jd",
  "class": "M5-population-widening",
  "find": "        if len(hits) == 1:",
  "replace": "        if hits:",
  "why": "take the FIRST position whose name matches when two match, which is the coin flip `role_addresses_on` refuses: it publishes another opening's text on this role's card.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "donor-never-renders-a-listing",
  "file": "enrich_matched_jd.py",
  "symbol": "_donor_candidates",
  "class": "M6-fallback-removal",
  "find": "            elif not found and renderer is not None and jdfill._render_shaped(",
  "replace": "            elif False and renderer is not None and jdfill._render_shaped(",
  "why": "never render a JavaScript listing whose plain body carries no cards. Discount Bank's Oracle site is 101,375 bytes and 80 characters of text plain; rendered it is 17,624 characters with all 67 cards, which is the only way a posting address on it can be found.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "archived-pass-renders-too",
  "file": "enrich_matched_jd.py",
  "symbol": "_run",
  "class": "M5-population-widening",
  "find": "                          log=lambda s: print(s, flush=True), probe_cell=probe_cell,\n                          renderer=None)",
  "replace": "                          log=lambda s: print(s, flush=True), probe_cell=probe_cell,\n                          renderer=renderer)",
  "why": "give the archived pass the 45-second rung as well. It has a quarter of the budget and forty times the rows, so the reserve that keeps archived roles moving at all would go on renders.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "activation-ok-token-dropped",
  "file": "pipeline/identity_gate.py",
  "symbol": "activation_ok",
  "class": "M8-argument-mis-passing",
  "find": "    return activation_verdict(name, api_url, n_jobs, html=html, token=token) == \"ok\"",
  "replace": "    return activation_verdict(name, api_url, n_jobs, html=html, token=\"\") == \"ok\"",
  "why": "the three gate WRAPPERS are what a future tool reaches for -- `activation_ok`'s own docstring says *for tools that verified jobs first*, which is the adamtotal case exactly. With the token dropped, `checkable_token(\"\", api)` falls through to the last slug candidate of `career.adamtotal.co.il` (`'il'`) and `board_vouches` refuses a DECLARED row for \"carrying a tenant it did not declare\" -- `not-ours`, before any page is read, on the one row of the 14 declared today whose tenant lives in a query string. A silent refusal of a company's own board (`621`'s gate note).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_three_gate_wrappers_forward_the_rows_token"
  ]
 },
 {
  "id": "write-ok-token-dropped",
  "file": "pipeline/identity_gate.py",
  "symbol": "ok_to_write",
  "class": "M8-argument-mis-passing",
  "find": "    return write_verdict(name, url, html=html, token=token) == \"ok\"",
  "replace": "    return write_verdict(name, url, html=html, token=\"\") == \"ok\"",
  "why": "the three gate WRAPPERS are what a future tool reaches for -- `activation_ok`'s own docstring says *for tools that verified jobs first*, which is the adamtotal case exactly. With the token dropped, `checkable_token(\"\", api)` falls through to the last slug candidate of `career.adamtotal.co.il` (`'il'`) and `board_vouches` refuses a DECLARED row for \"carrying a tenant it did not declare\" -- `not-ours`, before any page is read, on the one row of the 14 declared today whose tenant lives in a query string. A silent refusal of a company's own board (`621`'s gate note).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_three_gate_wrappers_forward_the_rows_token"
  ]
 },
 {
  "id": "identity-ok-token-dropped",
  "file": "pipeline/identity_gate.py",
  "symbol": "identity_ok",
  "class": "M8-argument-mis-passing",
  "find": "        return ok_to_write(name, url, html=html, token=token)",
  "replace": "        return ok_to_write(name, url, html=html, token=\"\")",
  "why": "the three gate WRAPPERS are what a future tool reaches for -- `activation_ok`'s own docstring says *for tools that verified jobs first*, which is the adamtotal case exactly. With the token dropped, `checkable_token(\"\", api)` falls through to the last slug candidate of `career.adamtotal.co.il` (`'il'`) and `board_vouches` refuses a DECLARED row for \"carrying a tenant it did not declare\" -- `not-ours`, before any page is read, on the one row of the 14 declared today whose tenant lives in a query string. A silent refusal of a company's own board (`621`'s gate note).",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_the_three_gate_wrappers_forward_the_rows_token"
  ]
 },
 {
  "id": "auto-expand-ats-token-dropped",
  "file": "auto_expand.py",
  "symbol": "_row_for_ats",
  "class": "M8-argument-mis-passing",
  "find": "    if not _gate.activation_ok(nm, api, n_all, html=html, token=tok):",
  "replace": "    if not _gate.activation_ok(nm, api, n_all, html=html, token=\"\"):",
  "why": "the row builder has the resolved tenant in scope (`tok`, which it writes into column 2 on accept) and dropped it. This is the call-site half of `activation-ok-token-dropped`: the wrapper can forward only what it is given, so a correct wrapper and a forgetful caller refuse the same board. The other two ATS activators (`bd_rescue`, `wayback_rescue`) hand the SAME token to `embedded_board_ok` on the adjacent line, which is what makes the omission visible rather than arguable.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_every_ats_activator_hands_the_gate_the_rows_token",
   "tests/test_registry.py::test_the_mutation_coverage_demand_is_pinned"
  ]
 },
 {
  "id": "stale-one-night-strike-in-resolve-broken",
  "file": "resolve_broken.py",
  "symbol": "candidates",
  "class": "M5-population-widening",
  "find": "                        if v.get(\"careers_url\") and not one_night_reading(v)]",
  "replace": "                        if v.get(\"careers_url\")]",
  "why": "the pool every strike is spent from. `_skip` abandons a row at `give_up_after` 5 and hands it to discovery for good, so a candidate is not free even when the resolve fails cheaply: over the fortnight to 2026-09-18 Workiz reached 4 of the 5 on four separate ONE-NIGHT regressions, and Dell, Highcon and SMARTECH were struck `attempt 1 - no working ATS` on a single night's reading. With the conjunct gone the self-heal is back to acting on a reading the MAIL itself refuses to call a regression (`health._watchful`), and it is invisible: a strike is a line in `resolve_attempts.json`, not a failure.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_one_night_reading_buys_no_resolver_strike"
  ]
 },
 {
  "id": "stale-one-night-input-in-discovery",
  "file": "discovery_daily.py",
  "symbol": "_targeted_inputs",
  "class": "M5-population-widening",
  "find": "                  and not one_night_reading(e)]",
  "replace": "                  ]",
  "why": "the other consumer of the same file, which is why `635` was filed as ONE item: a fix to the self-heal alone leaves a one-night reading buying a Bright Data LinkedIn input instead of a strike. `TARGETED_MIN_CAP` is 10 a day over ~88 targetable rows, so each name taken displaces a real one for a 22-day rotation, and the row it displaces is one whose board really has moved - the largest open coverage class in the repo.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_one_night_reading_buys_no_targeted_linkedin_input"
  ]
 },
 {
  "id": "stale-nights-default-to-a-first-night",
  "file": "pipeline/health.py",
  "symbol": "one_night_reading",
  "class": "M7-constant-drift",
  "find": "    if not isinstance(entry, dict) or _nights_of(entry) >= REGRESSION_NIGHTS:",
  "replace": "    if not isinstance(entry, dict) or _int(entry.get(\"nights\"), 1) >= REGRESSION_NIGHTS:",
  "why": "the default is the whole safety of this skip. `_nights_of` answers with the SETTLED value when the field is absent, so an entry that cannot tell us its streak is TAKEN; read as a first night instead, the skip silences every entry written before 2026-09-18 - 109 of 109 on 2026-09-19, i.e. the self-heal and the targeted sweep would both have gone quiet on their first morning and a real regression would wait for a field nothing had written yet. Suppressing a true regression is the expensive direction, which is the rule `_nights_of` and `_fetched_none` already state.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_one_night_reading_buys_no_resolver_strike"
  ]
 },
 {
  "id": "stale-transient-error-word-dropped",
  "file": "pipeline/health.py",
  "symbol": "one_night_reading",
  "class": "M5-population-widening",
  "find": "            or (reason == \"fetch-error\"\n                and _TRANSIENT_ERROR in (entry.get(\"error\") or \"\")))",
  "replace": "            or reason == \"fetch-error\")",
  "why": "`fetch-error` is TWO classes in one reason. `network error ... timed out` is a socket that never answered and says nothing about the address (the six SuccessFactors rows of 2026-09-18, cleared next night 5 of 5 times in the preceding fortnight); `scrape: http:403 (19 nights)` and `http:404` are the board ANSWERING, which is exactly the evidence a resolver acts on. Drop the word and a 403 or a 404 waits a second night before anything re-resolves it - a delay bought for the class that needs no delay, on a row whose address has demonstrably changed.",
  "must_be_killed_by_behavioural": true,
  "killers": [
   "tests/test_units.py::test_a_one_night_reading_buys_no_resolver_strike"
  ]
 },
 {
  "id": "csv-threeway-origin-column",
  "file": "merge_csv_rows.py",
  "symbol": "_merge_cols",
  "class": "M3-branch-narrowing",
  "find": "        if ov == bv:\n            row[i] = tv                    # only ORIGIN moved: its knowledge is newer",
  "replace": "        if ov == bv:\n            row[i] = ov                    # only ORIGIN moved: its knowledge is newer",
  "why": "644: ours' columns were copied wholesale, so a three-hour cron's start-of-run snapshot reverted an activation pushed 17 minutes earlier (Harel, 83/83 IL). A column ours did not touch is origin's.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "csv-conflict-keeps-ours",
  "file": "merge_csv_rows.py",
  "symbol": "_merge_cols",
  "class": "M2-gate-inversion",
  "find": "        elif tv != bv:\n            row[i] = tv                    # both moved, differently",
  "replace": "        elif tv != bv:\n            row[i] = ov                    # both moved, differently",
  "why": "644: a column BOTH sides moved keeps ORIGIN's (a session writes with the later, fuller knowledge; a cron re-stamps the next night) and is reported. Flipping it keeps the report and loses the resolution -- the shape a warning hides.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "csv-note-append-unbounded",
  "file": "merge_csv_rows.py",
  "symbol": "_merge_notes_threeway",
  "class": "M1-gate-removal",
  "find": "        cell = N.replace_own(cell, key, seg, cap)",
  "replace": "        cell = cell + \" | \" + seg",
  "why": "the three-way note merge writes through `notes.replace_own`, which is what spends the 220-char cap by the append-log's own rule (oldest UNPROTECTED first, never a slice) and what stops a tool's own previous segment surviving beside its new one.",
  "must_be_killed_by_behavioural": true
 },
 {
  "id": "csv-stale-address-verdict-kept",
  "file": "merge_csv_rows.py",
  "symbol": "_merge_notes_threeway",
  "class": "M1-gate-removal",
  "find": "        if address_moved and key in _ADDRESS_VERDICTS:",
  "replace": "        if False and key in _ADDRESS_VERDICTS:",
  "why": "644 / the 09-13 lesson that a ledger read is keyed by url: when origin has moved `api_url`, our `listing-hunt` / `dark-triage` verdict was made against a page this row no longer points at, and carrying it onto the new address re-arms a 14-day cooldown on a board nobody has read.",
  "must_be_killed_by_behavioural": true
 }
]
